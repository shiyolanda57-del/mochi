# FIX-REGRESSION — 修复点回归清单

用户反馈过的关键问题 → 对应验证方式。**每次构建/上传前对照本清单跑相关检查**，
防止「已修复的问题在新版本复发」（历史教训：修复被并行会话覆盖 / 编辑器旧缓冲
回写 / 新文件漏接入 build.mjs，构建照常通过但功能已丢）。

> ⚠️ **禁止通读本文件**（当前仅保留最近 42 条，历史在 `FIX-REGRESSION-archive.md`）：构建/自查时先 grep 编号或功能关键词，再读命中行；全绿才提交推送。

## 使用方法

1. 构建后先看 build.mjs 的「关键修复哨兵」输出（自动检查产物特征，缺失会醒目警告）。
2. 按本清单跑相关专项脚本（`node tools/verify-xxx.mjs`）。
3. 全绿才提交推送；有红项先定位再上线。
4. **真机验证状态约定（v3.27.x）**：主清单验证方式里的【真机:待验证】= 只有无头验证，用户真机反馈正常后改为【真机:已确认(机型+日期)】。修 B 碰到相关文件时：【已确认】的条目必须复测防回归；【待验证】的条目至少不破坏其修复代码（跑哨兵）。

## 设备索引（修 B 前先查：我要改的文件压过哪些机型的修复？）

> 改任何文件前，按下方「涉及文件」列 grep 本表；命中机型 = 你可能压到它的历史修复，修完按对应条目的验证方式复测。
> 生成方式：脚本扫描主清单症状文本（v3.27.x）；新条目登记时请同步维护本表。

| 设备/机型        | 相关条目            |
| ------------ | --------------- |
| 全机型通用      | 308（设备无关的漏绑，任一机型都可复现/验证）、314（收藏批量多选勾选清零，设备无关纯逻辑） |
| 小米 14U       | 82、86、88、90、103 |
| OPPO Find X9 | 103、124、190     |
| iPhone 12    | 85、114          |
| iPhone 16    | 84、100          |
| vivo X200s   | 104、117、149      |
| vivo Y35     | 96、100          |
| 苹果17         | 109、110、149、193      |
| 荣耀 x30i      | 96、100          |
| 荣耀 50se      | 199             |
| OPPO Reno14   | 201             |
| OPPO K13 Turbo Pro（HeyTapBrowser） | 236            |
| 一加Ace3（PJE110） | 180、214、216     |
| Oppo A5 Pro（Via） | 206            |
| OPPO Reno6 5G（雨见） | 169、200、228、239     |
| iPad Air     | 121、188         |
| iPad Safari  | 121             |
| iPadOS       | 121             |
| iPhone 12PM  | 114             |
| iPhone 12 Pro | 264            |
| iPhone 13    | 120             |
| iPhone 15    | 91、114          |
| iPhone13     | 112             |
| iPhone14     | 112             |
| iQOO Neo10   | 107             |
| iQOO12       | 211、212         |
| 华为 Mate 4    | 108             |
| 华为 P50E      | 92、215          |
| 小米15         | 106             |
| 小米15Pro      | 118、131、219、220         |
| 红米 K60       | 115             |
| 红米 K70       | 209             |
| 红米 K80       | 93              |
| 红米 Note12 Turbo（Chrome 150） | 231、232、233、234 |
| 红米 弱网或断网刷新   | 87              |
| 苹果16         | 111             |
| 荣耀x60i       | 98              |
| 荣耀 X50（HonorBrowser）    | 267             |
| 荣耀畅玩40 Plus（夸克） | 223、224         |
| 多机型（真我/荣耀/小米 Edge 等 LS 回滚家族） | 82、88、226、229、233、265         |
| 多机型（迁移漏键家族，与机型无关） | 231、232         |

## 清单

| #   | 用户反馈问题                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 修复要点                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 验证方式                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | <br /> | <br />                                                                                                                                                                                                                     |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :----- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 81  | 贴贴邀请：联系人发「申请贴贴」互动卡片，我同意后联系人回应的消息也显示「主动联系」小爱心标识（不应有）                                                                                                                                                                                                                                                                                                                                                                                                       | 根因（chat.js）：openInvitePanelFor 的 cuddle 分支同意后调 `addIn(name+' '+pick(CUDDLE_REPLIES), { initiative: true })`——`initiative:true` 在 renderMsg（chat.js:2017）触发 `msg-hi-heart` 小爱心（本意标记 TA 主动发起联系）。但贴贴回应是「我同意后触发的回应」非 TA 主动发起，不该带爱心。邀请消息本身（"想和你贴贴"）带爱心合理（TA 主动找你），仅同意后回应误带。修复：cuddle 回应去 `initiative:true` 改传 `{}`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 哨兵 `pick(CUDDLE_REPLIES), { initiative: true })`（build.mjs FIX\_SENTINELS，**absent**）/ 验证：node --check 过；待构建后真机：TA 发贴贴邀请→我同意→TA 回应不再显示小爱心；TA 主动发普通消息仍显示爱心（语义不变）；邀请消息本身仍显示爱心                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | <br /> | <br />                                                                                                                                                                                                                     |
| 82  | 小米 14U Edge：导出数据进度条走完、点了「确定」下载，浏览器不弹下载也没下载完；且导出后数据像是被清空（设置/联系人等本地存储全没，聊天/音乐等 IndexedDB 大键还在）                                                                                                                                                                                                                                                                                                                                                              | 根因一（data-backup.js anchorDownload）：blob URL 在 `setTimeout(1000ms)` 后被 `URL.revokeObjectURL` 作废——大备份（音乐/头像/聊天全量 base64 后轻松上百 MB）下载刚由浏览器接管、blob 还没读完 URL 就失效 → 无下载通知、无文件落盘（诊断实测该用户自动备份快照高达 172.7MB）。根因二（旧线上版本）：导出把整个 JSON **无条件**写进 IDB 自动备份副本键，大备份写副本成内存峰值主因 + 旧 idbSet 固定 4s 超时连败弹「存储异常」，导出过程被拖到崩溃边缘；崩溃/内存压力下安卓 Chromium 本地存储(LS)损坏 → LS 全空且后续写入全报 QuotaExceededError（该用户诊断：localStorage 0 键 + LS 写探针失败，IndexedDB 187.5MB 完好——数据其实没丢，全在 IDB）。修复：①anchorDownload 改长命 blob URL（pagehide 才释放 + 5 分钟兜底，anchor 不再 1s 移除）让大下载真正完成；②副本写入 >3MB 跳过（snapshotWritten 记录），且弹窗/toast 对未写副本的大备份如实提示「下载文件是唯一新备份」，不再谎称「已自动存入本机缓存」                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 哨兵改锚 `addEventListener('pagehide', function h()`（anchorDownload 长命 blob URL 特征；原 `const snapshotWritten` needle 已随副本机制下线删除，见 #86）/ 验证：node --check 过；构建哨兵全绿；真机（小米 14U Edge）：导出大备份→进度条走完→点「确定」→状态栏出现下载/文件正常保存；数据导出前后本地数据无任何变化（导出不写不删任何业务键）；导出弹窗提示「这个文件是唯一备份」                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | <br /> | <br />                                                                                                                                                                                                                     |
| 83  | 开屏「点击进入」在数据没加载完时就能点：浏览器 tab 还在转/loading，但开屏进入按钮已可点，进去后字卡/图片/聊天记录等数据不全（首次访问和老用户刷新都遇到）                                                                                                                                                                                                                                                                                                                                                                      | 根因（idb.js）：`idbRestore` 的 12 秒保险丝在 IndexedDB 回填超时（低端机大量图片/字卡键）时调 `sendReady()` 强制设 `window.__mochiDataReady=true` **静默放行开屏**，但后台回填循环还在跑、未完成 → 开屏 `ready()` 返回 true → 「点击进入」可点 → 用户进入后读到的是空/部分数据。注释原承认这是「卡死 vs 数据暂时不全」的权衡，但用户反馈「进去数据都没加载完」不可接受。修复：①idb.js 保险丝不再调 `sendReady()`，改为派发 `mochi-restore-slow` 事件 + 设 `__mochiDataSlow`，**不**设 `__mochiDataReady`——只有 `finish()`（真全部回填完/空数据/读 keys 失败）才设就绪标志；恢复循环继续后台补齐。②clock.js「点击进入」按钮仍只认 `__mochiDataReady`（真就绪），收到 `mochi-restore-slow` 或 20 秒兜底且未就绪时显示「仍要进入」小链接（弱化样式，非推荐入口），点击后 hide 开屏 + openModal 提示「数据仍在加载，部分内容可能暂时看不见，建议稍后刷新」；原 20 秒自动 `hide()` 兜底删除（不再静默放行）。③loading 文案在 slow 时改「数据较多，仍在加载…」让用户知道在加载而非卡死                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 哨兵 `mochi-restore-slow`（idb.js）+ `splash-force-enter`（clock.js）（build.mjs FIX\_SENTINELS）/ 验证：node --check 过；待构建后真机：①数据几秒内就绪→按钮可点→进入数据全；②数据超 12 秒未就绪→按钮置灰+「数据较多，仍在加载…」+「仍要进入」小链接出现；③点「仍要进入」→进入+提示数据可能不全；④IDB 真挂起→按钮永不放行，只能走「仍要进入」逃生口                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | <br /> | <br />                                                                                                                                                                                                                     |
| 84  | iPhone 16 Pro Safari（iOS 18.7，PWA standalone）：一直弹「存储异常：近期数据多次写入失败」弹窗，每会话必现；诊断里 IndexedDB 大键明细/开关持久化体检停在「读取中…」（IDB 读事务也慢/挂）                                                                                                                                                                                                                                                                                                                                | 根因（idb.js）：iOS 挂后台会杀 IndexedDB 服务进程，回前台后旧连接上事务失败。v3.25.x 的 `connLost` 只匹配 `InvalidStateError`，但 iOS 18 实测多报 `UnknownError`/`InternalError`/`TransactionInactiveError`（有时只挂起不报错）→ 连接永不重建 → 本会话后续写入全部失败 → 连续 5 次触发 `_idbFailNotify`「存储异常」弹窗（`_idbFailAlerted` 会话级，故每会话必现一次）。该用户诊断 9 条前后台切换（5 分钟内来回切 9 次），每次回前台都撞死连接。修复（idb.js）：①`connLost` 加宽匹配 iOS 连接级错误名（UnknownError/InternalError/TransactionInactiveError）；②新增 `armFgIdbReset()`（仅 iOS 启用）：`visibilitychange→visible`/`focus` 时主动作废旧连接引用并预拉起新连接，回前台后的首次写入不再撞死连接；③`idbSet`/`idbSetAll`/`idbDelete`/`idbClear` 补 `tx.onabort` 处理 + 所有事务错误都过 `connLost` 判死重建                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 哨兵 `armFgIdbReset`（build.mjs FIX\_SENTINELS）/ 验证：node --check 过；构建哨兵全绿；npm run verify 10/10；真机（iPhone 16 Pro Safari PWA）：进设置→看存储→回桌面挂后台→切回来再发消息/改设置→不再弹「存储异常」；诊断里 IndexedDB 明细不再长时间「读取中…」                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | <br /> | <br />                                                                                                                                                                                                                     |
| 85  | Safari 导出数据→Chrome 导入后丢失朋友圈内容数据和聊天记录数据（iPhone 12 Pro Max Safari 导出，Chrome 导入；诊断 v3.26.363：IndexedDB 只有 chat-msgs default=854B+cmtejn1u47jn=260B 极少，无 feed-posts；LS 有 feed-posts-snap=1.2KB 剥图快照）                                                                                                                                                                                                                                                          | 根因（data-backup.js + idb.js）：①导出 doExport `if (k in data.ls) continue` 让 chat-msgs 的 LS 有损小快照（≤20KB，liteSnapArray 剥图/截断）跳过 IDB 权威读取→导出有损快照丢图片/语音；②feed-posts >200KB 时 xyStore 只写 IDB 删 LS，Safari IDB 事务挂起/超时（idbGet 4s+4s）→主键彻底丢失只剩 LS 剥图快照；③导出 IDB 失败不读 memoryCache（idbRestore 回填值）无兜底；④导入 doImportGo 无条件跳过 chat-msgs 的 LS 写入，data.idb 无权威值时 chat-msgs 既不写 LS 也不进 IDB→彻底丢失。修复：①idb.js 新增 idbGetCached 暴露 memoryCache 读取；②导出权威键（chat-msgs/feed-posts）强制读 IDB 权威不跳过，IDB 失败→memoryCache 兜底→lsBig→LS，降级记入 exportMissing 导出结束在覆盖清单警告「以下数据可能未完整导出，请勿清空原设备」；③导入 chat-msgs 仅当 data.idb 有权威值才跳过 LS，否则把 LS 有损快照写进 IDB 兜底                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 哨兵 `idbGetCached`（idb.js）+ `isAuthorityKey`（data-backup.js）（build.mjs FIX\_SENTINELS）/ 验证：node --check 过；待构建后真机：Safari 发带图聊天+带图朋友圈→导出（无降级警告）→Chrome 导入→聊天记录和朋友圈完整含图；Safari IDB 挂起导出降级→弹窗显示「⚠ 聊天记录/朋友圈可能未完整导出」→用户知情不清原设备                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                                                                                                     |
| 86  | 「自动备份快照」占掉近一半存储且永远不会更新（用户全部数据的一份完整副本；实测 743.6MB / 小米 14U 172.7MB）。存储页还写着「下次你再导出数据时会自动重建一份」——v3.27/v3.28 加了 ≤3MB 阈值后，大数据量用户的导出根本不写副本，这句话对最需要它的用户是错的                                                                                                                                                                                                                                                                                                        | 经用户确认整体下线。①**写入端**（data-backup.js doExport）：删除副本写入（连 `snapshotWritten = json.length <= 3MB` 分支与「正在写入自动备份副本」进度步骤一起去掉），导出提示统一如实说明「这个文件是唯一备份，本机不再另存副本」；②**消费端**：删 `checkLostAndOfferRestore`（启动「检测到数据可能丢失→从副本恢复」弹窗）与 garden.js `offerSnapshotRecover`（**跨域改动**，理由见 WORKLOG）——后者会对整包副本 `JSON.parse`，数百 MB 遗留快照等于一次 OOM/长任务；③**清理端**：新增 `purgeLegacySnapshot()`，数据就绪后 1.5s 删掉 LS+IDB 两处遗留副本键（只删这一个键，业务数据不动，静默无提示），用户不必再进设置手动点删除；④**UI**：查看存储页去掉「自动备份快照」整卡（#st-snap / #st-clear-snap / #st-snap-hint）、`catOf` 分类、`.snap` 与 `.storage-clear.danger` 样式；**保留**导出/导入与 `idbRestore` 对该键的排除（清理落库前仍可能读到，防自包含无限增长与误回填）、device.js 诊断大键明细（副本若因 IDB 故障没清掉仍能在诊断里看到）。已知代价（用户已确认接受）：不再有「业务键几乎全空时从本机副本自动恢复」的保险，数据丢失只能靠用户自己保存的导出文件 → notice.json「关于数据丢失」补写「本机不保留自动副本，务必自己存好导出文件」                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 哨兵：`purgeLegacySnapshot`（存在）+ absent `idbSet(SNAPSHOT_KEY`（导出再写副本即报警，同时守住 #73/#82 根因）+ absent `offerSnapshotRecover`；旧 needle `st-clear-snap`/`json.length <= 3 \* 1024 \* 1024`/`const snapshotWritten` 已删除或改锚（#42/#73/#82 行已标注被本行取代或改锚） / 验证：node --check 全过；`node tools/verify-garden-dataloss.mjs`（Case B 已改写为「播种遗留副本 → 断言启动后 IDB+LS 均被自动清理、期间不再出现任何副本找回弹窗、dummy 业务键完好」）；真机：更新后启动一次 → 设置→查看存储，IndexedDB 占用应下降约一半 → 再导出一次，弹窗提示「这个文件是唯一备份」；诊断「IndexedDB 大键明细」不再出现 `__auto-backup-snapshot`。**同日两处补强（各配一条新哨兵，且都用反向对照实测过）**：① 清理触发原来只有事件路径，而 #83 之后 12 秒保险丝不再设 `__mochiDataReady`（只派发 `mochi-restore-slow`）→ IDB 整轮挂起的设备上 `mochi-restore-done` 永不到达、清理一次都不跑，而 IDB 最慢、遗留副本最大的恰好是同一批机型；补 `purgeOnce()` 幂等包装（与 #90 的「删→复核→重试」链共用，保证只起一套）+ 20 秒墙钟兜底（照 idb.js `wrjMergeFromIdb` 的挂起兜底同款做法，清理只碰副本键、与 restore 完成与否无关）。② `idb.js` 的 LS→IDB 大键迁移未排除该键 → 以 LS 形态存在的副本（远古版本或手工改过的备份包）必然远超 `LS_BIG_LIMIT` 而被收进 `bigKeys`，整包读进内存 + 写回 IDB + 常驻 `memoryCache`，等于把刚清掉的副本复活一份还白钉几百 MB 堆；补 `continue` 排除，交给 purge。反向对照：撤 ① → Case E 复现「35 秒内副本一直在」；撤 ② → Case D 实测 `window.idbGetCached` 命中 420045 字符。新哨兵 `function purgeOnce()`（data-backup.js）+ `if (k === 'xy-home-v2:__auto-backup-snapshot') continue;`（idb.js）；`tools/verify-garden-dataloss.mjs` 新增 Case D（LS-only 超大副本 → 断言不迁移不常驻内存）/ Case E（注入「restore 永不完成」→ 断言 20 秒兜底仍清理），全套 27/27。**⚠ 提交陷阱备忘**：负向对照时把修复行注释掉，若该形态被 staged blob 残留提交，`minifyJs` 会整行丢弃 `//` 注释 → 产物缺 needle → 哨兵会醒目报警而不会静默上线；本次线上产物正确，但已提交源码 `src/js/idb.js` 一度残留该注释（工作区已修正，待构建者随下次提交收口） | <br /> | <br />                                                                                                                                                                                                                     |
| 87  | iOS/安卓弱网（GitHub Pages 国内慢网）进入应用一段时间后「退回之前更新的旧版本然后白屏」，且退回的旧版桌面功能图标显示不全/少很多（iPhone Edge/iOS 18.7 反馈；诊断确认设备运行最新版 ts=1788067851963、SW 当前生效、.phone=812px 布局修复 78a2835 已生效）                                                                                                                                                                                                                                                                                     | 根因（src/pwa/sw\.js）：SW 两处挑旧缓存都不看版本——①fetch 导航回退 `keys.reduce` 按 caches.keys() 原顺序探测，命中第一个含 index.html 的缓存（顺序不定、通常最旧优先），当前 CACHE 缺 index.html（慢网 precache 超时失败）时用户被送回**最旧**旧版 → 旧版布局 bug（图标缺失）+ 白屏；②activate 兜底 `hits.find(Boolean)` 保留的也是第一个（最旧）旧缓存，下次导航继续退回最旧版。修复：①新增 cacheVersion() 解析 CACHE 名 `mochi-<构建时间戳 base36>`（build.mjs 每次构建替换、单调递增）得版本号，解析不了按 0；②fetch 导航回退 keys 先按版本**降序**排序再 reduce 探测，命中「最新」旧 index.html；③activate 兜底 keep 取版本降序第一个（最新）旧缓存，其余照删                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 哨兵 `sort((a, b) => cacheVersion(b) - cacheVersion(a))` + `cacheVersion`（build.mjs sw\.js 专项哨兵 3/3）/ 验证：node --check 过；构建哨兵 96/96；npm run verify 10/10；真机（iPhone/红米 弱网或断网刷新）：不再退回旧版布局/白屏，回退命中最新缓存；升级后首次弱网打开不再出现图标缺失                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | <br /> | <br />                                                                                                                                                                                                                     |
| 88  | 小米 14U Edge（Android/Edge 151，PWA standalone，诊断 v3.26.363 ts=1788074500839）两个症状同源：①「聊天记录会自己消失，差不多几个小时就消失不显示」；②「后台通知有时候会自己关闭」                                                                                                                                                                                                                                                                                                                               | 诊断证据：**本项目 localStorage 键=0 个**、写探针 `QuotaExceededError`，而 IndexedDB 184MB 完好、`storage.persisted=true`、563KB 聊天记录在 IDB 里 → LS 已整库写不进（GitHub Pages 同账号各项目共用一个 origin 的 LS 配额，路径不隔离，最可能是同域其他站点占满）。根因＝**「启动时同步读 localStorage」而 LS 已废、IDB 回填是异步的**：①`active-contact` 读不到 → 每次冷启动静默切回 `default` 桌面 → 用户看到「记录消失」（切回原桌面即可见）；②`bg-keepalive`/`bg-notify` 全局键启动读空 → 按默认值渲染成「关」（且旧逻辑会把读空当事实写回覆盖）；③真数据丢失风险：权威读超时后 `saveMsgs` 仍把内存数组整包写进 IDB，LS 快照在废机上救不回来 → 一条新消息即可抹掉全部历史。修复：①**contacts.js** 读 `active-contact` 改走 `xyStore`（内存缓存优先，接上 #40 小键写日志回放），并新增 `correctCidFromIdb()`——回填完成 / `mochi-wrj-heal` / 16s 兜底时按 IDB 权威值自动切回原桌面（用户手动切过桌面则不干预，联系人须存在才切；**且只在开屏未消失或当前就停在手机主页时才切**，`setActiveContact` 会强制回主页，用户正在聊天/设置里时本次放弃、留给下次冷启动）；**同一批还要收口** **`migrateLegacy.finish()`：原判空用裸** **`localStorage.getItem(active-contact)`，LS 失效机上恒真 → 每次启动把回填好的权威值连同内存缓存/写日志一起改回** **`default`，等于把** **`correctCidFromIdb`** **抵消，现改为** **`regStore().get`** **判空**；②**bg-keep.js** 新增 `reheatBgSwitches()`，回填完成后重读并重新应用两个开关，`keepUserTouched`/`notifyUserTouched` 保证手动操作不被覆盖；③**chat.js**（**跨域**，见 WORKLOG）`saveMsgs`/`saveMsgsNow` 加 `authOk` 闸门——本会话未读到该桌面权威值时只暂存 `pendingLocal`+写 LS 有损快照+`scheduleIdbRetry`，不再整包覆盖 IDB，15s 保险丝也改为补挂重试而非放行覆盖（宁可晚存几条，绝不覆盖全部）；④**device.js** 诊断单次遍历补报**整域** LS 键数/占用/非本项目最大键 + `localStorage 状态：…` 结论行（区分「同域其他站占满配额」与「本库损坏」），并新增 `__lsStatus` 自检：LS 写不进但 IDB 回填已完成时当场提示一次「本机浏览器本地存储受限，设置与记录已改用数据库存储，数据不会丢」（sessionStorage 去重；**提示走 device.js 自带的** **`#cc-toast`** **渲染，不要改成** **`window.toast(...)`——build.mjs 把每个 js 文件单独包进 IIFE，chat.js 顶层的** **`function toast`** **挂不上 window，全项目** **`window.toast`** **赋值为 0，调它等于静默不显示**；且须等开屏 `.hide` 后再弹，否则被 z-index 999 的 splash 盖住） | 哨兵 `correctCidFromIdb` + `if (!regStore().get('active-contact'))`（contacts.js）+ `reheatBgSwitches`（bg-keep.js）+ `const authOk = chatDbReady && authLoadedPrefix === window.activePrefix();`（chat.js）+ `localStorage 整域=`/`本机浏览器本地存储受限`（device.js，后者原 needle `__lsStatus` 两版都在、测不出提示丢失，已改锚用户可见文案）共 6 条（build.mjs FIX\_SENTINELS）/ 验证：node --check 全过；待构建后真机（该用户）：①冷启动不再回到 default 桌面、聊天记录直接可见（约 1\~2s 内自动校正）；②后台通知/保活开关不再自己变关；③诊断出现「localStorage 整域=… 非本项目… 键」可定性配额被谁占；④LS 废机启动后看到一次「已改用数据库存储」提示（该提示链路已用无头 Chromium 实测 6/6：写探针抛 `QuotaExceededError` → `__lsStatus=unwritable(...)` → splash 在场时不弹 → `.hide` 后 `#cc-toast.show` 可见、文案正确 → 2.6s 自动隐藏；同场实测页内 `typeof window.toast === 'undefined'`）。用户自救（本版本上线前）：设置→联系人切回常用桌面，563KB 历史即重新显示（下次冷启动仍会再隐藏）；建议给 mochi 换独立域名，避开同账号 Pages 共用 LS 配额                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | <br /> | <br />                                                                                                                                                                                                                     |
| 89  | 手机端（安卓 Chrome/Edge，红米/小米/三星）点击【发送】按钮后，手动按返回/点空白处收起输入法，收起那一刻明显卡顿                                                                                                                                                                                                                                                                                                                                                                                           | 根因（mobile-adapt.js 安卓键盘适配）：收起动画期 `visualViewport.resize` 高频触发 `syncAndroidKb`，其每帧写 `.phone.style.height` 后立即调 `_aPinPan()`——`_aPinPan` 读 `_aVV.offsetTop`/`scrollY` **强制同步 reflow**；叠加 `_aSchedCe`→`_aRefreshCe` 对所有 `.ce-box` 做 `void offsetHeight` 强制 reflow。每帧 resize 都触发，收起动画 \~15 帧内 \~50 次强制 reflow，主线程拥堵=卡顿。修复：新增 `_aClosing` 收起态标志（focusout 且键盘开着时置 true，focusin/复原时清 false），收起动画期 `syncAndroidKb` 只写 height 跟随 vv、**不调** **`_aPinPan`**（收起期 offsetTop 通常 \~0，平移无需逐帧归零，复原时 `_aPanComp` 统一兜底），`_aRefreshCe` 在 `_aClosing` 期跳过 ce-box reflow（收起期 ce-box 位置随 layout 自动更新合成层，无需 toggle transform）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 哨兵 `_aClosing`（build.mjs FIX\_SENTINELS）/ 验证：node --check 过；待构建后真机（红米/小米 Chrome）：聊天页输入文字→点发送→手动收起键盘→收起流畅无卡顿；弹键盘/打字/半框（问问TA/占卜）输入仍正常、文字不飞；v3.27.x「收起时输入行不飞」仍成立（复原时 \_aPanComp 兜底归零平移）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | <br /> | <br />                                                                                                                                                                                                                     |
| 90  | 与 #88 同机同报障的补刀（小米 14U Edge，LS 整库失效）：**记录被真覆盖**的通路仍在——「读库失败」被当成「这个桌面没历史」，随后一条消息就整包写回去；跨桌面追加（后台查岗/贴贴/求聊天通知回来时触发）是同款判定，一条通知即可抹掉那个桌面的全部记录（用户视角＝「几个小时自己消失」，且不需要用户动手）；导出时键清单读失败还会出一份提示「全部数据完整」的近空备份                                                                                                                                                                                                                                                             | **① idb.js**：新增严格三态探测 `idbListKeys()` / `idbHasKey(key)`（数组·true/false·null=没读到；超时/挂起统一按失败并作废连接重开），`idbGetAllKeys` 改为其兼容包装（失败退化成 `[]`，约 20 处老调用方行为零变化）；`chat-meta` 排除出 #40 写日志（防 LS 回滚把过期账本补回来）。**② chat.js**（**跨域**）：三处「读到 undefined」的复核全改走 `idbHasKey`，**只有** **`has === false`** **才认无历史**，true/null 一律按读取失败重试；跨桌面追加「读到有值却解析失败」时**绝不整包写回**；新增条数账本 `<prefix>:chat-meta` + `chatLedgerGuard` 缩水守卫（基线≥300 且新条数不足一半 → IDB 与 LS 快照都不写，并暂存 `pendingLocal` + 限流强制 `loadMsgs(true)` 重读合并；用户主动清空/整包导入会归零或对齐账本；判定全同步、零额外开销）。**③ contacts.js**：写 `active-contact=default` 前先向 IDB 确认库里确实没有（否则保持未设置，交给启动校正按权威值切，不再自己抹掉校正结果）；校正抽 `applyCidCorrection` 并支持直读 IDB（回填迟到也能切回）；default 联系人名取 `lbl-partner` 改走 xyStore。**④ data-backup.js**：导出清单读不到一律中止并如实提示（不出具假完整备份）；导入后聊天核对区分「没读到」与「确实没有」。**⑤ device.js**：新增「桌面归属体检」（三层 active-contact 并列 + 各桌面条数账本 + LS 残留快照键名，全程不 parse 大键），大键明细不再把「清单读取失败」显示成「无大键候选」                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `node tools/verify-chat-overwrite.mjs`（30 项源码结构断言，含「不得再用 `idbGetAllKeys` 判定键不存在」反模式扫描）+ 哨兵 8 条（`#90`）/ 验证：node --check 五文件全过；真机（该用户）下次报障直接看诊断「桌面归属体检」两行——`active-contact：…IDB=` 定性切错桌面，`条数账本` 定性是否被覆盖；正常路径回归：发若干条消息→切桌面/重进→记录完整、账本条数随之增长（诊断里 `【当前】<cid>=N条`）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | <br /> | <br />                                                                                                                                                                                                                     |
| 91  | iPhone 15 Plus（Safari / Edge / Chrome 三浏览器一致）：点进「系统预设字卡」可以正常滑动，但点【返回】那一刻卡住，即便卡回去了整个页面也会变得非常卡                                                                                                                                                                                                                                                                                                                                                              | 根因（headless 390×844 实测）：`default-cards.js` 把当前分类**整包铺进 DOM**——main 分类 4903 行 = 33221 个节点 / 4628 个 checkbox，全站节点 1.08 万 → 3.3\~4.4 万；①返回时各页 `MutationObserver` 的选择器扫描被膨胀文档放大（≥50ms 长任务 \[104,57]、点击到两帧 161.9ms）；②iOS 系浏览器全是 WebKit，`display:none` 会销毁数万渲染对象、再进时整棵重建，主线程直接堵死＝「返回卡住」；③返回后 33221 节点**常驻不释放**，之后每次切页都继续付税＝「整页持续卡」（二次进入仍要 524.6ms）。修复：**① default-cards.js** `mountCardView`（dc/fc/dk 三库共用）渲染改**真虚拟窗口**：数据拉平成 `flat`（`{header}` / `{c,cat}`）+ `Float64Array` 高度与前缀和 + 二分 `indexAt`，只渲染视口 ±0.8 屏（条目数下限 24），顶/底 `.cc-vspace` 占位块撑回全高（滚动范围 269786px 与旧版一致＝全量行仍可达），实测高度后按 `delta` 静默补正 scrollTop；滚动容器**动态判定**（`clipsContent` 启发 + capture 阶段 `scroll` 事件用 `e.target` 锁定，兼容 dc 页由 page 滚 / fc 列表自滚 / 窗口滚动三种形态）；rAF 合并 + containment 迟滞 + `hidden` 变化重排；跨 tab 搜索结果按真实分类 `rec.cat` 写开关（委托监听不变）。**② chatcard.js**（用户「一起修」）：`refreshLibCounts` 的 force 分支改走带缓存的 `pubGroupsRaw()`，返回字卡库不再把多 MB 公用库 `JSON.parse` 两遍。**③ chat-pages.css**：`.cc-vspace` 样式 + 把 v3.11.x「全量渲染对性能无影响」的旧结论标注为已被实测推翻（该结论正是本次踩坑依据）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 哨兵 4 条（`const V_PAD = 0.8` / `function clipsContent(` / `cc-vspace` / `countOf(pubGroupsRaw())`）+ `node tools/verify-preset-card-window.mjs`（20 项：DOM 有界／滚动范围保留／窗口随滚动推进／末条＝数据末条／可见区无占位空白／返回无长任务／二次进出不退化／单卡开关键／搜索收窄与恢复／功能字卡页同规则），新产物 **20/20**、旧产物（`git show HEAD` 对照）**12/20** 且失败项正是这四类形态。实测：进入后全站节点 10850→11284（旧 +22244）、列表子树 154\~285（旧 33221）、返回两帧 26.6ms 无长任务（旧 161.9ms）、二次进入 43.8ms / 返回 48ms（旧 524.6 / 160.1ms）。**【真机:待验证】**：iPhone 15 Plus 三浏览器复测「进预设字卡→滑动→返回」不再卡住、之后切页流畅；功能字卡页/查岗页滚动与搜索同样复核                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | <br /> | <br />                                                                                                                                                                                                                     |
| 92  | 华为 P50E（Android/Edge 151，PWA standalone，部署站 GitHub Pages）：「自定义字卡」→「公用字卡 / 专属字卡」→【表情包】添加图片，刷新重进网站后刚加的图片消失、没有保存（诊断时回复字卡池 sticker=0/image=0、IDB 大键明细无 cc-groups 键＝写入确实没落地）                                                                                                                                                                                                                                                                                   | 根因链：①字卡库带上图片后整包 JSON 常跨过 idb.js 的 **200KB 大键阈值** → `xyStore.set` 跳过 localStorage 同步快照，**只走 IndexedDB 异步 fire-and-forget 写入**（重启唯一依赖它）；②`scheduleSave` 的 **120ms 防抖**期间或 IDB 事务尚未提交时刷新/切走/切桌面，新增图片无任何备份直接丢；③chatcard.js **没有任何离页冲刷**（chat.js/group-chat.js 都有 beforeunload/visibilitychange 兜底，独缺字卡库）；④`openCcPage`/`leaveCcPageReset` 直接 `clearTimeout(editSaveTimer)`——120ms 窗口内刚上传的内容被静默丢弃；⑤切桌面时 `setActiveContact` 只在 `__activeCid` 变更前冲刷聊天（chatFlushSave），字卡库 pending 的防抖定时器切走后会把 A 桌面数据写进 B 桌面键。修复：**① chatcard.js** 新增 `ccDirty` 脏标记 + `flushCcSave()`（挂起中变更立即落盘，无变更零开销），挂 `beforeunload`/`pagehide`/`visibilitychange(hidden)` 三个离页事件（与 chat.js flushSave 同款口径）；`openCcPage`/`leaveCcPageReset` 改为先 `flushCcSave()` 再切作用域，不再清定时器丢数据；暴露 `window.ccFlushSave`。**② contacts.js** `setActiveContact` 在 `__activeCid` 变更前补调 `window.ccFlushSave()`（必须在本行之前——ccFlushSave 内 curStore 动态读 activePrefix）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 哨兵 2 条（`function flushCcSave`（chatcard.js）+ `if (window.ccFlushSave) window.ccFlushSave()`（contacts.js），build.mjs FIX\_SENTINELS）/ 验证：node --check 全过；构建哨兵 125/125、sw\.js 3/3、verify 10/10、verify-chat-overwrite 30/30、verify-preset-card-window 20/20。【真机:待验证】（该用户 P50E）：公用/专享字卡【表情包】上传图片→立即刷新/重进→图片仍在；上传后立刻切桌面→切回原桌面图片仍在、B 桌面无串数据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | <br /> | <br />                                                                                                                                                                                                                     |
| 93  | 红米 K80（Android/Chrome）：信箱里点联系人的信回了信，不显示内容；重新点这封信也看不到回信；寄出的信也不显示内容（用户补充「列表里就是空的，完全没有我写信的内容」；再补充「回信后只有等联系人回了我的回信后才显示，之前无法点击查看只有空白」）                                                                                                                                                                                                                                                                                                                         | **根因（代码推理定位）**：`mailDbReady=false`（切桌面后 idbGet 未返回/启动早期）→ `load()` 降级读剥图快照（content 空）→ `submitReply` 用该 list 设 myReply 后 `save` → `mailMergeFromIdb` 合并 IDB 完整版（content 有 + myReply 空）与快照版（content 空 + myReply 有）时，`mergeLists` 原实现按 `letterLen` 整体取更大一方 → content 或 myReply 丢失 → 点开空白；TA 回信后 partnerReply 落地使整版 letterLen 最大胜出才显示。**根本修复**：`mergeLists` 改字段级合并——content/myReply/partnerReply/read 各取更完整一方，不整体覆盖；`openLetter`/`openReply` 开头重新 `load().find(id)` 取最新完整数据（防 render list 的 l 来自剥图快照）。**防御性修复（已提交 f143621，保留）**：① submitReply 回信后补 `selectMailTab('in')`；② sendLetter 改 `showPage`+`selectMailTab` 后再 `render()`；③ openLetter tc-body 无 `.mail-paper` 重试注入；④ render() 列表项数不符重试 innerHTML                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 哨兵 1 条（`selectMailTab('in')` 在 submitReply，build.mjs FIX\_SENTINELS）/ 验证：node --check src/js/mail.js 过；待构建者 build 后跑 verify-mail-ios-reply.mjs + 真机（红米 K80 Chrome）：寄信→「寄出的信」tab 立即看到信件；回信→「收到的信」tab 立即看到来信带「已回信」标签；点开信件正文/回信内容正常显示；切桌面后回信再点开内容不丢                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | <br /> | <br />                                                                                                                                                                                                                     |
| 95  | 朋友圈动态里，联系人（或自己）发 1 张图、2 张图、多张图时，图片显示大小三档都不一样（单图又窄又高、双图最大、多图正常），列表看起来参差不齐                                                                                                                                                                                                                                                                                                                                                                                  | 根因（chat-pages.css v3.5.94 引入的 `.feed-imgs` 九宫格）：基础规则是 `grid-template-columns:repeat(3,1fr)` + `img{width:100%;aspect-ratio:1/1;object-fit:cover}`，但后面三条按张数特判把它改掉了——`.feed-imgs:has(img:only-of-type){max-width:66%}`、`.feed-imgs img:only-of-type{aspect-ratio:auto}`、`.feed-imgs:has(img:nth-of-type(2):last-of-type){grid-template-columns:repeat(2,1fr);max-width:80%}`。390×844 实测正文宽 324px 时格宽＝单图 67.3px（且 aspect-ratio:auto 让格高随原图比例自由变高，竖图高 100.9px、高宽比 1.5）／双图 126.6px／多图 104px。修复：删除这三条特判，所有张数统一按多图九宫格（每格恒 1/3 正文宽、1:1 裁切、图少时右侧留白）。**已知代价（按用户要求统一为多图档尺寸）**：单图不再是「原比例大图」，竖图/长图在列表里被裁成正方形缩略图，点开仍看完整大图（图片点击放大链路不变）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 哨兵 2 条 absent（`feed-imgs:has(`、`feed-imgs img:only-of-type`，特判被加回即报警）/ 验证：`node tools/verify-feed-img-size.mjs`（390×844 直读 src/css，测 1/2/3/4/9 图：反向对照重现 67.3/126.6/104 不一致，当前源码 5 档全 104px、高宽比全 1.00，6/6）。待构建 + 真机：同一条动态发 1/2/3 张图，格宽一致；点图仍看大图；表情包混排进图片网格后不再撑高                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | <br /> | <br />                                                                                                                                                                                                                     |
| 96  | 不同手机浏览器播放网易云歌单链接的歌曲，总是显示「点击播放被浏览器拦截，请再点一下屏幕继续播放」                                                                                                                                                                                                                                                                                                                                                                                                          | 根因：`music-player.js` 的 `startPlayback`/`toggle` 在 `audio.play()` 被 reject 时**不接收错误对象、不区分错误类型**，把所有失败都当自动播放策略拦截处理。网易云歌曲 `audio.src` 是跨域 meting URL，部分浏览器在跨域 media 数据未就绪时 `play()` 返回非 `NotAllowedError` 的 reject（`NotSupportedError`/`AbortError` 等，实为源加载失败/跨域/混合内容/meting 服务不可达），旧代码一律走 muted 静音解锁 → 仍失败 → 弹「被浏览器拦截」，吞掉了本应走的拉完整版直链/内置旋律兜底路径，且误导用户以为是浏览器问题。修复：`play().catch((err) => …)` 接收错误对象，仅 `err.name === 'NotAllowedError'`（真自动播放策略）才走 muted 静音解锁 + `handlePlayReject`；其他错误走 `retryWithHttpsUrl`（拉 https 直链重播）→ `demoFallbackOrError`（内置旋律/坏链提示），不再弹「被浏览器拦截」。`toggle`（暂停后再播）同款区分。**同批三处补强**：① `resolveNeteaseDirectUrl` 只接受 `r.redirected \|\| /^audio\\//i.test(ct)` 的响应为直链——meting 对 VIP/失效歌返回 200 空 text/html，旧代码当直链原样回投重播坏 URL，一进就「被拦截」；② 已死代理 `corsproxy.io`（返回 401 强制 API key）从网易云歌单/详情/时长/fee 四个源列表移除（vivo Y35+Edge 诊断实证只刷「网络失败 401」）；③ 播放拒绝按错误类型给提示——源加载失败不再谎报「被浏览器拦截」，改弹「在线歌曲加载失败：可能为会员歌曲、链接失效，或网络无法访问音乐源」（荣耀 x30i Wi-Fi 屏蔽音乐源的场景即靠此文案如实告知）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 哨兵 4 条（`err.name !== 'NotAllowedError'`、`r.redirected \|\| /^audio\\//i.test(ct)`、`https://corsproxy.io/?url=` absent、`在线歌曲加载失败`，build.mjs FIX\_SENTINELS）/ 验证：`node --check src/js/music-player.js` 过；`node tools/verify-music-vip-filter.mjs` 6/6（VIP 移除 + 无手势拒播不弹提示 + 手势拒播弹提示）；`node tools/verify-music-single-audio.mjs` 15/15（弱网换源不产生第二个播放器，Audio mock 的 reject 已改 `re.name='NotAllowedError'`、meting stub 带 302/audio 头匹配新校验）。真机：网易云歌曲点击播放——源正常直接出声；源失败走「正在获取完整版直链…」→ 兜底且不弹「被浏览器拦截」；本地歌曲/真自动播放场景仍走 muted 静音解锁不受影响                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                                                                                                     |
| 97  | 聊天里联系人主动发送的消息，左上角小爱心标识有莫名其妙的灰色阴影                                                                                                                                                                                                                                                                                                                                                                                                                          | `chat-main.css` `.msg-hi-heart` 删除双层 `filter:drop-shadow`（白色发光 + 黑色投影 `rgba(0,0,0,.22)`）；浅色气泡上黑色投影即用户所见灰色阴影，深色主题无此元素覆盖，一处删除即可                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 哨兵 1 条 absent（`drop-shadow(0 1px 1px rgba(0,0,0,.22))`，build.mjs FIX\_SENTINELS）/ 验证：构建后目视——联系人主动消息爱心为纯粉色、无任何投影/发光；浅色与深色主题各看一条                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | <br /> | <br />                                                                                                                                                                                                                     |
| 98  | 荣耀x60i/夸克浏览器：聊天里联系人有提问，但主页【提问记录】没显示                                                                                                                                                                                                                                                                                                                                                                                                                       | 根因：① 	a-ask.js pushAsk 发 ask-card 只写 chat-msgs 不写 ta-ask.history，history 只在 openAskReply 回答后写 → 未回答的提问不进记录；② 单选题点选项直接调 chatAskReply（chat.js:1367）不经 openAskReply → 单选题回答从不写 history（即使回答了记录也空），默认题库 11 道单选题用户大概率命中。修复：pushAsk 发卡时生成 askTs 透传进 chat-msgs 记录 + 同步写 pending history；包装 window\.chatAskReply 按 askTs 更新 pending→answered（覆盖文字题+单选题两条路径，排除 deskCk 查岗卡）；renderAskRecords 渲染 pending 显示"待回答"标签                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 哨兵 3 条（status: 'pending'/\_\_taAskReplyWrapped/	c-li-pending，build.mjs FIX\_SENTINELS）/ 验证：node --check 过；待构建 + 真机荣耀x60i/夸克：TA 提问后立即进提问记录（待回答），回答后更新为已回答（文字题+单选题）；deskCk 查岗不污染                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | <br /> | <br />                                                                                                                                                                                                                     |
| 99  | 桌面【音乐】→【联系人收藏歌曲】：我把音乐库里的歌删掉后，联系人收藏的记录也消失了（要求：删歌后收藏记录依旧保留）                                                                                                                                                                                                                                                                                                                                                                                                 | 根因：`music-favs-ta` 只存歌曲 ID 数组，渲染用 `findTrack(id).filter(Boolean)`——歌曲一删，ID 找不到对应曲目，记录整体消失成永远无法显示也无法移除的僵尸项。修复：`music-favs-ta` 改存完整快照 `{id,name,artist,neteaseId,url,cover,duration,favAt}`（addTaFav 入库时写全量）；旧纯 id 数据渲染时从库内回补快照自愈回写（renderTaFavList 迁移）；已删歌曲用快照展示 + 置灰 +「已删除」小标签 + 副行提示（可还原歌：「· 点击重新加入并播放」；不可还原：「· 文件已不在，无法播放」）；点击已删可还原歌 → 按 neteaseId/原 url 重新加入音乐库并起播（restoreTaFavSong，新条目 id 为 sm\_fav\_\* 并回写收藏条目指向新 id）；不可还原歌移除按钮仍可用                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 哨兵 2 条（`function taFavList()`、`ta-fav-gone`，build.mjs FIX\_SENTINELS）/ 验证：`node tools/verify-music-ta-fav-keep.mjs` 10/10（旧纯 id 数据自愈渲染+回写、已删歌置灰+标签+提示、删库内歌后收藏保留+点击重新入库起播+条目指向新 id、不可还原歌可移除）。真机：删掉音乐库歌曲后进「联系人收藏歌曲」记录仍在（置灰带「已删除」），点它重新入库播放                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | <br /> | <br />                                                                                                                                                                                                                     |
| 100 | Mochi 诊断信息读取中截断三修 + device 体检（vivo Y35/荣耀 x30i/iPhone 16 Pro 多台真机诊断报告「读取中…」永远不补全，报障信息缺失）                                                                                                                                                                                                                                                                                                                                                                  | ① `window.__jsErrors` 全项目无人初始化 → 功能文件启动异常被静默丢弃：新增 try 预初始化兜底；② 原 3s 单保险丝把 IDB 慢机的「最近错误/开关持久化体检/桌面归属体检/IDB 大键明细」整批截成裸「读取中…」：改软/硬双预算首屏标注（硬超时给「未完成（本机存储无响应…）」、软超时标「未读到（本机存储响应慢，稍后自动补全）」并后补）；③ 终态回填直写可见 #modal-textarea + 弹窗判活（ctl.text 的 setter 只写 hidden 的 #modal-input，回填曾静默失效）；④ 角标按最后一条错误时间戳判未读（原存条数，环形写满后永远算不出未读）；⑤ 最近错误环形上限 5→20 且调用栈只给最近 3 条（5 条窗口用户报障时早已刷掉；全带栈会把报障文本撑到剪贴板截断）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 哨兵 5 条（\_\_jsErrors 预初始化/双预算标注/modalAlive 判活/SEEN\_KEY 时间戳/ERR\_CAP=20，build.mjs FIX\_SENTINELS）；哑哨兵体检 `node tools/verify-sentinel-teeth.mjs`（逐条删 #100 修复行重建必报警且构建退出码=1）；诊断链路 18 项 `node tools/verify-diag-report.mjs`；一次性复跑全部回归脚本 `npm run verify:all`（= `node tools/verify-suite.mjs`，分 通过/断言失败/环境不满足/疑似撞车/超时 五类计数，并发跑批集中分配 `MOCHI_CDP_PORT` 防端口撞车，清单清干净后可加 `--strict` 当门禁）；runner 分类判据反向对照 `node tools/verify-suite-classify.mjs`（12 项，含「本机 ECONNREFUSED 必须判撞车而不是判需要外网」）；红项机械分类 `node tools/verify-triage.mjs --log tools/tmp-suite.log`（A 疑似漏接入 / B 期望过期 / C 运行时断言 / D 复跑转绿 / E 定位不到），其判据反向对照 `node tools/verify-triage-classify.mjs`（16 项，钉住「断言标签/测试输入回显/删除型反向断言/`join` 拼串/文件路径/helper 造元素」六类假期望过期）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | <br /> | <br />                                                                                                                                                                                                                     |
| 101 | 设置→【查看存储】看不出「大头是谁、哪个桌面占的、这些数能不能信」（用户反馈想看得更清晰）：明细是几十行流水账；展开区是 `xy-home-v2:cx1:chat-msgs` 这类机器键，用户读不出是谁的数据；总占用直接显示 `navigator.storage.estimate()` 的**整域**用量，把同域名下其他站点算进本应用头上（#88 同源）；IDB 清单读不到时退化成 `[]`，显示成「0 键」冒充「库里没有」                                                                                                                                                                                                                                    | ① 明细只列占用最大的 5 类 + 占比条（条长按**平方根**比例：实测某类占 94% 时线性条把第 3\~6 名全压到 1.5% 下限、彼此分不出座次；最大项仍满格，精确份额看行末百分比数字），其余折成「其他 N 项合计」，点开逐类列名列大小（核对覆盖没有变难；聚合行**刻意不画条**——多类加总与单类不同口径，画了会看着比第 4、5 名更大）；② 展开区键名走 `deskNames()`+`labelKey()`，per-cid 命名空间换成「桌面名 · 键名」（`window.getContacts()` 的 cid→name），键数超出列出上限时如实标注「共 N 个键，仅列前 M 个」；③ 总占用拆双口径分行：「本项目占用合计」（本页面统计到的 LS+IDB）／·localStorage／·IndexedDB／「同域其他站点（非本应用）」／「浏览器整域已用 · 配额」，`lsStats` 顺带统计非本项目前缀键，页头补一句口径说明；④ IDB 键清单改走 #90 严格三态 `window.idbListKeys`（读不到＝null，不再 `idbGetAllKeys` 退化成 `[]`），失败时明细顶部红条告警 + IndexedDB 行与合计都标「未计入」；⑤ 文案口径修正：估算大小写清按字符数 ×2（UTF-16）不是字节数                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 哨兵 5 条（`function pctOf`／`function labelKey`／`window.idbListKeys \|\| window.idbGetAllKeys`／产物「本项目占用合计」／`css/setting.css` 的 `.storage-cat-bar`）；`node tools/verify-storage-clarity.mjs` 20/20（无头 Chrome 打**产物**：播种两桌面+多分类+25 键分类+同域非本项目键，验 Top5 与聚合行、条长平方根且递减、桌面名键名与截断标注、双口径自洽 self≈LS+IDB、monkey-patch `idbListKeys→null` 验告警链、全程无 JS 错误）；`npm run verify` 10/10。**反向对照**：把「其他合计」行也画条 → A3/A6 复现（聚合行比第 4、5 名条更长、座次反了）；needle 换回注释文字 → 新增的「针在注释里」哑哨兵体检立即报出                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | <br /> | <br />                                                                                                                                                                                                                     |
| 102 | 群聊里使用【群聊决定】【帮我决定】，答案却发到了【聊天】里（而不是正常发送到群聊）                                                                                                                                                                                                                                                                                                                                                                                                                 | 根因：两个决定面板（chat-decision-panel/chat-gdecision-panel）嵌套在 page-chat（单人聊天页）内；群聊更多面板点这两项时 group-chat.js 捕获监听一律切到聊天页，且 decision.js/group-decision.js 结果统一走 chatAddIn 发到聊天消息列表。修复：① template.html 把两个面板移到 .phone 级（.poke-card 为 absolute 相对 .phone，聊天页/群聊页共用，z-index:70 高于 .page）；② group-chat.js 群聊更多面板点 more-decide/more-gdecide 不再切聊天页；③ group-chat.js 新增 gcSendDecisionText（结果作为 special:'system' 系统消息写入群聊 msgs 并渲染，居中 msg-poke 样式、换行转 br，持久化刷新仍在）+ gcIsVisible（判 page-group-chat 可见）挂 window；④ decision.js/group-decision.js openPanel 记录 panelFromGroup=gcIsVisible()，发送时群聊上下文→gcSendDecisionText，否则 chatAddIn；聊天页打开两功能行为不变（仍发到聊天）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 哨兵 `gcSendDecisionText`（build.mjs FIX\_SENTINELS）/ 验证：node --check 过；待构建后真机：群聊更多→帮我决定/多人决定→出结果，答案应显示在群聊消息流；聊天页更多→帮我决定→结果仍发到聊天                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | <br /> | <br />                                                                                                                                                                                                                     |
| 103 | OPPO Find X9 Chrome：数据导出一导出就崩溃/导不出来（本地数据约 100MB：IDB chat-msgs 55.6MB + cc-groups 40MB + emoji-groups 15.7MB）                                                                                                                                                                                                                                                                                                                                              | 根因（data-backup.js）：导出尾段 `JSON.stringify(data)` 把全部数据一把生成整个 JSON 字符串——大备份设备上「源数据 + 整包字符串 + stringify 内部缓冲」峰值内存近 2 倍文件体积，Chrome 安卓标签页 OOM 直接崩溃（同类：#73 iOS Safari 闪退 / #82 小米 14U Edge）。次要嫌疑：Blob→base64 先拼整块二进制串再 btoa（内存翻倍）+ `String.fromCharCode.apply(…,0x8000 个参数)` 栈溢出；`navigator.share({files})` 分享 100MB+ 文件复制进 intent。修复：① `jsonToBlobStreaming` 逐键序列化、每 \~4MB 合并进 Blob（Blob 拼接是引用不复制）、每序列化完一个大键立即 delete 释放、每 4 键 setTimeout(0) 让出主线程——峰值内存约砍半且不再有整包字符串；② `blobToBase64` 按 3 的倍数分块 btoa，去掉 apply 与巨型二进制临时串；③ `saveBackupFile` 分享面板加 50MB 上限，大备份统一走「确定后下载」；④ `exportCoverage` 超大键（>1MB）不再整包 JSON.parse 统计，只标「✓有（数据较大）」                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 哨兵 `jsonToBlobStreaming`（build.mjs FIX\_SENTINELS）/ 验证：node --check 过；build 哨兵 156/156 + sw\.js 3/3；npm run verify 10/10；tools/check-databackup.mjs 7/7（无头 Chrome 点击导出/导入无异常）；单元测试：jsonToBlobStreaming 输出与 JSON.stringify(data) 逐字节等价（含中文/emoji/引号/大 base64）+ blobToBase64 与标准 btoa 各长度一致。【真机:待验证】（OPPO Find X9）：导出大备份进度条正常走完→弹「备份已打包完成」→点确定能下载完整文件；导出前后本地数据无变化                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | <br /> | <br />                                                                                                                                                                                                                     |
| 104 | vivo X200s（V2458A）Android 16 + Edge 151：点【导出数据】一直停在「正在打包数据文件」，永远出不来文件。诊断实证：IDB 候选键合计约 806.8MB（default:chat-msgs 514.2MB、cc-groups-public 396.1MB、fav-msgs 28.3MB、15 个 music-file 各约 10MB），配额已用 970.9MB，LS 仅 1.9MB，未处理 promise 报 `RangeError: Invalid string length at doExport`（且「已是最新」——线上产物只到 #103 的逐键 stringify，未推）                                                                                                                                    | 根因三层叠加（data-backup.js）：① #103 只做到「逐键 stringify」，单个大键仍要一次分配约 2.57 亿字符（chat-msgs 514MB 是 UTF-16 字节数），加上 800MB 全量对象图在读完前常驻 → 跨不过 V8 64 位单串上限 kMaxLength 约 5.37 亿字符 → RangeError；② 入口是裸调用 `doExport()`，异常变成未处理 promise rejection，`impHide()` 永不执行 → 遮罩冻在「正在打包」（＝用户看到的现象），错误只在诊断里看得见；③ 路由用的 `byteLen(非字符串)` 内部又整包 stringify 一遍只为量长度＝再复制一份大键。修复：①`createJsonPack` 加 `packValue` 加 `packString`：值内按 `PACK_DEPTH` 逐元素下钻、超 `PACK_SLICE`（1M 字符）的字符串分片转义（代理对不劈开）、每 \~4MB 合并进 Blob、每 `PACK_YIELD` 片段让出主线程，任何时刻内存里最多一个大键且单片段恒 ≤1M 字符；②`readNext()` 逐键「读→就地序列化→立即释放」，`own` 标志确保只有 IDB 新读出的私有副本会被改写（memoryCache、LS 共用值一字不动，防把业务数据写坏）；③入口改 `Promise.resolve().then(askExportMode).then(doExport).catch(...)` 且 `doExport` 内 try/catch → 遮罩必定收起，`reportExportError` 如实报出错环节、键名、体积，`string length` 类直接指路更小范围；④`overSmallLimit` 廉价浅判（超阈值早退，绝不为量长度 stringify）替代 byteLen；⑤`askExportMode` 本机用量超 150MB 才弹备份范围（完整、不含音乐文件、只备份文字、取消，`lock` 强制做选择），成品超 120MB 如实提示「新设备可能导不回来」，完成文案按模式措辞（精简备份不再谎称「全部数据完整」），导入侧读大文件按错误类型给文案（不再谎报「无效的数据文件」）；⑥覆盖清单改 `cover.see` 逐键累加（打包后数据已释放，不再二次遍历），同键以 IDB 权威值覆盖 LS 有损快照                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 哨兵 6 条（`createJsonPack`、`function overSmallLimit(v, limit)`、`reportExportError`、`askExportMode`、`这份备份太大，本机读不进去`，加 absent 守卫 `doExport();`＝裸调用回来看见即失败）。专项脚本 `tools/verify-jsonpack.mjs`（28 项断言，从源码切出打包器直接测）：与 `JSON.stringify` **逐字节等价**（中文、emoji、引号反斜杠换行控制字符、U+2028/9、3M 字符超长串分片、代理对正好落在分片边界、undefined 与函数属性省略、Date/Map/Set/TypedArray 不拍平、NaN 与 Infinity → null、深嵌套越界、整份备份文件总装），加内存有界断言（40M 字符数据全程单片段 ≤1M、own=true 写完即释放、own=false 源值零改动、overSmallLimit 零 stringify 调用）。npm run verify:all 会自动带上本脚本；check-databackup.mjs 覆盖导出导入入口点击无异常。【真机:待验证】（vivo X200s Edge 构建后）：点【导出数据】→ 弹出范围选择（选「不含音乐文件」与「只备份文字」各试一次）→ 进度条走完 → 弹「备份已打包完成」→ 点确定能落盘；导出前后本机数据零变化；选「完整备份」若仍失败必须弹窗说明卡在哪一步而不是冻住。已知边界：「只备份文字」只剥已解析成对象/数组的值里的 base64，旧版本把整段 JSON 当文本存的大键内 dataURL 仍会保留（这类键体积可能不如预期小，需要时先清理再导）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | <br /> | <br />                                                                                                                                                                                                                     |
| 105 | 钓鱼小游戏「今日收获」里【你】和【TA】两栏各有一个「留」复选框，但**只想留 TA 钓的那条做不到**——只要品种相同，我这侧的鱼也被一起当成「留」不卖（用户反馈：「我只想留他钓的鱼，但品种一样就必须把我的也一起留下，这个是 bug 吗」）。附带：勾完「留」后底部「可出售 N 件 · +¥」不变，要等下一次重绘才更新                                                                                                                                                                                                                                                                                          | 根因（fishing.js）：`keep` 只按品种记一个开关（`keep[鱼id]`），两栏共用同一份、`sellAll` 在 `['mine','ta']` 循环里 `if (keep[id]) return;` 一次判定同时跳过两侧 → UI 给了两行独立复选框，状态层却没有归属维度。修复：① keep 键改带归属 `keepKey(side, id)`（`mine:<id>` / `ta:<id>`），渲染按行读 `keep[keepKey(side,id)]`、复选框带 `data-side`、事件按 `data-side+data-id` 写；② `loadToday` 内经 `normalizeToday` 把旧纯品种键（键不含 `:`；鱼 id 均为 `fish_xxx`/`sp_xxx`/`gift_xxx` 不含冒号，可安全判旧）一次性展开到两侧＝原「同品种两侧都不卖」语义，用户零感知，首次交互落盘后旧键自然被替换掉；③ 复选框 change 落盘后补 `render()`，合计即时同步；④ 出售落账后清掉该侧已无存货的残留「留」标记（否则同品种当天再钓到会被上次遗留的标记自动置留）。纪念品（`cat:'gift'`）本就不参与出售、不渲染复选框，行为不变                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 哨兵 5 条（`function keepKey(side, id)`／`if (keep[keepKey(side, id)]) return;`／`keep[keepKey('mine', k)] = 1`／`keepKey(cb.getAttribute('data-side')`／`if (!t[side] \|\| !t[side][id]) delete t.keep[k];`，均在 `js/fishing.js` 内唯一）。专项脚本 `node tools/verify-fishing-keep.mjs` **17/17**（静态 6 条含反向断言「旧 `if (keep[id]) return;` 已清除」；运行时用无头 Chrome 打自组装 src 页面真点复选框：只勾 TA 栏→存储只有 `ta:fish_small` 且我栏不受牵连、合计 5 件→2 件即时更新；出售只清我这侧，TA 留的 3 条与入账 400 分都精确；残留标记清理；旧纯品种键自愈展开且取消后不回灌；纪念品行无复选框）。运行时把 `window.setInterval` 打桩，防 TA 状态机随机加鱼让件数断言飘。**反向对照**：`sellAll` 判定退回 `keep[id]` → A1/A2 立即报红并中止（已实测）。npm run verify:all 会自动带上本脚本。已知边界（未做，需时再提）：同一侧同品种只记份数（`t.mine[id]=n`），「留」＝那一栏该品种整批留，做不到「5 条里留 2 条」；`cookMine` 烹饪取鱼不认「留」标记，会直接扣份数。【真机:待验证】：钓鱼→今日收获→只勾 TA 栏某品种→出售，我的同品种应被卖掉、TA 的保留                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | <br /> | <br />                                                                                                                                                                                                                     |
| 106 | 贪吃蛇**全屏**下（手机端进游戏即自动全屏）赢/输之后想再来一局：①整体格子偏大，得先把画面缩小才点得到【再来一局】；②【再来一局】按钮显示不完全（小米15 Pro + Chrome，视口 384×752）                                                                                                                                                                                                                                                                                                                                                  | 三处叠加：**A** 全屏结算从不重铺画布——`showResult` 只调 `refitNonFs`，而它 `if (…\|\| isFs) return` 在全屏下直接早退，地图原尺寸顶着结算块把按钮挤出屏（＝现象①）；**B** 高度预算漏算 `.snake-fs` 的 `gap:min(2vh,2vw)`，360/384/390/412 宽**空闲态**就已溢出 17～29px（＝现象②，与结算无关），同时删掉 `Math.max(240,·/260,·)` 虚报（极矮/横屏量到多少用多少）；**C** 量算误差（字号换行、gap 取整、字体渲染）无法归零 → `applyCell` 用 `scrollHeight - clientHeight` 实际溢出自查回退最多 4 轮，格子下限 9px；配套 `scrollAvail()` 统一两处重复量算、`refitAll()` 在结算/开始/空闲三处显隐后重铺、`renderBest()` 提前到 `toggleFs()` 之前（晚一行量不到最长纪录行）、CSS 兜底 `overflow:hidden auto`（横屏/极矮屏滚得到，canvas 有 `touch-action:none` 不影响滑动控制）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `tools/verify-snake-fs-result.mjs`（无头 Chrome：384×752 / 360×640 / 390×844 / 412×892 严格 + 752×384 横屏兜底 + 半框对照，逐视口断言 空闲/结算 按钮完整可见、`elementFromPoint` 命中按钮本体、画布非空白、结算地图 < 空闲地图、点按钮真开新局）。**反向对照已实测**：HEAD 旧源构建＝20/41（溢出 184～230px、`hit=offscreen`），修复后 41/41。哨兵 4 条（`js/snake-game.js` ×3 + `css/chat-pages.css` ×1）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | <br /> | <br />                                                                                                                                                                                                                     |
| 108 | 华为 Mate 40 Pro + Edge：清理会员歌曲（音乐日程卡片→自动清理）一直「无法自动清理会员歌曲，显示网络不可用」。诊断实证：`navigator.onLine=true`、存储/服务正常，仅两条 `https://proxy.cors.sh/…/api/song/detail…` 返回 **HTTP 520**                                                                                                                                                                                                                                                                                       | 不是用户断网，是「清理会员歌曲」依赖的**第三方 CORS 代理瞬时故障**（`proxy.cors.sh` 偶发 520 源站波动），而代码：① 排最前的 `api/v6/song/detail` 接口早已失效（实测返回 `{"code":404,"message":"接口未找到！"}`，每次白等 6s）；② 后备代理 `allorigins`/`codetabs`/`cors-anywhere` 现全部超时、`corsproxy.io` 需 API key——等于无可靠兜底。修复（music-player.js）：① 弃用失效 v6 接口，只用 legacy `api/song/detail/?ids=`（实测经 proxy.cors.sh 直达返回 fee=1/4）；② `fetchNeteaseFees` 重构为 `job/pr/retryLeft`：代理 `5xx/429` 视为瞬时抖动，`400ms` 后同代理自动重试 1 次再轮换；③ 失败文案由「网络不可用」改为「网易云查询服务暂不可用，请稍后重试」，不再误导用户以为断网。全程绝不把「查不到」当「免费」误删（维持原防误删语义）。【2026-09-07 #254 升级取代】proxy.cors.sh 域名 DNS 已注销（ENOTFOUND）+allorigins 522——代理重试救不回死域名，legacy fee 查询链路整体移除，本修复由 #254（meting 播放同源逐首探测，防误删语义不变）完整继承；哨兵 needle 随之同步                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 哨兵（build.mjs FIX\_SENTINELS）：`r.status >= 500 \|\| r.status === 429) throw { retry: true, msg: 'HTTP ' + r.status }`（music-player.js 内唯一）+ 文案「网易云查询服务暂不可用」。`node --check` 过；待构建后构建哨兵全绿 + 真机（华为 Mate 40 Pro Edge）音乐日程卡片→清理会员歌曲：有会员歌时应能列出可移除清单；代理 520 时不应再冒「网络不可用」，应重试后出结果或如实提示「查询服务暂不可用」                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                                                                                                     |
| 107 | iQOO Neo10 + Chrome（Android 16 / 视口 360×740 / DPR 3.5）：群聊的气泡和字体**不管怎么换颜色都是纯黑的**（换色无效、非群聊功能代码问题）                                                                                                                                                                                                                                                                                                                                                           | 根因定位：group-chat.js 的 `applyGcBeauty` 用 CSS 变量（`--msg-in-bg/ink/out-*`）内联写在 `#page-group-chat` 上，机制本身在无头 Chrome（浅色/深色/换色）实测完全正常（换 `out-bg` 为粉 → 气泡即变粉）；且该现象只在**这台机型**出现＝设备/系统环境因素，而非通用代码回归。最可疑元凶＝**安卓 Chrome 强制深色（Auto Dark）**：本 app 深色由 `data-theme` 手动开关、不跟随系统，浅色态 `:root` 未声明 `color-scheme`；当手机系统处于深色时，Chrome Auto Dark 会在系统深色下**无视网页配色**把内容整体压成纯黑（与「换色无效、全部纯黑、仅此机」完全吻合，且无法在无头桌面复现）。修复：base.css `:root` 显式声明 `color-scheme:light`（主动告知浏览器「本页为浅色设计、深色由本应用手动管理」，退出 Chrome 强制深色覆盖）；`[data-theme="dark"]` 同步补 `color-scheme:dark`（优先级靠后覆盖 :root，保证应用自己切深色时原生控件仍随深色）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 哨兵 `color-scheme:light`（css/base.css，build.mjs FIX\_SENTINELS）。验证：build 哑哨兵体检 0 条；npm run verify 10/10；临时无头复测——浅色 out=#111 底/白字、in=白底/#111 字，换 `out-bg` 粉→立即变粉，深色 data-theme=dark 下 out=#3a3a3a/白字、in=#2a2a2a/#f0f0f0 均正常。【真机:待验证】验收（iQOO Neo10）：① 若手机系统开深色、app 显示浅色 → 本版后群聊气泡/字体应恢复正常且能换色；② 若仍纯黑，请在群聊设置→美化里检查是否设置了**自定义气泡 CSS**（带 `!important` 会绕过颜色开关），清空即可                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | <br /> | <br />                                                                                                                                                                                                                     |
| 109 | 苹果17 自带浏览器 + **全屏模式**：不只聊天页，**其他所有功能页整体上移**，顶部内容/返回按钮被推出屏外点不到                                                                                                                                                                                                                                                                                                                                                                                             | 根因（base.css）：`.phone` 高度在 iOS 全屏/浏览器态由 `mobile-adapt.js` 实测的 `--mochi-ios-h`（=visualViewport.height，缩放后）接管；**该值在个别 iOS 版本 / 全屏过渡 / 工具条显隐时机超过** **`100dvh`**，而 `html,body` 是 `display:flex; align-items:center`——`.phone` 比视口高时 flex 居中把它顶部推到负值，整页上移（探针实测：h=900/950 在 844 视口下 `.phone.top` 为 -28/-53）。这是 `.phone` 级问题，**覆盖聊天页在内的所有功能页**。修复：两条 `--mochi-ios-h` 高度规则加 `min(var(--mochi-ios-h,100dvh), 100dvh)` 钳到 ≤视口（① 非 standalone 浏览器态 `html.ios-vv-fit:not(.ios-pwa-standalone) .phone`；② standalone+全屏 `.ios-pwa-standalone.ios-fs-active .phone`）。键盘期不受影响（该期摘除 `--mochi-ios-h` + 内联 height 接管）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 哨兵 2 条（build.mjs FIX\_SENTINELS，needle 各带完整选择器保证唯一）：`html.tablet.ios-vv-fit:not(.ios-pwa-standalone) .phone { height:min(var(--mochi-ios-h, 100dvh), 100dvh)` / `html.tablet.ios-pwa-standalone.ios-fs-active .phone { height:min(var(--mochi-ios-h, 100dvh), 100dvh)`。探针 `tools/tmp-fs-shift-probe.mjs` 验证机制（uncapped 负 top → capped 归 0）。【真机:待验证】（苹果17 自带浏览器+全屏）：聊天 / 字卡库 / 日历 / 设置 等任意功能页顶部栏与返回按钮均应完整可见可点；若仍上移请反馈具体是哪个页面                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | <br /> | <br />                                                                                                                                                                                                                     |
| 110 | 苹果17 自带浏览器 + **全屏模式**：聊天页 `--mochi-ios-h`（=visualViewport.height）个别全屏过渡/工具条显隐时机**低于** `100dvh`，`#109` 加的 `min(...)` 取到偏小值 → `.phone` 被压矮，底部聊天输入栏**整体偏上、不贴合手机底部**                                                                                                                                                                                                                                                                                          | 它的孪生场景：`--mochi-ios-h` 不在「偏大」（#109 整页上移）而是「偏小」（本行输入栏上浮）。修复（mobile-adapt.js `syncVvFit`）：**全屏态一律摘除** **`--mochi-ios-h`**、让 CSS 回退 `100dvh` 填满全屏——全屏下 `100dvh` 就是整块可视高，不需要也不该用实测值；判定类 `fs-active` / `fs-css-active` / `ios-fs-active` / `ios-native-fs` 任一命中即摘除返回。摘除后高度精确=`100dvh`（不超过视口，`#109` 的整页上移不会复发，两场景一并收敛）。键盘期逻辑不变（该期本就摘除）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 哨兵 1 条（build.mjs FIX\_SENTINELS，needle 在 mobile-adapt.js 内唯一）：\`d.classList.contains('ios-fs-active')                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | <br /> | d.classList.contains('ios-native-fs')`。【真机:待验证】（苹果17 自带浏览器+全屏）：进入聊天 → 底部输入栏应贴合手机底部；再退回桌面/进设置等页面 → 顶部栏与返回按钮仍完整可见可点（#109 不复现）。已有 #109 探针 ` tools/tmp-fs-shift-probe.mjs`理论仍适用（本修复后`.phone`不再用实测高、恒为`100dvh\`，top 恒 0）        |
| 111 | 苹果16 添加到主屏幕 + **全屏模式**：桌面顶部「Mochi / 时间 / 电量」那一行不见了、被遮挡                                                                                                                                                                                                                                                                                                                                                                                                    | 根因（base.css）：iOS 全屏模式（`.ios-fs-active`）原设计隐藏应用内模拟状态栏（`display:none`）把内容顶到系统状态栏下方、避免与 iOS 系统栏重复——但桌面顶部那一行是全站品牌信息，隐藏后用户只见空顶。修复：全屏态**不再隐藏状态栏**（删 `.ios-fs-active .phone .statusbar { display:none }`），同时删掉同规则的 `.ios-fs-active .phone { padding-top:max(env-safe-area,12px) }`——状态栏自身已带 `env(safe-area-inset-top)` 安全区留白（见手机端 `.statusbar` 规则），两者叠加会双倍白带。`.phone` 高度相关规则（#109/#110 那几条 `min(...)`/`100dvh`）不动                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 哨兵 1 条（build.mjs FIX\_SENTINELS，needle 在 base.css 内唯一）：`改为全屏时也保留状态栏。状态栏自身已带`。验收：构建哨兵全绿；无头探针 `tools/tmp-wp.mjs` 强制 `ios-pwa-standalone ios-vv-fit ios-fs-active` + 注入壁纸——`.statusbar` 由 `display:none` → `display:flex`（rect 高 33px），`.phone` padding-top 为 0；桌面首页顶部品牌行可见；底部 `.phone` 仍铺满全屏、壁纸不露白边，`ios-fs-active` 下 `.phone` 高度仍由 100dvh 填满（#109/#110 不复现）。【真机:待验证】（苹果16 添加到主屏幕+全屏）：桌面顶部应能看到 Mochi/时间/电量一行（iPhone 系统自带的状态栏会显示在其上方，形成上下两栏——用户已确认接受）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | <br /> | <br />                                                                                                                                                                                                                     |
| 112 | iPhone14 Safari 添加到主屏幕（standalone、**未开全屏**）：顶部被遮挡                                                                                                                                                                                                                                                                                                                                                                                                         | 根因（base.css）：`html.ios-pwa-standalone .phone` 这条普通 standalone 高度规则仍是裸 `height:100vh`——#109 的 `min()` 钳制只加了另外两条路径，漏了它。iOS standalone 下 `100vh`＝整屏高（iPhone14 实测 932px 含状态栏），而可视区（visualViewport）只有 873px → `.phone` 超出视口、`html,body` flex 居中把顶部推出负值（实测 top=-29、bottom=903）→ 顶栏/返回按钮被推出屏外点不到，底部也越界。修复：`height:min(100vh, var(--mochi-ios-h, 100dvh))`，与 #109/#110 同套 `min()` 钳制（`--mochi-ios-h`=syncVvFit 实测可视高、键盘期被摘除；不设时回退 100dvh），`.phone` 恒填满可视区不溢出                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 哨兵 1 条（build.mjs FIX\_SENTINELS，needle 在 base.css 内唯一）：`html.tablet.ios-pwa-standalone .phone { height:min(100vh, var(--mochi-ios-h, 100dvh)`。诊断实证（用户三台真机报告）：iPhone13/17 standalone+全屏（ios-fs-active）top=0 正常、iPhone14 standalone 未全屏 top=-29 复现。待构建+真机（iPhone14 添加到主屏幕、不开全屏）：桌面/任意功能页顶部栏与返回按钮应完整可见可点；iPhone13/17 已由 #111 覆盖（未推送），推后一并复测                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | <br /> | <br />                                                                                                                                                                                                                     |
| 113 | 【诊断信息】打开时弹出手机输入法又收起 → 灰屏                                                                                                                                                                                                                                                                                                                                                                                                                                  | 根因（device.js）：诊断弹窗**打开即自动复制**——`copyText()` 会临时建隐藏 textarea 并 `ta.focus()`（#键盘触发）、800ms 后随元素移除又收起，手机上表现为「输入法弹起→收起 + 灰屏」；且手机剪贴板有字数上限，打开就自动写长诊断会被静默截断。修复：**取消自动复制**（删 `autoCopy` 及终态 `copied` 判定），打开只读文本不再碰剪贴板、不再 focus textarea；需要发给开发者时由用户点【复制】/【导出txt】自行触发（导出 txt 不受字数上限影响）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 哨兵 1 条（build.mjs FIX\_SENTINELS，needle 在 device.js 内唯一）：`打开诊断就自动写长文本会被静默截断`。验收：手机上打开设置→诊断信息 → 不再弹输入法、无灰屏；正文照常读出更新；点【复制】/【导出txt】仍可用                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | <br /> | <br />                                                                                                                                                                                                                     |
| 114 | iPhone 12 Pro Max Chrome / 主屏幕打开 + 全屏模式：桌面顶部「Mochi / 时间 / 电量」这一行与 iPhone 系统状态栏（时间/电量）**重叠**（用户实测：系统栏在上、模拟栏在下应成两栏，实际两行叠在一起）。**v3.28.x 复发**（iPhone 15 + Safari 主屏幕全屏，诊断 v3.26.387）：同症状 + 聊天页状态栏返回键点不了 + 通话缩略窗卡顶部拖不动 + 聊天底部输入栏与手机底部空隙过大——原 #114 修法（`html.ios-fs-active .phone .statusbar` 加 `env(safe-area-inset-top)` 顶部留白）在该环境 **env(safe-area-inset-top) 实测返回 0**，且 `100dvh` 只算状态栏下方（物理屏 852 vs 可视高 793）→ .phone 顶部 59px 钻进系统状态栏区（重叠/返回键被吞点）、底部空 59px（空隙） | 根因（base.css）：窄屏 `@media(max-width:900px)` 里的 `.statusbar { padding-top:max(calc(14px + env(safe-area-inset-top,0px)),14px) }`（特异性 0,1,0）被**后加载的同特异性全局规则** `.statusbar { padding:4px 4px 12px }` 覆盖 → 全屏态模拟状态栏的 safe-area 顶部留白全部失效、内容顶到 y=0，与系统状态栏重叠（探针实测 padTop=4px）。**v3.28.x 修复（统一法，不再依赖 env()）**：① CSS：`.ios-pwa-standalone.ios-fs-active .phone` 改 `height:100vh`（铺满物理屏，消除底部 59px 空隙）+ `padding-top:max(var(--mochi-safe-top, env(safe-area-inset-top,0px)),12px)`（.phone 整体下移到系统状态栏下方）+ `padding-bottom:0`；html/body 同模式钉 100vh 防纵向可滚；`html.ios-fs-active .phone .statusbar` 顶部留白改回常态 14px（安全区由 .phone padding 统一承担，避免双倍白带）；statusbar/chat-head 的 env() 顶部留白全改 `var(--mochi-safe-top, env(...))`。② JS（mobile-adapt.js syncVvFit）：iOS standalone 下用 `screen.height - visualViewport.height`（范围 20-160 过滤）实测系统状态栏高度写 `--mochi-safe-top`（iPhone15 实测 59px）。③ JS（call.js）：通话缩略窗落位/拖拽上边界抬到 `miniSafeTop()`（系统状态栏下方），旧存档在状态栏区的自动下移，修「缩略窗在顶部动不了」（触点被系统栏吞）。iPhone17 Edge「图标显示不全」根因同为缺顶部安全区补偿，本规则一并修复                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 哨兵 3 条（build.mjs FIX\_SENTINELS）：`padding-top:max(var(--mochi-safe-top, env(safe-area-inset-top, 0px)), 12px);`（base.css）/ `d.style.setProperty('--mochi-safe-top'`（mobile-adapt.js）/ `y = Math.max(miniSafeTop(), Math.min(window.innerHeight - mh - 4, y))`（call.js）。【真机:待验证】（iPhone 15 Safari 主屏幕全屏）：①桌面顶部模拟状态栏显示在系统状态栏正下方不重叠；②聊天页返回键可点；③通话缩略窗可拖动且拖不出系统状态栏区；④聊天底部输入栏贴底（仅 Home 指示条避让）；iPhone 12PM / iPhone17 Edge 复测不复发                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | <br /> | <br />                                                                                                                                                                                                                     |
| 115 | 红米 K60 至尊版 + Edge 151（Android 16，视口 406×739 DPR3）：聊天页**输入栏打字不显示、始终空白**，导致无法发送消息（用户确认：任意文字都空白，不只重复短句）。诊断佐证：v3.26.380、键盘/锁残留三行全 `n/a`、聚焦元素 `div#chat-input.chat-input`、`default:chat-msgs` 142.6MB、帧率采样 18fps                                                                                                                                                                                                                                               | 该机型无法远程复现，按「字没进来 / 进来被清 / 进来没画」三条独立路径各堵一处 + 决定性埋点收口。**A 进来被清（已证实的真实缺陷，chat.js）**：v3.14 防复活守卫（`_mClearTxt` + 200/800ms 迟到兜底 + input 监听）只比「框内内容是否与刚发送文本完全一致」，用户发完短句立刻用输入法**整段上屏**重打同一条（「好的」「在吗」这类回复必撞）→ 上屏瞬间被静默清空，表现正是「打字不显示」。修复：新增 `lastUserEditAt` / `clearAppliedAt` / `userEditedAfterClear()` 真实编辑闸门，由 `keydown` / `compositionstart` / `beforeinput`（`insert*`）三类活动打点，三处守卫（input 监听、\[200,800] 兜底复查、`addMsg` 防重发）一律加判；无输入活动的内核迟到写回仍照清（v3.14 语义不退化），防双击连发仍生效。**B 进来没画（base.css）**：键盘期 `.phone` 被 `syncAndroidKb` 改 height + `alignSelf`、`_aPanComp` 写 `top`，而聊天输入栏是模板原生 contenteditable、**不走 ceConvert**，拿不到 `.ce-box` 那层合成层保护 → 重排/平移后文字画进失效旧层，DOM 有字、屏上空白（同款已证实手法见 chat-pages.css `.ta-add .ce-box:focus`）。补 `.phone .chat-input { will-change:transform }`（常驻，键盘平移前层就已存在）+ `.phone .chat-input:focus { transform:translateZ(0) }`，群聊 `#gc-input` 共用 `.chat-input` 类一并命中。**C 进来滚出视野（mobile-adapt.js）**：contenteditable 自身有内部滚动，键盘平移/收起后 `scrollTop` 残留会把文字裁出自家裁剪盒，框看着是空的。新增 `healEditableScroll()`（`scrollHeight` 不高于 `clientHeight+1` 且 `scrollTop>0` 才归零，多行真滚动不动），挂 `input` 捕获 + `nudgeInputVisible()`。**D 决定性埋点（device.js + mobile-adapt.js）**：诊断新增「聊天输入栏现场」一行（元素/聚焦/contenteditable/文本长/HTML长/内部滚动/颜色/底色/caret/opacity/visibility/fontSize/transform 是否提升/待清守卫/框 top-bottom/是否被键盘盖）＋输入轨迹环形缓冲（`xy-home-v2:__diag-inp`，focusin/compositionstart/compositionend/input 各记文本长与滚动，cap 8）＋`window.__mochiAndroidKb()` 安卓键盘探针并入 `mochiVvDiag().kb`（原先只有 iOS 有 kb，安卓报 `n/a`）                                                                                                                                                                                                             | `tools/verify-chat-input-guard.mjs` **17/17**（①a-d 合成层：产物两条规则 + computed `willChange=transform` + CSSOM 里 `:focus` translateZ 规则 + 群聊输入栏同样命中；②a-d 整段 IME 上屏重打同一条短句不被吞；③a-b 无输入活动的迟到写回仍被清＝v3.14 语义未退化；④ 双击连点只落一条；⑤a-b 内部滚动残留归零 / 多行真滚动不误伤；⑥a-c 探针并入诊断 + 轨迹已记录 + 诊断行已接入；⑦ 全程无新增 JS 异常）。**双向反向对照已实测**：闸门强制 `return false`（≈修复前）→ ②c/②d FAIL（文本被清空）；强制 `return true`（等于关掉防复活）→ ③b FAIL。②c 特意走 composition 整段提交——逐键 ASCII 输入第一个字符就走进 else 分支摘掉 `_mClearTxt`，永远测不到吞字判据。哨兵 10 条（另回填 v3.14 `document.execCommand('selectAll', false, null)` 那条，此前整块零保护）。哨兵有牙已验证：删掉 `userEditedAfterClear` 定义行 → 构建 exit 1 并如实报「src 里也没有＝修复真丢了」。**【真机:待验证】验收（红米 K60 至尊版 + Edge）**：输入栏打字应正常显示、能发送；若仍空白请**再发一次诊断信息**，按新增两行一次定分支——「输入轨迹」`n` 恒 0＝字根本没进来；`n` 涨过又掉回 0＝进来被清（A 未覆盖的路径）；文本长>0 且 transform=已提升＝进来没画（B 的层提升不够，需换 `-webkit-backface-visibility` 等手法）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | <br /> | <br />                                                                                                                                                                                                                     |
| 116 | 【花园·工坊】做不了花艺配方的花（用户反馈）。排查确认合成主链路无 bug（headless 实测：材料够→按钮出现→点击扣料/记成就/发花束全通），真实问题是**缺料时零反馈**：材料不够的配方卡不渲染合成按钮、也不说明缺什么，库存为空时整页像"功能坏了"；需求行只显示目标量不显示持有量；「奇迹」（烈焰玫瑰+蓝玫瑰）等稀有花配方只能先得稀有种子→种出→收获进花朵库存才可合成，无任何获取指引                                                                                                                                                                                                                                                     | 修复（garden.js renderCraft + garden.css，跨域改动见 WORKLOG）：①配方需求行改「已有/需求」格式（如 `🌹 2/3`），不足的一项标红加粗（`.rq.short`）；②缺料卡新增 `.recipe-lack` 提示行「材料不足，还缺 🌹×1、🌼×1（在花园种下并收获后可合成）」；③合成按钮常驻，缺料时置灰禁用（`disabled` + `[disabled]` 样式），材料齐即恢复可点——用户一眼看清"能做/缺什么/去哪补"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 哨兵 1 条（build.mjs FIX\_SENTINELS，needle 在 garden.js 内唯一）：`recipe-lack`。验证：node --check 过；headless 探针（种子库存 rose5/daisy4/sunflower1/lavender2）：初恋/热恋可合成、其余配方显示持有量与还缺数量、缺料按钮置灰不可点；【真机:待验证】：花园→工坊，缺料配方应显示红标持有量+还缺提示+灰按钮                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | <br /> | <br />                                                                                                                                                                                                                     |
| 117 | vivo X200s（V2458A）+ Edge 151：**本地音乐每次刷新后播放失败**，必须删掉再重新添加才能听（用户实测）。诊断实证：v3.26.376、点歌瞬间报 `资源加载失败 <audio> https://…/mochi/%7B%7D`（`%7B%7D`=URL 编码的 `{}`，即 `audio.src` 被赋成字符串 `'{}'`）、IDB `default:music-file:sm_…=33.0MB`（好文件还在）、交互轨迹点歌→报错→modal-ok（offerRemoveDamagedSong 连续失败弹「移出音乐库」）                                                                                                                                                                     | 根因（music-player.js）：本地歌播放链只判「值非空」就建 `<audio>` 并 `audio.src = v`——历史版本曾把 Blob 经 JSON 序列化（`JSON.stringify(Blob)`→`'{}'`）写进 `music-file` 键，脏值 `'{}'` 常驻 localStorage；每次刷新同步路径（`store.get('music-file:'+id)` 直接读 LS 原串）都把它当有效数据喂给 `<audio>` → 解析成站内路径 `/mochi/{}` 刷加载失败；删歌重加能听是因为重传覆盖了脏值。修复四道：① `plausibleLocalValue()` 形状校验——本地值只认 Blob / ≥10 字符字符串（有效 dataURL 远长于此，超短串必为脏值），脏值一律不喂 `<audio>`；② 同步路径读到脏 LS 值**只清 LS 副本、继续落 IDB 读权威值**（好 Blob 在 IDB 时刷新直接能播，无需删歌重加）；③ `loadLocal` 确认脏值后 `purgeLocalFile()` 清脏存储（仅当值已确认非法才删；null/undefined 缺失态不动 IDB，防挂起超时把好文件误删）；④ `playLocal` 第二层 `validAudioSrc` 兜底                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 哨兵 1 条（build.mjs FIX\_SENTINELS，needle 在 music-player.js 内唯一）：`function plausibleLocalValue(v) {`。验证：node --check 过、构建哨兵 193/193、哑哨兵 0、verify 10/10、verify-music-single-audio 15/15。【真机:待验证】（vivo X200s + Edge）：刷新后直接点本地歌应能播（IDB 好值路径）；脏值歌不再报 `%7B%7D` 且自动清脏                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | <br /> | <br />                                                                                                                                                                                                                     |
| 118 | 小米15Pro + Chrome：**邀请TA 管理页** ①「打字框变形，文字会超出框外」；②「分组和细分选项需要新增删除按钮和批量管理，打多了打错了无法修改」                                                                                                                                                                                                                                                                                                                                                                         | #118-a 打字框变形：`.ta-add` 添加表单的 select 用了 `ti-type tc-input` 但 CSS 没 `.ti-type` 规则（grep 确认），仅 `.tc-input` 生效给 `width:100%`，select 独占一行 + input 换行成 2 行布局（ta-ask 的 `.ta-type` 92px 同行布局更紧凑），窄屏视觉「变形」；同时页内 `#ti-search` / `#ti-batch` / 添加区 input 都是 `.tc-input` → 转 ce-box 后**没有** **`.ta-add .ce-box`** **那套** **`will-change:transform`** **+** **`:focus translateZ(0)`** **合成层保护**（聊天页键盘期 `.phone` 被 `_aPanComp` 写 `position:relative;top`、高度收缩，ce-box 文字合成层停在旧位不跟随布局 → 文字画在框下方/外，小米15Pro Chrome 既往实测复现族）。修：①`src/css/chat-pages.css` 新增 `.ti-type { flex:0 0 auto; width:92px }`（与 `.ta-type` 同款，添加表单 1 行排版 \[select 92px]\[input flex:1]\[button]）；②新增 `.tc-input.ce-box { will-change:transform }`（全站 tc-input 输入框合成层保护，搜索/批量导入 textarea/邀请话术输入 一并受益）；③跨域 `src/js/mobile-adapt.js` 修 ceConvert ce-ghost 类别名泄露——原序先 `inp.classList.add('ce-ghost')` 再 `box.className = 'ce-box ' + inp.className` 致可见 ce-box div 继承到 ce-ghost 类别名（CSS 当前只对 input/textarea 生效未致视觉异常，但属逻辑 bug；未来加 div.ce-ghost 规则会误伤），改为先存 `origClass` 再 add。#118-b 编辑 + 批量管理：用户原话「打多了打错了无法修改」「分组和细分选项需要新增删除按钮和批量管理」——当前 `.ta-row` 只有 ✕ 删除没有 ✎ 编辑、分组/未分组的细分（猜拳/Pong/贪吃蛇/贴贴）也无批量入口。`src/js/ta-invite.js` 补：①✎ **编辑**——每条自定义邀请行加 `.ta-edit` 按钮（26px 圆形灰底，✎ 字符；系统预设项隐藏），点击 → openModal 预填当前 text → 确认后更新 `q.text`/`tiSave`/重渲染；②批量管理 **toggle**——mine 面板顶部 `.mg-grp-row` 加「批量管理」按钮（与「新建分组」同行，激活态背景翻黑），开启后每行切到「batch checkbox + 文本（无 toggle/edit/del）」、页面底部贴出 sticky `.ti-batch-bar`（已选 N 条 + 全选 + 删除 + 取消）；batch checkbox 双向同步 tiSelected + 全选态、删除走 openModal noInput/staticText 确认后批量 splice（降序避免错位）+ tiSave + 重渲染 + 自动退出批量模式；状态 `tiBatchMode:bool` + `tiSelected:Set` 局部、`switchTiTab` 切 tab 自动退出                         | 哨兵 5 条（build.mjs FIX\_SENTINELS）：`.ti-type{flex:0 0 auto;width:92px`（chat-pages.css 内唯一）/ `.tc-input.ce-box{will-change:transform`（chat-pages.css 内唯一）/ `class="ta-edit" data-idx`（ta-invite.js 内唯一）/ `tiBatchMode`（ta-invite.js 内唯一）/ `'ce-box ' + origClass`（mobile-adapt.js 内唯一）。验证：`node --check src/js/ta-invite.js` 与 `node --check src/js/mobile-adapt.js` 全过；构建后由构建者跑 build.mjs 哨兵 5 条全绿 + `npm run verify` 现有套件验证。【真机:待验证】（小米15Pro + Chrome）：① 邀请TA → 我的添加 → 任意分组添加区：select/input/添加 三件套应在同一行（不再分两行变形）；聚焦搜索/批量/添加任一 tc-input 输入框打字不再出现「字出界/文字与框分离」症状。② 点击 ✎ 应弹「修改邀请话术」模态预填当前文案，修改保存后立即生效；点「批量管理」应出现底部操作条，可勾选多条后一次删除（带「删除选中的 N 条自定义邀请话术？此操作不可撤销」确认），取消/全选/退出批量模式均正常；切 tab（系统预设 ↔ 我的添加）自动退出批量模式                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | <br /> | <br />                                                                                                                                                                                                                     |
| 120 | iPhone 13 Safari��iOS 18.6��standalone PWA�����������ݺ��ٵ������ݣ��������ݲ�ȫ����ʧ�ܶࡣ���ʵ֤��LS 109 �� 451.8KB��ta-choose 83KB/avatar-user 54KB �ȴ���� LS����IDB �����ϸ�� 1 ����ѡ default:chat-msgs��**�ֿ�/�ظ�/�ղ���ϸ��LS 0�� 0B + IDB 0��**��cc-groups/quote-cards/fav-msgs/reply-\* ȫ����                                                                                                                                                                  | ���������� �ֿ���(cc-groups)/�Զ����ֿ�(quote-cards)/�ղ�(fav-msgs)���� IDB-only ����>200KB ʱ xyStore.set ���� LS ֻд IDB+memoryCache������ ����ʱ idbGet ����Щ����iOS Safari IDB �������/��ʱ��8s ���ޣ��� ���� undefined���� ��������ֻ�� chat-msgs/feed-posts��isAuthorityKey����¼����(exportMissing)��**��������Ĭ����**�������ļ�ȱ��Щ��������ʾ"ȫ����������"���� ����ʱ idbReplaceAll �� os.clear() ��� IDB �� put �� ȱʧ�ļ����׶�ʧ���޸���data-backup.js ���ࣩ��**������ A** IDB ��ȡʧ�����Դ� 1 ������ 3 �Σ���� 200ms �� armFgIdbReset �ؽ����ӻָ����ᣩ��**������ B** ȫ����ʧ����¼ exportMissing��ԭֻ isAuthorityKey��+�Ѻ����� nameOf��cc-groups/quote-cards/fav-msgs/avatar-*/music-file/reply-*/ta-\* �ȣ�+��ʵ��ʾ"�������ԭ�豸���ݣ�������������������µ���"��**����� C** idbReplaceAll ǰ���г���ǰ IDB �����ҳ�����û�еļ����Ҳ��� data.ls��������ֵ���� pairs �� clear ����Щ��Ҳ�� put ��ȥ �� �������ݡ���������ʱ������Ϊ�գ��������岻�䣨�滻����ֻ�б��ݲ�����ʱ�ű����ɼ����ϲ������ȶ����ݰ�ȫ                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | �ڱ� 4 ����build.mjs FIX\_SENTINELS��needle ���� data-backup.js ��Ψһ����\`for (let retry = 0; retry < 3 && (v === undefined                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | <br /> | v === null); retry++)`/`���ڶ����ж�ʧ����¼������������ʵ��ʾ�û����ݲ�����`/`TA�ظ��ֿ�(`/`�ѱ�������δ����`����֤��`node --check src/js/data-backup.js\` ���������������ڱ�+verify+������գ�iPhone / Safari ������������ֿ�/�ظ�/�ղ�Ӧ�ڣ� |
| 121 | iPad Air 7 + Safari（standalone PWA）：通话中刷新/关掉重开网页后通话不恢复（「通话设置→刷新后恢复通话」开着也没用），也不记通话中断                                                                                                                                                                                                                                                                                                                                                                       | 根因：通话进行中标记 `xy-home-v2:call-active` 只存 sessionStorage——同标签普通刷新能活，但关标签页/Safari 后重开、iPadOS 杀后台后重开（主屏幕 PWA 重开同此）sessionStorage 整体清空，call.js recoverCall() 读不到标记，既不恢复也不记中断。修复（src/js/call.js 四点 + src/js/contacts.js 一点）：①双写 localStorage 同键兜底（saveCallActive/clearCallActive 两存储同步）；②接通后 durationTimer 每 20 秒心跳刷新 ts（新鲜度判定依据）；③recoverCall sessionStorage 优先、为空读 localStorage 兜底，LS 命中且 ts 距今 ≤10 分钟才恢复（超窗静默清标记防翻旧账），恢复成功回写 sessionStorage 走快路径；④resume 关闭路径照旧记中断记录（LS 兜底后连「关浏览器重开」场景也能补记，旧方案这条会丢）；⑤call-active 加进 contacts.js migrateLegacy 排除清单（探针实测抓到：每次启动 migrateLegacy 把不带命名空间的全局根键当旧顶层键迁进 default 并删根键，LS 兜底副本启动即被搬走——同 fish-log/incoming-requests 既有先例）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 哨兵 4 条（含 contacts.js 'call-active'];）（build.mjs FIX\_SENTINELS，needle 均在 call.js 内唯一）：`localStorage.setItem(CALL_ACTIVE_KEY, payload)` / `localStorage.getItem(CALL_ACTIVE_KEY)` / `if (++hbCount >= 20) { hbCount = 0; saveCallActive(); }`。验证：`node --check src/js/call.js` 过；构建后哨兵全绿 + `npm run verify`。【真机:待验证】（iPad Safari）：通话接通→等 20 秒以上→杀掉 Safari/主屏幕 PWA 重开→通话小框应恢复且时长从接通时刻延续                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | <br /> | <br />                                                                                                                                                                                                                     |
| 122 | 字卡库搜索搜不到系统编码字卡（TA的心情 235 张/贴贴回应/邀请婉拒/朋友圈TA评论/番茄钟陪伴/群聊兜底等系统预设池未注册搜索源）                                                                                                                                                                                                                                                                                                                                                                                      | 各功能内置系统字卡池此前只有部分（查岗/邀请/情话/位置/寻踪/TA询问族）注册了 `window.__cardSearchFns` 跨分类搜索，TA\_MOOD\_DATA 235 张及若干内置回应池游离在外。补注册 5 个只读搜索来源：ta-mood.js「TA的心情」（cat=分组名，已关卡片带 ·已关 标记）、chat.js「聊天系统回应」（兜底/游戏邀请婉拒/贴贴婉拒/贴贴回应）、feed.js「朋友圈互动」（TA评论/TA回应回复）、p2-features.js「番茄钟陪伴」（开场/鼓励/完成/回应/累了五池）、group-chat.js「群聊系统回应」（兜底回复）；不改任何抽取/存储逻辑                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 哨兵 5 条（build.mjs FIX\_SENTINELS，needle 均在各自 file 内唯一）：`name: 'TA的心情'` / `name: '聊天系统回应'` / `name: '朋友圈互动'` / `name: '番茄钟陪伴'` / `name: '群聊系统回应'`。验证：`node --check` 五文件过；构建后哨兵全绿；字卡库列表页搜索「心情平静」等关键词应命中 TA的心情 来源                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | <br /> | <br />                                                                                                                                                                                                                     |
| 123 | 备份提醒条从未出现（数据只存本机，iOS Safari 会系统级清空 localStorage+IDB，唯一防线是定期导出备份）                                                                                                                                                                                                                                                                                                                                                                                          | pwa.js 定期备份提醒：`backup-remind-bar` 顶部提醒条——距上次成功导出（`__last-backup`）超 7 天且近 7 天未提醒过（`__last-backup-remind`）才显示，版本更新条优先显示时让位；`mochi-restore-done` 数据就绪后判定，`contacts` 键不存在（全新安装/空状态）不提醒；文案区分「已备份 N 天」与「从未备份」；点【去备份】走 `runBackupExport` 正常导出流程                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 哨兵 `backup-remind-bar`（build.mjs FIX\_SENTINELS，js/pwa.js + template.html 双登记）/ 验证：删除 `xy-home-v2:__last-backup` 后刷新 → 顶部出现提醒条；导出一次后 7 天内不再出现                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | <br /> | <br />                                                                                                                                                                                                                     |
| 124 | OPPO Find X9 + Chrome：打开网站容易崩溃、打开聊天也容易崩溃（低端机内存不足）。诊断实证：v3.26.385，LS `default:chat-msgs` 155MB、`default:cc-groups` 40MB——冷启动/切桌时同步预读数个超 8MB 大包，低端机一次性把 155MB 聊天历史读进内存直接 OOM崩溃                                                                                                                                                                                                                                                                              | 根因（chat.js）：冷启动与切桌路径无条件 `loadMsgs()` 预读当前桌面聊天历史，历史越大内存峰值越高（155MB 单桌面足以让低端机 OOM，开聊天再叠加渲染更易崩）。修复「大历史懒读」：①`chatLedgerSave` 落账本时把「已落盘字节估算」写成元数据 `b` 字段（内存缓存 dedupe 防重复写），`chat-meta` 账本由此携带历史大小信息；②新增 `chatPrefetchIfLight()` 门控——启动/`mochi-restore-done`/切桌三处预读入口统一改为先读账本 `b`：`b` 已知且 ≤ 8MB（`CHAT_LAZY_BYTES`）才预读（小历史保持快开），大包/未知大小一律跳过冷启动预读，等用户真正进入聊天页再读（此时是单一交互负载，比开屏时钟点更可控）；数据零风险：只推迟读取时机，不改任何持久化/合并/防丢逻辑，`b` 缺失按「未知」走保守跳读不破坏旧行为                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 哨兵 2 条（build.mjs FIX\_SENTINELS，needle 均在 chat.js 内唯一）：`function chatPrefetchIfLight(load) {` / `const chatLedgerBytes = {};`。验证：`node --check src/js/chat.js` 过；构建后哨兵全绿 + `npm run verify`。【真机:待验证】（OPPO Find X9 Chrome）：开屏不预读大聊天包、进入聊天页正常加载历史、不再崩溃                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | <br /> | <br />                                                                                                                                                                                                                     |
| 126 | 新功能（v3.30.x）：公用字卡 / 专属字卡里想「关闭某个分组的使用」——字卡不删，但该分组不再被联系人自动回复/拍一拍/表情包/语音/朋友圈/信箱/群聊/TA 主动分享等任何池使用，且随时可重新开启                                                                                                                                                                                                                                                                                                                                                    | chatcard.js 分组停用开关：①数据层新增独立键（不污染 cc-groups 结构）：公用 `xy-home-v2:cc-groups-public-off`（全局根键，已进 contacts.js EXCLUDE 防 migrateLegacy 迁走）/ 专属 `<cid>:cc-groups-off`，格式 `{分类:[分组名]}`——同名分组按分类区分，停用专属某组不影响公用同名组；②管理页每个分组 header 右侧新增眼睛开关按钮 + 停用态 `.off` 样式与「已停用」标签（render 与 rebuildGroupAfterRemove 两处 header 构建共用 `groupHeaderHtml/bindGroupToggle/refreshGroupHeaderUI`）；③回复池过滤：getCustomCards/getPokeCards/getPokeGroups/getMediaCards/getMediaGroups/getCustomCardsFor/getPokeCardsFor/getMediaCardsFor 与 warmShrunkCache 统一改走新合并视图 `replyPoolGroups/replyPoolGroupsFor`（`mergeFiltered`：专属/公用各自先 `filterGroupsByOff` 再 concat），getScopedGroups 面板同步过滤；管理页搜索/导出/统计等管理视角不受影响（仍走原 mergeWithPublic）；④切换联系人 `offInvalidate` 失效专属停用缓存                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 哨兵 2 条（build.mjs FIX\_SENTINELS，needle 各自 file 内唯一）：`const PUB_OFF_KEY = 'cc-groups-public-off';`（chatcard.js）/ `'cc-groups-public', 'cc-groups-public-off', 'cc-scope-migrated',`（contacts.js）。验证：`node --check` 三文件过；`node tools/verify-cc-group-off.mjs` **12/12**（header 开关 / 停用专属剔除回复池 / 同名公用保留 / 面板 getScopedGroups 过滤 / 公用独立停用全局生效 / 重新启用恢复）；构建哨兵我的 2 条在位。另修一条既有哨兵：删除型 `ta.focus();` needle 与 chat.js 等 4 处合法 `ta.focus()` 撞车必然误报（ZCode 登记时未构建验证过），收窄为 `appendChild(ta);ta.focus();`（device.js copyText 上下文唯一）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | <br /> | <br />                                                                                                                                                                                                                     |
| 127 | 手机上点【发送】按钮发消息后输入法自动收起，连续对话每条都要重新点输入框弹键盘（用户反馈）                                                                                                                                                                                                                                                                                                                                                                                                             | 根因：点按按钮的 `mousedown` 默认行为会把焦点从 contenteditable 输入框抢到按钮上，移动端输入法随即收起（输入框内容清空/re-render 后焦点未回）。修复（chat.js 单聊 + group-chat.js 群聊同款）：①发送按钮 `mousedown` preventDefault 阻止焦点转移（键盘不收）；②`click` 发送后 `input.focus()` 回焦兜底（部分内核 click 路径仍会失焦）；Enter 发送路径本就有焦点，不受影响                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 哨兵 2 条（build.mjs FIX\_SENTINELS，`send.addEventListener('mousedown'…preventDefault` chat.js 内唯一 / `sendBtn.addEventListener('mousedown'…` group-chat.js 内唯一）。验证：`node --check` 两文件过；构建哨兵在位。【真机:待验证】：手机聊天/群聊连发多条消息，键盘应保持弹出不再每条收起                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | <br /> | <br />                                                                                                                                                                                                                     |

\| 128 | iPhone 12 Pro + Safari（多角色，一直开启跨桌面查岗/打电话，频率「标准」）：最近两三天跨桌面查岗与来电一次都没触发（附诊断信息 v3.26.387） | 根因（contacts.js）：v3.26.x 新增的频率档位键 `desk-freq-mode` 遗漏在 EXCLUDE 列表之外，`migrateLegacy` 每次启动把它当「旧顶层业务键」迁进 default 桌面并删根键 → `incoming-requests.js deskFreqMode()` 读根键空，静默回退默认「安静」（1% 概率 + 3h 冷却），用户选的「标准」（2%/30min）失效。叠加 iOS Safari 浏览器（standalone=false）后台 setInterval 停摆、跨桌面来电额外要求前台（!document.hidden）才掷概率 → 实际掷概率窗口极小，两层叠加＝两三天 0 次。修复：① EXCLUDE 列表加 `desk-freq-mode`（防未来再迁移）；② migrateLegacy 的「误迁自愈」数组同列并入 `desk-freq-mode`（把已误迁进 default 的副本写回根键，存量一次性找回，幂等：根键已有值不覆盖）。临近的冷却键 `incoming-last:checkin|call:<cid>` 含冒号走 isExcluded「命名空间键保守不迁移」分支，本就安全，未动 | 哨兵 2 条（build.mjs FIX\_SENTINELS，均在 contacts.js 内唯一）：`'desk-call-en', 'desk-freq-mode'`（EXCLUDE 根因）+ `'hide-ta-sticker', 'desk-freq-mode']`（自愈数组）。验证：node --check contacts.js/build.mjs 过；node build.mjs --check-sentinels 我的 2 条锚点全部在位（总 241 条；另 7 条红为 #125 base.css 已知并发回归，非本次引入）。待构建后真机：iPhone 12 Pro Safari 设置里频率确为「标准」后，跨桌面查岗/来电恢复按 2%/30min 触发 |
\| 129 | iPhone（苹果×手机）自带 Safari 主屏幕打开（standalone PWA）：桌面组件显示不全、竖滑滑不到底（底部被 Home 指示条遮挡，能滑但看不见底部导航/组件） | 根因（mobile-adapt.js syncSafeBottom）：`screen.height - innerHeight > 60` 判定本意识别「浏览器工具条占空间」（标签页模式）→ 归零 `--mochi-safe-bottom`。但 iOS standalone 下该差值（诊断实测 screen 956 − innerHeight 894 = 62）是系统状态栏/Home 指示条而非浏览器工具条，且 standalone + viewport-fit=cover 下 Home 指示条在可视区内，归零让 tabbar 等底部组件不避让被 Home 指示条遮挡（tabbar 固定不随 page-slide 滚动，故「滑不到底」）。修复：归零条件加 `!ios-pwa-standalone` 守卫，standalone 下摘除属性让 CSS 回落 `env(safe-area-inset-bottom)` 正确避让 Home 指示条 | 哨兵 1 条（build.mjs FIX\_SENTINELS，needle 在 mobile-adapt.js 内唯一）：`sh - ih > 60 && !d.classList.contains('ios-pwa-standalone')`。验证：node --check 过；standalone 下 --mochi-safe-bottom 摘除、CSS 回落 env()。【真机:待验证】（iPhone 主屏幕打开）：桌面底部导航/组件不再被 Home 指示条遮挡，完整显示可点 |
\| 131 | 小米15Pro Chrome：邀请TA 管理页「我的添加」里输入栏打字时文字显示在输入框下方/外面（框移新位、文字停在旧位，合成层字出界） | 根因（ta-invite.js）：ta-invite 管理页的 .ta-add 输入框转 ce-box 后，安卓键盘弹起（interactive-widget=resizes-content）致 layout viewport 收缩 → page-ta-invite 重排 → ce-box 文字合成层停在旧位置。ta-ask.js 有 \_reflowAskCeBoxes（监听 vv/window resize 防抖刷新 .ta-add .ce-box 合成层）缓解此族问题，ta-invite.js 漏了同款缓解。mobile-adapt.js 的 \_aRefreshCe 虽扫描全站 .ce-box，但有 \_aUserTypos<500 闸门（敲键时跳过），敲键中重排场景覆盖不到。修复：ta-invite.js 补 \_reflowInviteCeBoxes（同 ta-ask.js 实现：监听 visualViewport.resize/window\.resize，120ms 防抖，仅 page-ta-invite 可见时对 .ta-add .ce-box toggle transform+reflow 强制合成层重新提交位置） | 哨兵 1 条（build.mjs FIX\_SENTINELS，needle pg.querySelectorAll('.ta-add .ce-box') 在 ta-invite.js 内唯一）：验证：node --check 过；node build.mjs --check-sentinels 244 条全绿、我的锚点在位。【真机:待验证】（小米15Pro Chrome）：邀请TA→我的添加→点输入框打字→文字在框内正常显示不再出界 |
\| 133 | 聊天页【邀请TA】半框：① 底部输入栏占位「想邀请TA做什么」的文字飞出输入栏（安卓键盘弹起半框平移时 ce-box 文字停在旧合成层位）；② 顶部分组栏右侧无管理：缺批量管理（全选/删除/移动）、自建分组缺重命名/删除（字卡单条编辑/删除原有） | 根因与修复（chat.js + chat-main.css）：**①** `#chat-ask-input` 转 `.ce-box` 后缺常驻合成层保护——半框（`poke-card`）被平移/键盘弹起时，`:focus translateZ(0)` 只在聚焦期建层，pan 让文字残留在旧合成层=「文字与框分离/飞出输入栏」，同 #118 `.tc-input.ce-box`。补 `.chat-ask-input.ce-box { will-change: transform; }`（仅安卓 ce-box 态）。**②** `renderInviteBank()` 新增批量管理模式态：顶部分组栏右侧新增「批量管理/完成」chip；进入后（预设分组拒进，toast 提示）自建分组 chip 显示迷你 ✎(重命名)/✕(删除分组连带其字卡)、字卡行切勾选框视图、底部贴 sticky 批量条（已选 N＋全选/移动/删除/取消，复用 `.ti-batch-*`，移动走 openModal pills 选目标组）；复用 `myInviteG()/myInviteGroupsSave()/myInviteCurGroupKey()`，`contact-switched`/切分组自动清选择、退出批量自动清理；预设为系统内置只可加字卡不可改名/删除。**回归修补**：初次实现批量态分组标签误用 `esc(g.label)`（chat.js 无全局 `esc`，仅 `escTxt`）→ 进批量态 groups.forEach 抛 `esc is not defined` 中断，整栏只剩预设分组、「批量管理」点了没反应=「阉割版用不了」。改用 `escTxt(g.label)`。**回归修补2（用户反馈「预设字卡没法单独编辑」）**：预设 cards 原为 `MY_INVITE_PRESETS.slice()` 实时生成、不落持久化 `myInviteG()`，且编辑按钮被 `if(cur.user)` 守卫（预设无 user），即便放开 `myInviteEdit/myInviteDel` 也因找不到 `__preset` 条目而失效。改造：`myInviteG()` 首启把系统字卡灌入 `['__preset', MY_INVITE_PRESETS.slice()]`（`unshift`+`myInviteGroupsSave`），`myInviteView()` 预设由持久化取且 `forEach` 跳过 `__preset`，字卡编辑按钮去掉守卫 → 预设/自建分组的字卡均可单独「修改/删除」并持久化（分组级批量管理仍限自建，符合预设系统内置不可改名/删分组） | 哨兵 6 条（build.mjs FIX\_SENTINELS）：`.chat-ask-input.ce-box { will-change: transform; }`（chat-main.css 唯一）/ `function toggleInviteBatch()`（chat.js 唯一）/ `inv-batch-cb-in`（chat.js 唯一）/ `data-op="rm">✕`（chat.js 唯一）/ `escTxt(g.label) + g.cards.length +`（chat.js 唯一）/ `if (!myInviteGroups.some(g => g[0] === '__preset')) {`（chat.js 唯一）。验证：node --check src/js/chat.js 过；node build.mjs --check-sentinels 255 全绿、我的锚点在位、哑哨兵 0；node tools/tmp-invite-ask.mjs 批量态勾选/全选+预设/自建字卡单独编辑 16/17 通过。待真机：预设与自建分组的字卡均可单独修改/删除并落库；批量管理（勾选/全选/删除/移动/重命名/删除分组）可用 |
\| 132 | 邀请TA「我的添加」批量管理只能批量删除，选中多条后无法一键移动到别的分组（字卡库 chatcard 已有「移动」按钮，ta-invite 缺） | 根因（ta-invite.js）：v3.26.x #118 加批量管理时只做了「全选/删除/取消」三条，漏了 chatcard.js 有的「移动到分组」。修复：①批量条 HTML 加「移动」按钮（删除前、取消前，disabled 态随选中数同步）；②bindTiBatchBar 加移动逻辑——点击弹 openModal pills 选择目标（未分组 + 现有分组），确定后遍历 tiSelected 改 q.grp 字段（空=未分组 delete q.grp），保存后退出批量模式 + 重渲染 + toast 提示移动数和目标组名；③行内 checkbox 切换时同步移动按钮 disabled 态 | 哨兵 1 条（build.mjs FIX\_SENTINELS，needle id="ti-batch-move" 在 ta-invite.js 内唯一）：验证：node --check 过；node build.mjs --check-sentinels 244 条全绿、我的锚点在位。【真机:待验证】：邀请TA→我的添加→批量管理→勾选多条→移动→选目标分组→选中条移到该组、数量正确、toast 提示 |
\| 134 | iPhone X（iOS 16.7 Safari 主屏幕）桌面图标按钮/小组件整批缺失且调不出来，「总是打开都缺」；诊断：openDecision/openGroupDecision 功能入口缺失、html 类=(空)（ios-pwa-standalone 都没加上）、启动文件异常=无。同族问题 #87（iOS 各机型反复：iPhone17 Edge 图标不全等）。同报：装修模式拖拽图标 HierarchyRequestError: The operation would yield an incorrect node tree（appendChild@native doDrop@…onUp@…，拖拽功能报废） | 根因①（图标/功能缺失，主根因）：产物 index.html ≈3.6MB、8 个脚本块，块6/7（决策/全屏/移动适配/pwa 更新器）在最尾部；弱网下响应被中途截断，fetch 仍 ok → SW 三处写缓存（install precache / fetch 导航写回 / activate 补写 + PRECACHE_NOW）均无完整性校验，截断体进缓存 → 尾部脚本块整体丢失且 HTML 解析不报错（「启动文件异常：无」实锤=非 JS 抛错）；pwa.js 更新器也在尾部块，页面失去自愈通道 → 之后每次导航回退都命中残缺缓存，反复发作。修复（三层防线）：① template.html body 末加 DOM 锚点 `<span id="mochi-html-eof" hidden>` + `__MOCHI_EOF__` 注释，build.mjs 再在 </html> 后追加 EOF 兜底注释（最末字节）；② src/pwa/sw.js 新增 isCompleteHtml()（含 EOF 标记才完整），四处写缓存点全部校验、截断体一律不落盘 + activate 自愈删除存量残缺缓存 + 新增 PURGE_INDEX 消息（删除所有缓存里的 index.html 并回执 PURGE_DONE）；③ device.js（第一文件恒执行）尾部新增文档完整性自检：load 后 60s 查「#mochi-html-eof 存在 + openDecision 可用 + standalone 下 html 有 ios-pwa-standalone 类」，任一缺失=截断实锤 → 发 PURGE_INDEX 等 PURGE_DONE（1.2s 超时兜底）后 reload，sessionStorage 限 1 次防循环重载。根因②（拖拽崩溃）：装修模式 pointerdown 的 `closest(〔data-desk-widget〕, .app)` 会把整组图标网格（.app-grid 自带 data-desk-widget=apps/p2apps/p3apps）本身当拖拽对象，computeDrop 命中 `.app-grid` 分支、落点 ref 是网格的子图标 → doDrop `insertBefore(网格, 子图标引用)`=节点插进自己内部 → HierarchyRequestError。修复：computeDrop 开头 `dragged.classList.contains('app-grid')` 直接返回 null（整组网格不可拖，onUp 已有判空）；doDrop grid 分支加 `dragged.contains(info.ref)` 自嵌套兜底防线（任何路径都不允许把节点插进自己子孙位置） | 哨兵 6 条（build.mjs FIX_SENTINELS）：sw.js 2 条（isCompleteHtml/PURGE_INDEX）、template.html 1 条（mochi-html-eof 锚点）、device.js 1 条（mochi-trunc-reloaded）、personalize.js 2 条（contains(info.ref)/contains('app-grid')）。验证：node --check 全过；`node build.mjs --check-sentinels` 257 条全绿、哑哨兵 0、sw.js 源锚点 3/3；构建后哨兵含 6 条新锚点。【真机:待验证】（iPhone X iOS16.7 Safari 主屏幕）：①开关全屏后桌面图标/小组件齐全（若曾损坏，新 SW 激活后 60s 内自动刷新一次恢复）；②装修模式拖拽图标不再崩溃；③弱网反复开关应用不再出现功能整块缺失。iPhone 15/12PM/17 等机型同法复测 | <br /> | <br /> |
\| 135 | iPad 7 + Edge：开屏卡在「正在加载数据…」不出现「点击进入」，也无法滑动到底部再进入（开屏死锁） | 根因（idb.js open()）：indexedDB.open 请求在部分 iOS/Edge 内核存在「既不 onsuccess 也不 onerror 也不 onblocked」的挂起形态（IDB 服务进程被杀瞬间发起/版本升级被旧连接 blocked），而 open() 原实现无 onblocked 处理、无落地超时——各事务入口（idbGet/idbGetMany/idbListKeys/idbSet 等）的超时计时器全部注册在 open().then 里，open 不落地则计时器永不启动 → 启动回填 idbRestore 的 Promise.all 永久挂起 → finish() 永不执行 → __mochiDataReady 永不置位 → clock.js 开屏门控 updateEnterState 的 ready() 恒假，「点击进入/仍要进入」永远出不来，开屏彻底死锁。修复（双层）：① idb.js open() 加 8s 兜底落地超时（未落地清 dbPromise+reject，下次调用重建连接，调用方 catch 走 LS 兜底/慢保险丝）+ onblocked 处理（blocked 时主动失败本次 open，旧连接方释放后自然恢复）；② clock.js 开屏 20s 硬保险丝 readyForced：数据仍未就绪时解除 ready() 门控——进入按钮按已就绪放行（仍要求滑到底），点进入改走 forceEnter（隐藏开屏+弹「数据仍在加载」提示，不静默进入）；数据随后真就绪时 ready() 优先、readyForced 自动失效，正常用户零影响 | 哨兵 3 条（build.mjs FIX_SENTINELS）：idb.js 2 条（idb open hang / req.onblocked）+ clock.js 1 条（readyForced）。验证：node --check 过；--check-sentinels 259 全绿哑哨兵 0。【真机:待验证】（iPad 7 + Edge）：开屏最长 20 秒后必然出现可点状态，滑到底点「点击进入」能进入（数据多时弹数据不全提示）；正常设备开屏行为不变（几秒就绪即正常进入） | <br /> | <br /> |
| 136 | vivo（V2359A + Chrome 149）：网站打开异常，「出现单独 mochi 英文字母图片插图，无法正常进入开屏，无法正常进入网站」（间歇性）；诊断实证：远端 version.json 获取失败（离线或网络受限）但 navigator.onLine=true——GitHub Pages 国内网络不可达时段，Chrome 网络错误页顶头就是站点图标 icon-512（带 mochi 字母），即用户看到的「mochi 字母图」 | 根因（sw.js 导航离线兜底链，三层叠加）：① 导航成功后缓存写 `req.url` 键（如 `.../mochi/`），而离线兜底只 `match('./index.html')`（=`.../mochi/index.html`）——键不一致，缓存里明明有完整 index 也兜不住；② install 预缓存逐文件 3.5s 超时（GitHub Pages 弱网常态）后当前 CACHE 可能只有 req.url 键没有 canonical 键；③ activate 旧缓存清理后补拉靠网络，被墙时留空窗。修复（sw.js 三处，#134 EOF 校验全部保留）：①导航成功统一写 canonical `c.put('./index.html', res.clone())`；②兜底链 canonical 键 miss 后补一枪 `caches.match(req)` 接住存量 req.url 键缓存（老设备不丢兜底）；③activate 删旧缓存前「抢救」最新一份带 EOF 标记的完整 index（截断/无标记旧体一律不救），当前缓存缺 index 或残缺时先用它顶上再网络补新——离线也永远有可用兜底，不再落 Chrome 错误页 | 哨兵（build.mjs swNeedles/swNeedlesSrc 各 +3 条）：`m || caches.match(req)` / `c.put('./index.html', res.clone())` / `rescued ? c.put('./index.html', rescued)`。验证：node --check 过；--check-sentinels 259 全绿哑哨兵 0 + sw 源锚点 5/5。【真机:待验证】（vivo V2359A + Chrome）：飞行模式/弱网下冷启动网站能从缓存进入开屏并点进入（不再出现 mochi 字母错误页）；联网后版本能正常更新；iPhone/其他安卓开屏行为不变 |
\| 137 | iPhone 15 + Safari 主屏幕 + 全屏模式（iOS 18.7）：通话小框固定在顶部栏右上角与手机系统状态栏重叠——点不了挂断、拖不动（触点全被系统栏吞） | 根因（call.js miniSafeTop 差值法漏网环境）：v3.28.x #114 的 miniSafeTop 用 screen.height−innerHeight 差值实测系统状态栏高度（20-160 过滤），但 v3.28.x #114 复现修复把 .phone 改 100vh 铺满物理屏后，该环境实测 screen=852=vv=852 → 差值=0 落在过滤区间外返回 0；小框存档位置 y≈0（此前被拖到顶部）时恢复守卫 \_st>0 不成立不抬升，56px 高的胶囊整体落进系统状态栏悬浮区 → 点挂断无反应、拖拽上边界 max(0,…) 也拖不出状态栏区（诊断 v3.26.392 实锤：ios-fs-active + screen=vv=852）。修复：miniSafeTop 三级探测链——① env() 探针（隐藏 fixed 元素实测 env(safe-area-inset-top)，viewport-fit=cover 下返回真实系统栏高度）；② 原 screen-vv 差值法保留；③ 47px 保守兜底（iPhone 刘海/灵动岛系统栏最小 47-62px 取下限，仅 standalone iOS 生效）+ \_miniSafeTopCache 会话缓存。恢复守卫与拖拽钳制同走此函数，任何 iOS 型号小框永不落进状态栏区 | 回归修补（用户复测仍卡顶）：①47px 兜底改 59px（iPhone15 实测系统栏 59px，47 不够）；②新增显示时抬升 liftMiniIntoSafeArea——小框有 5 处显示点（接通 2s 自动最小化/手动缩小/刷新恢复通话/设置开关/恢复通话），任何路径显示「内联 top 低于安全线」的坐标都会复现卡顶，统一在显示后校正并回写存档（默认底部居中无内联 top 不受影响）。哨兵 2 条（build.mjs FIX_SENTINELS，call.js `if (!top) top = 59;` + `function liftMiniIntoSafeArea()`）。验证：node --check 过；--check-sentinels 277 全绿哑哨兵 0；四场景功能测试（#136 env=0+diff=0→47 / env=59→59 / diff=59→59 / 非 standalone→0）全过。【真机:待验证】（iPhone 15 iOS 18.7 全屏态）：通话缩小后小框自动离开系统状态栏区（y≥47px）可点挂断可拖动；iPhone 15 旧存档位置被自动抬升 | <br /> | <br /> |
| 139 | 用户机（24117RK2CC + Chrome 151 安卓）存储膨胀 747MB：① localStorage 写满（QuotaExceededError）设置/桌面保存失败靠 IDB 校正；② 字卡库 cc-groups 家族 ≈563MB（公用 138.22MB + 三桌面专属 148.89/138.22/138.22MB，三份逐字节同大小=整份复制）；③ 收藏 fav-msgs ≈21MB（与聊天记录重复存同一批原图）；④ GIF 直存原图无上限；⑤ JS 堆 440MB/帧率 35（大键 JSON 序列化+base64 解码） | 修复（四件套，全部「宁可不删不可删错」）：① idb.js `lsResidueSweep`——restore/备份导入（mochi-restore-done）后延迟清扫 LS >200KB 项目残留（排除 chat-msgs 快照/music-file/元键）：IDB 同值→纯去重删 LS；IDB 缺失/落后→按 retainValue 规则以 LS 追平 IDB、写成功且 LS 未被业务再写才删（CAS，绝不先删后写）；② chatcard.js 专属库去重模块——预检用 __big-idx 尺寸+体检标记 `cc-dedupe-v1`（稳态零开销、免读大值），整库相等→删专属键（storeFor.remove 清四路），分组级同名同分类内容一致→剔除、剔空删键、剩余 <15MB 才回写；公用库只读不动；deviceMemory<4 不跑；③ chatcard.js 导入兜底防复制——专属页导入全量备份取不到专属键时公用库兜底改最后位 + fromPubFallback 守卫（合并结果与公用库完全相同不写专属键，回复池本就合并公用+专属零损失）；④ chat.js 收藏图片压缩——saveFav 钩子 + 启动/换桌面一次性迁移（标记 fav-img-cmp-v1）：只压 data:image（GIF/SVG/<4KB 跳过）、480px、WebP 优先（iOS 回退检测转 JPEG 白底）、必须比原图小才采用、写前 CAS 比对防并发覆盖；⑤ chatcard.js GIF 上传上限 CC_GIF_MAX_B64（≈3MB 文件，超限跳过提示）。媒体池按内容哈希去重+Blob 直存（再省 25%）为后续单独立项 | 哨兵 5 条（build.mjs FIX_SENTINELS）：idb.js（CAS 删 LS 判等行）、chatcard.js（去重预检长度对/fromPubFallback 守卫/CC_GIF_MAX_B64 超限比较）、chat.js（rawNow!==rawSnap CAS 行），全部在自己文件里唯一、哑哨兵 0。验证：node --check 三文件过；构建后哨兵全绿。【真机:待验证】（用户机 24117RK2CC）：升级后空闲约 1 分钟出现清理 toast，重新生成诊断：cc-groups 总量从 ≈563MB 降至 ≈150MB 内、fav-msgs 显著下降、LS 写探针恢复成功、设置/桌面保存正常、字卡库/回复/收藏内容无缺失 |
| 140 | 华为 Pura70Pro+（HBN-AL80 + Chrome 122 安卓）：桌面小组件的卡片大部分不显示（只剩图标网格）；用户反馈安卓其他机型也出现过。诊断实证：LS 3.6MB 高占用 + IDB 22.5MB + 长任务频繁（高 IO/配额压力设备），启动文件异常=无、功能入口全就绪（非截断 #134 家族） | 根因：`desk-layout` 持久化值损坏/空壳（`[[],[]…]` / 页数超限 / 重复组件 id，高 IO/配额压力下产生）时 `applyDeskLayout` 把布局外全部小组件卡整批扫进隐藏池（`#desk-widget-pool` display:none），只剩 apps/p2apps/p3apps 图标网格可见；坏键同时落在 IndexedDB，每次启动 `idbRestore` 回填复发（同 #87/#134/#136「存量大数据+慢 IO」家族，跨安卓机型通病）。修复（personalize.js 三处 + desktop-slider.js 一处）：① `deskLayout()` 读时完整性校验（数组结构/页数 2~5/至少一页非空/组件 id 不重复），不过 → 当作无布局回默认 DOM 并当场清坏键（防 IDB 回填复活）；② 隐藏池扫描改「布局数组里有名（哪怕列在已不存在的页）就不进池」，防删页/重建后误判布局外整批隐藏；③ `saveDeskLayout()` 写前防损坏（重复 id/页数超界放弃保存并清键，不把坏值固化进 IDB）；④ `deskRebuild()` 页数钳制（idx≥slides.length 不再把 scrollLeft 设到超界空白页位，修「滑页停在空白=卡片全不显示」的视觉形态） | 哨兵 4 条（build.mjs FIX_SENTINELS，needle 均为逻辑锚点且各自文件内唯一）：`console.info(\x27[mochi] desk-layout 校验失败（损坏/空壳），忽略并清除\x27)` / `if (inAnyPage[wid]) return;` / `if (!ok) { try { store.remove(\x27desk-layout\x27); } catch (e) {} return lay; }` / `Math.min(Math.max(slides.length - 1, 0), idx)`。回归脚本：`tools/verify-desk-layout-guard.mjs`（新增，无头 Chrome 加载产物后同文档调 `window.applyDeskLayout()`，5 场景 10 断言）。验证：node --check 过；`--check-sentinels` 264 全绿哑哨兵 0；**修复版 10/10 通过**；**无修复基线反向对照 3/10**（空壳/截断/重复 id 布局把 deco/情话/打卡/音乐/周常/周末/经期/备忘 7~8 张卡整批吞进隐藏池且坏键留存=用户症状实锤）；基线 verify:all 121 通过/69 断言失败/2 超时与 #134 时代基线一致（均历史存量，非本次引入）。【真机:待验证】（Pura70Pro+ Chrome 122）：升级后冷启动一次坏键自动清除回默认布局、小组件卡片全部显示；已装修用户布局保持不变；其他安卓/iOS 机型装修保存/删除页/恢复默认桌面正常 |
| 141 | 红米 K80 + Chrome / 小米15Pro + Chrome / 多安卓机型通用：聊天输入栏打字后【按返回键/手势收起键盘】，输入栏这一行下方露出灰色块，几秒才收起（点发送失焦收起不复发——#89 已修路径之外的新路径） | 根因（mobile-adapt.js 安卓键盘适配）：#89 的收起卡顿修复把 `_aClosing` 闸门挂在 focusout（失焦=正在收起）上；但安卓上【返回键/手势收键盘】时内核保留输入框焦点、focusout 根本不触发 → 收起动画期每帧 visualViewport.resize 照旧跑 `_aPinPan()`/`nudgeInputVisible()` 的强制布局读取（读 offsetTop/scrollY/getBoundingClientRect），重聊天页（数千条消息，小米15Pro 诊断 chat-msgs 124.7MB/3238条）单帧 reflow ~100ms（诊断长任务三连 98ms 即此时段），主线程积压 → .phone 高度跟不上键盘收起动画 → 输入栏下方露 body 灰底数秒。修复（四处，全在 mobile-adapt.js 安卓分支）：① syncAndroidKb 顶部加「h 高于上一帧且键盘开启态=收起动画」探测置 `_aClosing`（不依赖 focusout，与 #89 失焦路径汇合同一收起分支）；② 250ms 轮询内同判据补置（resize 漏触发内核兜底）；③ 收起复原时把无键盘基准 `_aH` 钳回 window.innerHeight（防键盘期被瞬态值抬错停留低位 → 下一帧误判键盘又弹出把 .phone 锁死中间高度）；④ 悬浮键盘推定停靠（_aProv）加用户键入 1200ms 内推定收口 _aProvUserConfirm（不等 2200ms 无活动自愈，修 vv 不变化内核的灰块残留）。 | 哨兵 3 条（build.mjs FIX_SENTINELS，needle 均在 mobile-adapt.js 内唯一）：`if (_aKb && h > _aPrevH && _aPrevH > 0) {` / `if (_aH < window.innerHeight - 12) _aH = window.innerHeight;` / `if (!tgt || Date.now() - _aUserTypos > 1200) return;`。行为断言 `tools/verify-android-kb-close.mjs` **5/5**（vv.height 逐帧回升模拟返回键收起：弹出收缩/动画期 offsetTop 探针读数=0/动画期高度跟随上浮/复原 inline 清空/复弹基线健康），已实测「有牙」：移除修复重建 → 探针 15 reads FAIL。【真机:待验证】（红米 K80/小米15Pro + Chrome）：聊天打字后按返回键收键盘 → 输入栏下方灰块随手收起不残留；点发送失焦收起、弹键盘、打字、半框（问问TA/占卜）输入、面板停靠均不回归 |
| 143 | 一加Ace2（PHK110 + Edge 安卓）开屏「整页只有 mochi 英文字母图」、进不了开屏公告/网站；与 #136 vivo+Chrome 同症状跨机型复发（用户反馈此前多机型也出现过） | 根因（sw.js fetch 最终重试写点——v3.27.x 引入的、#134 四处写缓存点之外漏挂校验的第 5 处）：网络优先 3.5s 超时后的 catch 兜底同时接住**非导航请求**（慢网络下的 icon-512.png 等），原实现把任何成功体一律 `c.put('./index.html', copy)` 写进 canonical index 键且不过完整性校验——双漏洞：① 图标 PNG 占位 canonical 键后，之后 GitHub Pages 不可达时段离线导航兜底第一级 match 就命中 PNG → 浏览器把图片当文档渲染 = 整页只有一张 mochi 字母图（#136 修复后 canonical 键是兜底链第一优先，污染条目还会遮蔽旧缓存里的好 index）；② 图片文档不跑任何 JS，#134 完整性自检/PURGE 自愈在图片页上无法触发，卡死到网络恢复；③ 截断 HTML 亦可经此绕过 #134 铁律。修复（sw.js 两处，#134/#136 锚点全部保留）：① 最终重试写点收口——仅导航请求才写 canonical 键且必须过 isCompleteHtml（截断体透传不落盘），非导航资源只透传绝不写 index 键（图标缓存由主成功路径按自身键写入）；② 导航兜底命中加 content-type 守卫——非 text/html 缓存一律当未命中放行去旧缓存扫描/网络重试（防已污染设备被 PNG 遮蔽好缓存）。存量污染设备治愈通道：activate 既有自愈（canonical hit 过 EOF 校验失败→删+抢救旧完整版）+ 联网后导航成功覆写 canonical 键 | 哨兵 2 条（build.mjs swNeedles/swNeedlesSrc 各 +2，needle 为逻辑锚点且 sw.js 内唯一）：`res.ok && req.mode === 'navigate'` / `m.headers.get('content-type')`。回归脚本 `tools/verify-sw-nav-fallback.mjs`（新增，src+产物双份 16 断言：重试写点仅导航/先校验后落盘/content-type 守卫/全部 5 处 canonical 写点受 isCompleteHtml·rescued 保护/#134/#136 锚点仍在）。验证：node --check 过；node build.mjs 哨兵 277/277 哑哨兵 0 + sw.js 9/9；verify-sw-nav-fallback 16/16；verify-html-eof 20/20；npm run verify 布局 10/10。【真机:待验证】（一加Ace2+Edge）：联网打开一次让新 SW 激活后，飞行模式/弱网冷启动能从缓存进入开屏（不再出现整页字母图）；此前复发过的 vivo 等安卓机型 + iOS 开屏行为不变 |
| 144 | iPad Air 7 + Safari 主屏幕（iPadOS 伪装 UA）：设置里点【全屏模式】无任何反应（顶部未全屏）；诊断实锤 html 类=(空)、iOS=false、平板=false（UA谎称桌面=true, platform=MacIntel, maxTouchPoints=5） | 根因（device.js isIOS）：iPadOS 13+ Safari 把 UA 伪装成 Macintosh 桌面 UA，原判定 /iphone\|ipad\|ipod/i.test(ua) 全部落空 → iOS=false → fullscreen.js 走错分支（iPad 无 Fullscreen API，诊断 Fullscreen API=false → 开关被拒绝什么都不做），且 fullscreen.js 不加 ios-pwa-standalone 类 → #114/#129 安全区补偿在 iPad 上全部失效、html 类为空。isTablet 判定本有 MacIntel+触摸分支但被用户手动布局偏好 pref:mobile 覆盖成 isTablet=false（平板=false）。修复：① device.js isIOS 补 Macintosh 伪装分支（(platform==='MacIntel' \|\| /Macintosh/i.test(ua)) && maxTouchPoints>1 && ontouchstart——真桌面 Mac maxTouchPoints=0 不会误判，与 isTablet 第二分支同信号）；② idb.js armFgIdbReset 同款 UA 检查补 touchMac 分支（伪装 UA 的 iPad 杀后台后回前台也能重建 IDB 连接）。用户的手动布局偏好 mobile 保留（只管布局不管设备身份）。**收口第二批（2026-09-07）**：②判定改读唯一判定源 mochiDevice.isIOS（device.js isIOS 本身含伪装分支；idb.js 删除复刻的正则+touchMac 局部实现，哨兵 needle 同步为消费表达式，行为语义零变化） | 哨兵 2 条（build.mjs FIX_SENTINELS）：device.js isIOS 双括号语句 + idb.js mochiDevice.isIOS gate（收口第二批同步）。验证：node --check 过；五场景功能测试（iPadOS伪装→true / 真Mac→false / 老iPad UA→true / iPhone→true / Windows→false）全过；tools/verify-device-env.mjs 含 idb gate 行为断言；--check-sentinels 全绿哑哨兵 0。【真机:待验证】（iPad Air 7 + Safari 主屏幕）：①点全屏模式开后有反应（内容顶满/开屏说明弹出），再点关恢复；②诊断 html 类应含 ios-pwa-standalone（iOS=true）；③iPad 杀后台重开后字卡库/聊天数据正常加载 | <br /> | <br /> |

| 145 | 聊天输入栏左侧【表情包】按钮：面板打开后再点一次按钮无法关闭（用户反馈；群聊输入栏 gc-emoji-btn 同族） | 根因（chat.js）：表情按钮 click 处理无条件调用 openEmojiPanel()，且「点击面板外关闭」监听特意排除按钮本身（!emojiBtn.contains(e.target)）——再次点击既不触发外点关闭、也不会切换状态=永远关不掉。群聊 gc-emoji-btn 同族更绕：先 openEmojiPanelForInsert 重开、再冒泡到 document 外点关闭，一开一关互相覆盖后终态仍为打开。修复：①chat.js 按钮改切换开关——面板已开时先 closeEmojiPanel() 再 return；②chat.js 导出 window.closeEmojiPanelForInsert=closeEmojiPanel（面板本就是聊天页共享浮层）；③group-chat.js gc-emoji-btn 同款切换（面板已开先关不重开，复用导出的 closeEmojiPanelForInsert） | 哨兵 2 条（build.mjs FIX_SENTINELS，needle 均为 window. 属性名锚点、minify 改名/剥注释不影响）：chat.js window.closeEmojiPanelForInsert + group-chat.js window.closeEmojiPanelForInsert &&。验证：node --check 三文件过；构建哨兵 281/281 全绿哑哨兵 0。【真机:待验证】：聊天/群聊输入栏点表情按钮打开面板 → 再点同一按钮面板关闭；点面板外关闭、选表情发送、信箱/批量插入模式不受影响 | <br /> | <br /> |

| 146 | 设置→美化里点【一键随机美化】后桌面小组件全部消失变全透明，【恢复默认布局】也救不回来（用户反馈，要求删掉该功能） | 根因（personalize.js）：随机美化把 `widget-opacity` 写成小数（0.85/0.9/0.95/1 → String 后如 "0.9"/"1"），而全部读取点（启动 1720 / 卡片菜单透明度滑杆 / 切桌面重应用 6415）用 `parseInt` 按百分比整数解析 → parseInt("0.9")=0 → `--widget-opacity:0` 小组件全透明；且该键属美化键，【恢复默认布局】只清 `desk-layout` 不清它，用户无从恢复。修复：①「一键随机美化」功能整体删除（template.html row-beauty-random 行 + desk-quick dq-random 按钮；personalize.js 处理块 + bind）；②新增统一解析 `opacityRawToPct`（parseFloat，≤1 按 ×100 换算成百分比，≥2 视为已是百分比），三处读取点全部换用；③启动时把历史小数脏值改写为百分比存储（一次性自愈，存量受影响用户升级后小组件自动恢复可见） | 哨兵 3 条（build.mjs FIX_SENTINELS）：`opacityRawToPct`（personalize.js 存在型）/ `getElementById('row-beauty-random')`（personalize.js 删除型 absent）/ `row-beauty-random`（template.html 删除型 absent）。行为断言 `tools/verify-widget-opacity.mjs`（新增）：**8/8**（脏值 "0.9" 启动自愈存储 90+应用 0.9+computed 可见 / 脏值 "1" 自愈 100 全可见 / 正常 "80" 不误改 / row-beauty-random 与 dq-random 均不存在）。验证：node --check 过；--check-sentinels 284 全绿哑哨兵 0（随并行会话 #147 构建一并入库，产物已含修复）。【真机:待验证】：曾点过随机美化的设备升级后小组件恢复显示；美化页与快捷面板不再有随机美化入口 |
| 147 | iPhone 16 Pro + Safari 浏览器模式（非主屏幕）：进入网站正常，点进聊天再退回桌面巨卡无比、之后所有功能持续卡顿 | 根因（personalize.js 壁纸切换路径）：applyBgVisibility 每次进出桌面都把 .phone 的 backgroundImage 清空/重设，该用户壁纸 default:phone-bg=2.1MB——iOS Safari 上每次重设 backgroundImage 都要主线程重新解码整张大图；且 chat-back 直挂监听 + page-phone MutationObserver 双触发（一次返回解码两次）。页面切换越频繁解码越频繁 = 「之后点开其他功能都一直卡」。修复：壁纸改写 .phone 内常驻图层 #phone-bg-layer（position:absolute inset:0 z-index:1 pointer-events:none，低于 .page/.tabbar/.statusbar 的 z-index:2）——壁纸只在值变化时赋值（同值写也会触发样式失效），页面切换只切图层 opacity（透明图层纹理在合成器保持存活，不再解码）；视觉语义与原清空/重设一致（桌面透明页透出、其他页遮挡） | 哨兵 2 条（build.mjs FIX_SENTINELS）：setBgLayerImage 定义 + setBgLayerVisible opacity 短路。验证：node --check 过；--check-sentinels 286 全绿哑哨兵 0；verify-desk-beauty 断言同步改图层后真实浏览器全流程过。【真机:待验证】（iPhone 16 Pro Safari 浏览器模式）：①聊天↔桌面来回切换流畅；②2MB 壁纸用户桌面滚动/开应用不再卡顿；③壁纸显示/隐藏与原行为一致（含预设渐变、纯色、深色模式） | <br /> | <br /> |
| 148 | 开屏「防骗提醒」「转载署名·严禁倒卖」置顶声明可被二传者从自己的部署里删掉/改掉（防倒卖需求：署名声明要钉死在开屏最顶）；且该运行时回填机制 f7a8b5c 首建后曾在 0965278 清理时被整块移除（clock.js -62 行，属「修复被并行会话覆盖」典型场景） | 机制（clock.js 顶部 IIFE，恢复+扩展双条）：每次加载用 JS 常量兜底 + fetch 官方部署地址 notice.json（OFFICIAL_NOTICE）取权威 alert/alert2 文案，强刷「开屏两条置顶声明（data-anti-scam=1 防骗 / =2 转载署名·严禁倒卖）+ 设置页底部 set-alert」——元素缺失按官方文案重建并插到公告区最顶（条2紧跟条1），文案被改（标题+全部特征词 marks 不在位）则重写回官方版；二传者自己部署的副本也会向官方域名拉取，想删必须连回填逻辑一起删（改代码本身）。notice.json 新增 alert2 字段为权威文案源 | 哨兵 3 条（build.mjs FIX_SENTINELS，needle 均为 clock.js 内唯一逻辑锚点）：`insertBefore(box, refNode || notice.firstChild)`（缺失重建）/ `OFFICIAL_NOTICE, { cache: 'no-store' }`（远程强刷）/ `bar.marks.every`（在位判定）。验证：node --check 过；构建哨兵 287/287 全绿哑哨兵 0。行为断言 `tools/verify-anti-scam-backfill.mjs`（新增，verify:all 自动纳入）**13/13**：①官方正常加载两条置顶条在最顶+设置页声明 ②二传副本删条→重建到最顶 ③篡改成收费文案→重写回官方版 ④拦截官方源模拟断网→静态兜底在位 ⑤删条+断网叠加→JS 常量重建。后续 marked() 空白归一化修复（文案「小红书 @言序」带空格 vs 锚点串无空格，条2 永远判不在位每次重写）随本条覆盖。【真机:待验证】：官方站开屏最顶两条声明正常显示、无重复条；断网打开开屏仍有静态兜底；设置页底部声明含署名禁倒卖句 |
| 149 | 苹果17 Safari（13:37 诊断）+ vivo X200s（V2458A）Edge 151（13:38 诊断）等多机型（与机型无关，#142 媒体池版本通病）：聊天里引用联系人的图片/表情包消息，发送后引用块**不显示图片缩略图**；引用预览条缩略图时有时无、偶尔引用块出现 `@@m:hash` 乱码文本。vivo 诊断采集于 13:38:56、构建 ts=1788499094635（13:18 版）＝**部署修复（13:49）之前的旧版**，属同一 bug 的第二台确认设备，非新根因 | 根因（chat.js 引用链 vs #142 媒体池令牌）：聊天图片落库后被 mediaNormalizePass 令牌化为 `@@m:<hash>`（不再是 `data:` URL），而引用链四处仍按 `indexOf('data:') === 0` 判定图片载荷——①`quoteTextOf` 快照：令牌串被当引用文本存进 quote（含历史坏数据 t=令牌）；②`quoteHtml` 对象引用 imgs 过滤把令牌整段丢掉→缩略图消失；③字符串引用分支同样不认令牌；④`quoteTextSafe` 直出令牌串。修复：四处判定统一扩为「`data:` 或 `window.mochiMediaIsToken()`」，令牌照常渲染成 `<img src>` 交 media-pool 文档级观察器解析成池数据（与消息本体图片同一机制，业务侧不解引用）；顺带修聊天搜索结果对令牌化图片消息直出令牌串。群聊不受影响（群聊消息不令牌化） | 哨兵 4 条（build.mjs FIX\_SENTINELS，needle 各取四处判定的完整表达式、在 chat.js 内唯一）+ 专项脚本 `tools/verify-quote-image.mjs`（场景 E 令牌化：预览条/发送后缩略图、src 解析、历史坏数据兜底，20/20）。【真机:待验证】（苹果17 Safari / vivo X200s Edge 及任意机型，**须先更新到构建 ts=1788500971146 之后**——杀掉 PWA 重开或浏览器刷新两次触发 sw 更新）：对一张**早前发过**的图片/表情包消息点「引用」→ 输入栏引用预览条出缩略图 → 发送后气泡引用块显示缩略图、无 `@@m:` 串；引用纯文字消息与点击引用块跳转原消息不回归 |

| 150 | 任意机型：联系人主动来电（同桌面）在浏览器挂后台时**无任何后台通知弹窗**；后台期间永远不来电，响铃中切后台也静默变未接 | 根因（call.js）：①maybeIncoming 开头 `if (document.hidden) return;`（v3.5.127 后台不触发来电）——后台掷中概率直接丢弃；②响铃中 visibilitychange→hidden 只 endCall('未接听')，无系统通知；后台无法弹来电 UI 也无法接听，但用户至少应收到通知。修复：①后台命中来电→写「未接来电」通话记录+聊天系统消息+SW 系统通知（bgCallNotify 走 bg-keep showSysNotification 链路，force+avFixed）；②响铃中切后台 endCall 后补发同款通知；③bg-keep.js bgNotifyCheck 增 extra.force 通道——来电是错过就没的单发事件，绕过 15s 过渡期闸门与通知/已看/聊天去重闸门（消息类通知不受影响） | 哨兵 3 条（build.mjs FIX_SENTINELS）：`const force = !!extra.force;`（bg-keep.js）/ `if (document.hidden) {`（call.js，文件内唯一）/ `function bgCallNotify(name) {`（call.js）。验证：node --check 过；构建后哨兵全绿。【真机:待验证】（任意机型）：①开启后台保活+通知权限后挂后台，等 TA 来电（或临时调高来电概率）→ 通知栏弹「XX来电」；②响铃中切后台 → 通知栏弹未接来电，聊天有「XX 来电 · 未接听」系统消息；③回前台正常来电 UI 不回归；④后台普通消息通知仍正常（不被 force 误放行去重） |
| 151 | 小米14U Edge 等安卓多机型（用户反馈其他安卓机型也有）：联系人页切换联系人到别的桌面再切回来，桌面小组件被隐藏但位置还能点中；不同桌面显示串（上一桌面的排布/透明度/壁纸状态残留）；桌面背景图片没有按正常比例铺满 | 三个独立根因叠加（personalize.js）：①美化键跨桌面残留——`--widget-opacity`/`--desk-bg-blur`/`--desk-bg-mask-op`/`--desk-card-radius` 挂在 documentElement 全局，而键按桌面（per-cid）存储，切到「没有该键」的桌面时原逻辑跳过应用 → 上一桌面的透明度/蒙层残留（opacity 低的组件不可见但仍在原位可点中=「小组件隐藏但可以点的到」；不同桌面显示互相串）。②无布局桌面切回不归还——上个桌面（有 desk-layout）切走时按其布局把本桌组件扫进 `#desk-widget-pool`，切回无布局桌面时 applyDeskLayout 对 `!lay` 直接 return，池内组件永不归还、桌面停留在他人排布上。③#147「值变才写」把 backgroundSize/Position 也锁进「图变才写」守卫——壁纸定位/缩放（phone-bg-pos-*）改键后图层不重应用、两桌面同图不同 pos 互相串用 → 背景不按比例铺满。附带两处：切桌面触发 buildDeskPages 删页收缩时 `saveDeskLayout()` 会把上一桌面排布写进新桌面 desk-layout（页数不同的桌面来回切即互相污染持久化）；美化快捷抽屉透明度滑杆仍存 #146 同族小数脏值（"0.85"）且初始化把存量 "90" 算成 9000。修复（全在 personalize.js）：①切桌面时美化键缺键复位默认（applyWidgetOpacity(100)/applyBgBlur/applyBgMaskOp/applyCardRadius）；②脚本加载期捕获模板默认排布快照 TEMPLATE_DESK_ARR，applyDeskLayout 无布局时 restoreTemplateDesk() 归还池内组件回默认位（可选组件保持池语义、页数不足的页不动）；③setBgLayerImage 的 size/pos 移出图变守卫每次值变才写（backgroundImage 本身仍值变才写，#147 防 iOS 重解码语义不变）；④切桌面期间 buildDeskPages 删页收缩不落盘（deskSwitchBuild 标记）；⑤美化抽屉滑杆统一 opacityRawToPct 解析+存百分比整数 | 哨兵 5 条（build.mjs FIX_SENTINELS，needle 均为逻辑锚点且各自文件内唯一）+ 行为断言 tools/verify-desk-switch.mjs（新增，verify:all 自动纳入）：**15/15**（无布局切回池组件归还+可选组件不误归还+布局键不被写串 / 透明度残留复位+按桌面应用 / 同图异 pos 图层 size 跟桌面走 / 删页不污染+有布局桌面正常归还）。验证：node --check 过；--check-sentinels 301 全绿哑哨兵 0。【真机:待验证】（小米14U Edge 及任意安卓/iOS）：联系人切换来回切，小组件保持显示且各桌面布局/透明度/壁纸互不串；壁纸定位/缩放滑杆实时生效；曾被污染过的设备点一次「恢复默认桌面」或重新装修即可清除历史污染布局 |
| 152 | iQOO Neo10Pro Chrome 149 等安卓多机型（用户反馈其他安卓机型也有）：回复设置开启「让对方继续说【按正常回复时间】【底部聊天栏按钮触发】」后，点底部聊天栏「继续说」按钮，联系人不回复、无打字提示、无报错 | 根因（chat.js/group-chat.js 事件时序）：安卓上键盘收起与点按手势重叠时（打字后立刻点「继续说」最典型；另见 #141 键盘状态机在部分真机长时间滞留 kbActive/收起动画期的高危环境），输入栏随视口回弹下移，touchend 的二次命中测试落在位移后的元素上，浏览器合成 click 被派发到错误元素（无头 CDP 复现：click 落到 span.msg-time）——按钮监听器不触发、无异常、无回复，全部静默。诊断快照交互轨迹显示 click 命中按钮但仍无回复的个案，则与 cs-normal=1 下「回复速度」设置的长延时（默认最长 40s）叠加有关，观感同为「点了没反应」。修复：聊天页 chat-continue-btn 与群聊 gc-continue-btn 触摸改 pointerdown「按下即触发」（目标是真实按压元素，不经历触摸后的二次命中测试，布局位移吞不掉）+ 1.2s 防重入挡合成 click 双触发（pointerdown 已触发时干净点按的补发 click 不再重复）；鼠标仍走 click（不响应按下半程）；无 PointerEvent 的老内核 click 路径照常兜底。continueChat/gcContinueSay 主逻辑不动 | 哨兵 2 条（build.mjs FIX_SENTINELS：两文件各自的 pointerdown 绑定+鼠标排除完整语句）+ 专项行为断言 tools/verify-continue-btn.mjs（verify:all 自动纳入）**6/6**：①键盘收起吞 click 场景（touchstart 后收键盘布局位移、touchend 落点漂移）continueChat 仍触发（修复前=0）②干净点按防重入只触发一次 ③纯 click 路径不回归 ④cs-normal=1 延时分条回复链路正常。验证：node --check 过；临时目录真实构建哨兵 303/303 全绿哑哨兵 0。【真机:待验证】（iQOO Neo10Pro 及任意安卓/iOS）：打字后（键盘开着）立刻点「继续说」→ 出现打字提示并按设置的回复速度收到回复；不碰输入框直接点按钮同样生效；群聊输入栏「继续说」同款场景生效；连点不产生双倍回复 |
| 153 | 安卓多机型（用户反馈多台手机/浏览器同时出现，机型无关）：浏览器挂后台不再弹 TA 消息系统通知，回前台后积压消息一口气全部弹出 | 根因（环境变化+保活机制缺口叠加）：Chromium 139 起安卓后台页面冻结从 5 分钟缩短到 1 分钟（Chrome Platform Status「Faster background freezing on Android」，Finch 名 stop-in-background，Chrome for Android 139 发布，Edge 等 chromium 系浏览器随内核版本跟进，2026 年已普遍铺开）——正在播放音频（保活）的页面可豁免冻结，但保活音频一旦暂停（后台被其他 App 抢音频焦点最典型），v3.13.x 退避补播最长间隔 60s，静默窗口直接跨过 1 分钟冻结线→页面整个冻结（定时器全停=消息不产生+通知不弹）；回前台解冻后积压定时器+mochi-fg-resume 补触发一口气补跑=「得回前台才弹」。修复（bg-keep.js，通用根因修复无机型分支）：①切后台方向新增保活自愈 visibilitychange→hidden：音频处于暂停时清退避轨道+立即补播一次+按最快档 5s 排下一次（原只有回前台 healKeepAlive，切后台方向是空白）；②页面隐藏期间补播退避封顶 20s（前台仍 60s 不变，不回归 v3.13.x 音频拉锯修复）——冻结线内保证 2~3 次重试，音频焦点一让位即恢复「正在播放」豁免躲过冻结 | 哨兵 2 条（build.mjs FIX_SENTINELS，needle 均在 bg-keep.js 内唯一）。验证：node --check 过；构建哨兵 305/305 全绿哑哨兵 0。【真机:待验证】（任意安卓 Chrome/Edge 内核 139+）：开保活+通知后挂后台 10 分钟以上（期间勿手动杀浏览器）→ 通知栏按主动发送间隔弹 TA 消息通知，回前台不再大量积压一次性涌出；前台听歌切后台场景保活音频照常让位/收回，其他 App 音乐不被频繁打断（前台退避节奏不变） |
| 155 | 防倒卖防线加固（承接 #148）：①clock.js 回填单点被删则全部失效（二传者只需删一段代码）；②公告删不掉但缺「远程插播」能力——发现倒卖时无法对所有副本远程挂提醒；③二传者可在页面加载后运行时删声明，加载期回填不再重跑 | 扩展（对正常用户与二传自部署零影响、默认完全惰性）：①clock.js 回填 IIFE 增加 bulletin 远程时效公告——官方 notice.json 可下发 { bulletin: { text, until } }（until=epoch 毫秒），所有联网副本（含二传）开屏显示标题「公告」条，内容变化重写、过期自动摘除、notice.json 不带字段=完全不渲染；②pwa.js 末尾新增独立「在位看门狗」（第二锚点，与 clock.js 回填互为备份）：每 5s 检查两条官方声明，缺失即用本地常量补回（只补缺失不改写已在位内容，两处互不干扰）——想去掉声明必须同时改 clock.js 与 pwa.js 两个文件，且运行时删除 5 秒内自动恢复 | 哨兵 3 条（build.mjs FIX_SENTINELS，needle 均唯一逻辑锚点）：clock.js `(!bulletin.until || Date.now() < bulletin.until)` / clock.js `if (box.textContent !== '公告' + want)` / pwa.js `n.insertBefore(mkWatchBar('1', '防骗提醒', W1), n.firstChild)`。行为断言 tools/verify-anti-scam-backfill.mjs 扩至 **18/18**（原 5 用例 13 断言 + 用例6 运行时删条看门狗补回 + 用例7 拦截官方域 fulfill 假应答测 bulletin 下发显示 + 用例8 过期自动摘除）。验证：node --check 过；构建哨兵 308/308 全绿哑哨兵 0。【真机:待验证】：官方站无 bulletin 时开屏只有两条声明无第三条；改 notice.json 加 bulletin 部署后刷新开屏出现「公告」条、过期后消失；开屏加载完手动删声明（DevTools）5s 内自动回来 |
| 156 | 任意机型（用户反馈）：开启【群聊模式】后，桌面【占卜】图标没有隐藏——装修过桌面把占卜拖出图标组排在任意页、或从组件库重新加回时必现；未装修的默认桌面不受影响 | 根因（personalize.js applyGroupChatMode）：v3.8 原逻辑只在占卜图标仍位于第一页 app-grid（模板原位）时才收进隐藏池（`divBtn.parentNode === mainGrid`），其余位置一律不动——与「群聊模式隐藏占卜」预期冲突。另有复活路径：启动 150ms ensureP2AppsBelowWeekend 兜底重跑 bare applyDeskLayout，会把已在池的占卜按 desk-layout 放回桌面（它跑在群聊模式首次应用之后、mochi-restore-done 重应用之前，中间有一段可见窗口）。修复（personalize.js）：①开启分支改 `divBtn.parentNode !== pool` 即强制收池（任意位置：图标组/任意页顶层/组件库加回的）；②applyDeskLayout 末尾统一重应用一次 applyGroupChatMode（同 IIFE 函数声明提升可用；群聊关闭时对已归位图标无操作；关闭分支回引 applyDeskLayout 最多两层收敛，幂等）；③关闭分支维持 v3.8 原语义放回首页图标组默认位（desk-layout 残留占卜条目因图标已回网格被 inGrid 网格管理规则忽略，下次装修保存自动校正） | 哨兵 2 条（build.mjs FIX_SENTINELS）：`if (divBtn && divBtn.parentNode !== pool) {` / `try { applyGroupChatMode(); } catch (e) {}`（均 personalize.js 内唯一）+ 专项行为断言 tools/verify-group-desk-icon.mjs（新增，verify:all 自动纳入）**6/6**：T1 群聊关占卜在首页图标组 / T2-pre 拖出网格落盘布局生效 / T2 开群聊任意位置强制收池 / T3 群聊开着 bare applyDeskLayout 不复活 / T4 关群聊回首页图标组默认位 / T5 清场恢复默认。验证：node --check 过；临时目录真实构建哨兵 312/312 全绿哑哨兵 0；红控（沙箱内回退两处修复重建）T2/T3 转红=精确复现用户场景。【真机:待验证】（任意机型）：装修桌面把占卜拖到第 2 页→设置开启群聊→占卜从桌面消失且聊天按钮右侧出现群聊按钮；关闭群聊→占卜回首页图标组；开启群聊状态下刷新/切联系人，占卜不闪现不残留 |
| 157 | 多机型安卓（首报小米15Pro+Chrome，用户明确「安卓的其他手机型号也有出现」）三连报：①「使用默认字卡」使用概率设 5%，联系人聊天基本仍用默认字卡、自定义字卡很少出现；②关掉「使用默认字卡」总开关，聊天偶尔仍出现少量默认字卡（几乎都跟前缀/动作有关），除非把字卡库里的逐张细分按钮也关掉；③浏览器标签页可正常打开，桌面快捷方式（standalone）经常要刷新很多次才能打开甚至打不开（更新后才出现） | 根因三条：①聊天 getPool / 群聊 gcPool 把 4600+ 张默认主字卡【无条件全量】并入回复池（dc-enabled+useChat+catOn 门控，无任何概率），dc-overall 概率只管 genOneReply 里 drawCards 那条额外混入路径——诊断实锤：池 text=650（自定义）+ 4600 默认 → 均匀随机 ≈87% 默认，5% 设置形同虚设；②period.js warmText（经期聊天 25% 叠加「温柔前缀/温柔动作」，文案与 DEFAULT_CARD_DATA.period 同源）只认逐张开关 dc-off-period:*，完全无视总开关/聊天使用——「漏的都是前缀、动作」即此；③产物 index.html 已 4MB+，sw.js 对导航走「网络优先 3.5s 超时」，国内访问 GitHub Pages 4MB 必然超时 → standalone 每次启动导航都先白等超时再回退缓存；install 预缓存/activate 补拉/PRECACHE_NOW 同为 3.5s 对 4MB 必败 → 新缓存常年缺 index、更新永远刷不上（浏览器标签页有 HTTP 缓存不明显） | ①chat.js getPool + group-chat.js gcPool：主字卡改 `catOn('main') && !text.length` 只在自定义 text 池为空时兜底并入（对齐颜文字/emoji 分支既有兜底语义），概率混入交 drawCards/getDefaultCardsFor（dc-overall 生效）；②period.js warmText 开头加门控：`_dcfg.enabled === false` 或 `!defaultCardUse('chat')` 直接原样返回；③sw.js：导航请求改「缓存优先+后台静默刷新」（navCached：canonical→req 键→content-type 守卫，命中秒开并 e.waitUntil 用长超时后台拉最新完整体、过 EOF 校验写 canonical 第 6 写点）；新增 INDEX_NETWORK_TIMEOUT=30000 接入 install 预缓存/activate 补拉/PRECACHE_NOW 的 index 分支（小资源仍 3.5s） | 哨兵 4 条（build.mjs FIX_SENTINELS）：chat.js/group-chat.js `if (catOn('main') && !text.length) {`（chat.js）与 `text.length === 0`（group-chat.js 等价写法，构建剥缩进故两文件 needle 需可区分）、period.js `if (_dcfg.enabled === false) return text;`、sw.js `const navCached = req.mode === 'navigate'`（均各自文件内唯一）+ 专项断言 tools/verify-default-card-mix.mjs（13 断言，src+产物双验）+ verify-sw-nav-fallback 写点数 5→6。【真机:待验证】（小米15Pro+Chrome standalone）：①概率 5% 时默认字卡约 1/20；②总开关关闭后聊天不再出现前缀/动作字卡；③快捷方式打开秒进，更新条点一次刷新即可上新版 |
| 158 | 任意机型：TA 主动邀请玩贪吃蛇（弹窗）我点「拒绝」后，聊天里以「我」的名义发出「等会儿再陪我玩好不好」，用户指出这句像是联系人（TA）该说的话 | 根因：INVITE_DECLINE 婉拒池的语义是「我」拒绝 TA 邀请后自己发一句婉拒（同贴贴 CUDDLE_DECLINE，template 文案「拒绝都回一句婉拒」），但池内第二条「等会儿再陪我玩好不好」误写成邀请者（TA）视角——「陪我玩」=要对方陪，是 TA 的口吻，拒绝者视角应为「陪你玩」 | chat.js INVITE_DECLINE 第二条改「等会儿再陪你玩好不好」+FIX #158 注释；行为不改（拒绝后仍由我发一句婉拒） | 构建哨兵 315/315 哑哨兵 0；verify-default-card-mix 15/15；npm run verify 10/10；产物含新串且旧串 0 命中【真机:待验证】（拒绝 TA 的猜拳/Pong/贪吃蛇/贴贴邀请，我发出的婉拒话术通顺、不再出现「陪我玩」） |
| 159 | 任意机型：跨桌面联系人挂后台时浏览器永不来电、通知栏无「XX来电」弹窗（用户报障「联系人打电话，手机浏览器总是没有后台弹窗」；#150 只修了同桌面 call.js 半边，跨桌面半边漏网） | 根因（incoming-requests.js）：maybeIncoming 跨桌面来电分支 `if (deskCallEn() && !document.hidden)`——后台永不掷概率；而 deliver() 里现成的 document.hidden 分支（发「XX来电」系统通知+释放 pending，v3.19.x 注释明说「来电只保留系统通知」）对 call 永远走不到＝死代码。修复：①去掉前台门控，后台命中来电→走 deliver hidden 分支发系统通知（与 #150 同桌面口径对齐）；②call 分支通知加 force:true（同 #150 bgCallNotify 口径，绕过 15s 过渡期/通知去重闸门，防切后台头 15s 内轮询首拍被吞）。查岗/求聊天通知不加 force（内容类落卡聊天，去重闸门仍生效） | 哨兵 2 条（build.mjs FIX_SENTINELS 尾部）：`if (deskCallEn()) {`（门控移除，文件内唯一）+ `avFixed: true, force: true }`（force 通道，文件内唯一）。验证：node --check 过；构建哨兵 317/317 全绿哑哨兵 0；verify 10/10。【真机:待验证】多联系人+开跨桌面来电，挂后台（后台通知+保活开启）等冷却→通知栏弹「XX来电」；回前台无来电弹窗残留 |
| 160 | iPhone（iOS WebKit，Edge/Safari 均可复现）：网页动不动卡死。诊断：default:cc-groups=40.5MB + cmtlsammb56c:cc-groups=22.3MB（双作用域合计 62.8MB，大头是 base64 GIF/大图字卡），每次保存/读取对整库 JSON.stringify/parse 在 iOS WebKit 是秒级长任务+内存压力 → 主线程冻结/页面被挂起 | 根因：#139 给 GIF 上限设 4MB base64（≈3MB 文件）仍太大，用户自定义字卡 2094 张里大 GIF 逐年堆积成几十 MB 单键；代码侧根治（大字卡只存 IDB）涉及双写/恢复时序风险大，本条走稳妥路线：上限砍到 512KB base64（≈380KB 文件）+ 用户手动清理存量（先导出备份再删大 GIF） | chatcard.js CC_GIF_MAX_B64 4MB→512KB + 超限提示文案 3MB→380KB；build.mjs FIX_SENTINELS +1（#160，逻辑锚点=数值表达式） | 构建哨兵全绿哑哨兵 0；node --check 过。【真机:待验证】iOS 清理大 GIF 后 cc-groups 降到 5MB 内，卡死消失；再传 >380KB GIF 被拒并提示 |
| 161 | 任意机型（#159 后续）：后台来电通知能收到，但点开浏览器时联系人「已经挂断」永远接不到——后台命中来电被设计成「即判未接」（写未接记录+通知），通知纯告知无接听入口；响铃中切后台同病（用户明确反馈「设计有问题」） | 根因（call.js 设计缺陷）：#150/#159 的后台路径收到来电即写「未接来电」并只发告知性通知，回前台无任何来电 UI。修复＝改「响铃挂起」设计：①holdIncomingCall——后台/切走命中来电先发通知（bgCallNotify 加 hint 尾缀「快回来接听，对方会等你几分钟」）并把来电挂起进全局根键 call-hold（LS+IDB 双写，含归属 cid/name/ts）；②resumeHeldCall——visibilitychange 回前台/冷启动 restore 后（1.2s 延时）检查挂起：CALL_HOLD_MS=3 分钟内且归属当前桌面且无进行中通话 → incomingCall(true) 重新响铃可接听（replay 不重复发系统消息）；③超时/桌面不匹配/已在通话 → notifyCallEnd 补写「未接来电」记录+系统消息（跨桌面自动落归属桌面）；④endCall 加 holdSilent 第二参——响铃中切后台静默收尾只清 UI 不写未接；⑤挂起覆盖前先补写上一条已超时挂起（页面冻结期二次来电不丢）；clearCallHold 写 {ts:0} 防.idbRestore 回填幽灵挂起；⑥call-hold 登记 contacts.js EXCLUDE（全局根键防 migrateLegacy 迁移，同 bg-\* 道理；新键无存量故不进自愈数组） | 哨兵：build.mjs #150 bgCallNotify needle 随签名更新为 `function bgCallNotify(name, hint) {`；新增 3 条 `function holdIncomingCall(name, cid) {` / `function resumeHeldCall() {` / `function endCall(text, holdSilent) {`。验证：node --check 过；构建哨兵 321/321 全绿哑哨兵 0；verify 10/10。【真机:待验证】①后台来电通知→3 分钟内点开应用→铃响可接听；②超 3 分钟打开→聊天有「来电 · 未接听」；③响铃中切走再 3 分钟内回来→重响；④重开浏览器 3 分钟内→重响 |
| 162 | iPad Air 7 + iPadOS 26 Safari（standalone 主屏 PWA，820×1180 移动布局）：聊天页对方每回一条消息视图就向上漂一次（回一条滑一次），不贴最新消息底部 | 根因（chat.js 滚动鲁棒性不足 + iPadOS 26 WebKit 滚动回归簇）：①来消息侧 maybeScrollChatBottom 只做一次 scrollTop=scrollHeight 同步写入——out 侧本就有 rAF+120ms 三连写而 in 侧没有，iPadOS 26 Safari（scroll/positioning 回归多发，见 iOS 26 相关公开报告）单次写入可能被丢弃或被迟到布局顶开；②消息图片全部 loading=lazy+decoding=async，onload 晚于滚底，加载后内容长高把视图从底部顶开（该用户 GIF/表情池很大，极易命中）。修复＝贴底钉住态：scrollChatBottom 置 chatPinnedBottom=true，用户 touchstart/wheel 解除；in 侧补 rAF+150ms 复写（仅钉住时）；chat-body 捕获阶段监听 img load，钉住期间加载完成回到底部。无头 WebKit 820×1180 探针验证：来消息后 st 恒等于 sh-ch（贴底） | 哨兵 3 条（chatPinnedBottom 声明/in 侧复写/load 钉住回底）。验证：node --check 过；--check-sentinels 全绿；820×1180 WebKit 探针 3 条来消息均贴底。【真机:待验证】iPad Air 7：对方连发多条消息视图应始终贴最新消息；手动上滑阅读时来消息不抢滚动 |  |  |

| 163 | 红米K60至尊版+Edge（用户报障，任意机型通病）：默认字卡使用概率已调到 80-90%，联系人却总发我自己设置的自定义字卡、反复出现——按设置本应默认字卡占八成、自定义只占十几 | 根因：dc-overall-chat 场景概率只接到【回复的纯文本路径】（genOneReply 里 getDefaultCards 覆盖，#157 修的那条），另有三条回复路径完全绕开它：①单聊【主动消息】tryAutoSend→autoMsg 固定比例 15%贴纸+10%图片+15%颜文字+15%emoji+45%文字，全部只从用户自定义池抽（该用户池 text=3/emoji=1，仅有的几张卡反复出现=主诉实锤；颜文字因用户无自定义才落到默认库）；②群聊 gcGenReply 文本回复从不混默认字卡（#157 注释说概率混入交 getDefaultCardsFor，实际只有拍一拍 gcPokeText 用了，文本半边漏网）；③贴纸/图片回复按设计走聊天设置的媒体概率（默认 10%/5%），默认库无媒体不覆盖（非本条缺陷）。修复：①autoMsg 先掷 getDefaultCards()（内部 dc-overall-chat 概率+分类占比+总开关/聊天使用/分类/单卡开关同源生效），命中非拍一拍即用默认字卡，未掷中才落自定义池原比例；②gcGenReply 文本类型按成员所在桌面 getDefaultCardsFor(st) 覆盖（同单聊 genOneReply 语义），回复/撤回补发两调用点同享；③__replyPoolDiag 补「默认概率chat/主卡占比」现场，下次同类报障诊断直接可见滑杆值 | 哨兵 2 条（build.mjs FIX_SENTINELS）：chat.js `if (defs && defs.type !== 'poke' && defs.text) return { text: defs.text, type: 'text' };` + group-chat.js `if (defs && defs.type === 'text' && defs.text) t = defs.text;`（均文件内唯一）。verify-default-card-mix.mjs 扩至 17 断言（+2）。验证：node --check 过；--check-sentinels 326 全绿哑哨兵 0。【真机:待验证】（红米K60 至尊版+Edge）：默认概率 85% 下，TA 主动消息与回复绝大多数为系统默认字卡；自定义字卡（尤其仅有的几张文字卡/emoji）降到两成内；群聊成员文本回复同样混默认字卡 |
| 166 | 存储优化包（非报障，存储评估收口 #142/#160 遗留债务）：①媒体池只增不删——消息/收藏删除后池内图片永留（#142 官方注释自认「v1 无 GC」），长账用户池底越滚越大；②写日志标记每小键 set 各发一个 IDB 事务（值+标记双事务，小键写入事务数翻倍）；③媒体池无统计无清理入口，「存储已用 1.x GB」只能看不能治 | 根因：media-pool.js v1 设计只增不删；idb.js wrjMark 逐键即时落库；查看存储页只有诊断类清理。修复：①media-pool.js 新增 mochiMediaGC（mark-and-sweep：mark=全部 \*:chat-msgs+\*:fav-msgs 键内令牌∪本会话 map/writeBuf/inflight；引用键逐键串行读、读完即弃，峰值≈最大单聊天包；清单读失败/任一引用键读不到→整次放弃绝不盲删——「没读到」可能藏着唯一引用）+ mochiMediaGCApply（逐条删除，返回条数）；②idb.js wrjMark 改 150ms 微批 idbSetAll 单事务（失败退回逐键 idbSet 自带 3 次重试；pagehide/visibilitychange hidden 即时冲刷；wrjUnmark 先撤销未落库标记），值事务仍在 set() 同步先发出、语义不变；③查看存储页新增「媒体池（占用+孤儿扫描清理）」「持久存储（navigator.storage.persist/persisted）」两卡，媒体池在明细 catOf 单独成类（\^media:\[0-9a-f\]{32}\$） | 哨兵 3 条（build.mjs FIX_SENTINELS）：media-pool.js `/(?:^|:)(?:chat-msgs|fav-msgs)$/`（GC 引用面）+ idb.js `setTimeout(wrjMarkFlush, WRJ_MARK_FLUSH_MS)`（批量落库）+ personalize.js `getElementById('st-media-gc')`（面板接线）。tools/verify-storage-opt.mjs 纯 Node 桩断言 20/20（孤儿判定/只删孤儿/引用读不到整次放弃/数组形态引用/清单失败放弃/静态锚点；刻意零浏览器依赖防 #162 型 verify 泄漏涨盘）。验证：node --check 过；构建哨兵全绿。【真机:待验证】设置→查看存储显示媒体池占用；删几条含图消息/收藏→扫描报孤儿→确认删除→池瘦身且聊天/收藏图片完好；持久存储行可读、未持久化时出「申请」按钮 | <br /> | <br /> |
| 167 | 荣耀平板10Pro+Edge（用户报障，单桌面单联系人）：回复设置已关闭「多字卡回复」，联系人一句话仍回多条字卡 | 排查：无头实测（冻结随机数对照，6/6）证明开关链路（UI点击→reply-py-en 落盘→genOneReply 闸门）无缺陷；多条实为同页另一设置「回复条数最少/最多」（reply-min/max 默认 1~2）在 scheduleReply/continueChat 拆条发送，与多字卡开关互不知晓——但用户预期「关=彻底只回一条」合理，产品语义确属易混淆（e37f893 构建曾把本会话中间态说明文案裹挟上线，已随本次覆盖）。修复（产品语义升级）：「多字卡回复」升级为总开关——py-en 关闭时单聊 scheduleReply/continueChat 回复条数强制 1（count=(c['py-en']===1)?randInt(rpMin,rpMax):1），群聊同款 gc-py-en；开启后行为不变（拆几条仍由「回复条数」管、每条拼几张由「最少/最多条数」管）；单聊/群聊两组设置页补总开关语义说明 | 哨兵 5 条（build.mjs FIX_SENTINELS）：chat.js 2（scheduleReply/continueChat 逻辑锚点）+ group-chat.js 1 + template.html 2（总开关说明文案）。tools/verify-multicard-master.mjs 无头行为断言（关=只回一条/开=按条数拆条、UI点击→落盘→生效全链路；旧产物上「关→1条」用例会红=修复未构建，预期）。验证：node --check 过；--check-sentinels 全绿哑哨兵 0；构建后 verify-multicard-master 全过 + verify 10/10。【真机:待验证】荣耀平板：关多字卡后一句话只回一条；开多字卡仍按条数拆条/拼卡 | <br /> | <br /> |
| 168 | iPhone（iOS 18.7/Safari 26.1，402×874）主屏幕+全屏模式：整页 UI 位置偏下——系统状态栏与 Mochi 模拟状态栏之间一大段空白，Mochi 行掉到屏幕 ~150px 处；底部 tabbar 被裁出屏幕外（诊断实锤 .phone高=874 > 可视 812、底部空隙=-62）。iOS 多型号反复（#114 同族第三形态） | 根因（mobile-adapt syncVvFit + base.css #114 规则，两个 iOS 26.x 形态差异）：①顶部避让用差值法（screen−inner=62px 写 --mochi-safe-top），但该 iOS 形态系统已把网页起点放在状态栏下方（innerHeight=874−62），页面再加 62px padding = 与系统避让双重叠加（.phone 62 + .statusbar 76(calc) ≈ Mochi 掉到 150px）；②fs 态 .phone 高度回落 100vh=874 高于可视 812 → 底部被裁。修复：①syncVvFit 顶部避让改 env() 探针实测（探针缓存+旋转失效）——「系统已避让」形态 env=0 不再加页面 padding，「内容覆盖到状态栏下」形态 env=真实高度照旧避让；②fs 态（fs-active/fs-css-active/ios-fs-active/ios-native-fs）健康时写 --mochi-ios-h=vv 实测可视高（键盘/推定态仍摘除），base.css #114 规则高度改 var(--mochi-ios-h,100vh)、html/body 同步——两种形态 .phone 都贴合可视区（#114 原 padding 锚点串原样保留） | 哨兵：#114 原 3 条保留（padding 串未动）+ 新增 2 条（build.mjs）：mobile-adapt env 探针 cssText、base.css height:var(--mochi-ios-h, 100vh)。验证：node --check 过；--check-sentinels 330 全绿哑哨兵 0；CDP 探针（Chrome 侧 env=0 等价「已避让」形态）实测：--mochi-safe-top 摘除、.phone padding=12px、statusbar=14px（无双重叠加）；CSS 链路 --mochi-ios-h 消费/动态更新/100vh 回落全过。【真机:待验证】（同机型主屏幕+全屏模式）：①Mochi 行紧贴系统状态栏下方（空白消失）；②底部 tabbar 完整可见不被裁；③聊天输入栏贴底；④iPhone 15（inner==screen 形态）全屏态回归不变形 | <br /> | <br /> |
| 169 | OPPO Reno6 5G + 雨见浏览器（用户报障，慢壳/连点通病）：发语音「有时候一直提示语音已达最长60秒」，无法正常录音，面板关掉仍弹个不停，只能刷新页面 | 根因（chat.js 录音）：startVoiceRec 是 async，先 await getUserMedia——雨见等慢壳开麦可达数秒、期间「开始录音」按钮文案未变，用户必然连点；二次进入会整体覆盖 voiceRec/voiceStartTs，并把 voiceTimer 覆盖成新 id：旧计时器 id 永久丢失成孤儿，每 250ms 检查 `Date.now()-voiceStartTs>=60000`，而 voiceStartTs 停止后从不清零，录音停止 60 秒后孤儿开始每 250ms 误报一次「已达最长 60 秒，自动停止」且永不自停（面板关闭仍弹）；同时第一路 MediaRecorder+麦克风流被覆盖泄漏（麦克风常驻占用、后续开麦更慢→更易连点，恶性循环）。修复：①入口加 voiceStarting 闸门（try/finally 兜底复位），启动进行中重复调用直接忽略；②计时器自证——闭包捕获自身 id，`voiceTimer!==tid` 即自毁、`!voiceRec||state!=='recording'` 不判 60s；③入场先清残留 voiceTimer | 哨兵 1 条（build.mjs FIX_SENTINELS）：chat.js `if (voiceTimer !== voiceTid) { clearInterval(voiceTid); return; }`（孤儿自毁逻辑锚点，文件内唯一）。验证：node --check 过；--check-sentinels 全绿哑哨兵 0。【真机:待验证】（OPPO Reno6+雨见）：录音中快速连点「开始录音」只录一路、到 60 秒正常自动停一次；停止/关面板/切桌面后不再弹「已达最长 60 秒」；正常录音试听发送不受影响 | <br /> | <br /> |
| 170 | 存储优化第二批（非报障，TASKS #128 收口）：字卡库体积无分组级视图、存量大 GIF/大图清理只能去字卡管理页逐组盲翻（#160 实测双作用域 62.8MB，砍上传上限只管增量） | 根因：cc-groups 是 `{分类:[[组名,卡数组],...]}` 单键大 JSON，无按组体积的观测与删除入口。修复：新增 src/js/storage-slim.js（纯逻辑+IDB，无 DOM）——mochiCcSlimScan 枚举公用键/旧版顶层残留/各联系人专属键按【分组】统计体积卡数降序（读走 idbGet 权威层，大库挂 \_\_xyIdbDeferredKeys 时 store.get 会假空）；mochiCcSlimDeleteGroup 整组删除（与字卡管理页删组同语义；删除前重读当前值、在当前值上删组再写回——扫描到确认之间的用户编辑不丢；组名匹配不到/结构不对→false 不动）；写回走 xyStore.set 三路同拍（chatcard 各缓存按原始串身份自动失效）。查看存储页新增「字卡库瘦身」卡（各库合计+Top12 分组体积+整组删除确认） | 哨兵 2 条（build.mjs FIX_SENTINELS）：storage-slim.js `if (g[cat].length === before) return false;` + personalize.js `getElementById('st-cc-scan')`；storage-slim.js 接入 jsFiles（media-pool 之后）。tools/verify-storage-opt.mjs 扩至 31 断言（+11：分组扫描/体积降序/卡数字节统计/当前值上删组防快照覆盖/xyStore 写回/组名不匹配不动）。验证：node --check 过。【真机:待验证】查看存储页扫描出分组列表且公用/专属合计与「各功能占用明细」对得上；删一个大 GIF 组→字卡管理页该组消失、其余组与聊天回复池正常；删除后 cc-groups 体积下降 | <br /> | <br /> |
| 171 | iOS 16 Safari（用户报障）：导入 milk 网站导出的 json 字卡库显示「导入失败：文件格式不正确」，无法导入公用/专属字卡（milk 格式兼容 v3.6.x 已在，同文件桌面端可导入） | 根因：导入入口一个 catch 把三类完全不同的失败——①JSON 解析失败②文件在 iOS 转存链路被损坏（微信/邮件/文本编辑转存常把文件变 UTF-16、iCloud 未下载完整读到空内容、网页另存成 HTML）③applyImportData 自身抛错（如存储配额）——混成同一句「文件格式不正确」，iOS 上真因永远看不到；milk 格式识别分支本身无恙（8 月转换模拟 477+6 张全过）。修复（chatcard.js pickImportFile）：①解析失败按现场分流提示并自救——读出成串 NUL→数奇偶位定字节序换 utf-16le/be 重读一次；前后被包说明文字→裁剪首个{到末个}再试；空内容→指引先在「文件」App 下载完整；HTML→指引回 milk 重新导出选「存储到文件」；其余带出 JSON.parse 真实报错②applyImportData 抛错单独提示「导入处理失败：…」不再伪装成格式问题③失败现场（原因+文件名+大小+开头100字符）写 __jsErrors，设置页「复制诊断信息」直接带出，iOS 报障自证 | 哨兵 3 条（build.mjs FIX_SENTINELS）：chatcard.js utf-16le 重读表达式／[字卡导入] 诊断前缀／导入处理失败 提示（均文件内唯一）。tools/verify-cc-import-parse.mjs 纯 Node 抽源码真函数 18 断言（合法/BOM/UTF-16LE带BOM/UTF-16BE无BOM/包文字裁剪/空文件/HTML/顶层数组/损坏JSON/处理异常不报格式/成功不写诊断；零浏览器依赖防 #162 型泄漏）。验证：node --check 过；--check-sentinels 全绿。【真机:待验证】iOS 16 Safari 重导 milk json——若仍失败，诊断信息「启动文件异常」一节的 [字卡导入] 行直接给真因 | <br /> | <br /> |
| 172 | 华为畅享70Pro+Chrome（2026-09-05 01:48 诊断）用户报障：字卡库自己添加的字卡和表情包（我的表情包），每次刷新重新打开网页总是丢失。诊断实证 IDB 里 `my-emoji-groups` 仍有 34.93MB＝**数据没丢、恢复链断了** | 根因链（chat.js「我的表情包」恢复链路，与字卡库 cc-groups 三轮收口过的 hydrate 体系同族漏网）：①34.93MB 超启动回填预算（低内存机 12MB）→ 每次刷新被 idbRestore 挂进 `__xyIdbDeferredKeys`，大键又从不落 localStorage 快照 → store 三路全空；②唯一恢复链 tryRestore（启动）/reloadMyEmojiFromIdb（开面板）用**裸 idbGet 固定 4s+4s 超时**，低端机读不完即静默放弃 → 面板永远空＝「每次刷新全丢」；③恢复失败期间任何保存（新建分组/上传）会把空态写回、把 IDB 全量顶掉＝真丢。修复（chat.js 三件套，与 chatcard hydrateScope 同机制）：①`myeApplyIdb` 统一应用（原两处重复逻辑收口；比较基准从 store 快照改内存 myGroups——hydrate 写 store 后二者脱节，按快照会误判「同量不覆盖」恢复永不落内存）；②`myeHydrateFallback`：idbGet 读空改走 `idbHydrateKey` 按需取回（6s+8s 慢读友好、成功进驻存并移出挂起名单），tryRestore 重试穷尽与 reload 共用；③`myEmojiSave` 防覆盖闸门：键仍挂起时先取回 IDB 全量与内存新增按分组去重合并再写，取回失败（false）不写回防小包顶掉全量，null（健康连接确认无键）直接写。诊断「自定义字卡=0」另因：专属字卡按联系人桌面隔离，该用户字卡在 default 桌面 cc-groups（12.3KB 在库），当前桌面 cmtmi25vy3j8 无专属键，非丢失 | 哨兵 2 条（build.mjs FIX_SENTINELS）：chat.js `if (!v) { myeHydrateFallback(); return; }` + `window.__xyIdbDeferredKeys.indexOf(MYE_KEY()) >= 0`（文件内唯一，哑哨兵体检 0）。行为断言 tools/verify-mye-hydrate.mjs 纯 Node 抽源码真函数 **14 断言**（读空走 hydrate/恢复落内存/面板开着即重绘/读到值不 hydrate/内存多不回退/挂起保存=全量+新增合并/失败不写回/null 直写/已恢复零开销/tryRestore 重试穷尽兜底/静态锚点；零浏览器依赖防 #162 型泄漏）。验证：node --check 过；--check-sentinels 349 全绿。【真机:待验证】（构建收口推送后，华为机）：进聊天→表情包面板「我的」——34.93MB 表情包应自动恢复（首次打开面板可能转几秒）；上传新表情→立即刷新重进→新表情与存量都在 | <br /> | <br /> |
| 173 | 任意机型（用户报障：桌面美化和聊天美化的美化方案无法导出也无法导入）：美化方案导出/导入四条路全断——桌面导出点「导出文件」没反应、桌面导入选文件没反应；聊天导出文件同样下载不了 | 根因（环境能力缺失，非逻辑缺陷——无头 Chromium 四流程 11/11 全过实证代码链路正常）：f4158f6（2026-08-29）把桌面美化导出收敛为裸 a[download]、导入收敛为仅文件选择；而 **iPhone 主屏安装（standalone PWA）没有下载管理器，a[download] 静默无反应、文件选择器也常不弹**，夸克等壳浏览器同样拦截——桌面导出/导入全断；聊天导出虽有「复制文字」，但方案含壁纸 data URL 时 JSON 巨大剪贴板写不进（复制失败），「导出文件」与桌面同病。data-backup.js 早在 v3.9.x 就为同样问题给数据备份做了三级降级保存，美化导出没跟上。修复：①data-backup.js 暴露 window.mochiExportFile（复用 saveBackupFile 三级降级：系统分享面板[navigator.share files，standalone 唯一可用保存通道]→showSaveFilePicker→确认弹窗后 anchorDownload 长命 URL 下载）；②personalize.js 桌面导出接统一链（downloadBeautyFile 降为兜底）+ 无方案也弹方式选择 + 补回「复制文字」（>3MB 拒绝防剪贴板截断）+ 文件名改本地日期（原 toISOString UTC 凌晨错天，同 data-backup v3.9.x）；③chat-settings.js 聊天导出「导出文件」接统一链（裸 a[download] 降为兜底）+ 文件名本地日期；④personalize.js 桌面导入补回「粘贴文本」通道（textarea 与从文件并存、txtImportAuto 选完文件仍自动应用；同聊天导入双通道），修 standalone 文件选择器不弹=无法导入 | 哨兵 4 条（build.mjs FIX_SENTINELS）：data-backup.js `window.mochiExportFile = function` + personalize.js `window.mochiExportFile(json, fname, 'mochi美化方案')` + personalize.js `粘贴美化方案文本` + chat-settings.js `window.mochiExportFile(json, fname, 'mochi聊天美化方案')`（各自文件内唯一，哑哨兵体检 0）。行为断言 tools/verify-beauty-io.mjs（新增，verify:all 按文件名自动纳入）：**16/16** 真实点击链路（无方案导出弹方式选择不裸下载/复制文字进剪贴板/打包确认后下载/有方案来源→方式嵌套弹窗/导入粘贴+文件双通道+自动备份/聊天导出复制+下载/聊天导入文件→textarea→应用）。验证：node --check 三文件过；--check-sentinels 349 全绿哑哨兵 0。【真机:待验证】（iPhone 主屏安装 standalone）：①桌面美化导出点「导出文件」→弹分享面板可存到「文件」；②聊天美化导出同；③导入：微信收到的方案文件存到「文件」App 后从文件导入，或直接粘贴文本导入；④普通浏览器（安卓 Chrome/Edge）导出=确认后下载不回归 | <br /> | <br /> |
| 174 | iPhone 15 Pro + Safari 主屏幕（iOS 18.7/Safari 26.1）：顶部状态栏区域三分之一显示为白色、页面显示异常；开不开全屏模式都有，浏览器内正常。诊断实锤 visualViewport=462×932 **scale=0.85**（页面被整体缩小到 0.85 倍：布局视口 462×932 > 物理屏 393×852，缩小后盖不满屏幕 → 顶部露出白底） | 根因：iOS 26.x 主屏幕形态的缩放异常——页面被缩到 scale≈0.85（数值恰为 393/462），maximum-scale=1.0/user-scalable=no 拦不住这种被动缩小；缩小后页面物理覆盖只剩 393×792（高 852 差 60），顶部区域露白。修复（双保险）：①viewport meta 全部加 **minimum-scale=1.0**（template.html + device.js 两处改写点）——缩放下限锁 1，被动缩小无法再发生（原 meta 已有 maximum-scale=1，本就无障碍损失）；②mobile-adapt healViewport 自愈：standalone 下检测 vv.scale<0.95 → 重写 viewport meta（content 变化强制 Safari 重新解析、按 initial-scale 吸附回 1，苹果无直接设 scale 的 API），每会话限 3 次+间隔 4s 防循环——存量已缩小的会话刷新/重开也能自动恢复 | 哨兵 2 条（build.mjs FIX_SENTINELS）：template.html minimum-scale=1.0 + mobile-adapt 自愈计数条件。验证：node --check 过；--check-sentinels 351 全绿哑哨兵 0。【真机:待验证】（iPhone 15 Pro 主屏幕）：①重新安装/刷新到本版后顶部白带消失、UI 恢复 1:1 大小；②聊天打字/开关全屏/前后台切换后不再出现缩小；③iPhone 15（26.6）/16 Pro（26.1）等机型回归不变形 | <br /> | <br /> |
| 175 | 功能：设置页新增【屏幕适配诊断】（与【信息诊断】分开）——跨设备 iOS 屏幕适配问题（#114 顶部重叠 / #148 双倍避让+底部裁切 / #174 缩放异常 / #168 已避让形态）精准定位工具：一键采集 env()/--mochi-safe-top/diff 三源对比、vv.scale 缩放、.phone/.statusbar 实测计算值与位置，自动判定 ✗/✓ 六形态（缩放异常/顶部双倍避让/顶部重叠/底部裁切/ios-h 不符/形态说明），每条失败带对应修复条目号，弹窗展示+自动复制 | 实现：template.html 设置页 row-screen-diag 锚点；device.js collectScreenDiag 采集（纯只读探测）+ screenDiagJudge 纯函数判定器（可单测）+ openModal 大窗展示 + copyText 自动复制 | 哨兵 2 条（template row 锚点 + device.js 判定器）。验证：node --check 过；判定器六场景单测全过（#174 缩放态/#148 双避让+底裁/#114 顶部重叠/健康覆盖/健康浏览器/#168 已避让全屏关）；--check-sentinels 353 全绿。补强（#176）：报告新增 方向（横/竖屏+旋转再测提示）/env(safe-area-inset-bottom)/.tabbar 底边/视口平移残留(vv.offset)/键盘残留(__mochiIosKb) 五项采集与判定；新增判定「底部导航栏被裁 Xpx」（#148 同族）与「视口平移残留」（#109 形态）。另 #176：设置页【复制诊断信息】改名【设备兼容诊断】，两行上方加【信息诊断】gs-title 分组说明。哨兵仍 2 条。补强（#178 三件套）：①历史快照对比——手动/自动诊断各存档一份（xy-home-v2:screen-diag-hist，上限 8 份），报告末尾「== 历史对比 ==」自动列出与上一次的变化项（scale/envTop/diff/phoneH/sbTop/tabBottom 等 14 键）；②常驻监视——每 5s 轻量采集（仅可见页），✗ 形态签名出现/变化（状态沿）才存档 trig=auto + 静默写错误环 xy-home-v2:__diag-errs（与信息诊断「最近错误」同键同格式，不用用户手发）；③手测报告附「无历史快照→存 baseline」。验证：判定器七场景+对比变化/一致单测全过；CDP 端到端：手测存 baseline→注入 .phone 超高→监视 ≤6.5s 自动捕获 trig=auto（底部裁切/导航栏被裁）+错误环出现「[屏幕适配] 底部导航栏被裁、底部裁切（自动采集）」。--check-sentinels 353 全绿。【真机:待验证】（任意机型）：设置→屏幕适配诊断→弹报告+自动复制，健康设备全 ✓；故障设备 ✗ 条目直接对号 | <br /> | <br /> |
| 180 | 多机型反复（一加 ACE3/PJE110+Edge 2026-09-05 诊断，同族此前有小米 14U/华为/真我/荣耀等）：刷新重开网站丢失最近在聊的一部分聊天记录——最近消息只在内存/低频整包落盘窗口，无第二副本（16MB 级异步 IDB 事务在部分安卓内核挂起/随进程被杀回滚，离页 flushSave 事务未必提交；且 LS 快照超 2MB 原实现静默弃写=兜底全空） | chat.js 三件套：①chatTailAppend 每条新消息同步落 LS 尾巴日志 <cid>:chat-tail（≤60 条纯文本轻副本，img/voice/大parts 不进）②chatTailMerge 权威读库成功后按签名去重回放（撤回/局部撤回 chatTailDrop 摘除、清空记录/整包导入清日志，防复活）③performLsSnapWrite 超 2MB 折半保尾不弃写；saveMsgs #88/#90 守卫链不动 | node tools/verify-chat-tail.mjs（21 断言）+ 哨兵 3 条；真机：聊天几条→立即杀浏览器重开→最近几条应在 |
| 181 | 多机型反复（EC-PAD01 SE 安卓平板+Edge 2026-09-05 诊断报障；vivo/荣耀 Edge 等此前同症状）：聊天设置里上传网页模板气泡 CSS 后提示「已应用」但气泡零变化——与机型无关、只与上传内容有关：旧映射别名表认不出模板类名（.bubble-left/.bubble-right/.you/.sent 等常见名不在表）时，整份替换后一条规则都匹配不到节点=「显示已设置但界面不变」 | chat.js 新增共享导出 window.mochiMapBubbleCss(css, scope)：别名表扩充（bubble-left/right、msg/message/chat-left/right、you/sent/received/incoming 等）+ 剥注释/跳 keyframes 帧 + keyframes 与空选择器声明块直套；**核心兜底：映射后没有任何规则命中气泡时，把模板全部声明块（含 --var）抽出整体套用到双方气泡 !important 并 toast 提示**——保证上传必有可见变化；:root/body/* 不原样注入防全局重置泄漏。单聊 chat-settings.js applyCss 与群聊 group-chat.js applyGcCss（#page-group-chat 作用域）统一改走该函数 | node tools/verify-bubble-css-map.mjs（23 断言，纯 Node 抽真函数）+ 旧端到端 tools/verify-bubble-css.mjs（UI 全链路存取）+ 哨兵 3 条；真机：粘贴任意网页气泡模板 CSS→应用→气泡必有变化+提示语 |
| 182 | 多机型反复（iPhone 16 Pro+CriOS 2026-09-05 诊断报障；200MB 级字卡库随迁移/重装普遍存在）：换浏览器导入 mochi 字卡库 json 报「导入失败」——诊断实锤 default:cc-groups=203.74MB，这类库在 iOS WebKit（含 iOS Chrome）上导入全程内存峰值可达 GB 级（源文本+JSON.parse 结果+merge 后 groups+JSON.stringify 新串+IDB 克隆叠加），撞 WebKit 单进程上限即 OOM/RangeError，被旧 catch 吞成「文件格式不正确」；与机型无关、只与库体积有关，任何手机都可能踩 | chatcard.js pickImportFile 内存加固三件：①读入即只留 rawHead（先 slice(0,300) 再 replace——旧诊断串「全文 replace 再 slice」对 200MB 文件本身就是一次全文扫描+巨型中间串）；onload 尾松开 reader.onload/onerror 引用让 200MB result 可回收；②解析成功进写盘阶段前置空源文本引用（txt=''; raw=null）——JSON.stringify(groups) 是同级新分配，两个大头不得同时钉在堆上；③OOM（RangeError/Out of memory/内存）单独识别给「瘦身+整包恢复」指引（解析/写盘两阶段都接），不再误报格式错误；附带 #182：trim 自救对 >80MB 文件不启用（slice 复制本身就是峰值推手） | tools/verify-cc-import-parse.mjs 扩至 23 断言（+5：写盘 OOM 指引/带体积不含格式/异常进诊断/诊断带头现场/松引用后正常导入）+ 哨兵 3 条（build.mjs）：txt=''; raw=null 松引用表达式／rangeerror|out of memory OOM 正则体／reader.onload=null 松绑行。验证：node --check 过；--check-sentinels 全绿哑哨兵 0。真机：200MB 级库导入——成功则直达；若报内存不足，按提示先「查看存储→字卡库瘦身」删超大分组再分批导，诊断信息 [字卡导入] 行会给出真实失败点 |
| 183 | 功能：设置页新增【功能诊断】（与信息诊断/屏幕适配诊断并列）——一键逐项测试全部功能哪些正常哪些异常，回答「功能诊断测试全部功能」诉求 | 实现（device.js collectFuncDiag，三级测试）：25 项功能逐项测——T1 入口函数 typeof（帮我决定/多人决定/TA 询问/TA 心情/TA 吐槽）；T2 页面容器+桌面图标节点存在；T3 真实打开测试（安全子集 20 项纯页面查看器：点桌面图标 → 目标页可见 → 点 .ch-back 返回 → 回桌面+关浮层+复位 tab，计耗时）。门控功能（群聊）打开未生效时标 ⚠ 需注意不判异常；测试后强制恢复桌面+关浮层。报告含基础就绪（数据/SW/LS/在线）+逐项明细+汇总（正常/需注意/异常/跳过）+✗ 清单，弹窗大窗展示+自动复制 | 寻踪打卡标注 gated（可能需先绑定 TA/授权定位会先弹引导，未生效记 ⚠ 不判异常）。哨兵 2 条（build.mjs FIX_SENTINELS）：template row-func-diag 锚点 + device.js collectFuncDiag。验证：node --check 过；--check-sentinels 364 全绿；CDP 端到端（无头跑完整 25 项流程）：全部 ✓ + 仅寻踪打卡 ⚠（无头无授权属预期）。【真机:待验证】（任意机型）：设置→功能诊断→约 15 秒逐项打开测试→报告正常项 ✓/异常项 ✗；群聊未开启显示 ⚠ | <br /> | <br /> |
| 184 | 多机型（2026-09-05 用户反馈）：开屏顶部署名行下方新增的 Mochi 名字澄清长句不换行超出手机屏幕——该行复用 .splash-source-line，其 white-space:nowrap 本为单行网址/署名设计，长句被禁止换行横向溢出 | base.css 新增 .splash-source-line.splash-clarify { white-space:normal; padding:0 10px }，澄清行加 splash-clarify 类放开换行；原单行网址/署名行为不变 | 哨兵 1 条（build.mjs）：.splash-source-line.splash-clarify { white-space:normal。真机：开屏顶部澄清句应自动折行不横向超出 |
| 185 | 多设备（2026-09-05 用户反馈）：聊天里联系人发出空白内容的气泡，里面什么都没有——回复链路对字卡内容零非空校验：①回复池混入空串/纯空白/零宽字符字卡（导入包/手编均可产生）时，单抽空串虽有 `|| FALLBACK` 兜底但纯空格是 truthy 直接发出；②开多字卡回复时 `pickN(pool.text,n).join(' ')` 无滤空，可拼出纯空格；③固定回复字卡（getReplyCard）/默认主字卡（defs.text）被设成空白时无条件覆盖抽好的回复；④渲染端对空文本消息渲染成空壳气泡，历史脏数据永远显示为空；⑤用户删除该空白消息后刷新又复活——消息操作菜单「删除」走 msgs.splice+saveMsgs 但没同步 #180 尾巴日志，chatTailMerge 刷新回放时发现日志里有、历史里没有，把删掉的消息拼回 | 删除复活（chat.js）：del 分支 splice 前补 chatTailDrop(msgs[idx])（与撤回/局部撤回同口径）。生成端（chat.js）：新增 pickNonBlank（跳空白抽卡，20 次抽不中回走上层兜底）替换 genReplyText 五处 pick；多字卡拼接前 `filter(s=>s.trim())`；genOneReply 在 replyWord/defs 覆盖后加最终非空兜底 `if (typeof t !== 'string' || !t.trim()) t = pick(FALLBACK_REPLY_POOL)`。渲染端（renderMsg）：普通文本分支与 parts 分支对空白内容显示「（空白消息）」占位（opacity .5 小字）——已存了空白记录的其他设备更新后也能看出占位而非空壳 | 哨兵 3 条（build.mjs）：genOneReply 最终兜底整行表达式 + renderMsg `const __blankMsg = !__rawText.trim();` + del 分支 `chatTailDrop(msgs[idx]); // FIX 2026-09-05 #185`。验证：node --check 过；--check-sentinels 全绿；【真机/其他设备：更新后重进聊天，历史空白气泡显示「（空白消息）」占位；新回复不再出现空气泡；字卡管理页可顺手删掉内容为空的字卡】 |
| 186 | 多设备（2026-09-05 用户反馈，与 #185 同症状不同根因）：联系人单发【表情包】/【图片】变空白气泡——媒体消息渲染 `<img src="@@m:哈希">` 依赖媒体池（IDB xy-home-v2:media:<hash>）解析，池数据丢失即无声空白。丢数据主因：①「清理孤儿媒体」GC 引用扫描正则 /(?:^|:)(?:chat-msgs|fav-msgs)$/ 漏了群聊键（xy-home-v2:group-chat-msgs 前缀是 group- 不是 :，:gc-msgs-<gid> 同理）和 LS 快照/尾巴日志（#180 下 IDB 写失败机型消息只在 LS）→ 被引用图片被误判孤儿删除，不可逆；②备份导入未带全池键；③mediaNormalizePass 写池不校验返回值——mochiMediaFlush 失败返回 false 仍 saveMsgs 带令牌落库，池数据只在内存重试队列，vivo X200s+Edge 等大库机（IDB≈3GB）页面被杀即令牌失去数据，安卓多机型反复。用户诊断实锤：chat 页 12 条 <img src="https://…/@@m:hash"> 资源加载失败=令牌未解析 | media-pool.js mochiMediaGC：REFS 扩为 /(?:^|:)(?:chat-msgs|fav-msgs|group-chat-msgs|gc-msgs-[0-9A-Za-z_-]+|chat-tail)$/，并追加扫描 localStorage 同名键（__ls__ 前缀标记，读到的令牌全进 keep；LS 整体读异常→放弃本次清理，宁漏删不误删）；引用读循环分支处理 LS 键（单键缺失不构成放弃条件）。chat.js mediaNormalizePass：记录回滚账，flush 返回 false 时回滚全部令牌化+WeakSet 去标记+8s 后重试，绝不带令牌落库。chat.js renderMsg：sticker/image 分支对令牌 src 挂 error 监听，池缺失时渲染「（图片丢失：媒体数据缺失，可用数据备份重新导入恢复）」占位，不再空壳 | 哨兵 3 条（build.mjs）：media-pool 新 REFS 正则整行 + chat.js `const _tokImg` + chat.js `if (_ok === false) {` = b.querySelector('.msg-img');`。验证：node --check 过；--check-sentinels 全绿。【真机：①设置→查看存储→清理孤儿媒体→群聊里的表情/图片刷新后仍正常；②历史已空白的表情气泡更新后显示「图片丢失」占位】 |
| 179 | iPhone 14 Pro + Safari 主屏幕全屏（iOS 18.7/Safari 26.6）：网页底部出现空白（#148 回归：覆盖形态被 #148 高度公式误伤）。屏幕适配诊断自动监视捕获（错误环 7 条自动采集实锤）+手测报告全 ✓ 但用户见底部白带——判定规则盲区 | 根因：#148 把 fs 态 .phone 高度改写 vv 实测（inner=793），但该机是「覆盖形态」（env-top=59，内容垫到状态栏下方，可视=整块物理屏 852）→ 高度少算顶部安全区 59px，底部 793..852 露白 60px。两类形态的正确高度统一公式：**高度 = env-top(实测) + inner-height**（覆盖=852 整屏；已避让=812 inner）。修复：①mobile-adapt syncVvFit fs 态 --mochi-ios-h 改 Math.min(safeTop+inner, screen)；②非全屏 standalone 同步（同样白带形态）；③判定器底部期望底边改 envTop+inner（少填=白带 ✗/超出=裁切 ✗）、tabbar 期望底边改 屏底−env-bottom、--mochi-ios-h 对比期望屏高；④常驻监视跳过键盘/聚焦会话（瞬态误报「顶部重叠」刷错误环，14 Pro 实测 12:36-12:39 连环误报） | 哨兵：#148 mobile-adapt needle 更新为 Math.min(_safeTop + _ih2, _sh2（公式变更锚点同步）。验证：node --check 过；判定器七场景（#179 修复前 3✗/修复后全✓/#168/#148 坏态 4✗/#114/健康浏览器/平移残留）全过；--check-sentinels 371 全绿。【真机:待验证】（iPhone 14 Pro 主屏幕全屏）：底部白带消失 tabbar 贴底（Home 横条 34px 避让保留）；iPhone 15 Pro/16 Pro 回归不变形 | <br /> | <br /> |
| 187 | 多机型（2026-09-05 vivo IQ12+Edge 诊断报障，用户反馈其他型号设备也出现）：「A 角色桌面的聊天里，A 给我发了 B 桌面的专属字卡」——专属字卡跨桌面串用                                                                                                                                                                                                                                                                                   | 根因：chat.js tryAutoSend（TA 主动消息）异步链无跨桌面守卫——await ensureReplyCardsReady() 取回字卡可等数秒（大库/慢 IDB 更久），随后每条消息 setTimeout 再 900~2600ms；期间用户从 B 桌面切到 A 桌面，pool 仍是 B 桌面的（getPool 在 await 后一次性构建），addIn 把 B 池专属卡发进 A 桌面聊天。scheduleReply/replyOnce/continueChat 均有 sameCid 守卫，唯独主动消息整条链漏了，与设备无关。修复：tryAutoSend 入口捕获 autoCid + sameAutoCid；await 取回完成后放行前拦截（已切桌面整条放弃）；消息主 setTimeout、rc-prob 撤回重发链、尾部 callMaybeTrigger/系统自动发送等每个 setTimeout 入口逐层拦截；pool 构建保持在守卫之后                                                                                                   | 哨兵 2 条（build.mjs）：sameAutoCid 定义行 + await 后拦截行（逻辑锚点，非函数名）。验证：node tools/verify-auto-cid-guard.mjs 8/8（抽取 tryAutoSend 真实源码断言守卫链结构：入口捕获/await 后拦截/每个定时器入口守卫/既有 sameCid 未被动）；无头 Chromium 双桌面种子库（>200KB IDB-only 大库）切桌面读池基线全对；【真机：更新后在 B 桌面等 TA 主动消息触发瞬间切到 A 桌面，A 不再发出 B 的专属卡】 |
| 188 | iPad Air 6 + QQ浏览器 20.5.3（2026-09-05 诊断报障 ts=1788590064062）：「朋友圈里不显示图片」——动态文字/点赞/评论正常、配图全部不显示；关键佐证：用户 15:00:28 进朋友圈前后错误环零新增（若有 img 元素只是解码失败必留「资源加载失败」记录）＝feed 页根本没有 img 元素，是数据层无图而非解码失败；同窗口聊天页另有 3 条 data:image 截断加载失败（同族症状：IDB 读失败回退有损快照） | 根因：feed-posts（朋友圈权威数据）超 200KB 后按大键分流只写 IndexedDB（LS 副本被迁走删除）；WKWebView 系（iPad QQ浏览器/Safari 家族，iOS 挂后台杀 IDB 连接）首发 idbGet 4s+4s 超时返回 undefined——与「键不存在」不可分，此时启动回填未到、store.get(KEY)=null，load() 手上只剩【剥图快照】（feed-posts-snap：图片 dataURL 全被换成 [图片]、imgs/头像清空）。旧代码有四条路径会把这个无图版本整包写回权威键：①feedMergeFromIdb 权威读失败仍 store.set(merged)；②save() 未就绪非空直写；③15s 保险丝 load() 写回；④发布兜底直写——一经写入图片从数据层永久消失（IDB 被无图版覆盖，快照本就无图），之后 IDB 恢复也救不回，与 #85/#120 同族（IDB 挂起→主键只剩剥图快照）但伤在「写回覆盖」而非只读丢失。修复（feed.js 权威键守卫 #188）：新增 feedAuthSeen（本会话读到过权威完整数据，含清空 '[]'）+ feedGuardWrite 守卫写——本会话见过权威 或 store.get(KEY) 非空（回填已完成）→ 照常写；否则 window.idbHasKey 探测权威键：确认存在＝绝不写回（增量留内存 pending + persistSnap 文本兜底，10s 后重读权威最多 2 次恢复完整视图），确认不存在（全新安装/丢库重建）＝放行写（保住原「并集保数据」行为），探测失败（null=存储繁忙）按存在处理宁拒写；feedMergeFromIdb 只在写回成功后清 pending（拒写时保留并新增「就绪后也合并 pending」渲染）；四条写回路径全部改走守卫 | 哨兵 6 条（build.mjs，全为逻辑锚点）：idbHasKey 探测表达式 / feedMergeFromIdb 守卫调用 / 15s 保险丝调用 / 发布兜底调用 / save 预就绪分支相邻对（feedGuardWrite(raw)+persistSnap(arr)）/ save post-ready 分支相邻对。验证：node tools/verify-feed-auth-guard.mjs 16/16（抽 feed.js 真实守卫+load+feedMergeFromIdb 源码桩环境断言：超时拒写/键不存在放行/探测失败按存在处理/无接口旧行为/10s 有界重读上限 2/seen 置位/pending 保留与清空/就绪后合并 pending）；node --check 过；--check-sentinels 全绿哑哨兵 0。给用户：本修复防复发+读失败窗口内自动恢复；若权威键已被旧版无图版本覆盖过（更新后重进朋友圈仍无图），图片需从「数据备份」导出包恢复（其他设备导出→本机导入） |
| 190 | OPPO Find X9 手机自带浏览器 HeyTapBrowser 40.10（2026-09-05 用户报障，自述多机型通病）：「一进去网页就有底噪/电流声」——进页面即持续的嗡声，与任何消息音效无关（sfx 默认关闭，诊断无音效选择键） | 根因（bg-keep.js 保活音频）：后台保活用 1 秒循环 220Hz 正弦波 <audio>（volume 0.05）维持「页面在出声」躲 Chromium 后台冻结——iOS 曾因同类可闻（v3.15.x「嘟嘟嘟」）把幅度降到 0.002（≈-80dBFS 物理不可闻），安卓当时按「近零音量会被无声节流」的保守估计保留 0.02（×0.05 ≈ -60dBFS）：220Hz 正在人耳最敏感低频段、循环常播，灵敏扬声器/安静环境实听即持续底噪。修复：安卓幅度 0.02 → 0.006（×0.05 ≈ -88dBFS，任何扬声器物理不可闻）——防无声节流的有效条件是「样本非零 + volume>0」（浏览器静音检测按零样本/静音状态判定，不按响度），非零即保活有效；iOS 维持 0.002。若后续真机发现保活失效（后台被冻结）再回调上限并改用其他豁免信号，不回 220Hz 大音量 | 哨兵 1 条（build.mjs）：bg-keep.js `kaIsIOS() ? 0.002 : 0.006`（幅度表达式逻辑锚点，文件内唯一）。验证：node --check 过；--check-sentinels 380 全绿哑哨兵 0。【真机:待验证】（OPPO Find X9 自带浏览器）：进网页不再有底噪/电流声；开后台保活切后台 1 分钟以上回前台，TA 消息/通知仍正常产生（保活未被节流） |
| 192 | 用户反馈「装修模式小组件透明度是全局统一的，要每个组件单独调」：原「组件透明度」入口写全局 widget-opacity（--widget-opacity 变量），所有组件一起变 | 每组件独立透明度：按类型存 widget-opacity-<type>（per-cid 随桌面），渲染时内联 style.opacity 覆盖全局变量，未设/100 时清内联回落全局；装修模式点卡片→组件透明度滑条只改本组件，新增「应用到全部小组件」一键把当前值写入所有 [data-card-bg] 组件键；启动/切桌面/refreshDeskVisuals 统一 applyAllWidgetOpacities 回放；设置页与边看边调抽屉的全局滑条保留为「全局默认」。验证：node --check 过；--check-sentinels 391 全绿哑哨兵 0（哨兵锚 widgetOpKey 定义行） |（真机:待验证）：装修模式点任一组件调透明度只影响该组件；「应用到全部」后所有组件同值；恢复默认后跟随全局 |
| 193 | iPhone 17 Pro + iOS 自带浏览器 Safari 18（2026-09-05 诊断报障 ts=1788577876618，v3.26.445）：「字卡库导入字卡，一次性导入太多导致之前字卡全部消失」——公用库 cc-groups-public 达 17.67MB（大键挂起形态），输入轨迹 18:23:48 向批量导入弹窗粘贴 51695 字符后旧字卡消失 | 根因（chatcard.js，#188/#120/#85 同族第三例，伤在「写回覆盖」）：公用大库被启动回填挂起在 IndexedDB（__xyIdbDeferredKeys）时，openCcPage 显示页面后 hydrateCurScope 异步取回权威库；该窗口内（iOS 慢 IDB/挂后台打断/大库 JSON.parse 期间可达数秒甚至失败）内存 groups 是空库或残缺快照，批量导入（含上传图片/编辑/删除等全部写路径）把新内容合并进残缺库后 saveGroups 整包写回权威键——旧字卡被小库整体覆盖且不可恢复；#139 防复制守卫只护 JSON 文件导入路径，批量粘贴导入无任何守卫。修复（chatcard.js 权威键写守卫 #193）：新增 ccAuthSeen 双作用域标记（启动恢复 applyRestored 读到权威/hydrateScope 三出口——hasData 预检、idbHydrateKey 成功、健康连接确认无键 ok===null——且 fullKey===curFullKey() 时置位；contact-switched 重置 own），saveGroups 守卫：本会话未确认权威库已取回时不直写，先 idbHasKey 探测权威键——确认存在=绝不整包写回，走 rescueCcOverwrite 合并营救（先保住内存增量 mem，hydrateCurScope 取回权威库后 mergeCcGroupsInto 按分组把内存增量去重并进权威再写，旧字卡与本次导入都不丢+重绘界面），确认无键（新装/空库）=放行直写，探测/取回失败按存在处理宁缓写；幂等 ccRescueInflight 去重，ccDirty 保持待写供离页冲刷复用 | 哨兵 3 条（build.mjs，全为逻辑锚点、chatcard.js 内唯一）：守卫表达式 / mergeCcGroupsInto 合并写回 / 确认无键放行分支。验证：node --check 过；node tools/verify-cc-import-guard.mjs（行为断言）；--check-sentinels 全绿哑哨兵 0。【真机:待验证】（iPhone 17 Pro Safari）：字卡库批量导入大量字卡后旧字卡不再消失；正常导入/上传/编辑/删除功能不受守卫拖慢（已取回过权威库时零开销直写） |
| 189 | 多机型反复（iPad Air 7+Safari 主屏/浏览器 2026-09-05 用户报障「全屏模式没有生效」+「全屏模式下滑动一直闪烁」，用户明说其他型号设备也有；#144 修完设备判定后仍复发，iPad Safari/iPadOS 均命中） | 三根因五处修复：①fullscreen.js iOS 全程不启动方向监视/横屏兜底（新增 startFsMonitorSafe 出口供 iosTryNativeFs 与两处 fullscreenchange 使用 + orientationchange 监听 iOS 出口 + 开关 1500ms 复核 `!isIOS` 门）——Safari 无 orientation.lock（WebKit Bugzilla #257695，全屏态调用也只报错），iPad 横屏持握点全屏约 2 秒后被 handleLandscapeForced 自动退出+误报「网页全屏会转横屏」弹窗+FB_KEY 被永久写坏=「没有生效」；②base.css html.tablet.ios-pwa-standalone.ios-fs-active 隐藏模拟状态栏——#111 手机保留行为原样不动，iPad 无灵动岛、系统栏独立成条网页盖不住，保留=开关零视觉变化；③mobile-adapt.js healViewport 补裸 d 声明（v3.26 重写漏写，d 解析到 window.d=undefined → TypeError 被 try 吞=稳态残留清理/大平移归零/#174 缩放自愈整层死代码）+ 稳态 pinScrollTop 改条件式（真清了键盘残留或窗口大偏移才归零）+ 全屏底边容差计入 --mochi-safe-top + 全屏态跳过 vv offset 归零判定 + --mochi-ios-h 全屏/非全屏两分支 ≥6px 写入迟滞——#179 后 .phone 底边天然超出 vv 一个顶部安全区，无条件 pin 与用户滚动每秒对打、vv 逐帧抖动逐次重排=「滑动一直闪烁」 | node tools/verify-fullscreen-ipad.mjs（25 断言：isIOS 五场景行为求值+四守卫接线+d 声明先于使用+迟滞+#111 语义）+ 哨兵 10 条（build.mjs FIX_SENTINELS）；真机：iPad 横屏持握浏览器点全屏应保持全屏不退出不弹窗；iPad 主屏幕开全屏=Mochi 模拟状态栏消失内容顶到系统栏下方；iPhone/iPad 全屏下反复滑动页面不再闪跳 |
| 196 | 小米15 Pro + Chrome 151（2026-09-05 用户报障）：「mj 发的字卡频繁出现『握紧你的手』『摸了摸你发顶』『傻瓜』『我在呢』等，每次聊天都是这几句，感觉是 bug」——经查全部来自 DEFAULT_CARD_DATA.period「温柔前缀/温柔动作」分组（period.js 经期温柔语态：经期中 TA 每条文字回复 25% 概率随机拼前缀/动作，#157 已随总开关/聊天使用联动） | 根因：前缀/动作两池各只有 6 条且为纯均匀随机、无任何去重记忆——数学期望上每 4 条回复就出现一次、且高频连抽同一句，用户感知「只会这几句」。修复（period.js）：新增 warmRecent 近期记忆（前缀/动作各记最近 3 条），warmPick 先从「不在近期」的候选里抽，全部用过才放宽回整池——同句连抽被消除，6 条池内轮换观感自然；触发概率 25% 与拼装比例（前缀/后缀/双拼 45/35/20）维持 #157 原设计不动 | 哨兵 1 条（build.mjs）：warmPick 的 fresh 过滤表达式（逻辑锚点，period.js 内唯一）。验证：node --check 过；--check-sentinels 全绿哑哨兵 0。【真机:待验证】经期中连续聊天，同一句温柔前缀/动作不再连续出现；字卡库关掉单张前缀/动作后该句不再被拼出 |
| 197 | 小米15 Pro + Chrome 151（2026-09-05 用户报障）：「心愿单概率无法修改，改完退出再点回去恢复默认概率」——用户明说其他设备型号也有；实为全安卓通病且不止心愿单：全站挂在 input change 事件上的设置保存（数字/日期类输入）在安卓上全部静默失效，症状各处不同（改了没存/退出回默认/保存按钮无反应） | 根因（mobile-adapt.js ce-box，机制级）：安卓上文本输入框被转成 contenteditable div（.ce-box），而 contenteditable 规范上不派发 change 事件（v3.9.x 注释早已自知）；ceConvert 的转发链是「box 自己发出 change 才克隆给原 input」——box 永远不会自发发出，于是原 input 的 change 监听（gift-shop.js 心愿单概率 wlBuyPct/wlAddPct/selfPct、以及全站同模式的历史代码）在安卓上永不等触发；开关类（click）能存、输入类（change）不能存，与「开关正常、概率回默认」的症状完全吻合，iOS 不转换不受影响。修复（mobile-adapt.js）：box 聚焦时记录基线文本，blur 时若内容相对基线有变化则对 box 补派一次 change（走既有转发链到原 input），语义与原生 input 的 change 一致；无变化不派，程序化回填不受干扰 | 哨兵 2 条（build.mjs）：blur 补派 dispatchEvent 行 + focus 基线记录行（mobile-adapt.js 内唯一，删任一即哑火）。验证：node --check 过；--check-sentinels 全绿哑哨兵 0。【真机:待验证】（安卓任意机型）心意集市和心意柜设置改概率→退出重进数值保留；顺带回归检查其他数字设置项（存钱罐金额等）安卓上改完能存 |
| 198 | 小米15 Pro + Chrome 151（2026-09-05 用户报障）：「桌面经期组件卡片无法上传壁纸」——装修模式点经期卡可看到上传入口，传完提示已设置但不生效，重启也不显示 | 根因（personalize.js，#192 收口时已注意到 desk-period 是「裸类型」但只修了透明度漏了壁纸）：模板 template.html 上经期卡带 data-card-bg="desk-period"，但 CARD_BG_TYPES 白名单没有该类型——cardBgSel 返回空串，applyCardBg 开头静默 return：上传时键已写入但样式永不应用；applyAllCardBgs 与 rescueDeskVisuals（IDB 大图直读兜底）也只遍历白名单，即使键在（切换桌面/重启/仅存 IDB）也永不被应用。修复（personalize.js）：①cardBgSel 对未登记类型回退 `[data-card-bg="<type>"]` 按属性直选；②新增 cardBgAllTypes 从 DOM 收集全部卡片类型（白名单+裸类型去重，#192 applyAllWidgetOpacities 同法），applyAllCardBgs 与 rescueDeskVisuals 的键清单改走它——上传即时生效、重启/切桌面/IDB-only 兜底三路都覆盖裸类型；设置页列表与已有卡菜单行为不变 | 哨兵 2 条（build.mjs）：cardBgSel 兜底返回表达式 + applyAllCardBgs 全类型遍历行（personalize.js 内唯一）。验证：node --check 过；--check-sentinels 全绿哑哨兵 0。【真机:待验证】装修模式点经期卡→上传图片立即生效；杀进程重进/切换联系人桌面后壁纸仍在；已登记白名单的九类卡片壁纸行为无变化 |
| 199 | 荣耀50se + 雨见浏览器（Gecko/Firefox 152 内核，安卓 12；2026-09-05 诊断报障 v3.26.461 ts=1788602270314，用户明说其他设备型号也有）：「聊天界面偶尔会掉下来一半。还有删消息和对方回消息的时候，屏幕会往上移」——诊断 ✗顶部重叠（状态栏顶位 4px 钻进 35px 系统状态栏区，#114 形态）+ ✗底部少填 35px（#179 判定） | 根因两条：①沉浸式安卓浏览器覆盖形态（雨见/Via 等自绘壳：页面顶到系统状态栏下、screen==inner、standalone=false）无人避让——mobile-adapt.js 的 env() 探针只认 ios-pwa-standalone，该形态 env 实测 35px 却没人写 --mochi-safe-top；且 base.css 窄屏 @media 里带 env() 的 .statusbar 避让规则被同特异性后加载的 .statusbar{padding:4px 4px 12px} 压死（#114 台账记过的根因，v3.28.x 只给 iOS standalone+全屏形态绕过），CSS 侧无法用「var 已设」表达条件。②Gecko 滚动锚定（scroll anchoring）：删消息/对方回消息改动 chat-body 上方内容时，Firefox 系内核自行调整 scrollTop，与 chat.js #162 贴底钉住逻辑对打=「屏幕往上移」（Chromium 锚定行为不同+Chromium 有 #162 三连写兜底，故安卓 Chrome 系少见）。修复：①mobile-adapt.js syncVvFit env 探针扩展——非 standalone 且 screen−inner≤2（无浏览器 chrome=覆盖形态）同样探 env、写 --mochi-safe-top 并挂 mochi-cover-top 类（iOS standalone 不挂类，既有 .phone padding/ios-fs-active 避让链不动、不会双倍）；base.css 新增 html.mochi-cover-top .phone .statusbar 提特异性夺回顶部避让（padding-top:max(calc(14px+var),14px)）。②刻意**不**扩 #179 的 envTop+inner 高度公式到该形态——布局视口=inner，.phone 超出可视区会造出 35px 文档滚动量、被看门狗 pinScrollTop 拽动=页面跳动；正确做法是 .phone 仍贴 inner、内容在状态栏下方收缩避让。③base.css 给 .chat-body 关滚动锚定（overflow-anchor:none，跨内核安全）。④device.js 判定器适配：浏览器覆盖形态期望底边=可视区底（否则修好后 #179 判定误报） | 哨兵 5 条（build.mjs，全逻辑锚点、各自文件内唯一）：_coverBrowser 判定 / mochi-cover-top toggle / base.css mochi-cover-top 状态栏规则 / .chat-body overflow-anchor / 判定器 coverBrowser 表达式。验证：node --check 过；--check-sentinels 全绿哑哨兵 0。【真机:待验证】（荣耀50se+雨见）：①再跑屏幕适配诊断，顶部重叠/底部少填两条 ✗ 应消失（状态栏顶位≥35px）；②聊天页顶部不被系统状态栏盖住；③删消息/对方回消息时屏幕不再上移、始终贴最新消息。回归面：iOS standalone（挂类豁免）、常规带地址栏浏览器（探针不触发）、Chromium 系聊天滚动（overflow-anchor 行为一致） |
| 200 | OPPO Reno6 5G + 雨见浏览器（Firefox 152 内核，安卓 13；2026-09-05 用户报障，明说其他设备型号也有）：「跟部分角色打电话，通话设置挂断几率为 0 但还是挂了，有的角色不会这样」 | 根因（call.js/reply-settings.js）：①挂断概率按联系人桌面（per-cid）隔离存储（reply-call-hangup 存各自命名空间），只在某一个联系人桌面把几率调成 0 时，其余从未保存过该键的联系人会静默回落默认 2%——「有的角色不会这样」与「设 0 仍被挂断」同源；②安卓上挂断概率 stepper（步长 0.01）经 ce-box 转换后失焦不派 change 导致输入值没保存（#197 已修机制），存量设备旧构建仍中招。修复：①新增「禁止联系人挂断电话」总开关（call-no-hangup，通话设置页，默认关、需手动开启，按联系人各自保存）——callCfg 读入 nohangup，通话中挂断掷骰处硬闸：`!(hp.nohangup \|\| hp.hangup <= 0)` 才掷骰，总开关开或几率为 0 一律不挂断，兜住一切回落默认值的路径；②template.html 通话设置页「对方挂断」组新增开关行+说明文案（写明按联系人隔离的坑）；③reply-settings.js DEFAULTS 登记 + 三处开关清单同步（syncUI/交互/save 全量保存） | 哨兵 4 条（build.mjs，全逻辑锚点、各自文件内唯一）：call.js 掷骰硬闸表达式 / call.js nohangup 配置读取 / reply-settings.js 默认值登记 / template.html 开关行。验证：node --check 过；--check-sentinels 全绿哑哨兵 0。【真机:待验证】（OPPO Reno6 5G + 雨见，多机型通用）：①通话设置页出现「禁止联系人挂断电话」开关（默认关）；②开启后任一联系人通话超 3 分钟以上不再被对方挂断；③关闭总开关、挂断概率设 0 的联系人通话不被挂断；④关闭总开关、概率保持默认的联系人仍可能被挂断（机制未被一刀切）。回归面：通话其余概率（来电/接听/忙线/拒接）、刷新恢复通话、响铃挂起 #161 均不受影响 |
| 201 | OPPO Reno14 + Edge 浏览器（安卓，2026-09-05 诊断报障 v3.26.464 ts=1788604688211，用户明说其他设备型号也有）：「页面没有正常显示贴合屏幕，顶部有一块空的黑边」——诊断全绿：已避让形态、贴底/导航栏正常，diff(screen−inner)=41px 即页面顶上那条被涂黑的系统区域 | 根因：`<meta name="theme-color">` 写死 `#111111`（近黑，历史上疑似为压 Android 强制深色所留）。安卓 Edge/Chromium 在「页面下移避让系统栏」形态（本例 diff=41px）会用 theme-color 涂页面外的系统区域，页面本体浅色 → 顶部一条黑边；深色模式下反而正确，故浅色用户全量中招。修复：①template.html 静态默认改 `#e9e9e9`（浅色 --page-bg）；②personalize.js applyThemeMode 末尾把 meta theme-color 同步为当前生效主题的 `getComputedStyle(--page-bg)`，浅/深/auto 三档及系统主题变化全跟随。布局链零改动（诊断本就全绿，不动 #179/#199 高度与避让公式） | 哨兵 2 条（build.mjs，逻辑锚点、各自文件内唯一）：template theme-color `content="#e9e9e9"` / personalize `meta.setAttribute('content', bg)`。验证：node --check 过；--check-sentinels 410 全绿哑哨兵 0。【真机:待验证】（OPPO Reno14+Edge）：顶部黑边应变为与页面一致的浅色；切深色模式后该区域应跟随变深；iOS/其他浏览器无回归（theme-color 仅影响浏览器 chrome 涂色，不参与布局） |
| 202 | 多设备续报（2026-09-05 iQOO12+Chrome，已是最新构建仍报「联系人单发表情包变空白」；错误环无 @@m: 报错但有断网记录）：#186 占位只挂在「媒体池令牌」src 上，表情/图片还有三类失败路径完全无处理——①远程 http(s) 链接图（v3.11 起字卡库支持）断网/原图失效/https 页面拦混合内容；②dataURL 损坏解码失败；③parts 混合消息（文字+图）里的 img 根本没挂 error 监听——全部渲染成无声空白气泡 | chat.js 新增 bindMediaFailPlaceholder(b)：给气泡内全部 .msg-img 挂 error 监听（dataset.errBound 防重），error 后延时 1.5s 复核 im.complete 且 naturalWidth=0（给 #186 池观察器改写 src 的竞态和慢网加载留窗口，不误伤正常图）且令牌场景池 expand miss 确认，才把 img 节点 replaceWith 成占位 span（令牌=「图片丢失：媒体数据缺失…」/其余=「表情/图片加载失败：网络不通或原图已失效」）；sticker/image 分支与 parts 分支统一接入（只换 img 节点，引用块/情绪 chip 不动）；build.mjs #186 渲染哨兵锚点同步改写为两个新锚 | 哨兵 2 条（build.mjs）：`function bindMediaFailPlaceholder(b) {` + `if (im.naturalWidth !== 0) return;`。验证：node --check 过；--check-sentinels 全绿；verify-quote-image / verify-media-pool 复跑。【真机：iQOO12 等设备断网时联系人发表情→气泡显示「加载失败」占位而非空白；联网后新表情正常】 |
| 203 | iPhone 15 Pro + Safari 18.3 主屏幕（standalone，2026-09-05 诊断报障 v3.26.464 ts=1788604688211，用户报「滑动和切换卡顿」并明说 iOS 其他机型也有）：自检自动采集 ✗顶部重叠+底部少填，.phone 高=852 顶=-29/底=823 | 根因：iOS 18.x 起 standalone 状态栏行为变更（多方报道 black-translucent 透明被移除/改变）——「系统保留形态」：inner=793=screen−envTop（系统已把页面起点放在状态栏下方）但 env() 仍返回真实 59px。既有链在该形态双重出错：① --mochi-safe-top=59 → 状态栏双重避让；② #179 公式 safeTop+inner 把 .phone 写到 852，超出布局视口 793——html/body 是 flex 居中容器 → 整壳被居中裁切（顶−29/底 823），文档恒溢出 59px 与自愈 pinScrollTop/1s 看门狗长期对打 = 滑动/切换顿挫。修复（mobile-adapt.js syncVvFit + device.js 判定器）：新增保留形态甄别 `standalone && _safeTop≥20 && screen−inner ≥ _safeTop−8 && iOS≥18`——命中时 _safeTop 归 0 且显式写 '0px'（摘除属性会回落 env() 反而避让）：高度 bump 自然失效贴回 793 可视区（无文档溢出/无裁切），顶部避让交还系统；iOS≥18 门槛保护 17.x 及更早覆盖形态设备（同款信号靠 #179 链已真机验证）零回归；#148 已避让形态（env=0）与 #199 浏览器覆盖形态（非 standalone）均不命中零扰动。判定器同步：保留形态期望底边=inner、sbTop 期望 12、fs 期望屏高=inner（修好后不误报），采集器补 iosMajor | 哨兵 4 条（build.mjs，全逻辑锚点、各自文件内唯一）：mobile-adapt 甄别式+显式 0px / device 判定器甄别式+期望底边。验证：node --check 过；--check-sentinels 全绿哑哨兵 0；tools/verify-ios-reserved-standalone.mjs 25 断言（甄别式四形态场景+判定器 9 场景含 iOS17 防回归）。【真机:待验证】（iPhone 15 Pro iOS 18.3 主屏幕，多 iOS18 机型通用）：①诊断自检屏幕适配段全绿（无顶部重叠/底部少填）；②.phone 高=793 顶=0 贴可视区；③桌面滑动、tab 切换不再顿挫/跳动；④iOS 17 及更早设备、浏览器模式、安卓均不受影响（甄别不命中） | <br /> | <br /> |
| 204 | 多设备通用（2026-09-05 用户报障：「后台浏览器弹通知显示联系人给我打电话，马上切回浏览器却没有来电弹窗」）：#161 响铃挂起只覆盖当前桌面来电（call.js maybeIncoming hidden 分支）与响铃中切走两条路径；跨桌面来电（incoming-requests.js deliver() hidden 分支）仍是 #159 口径——发完「XX 来电了」系统通知后直接 setStatus(seen) 丢弃，call-hold 从未写入，切回应用必然无来电 UI、超时也不补未接记录 | 修复（call.js + incoming-requests.js 跨域，call.js 只暴露接口）：①holdIncomingCall/bgCallNotify 加可选 avOverride 参数（跨桌面来电通知头像用归属联系人 cAvatar，不借当前桌面 partnerAv）并暴露 window.callHoldIncoming；②deliver() hidden 分支 kind=call 改走 callHoldIncoming(name, req.cid, av)——同口径发「快回来接听，对方会等你几分钟」通知+写 call-hold（含归属 cid），3 分钟内回前台 resumeHeldCall 重响可接听（cid 不匹配/超时/已在通话照旧补写未接，跨桌面自动落归属桌面），超时未回补写「来电 · 未接听」 | 哨兵 2 条（build.mjs）：call.js ｀window.callHoldIncoming = holdIncomingCall;｀ / incoming-requests.js ｀if (window.callHoldIncoming) window.callHoldIncoming(name, req.cid, av);｀；另同步 #150/#161 两条 needle 至新签名。验证：node --check 过；--check-sentinels 415 全绿哑哨兵 0；无头实测（tools 外探针）挂起→visible 重响两场景过、verify-call-dur 复跑过。【真机:待验证】后台收到跨桌面来电通知→3 分钟内切回应重响可接听；超 3 分钟→归属桌面出现「来电 · 未接听」；当前桌面后台来电/响铃切走重响不受影响 |
| 184 | iPad Air 7 + Safari 主屏幕全屏：屏幕适配诊断报 ✗底部少填 32px 白带 / ✗--mochi-ios-h 不符（期望 1212>屏高 1180），用户感知「全屏模式依旧不正常」 | 根因（device.js 判定器公式盲区，非布局错误——实测 .phone=1180=屏高、statusbar 顶位 32、tabbar 底边 1160 全部正确）：iPad 形态 inner=屏高已含整屏（diff=0），旧判定 expBase=envTop+inner 把状态栏 32px 双算（1212>1180）→ 误报。修复：判定器 expBase/ios-h/tabbar 期望统一改「min(屏高, envTop+inner)」并新增 ipadForm 例外（standalone+env≥20+diff≤2+inner≈屏高 → 期望=inner），与 #199 浏览器例外/#200 保留例外并列 | 哨兵：#203 expBase needle 同步为含 ipadForm 版本。验证：node --check 过；八场景判定回归（iPad 形态全✓/14Pro 覆盖/16Pro 已避让/#148 坏态 4✗/#114/健康浏览器/平移残留）全过；--check-sentinels 413 全绿。【真机:待验证】（iPad Air 7 主屏幕全屏）：更新后屏幕适配诊断全 ✓；布局无变化（原本正确） | <br /> | <br /> |
| 205 | 多设备续报（2026-09-05 用户强调「好多手机都有」；iQOO12 诊断错误环无任何图片加载报错仍见空白表情）：#186/#202 覆盖了「加载失败」全部路径，但还有最后一类真空白=图片**加载成功而内容本身没有画面**——全透明/空白图。导入的字卡包（多设备共用同一批 cc-groups/公共库 400MB 级）里混有全透明图时，各机同时表现为空气泡且零报错 | chat.js bindMediaFailPlaceholder 增设 load 监听：24×24 canvas 采样 alpha，**全部像素 alpha=0** 才判空，replaceWith 占位「（表情内容为空：这张字卡图本身没有画面，可长按撤回或到字卡库清理该分组）」；有任一非透明像素（有字/有内容）即放行，GIF 取当前帧，跨域图 getImageData 抛错 catch 放行不误伤；dataset.alphaChecked 每图只查一次。另：#202 两条哨兵因并行 #203 收口 stash 隔离在 build.mjs 丢失（源码/台账未丢），本次重登记+新增 #205 共 3 条 | 哨兵 3 条（build.mjs）：bindMediaFailPlaceholder 函数行 + `if (im.naturalWidth !== 0) return;` + `if (im.dataset.alphaChecked) return;`。验证：node --check 过；--check-sentinels 418 全绿哑哨兵 0；tmp 端到端探针四场景过（令牌无池数据/远程不可达/损坏 dataURL→各自占位，正常图→渲染）。【真机：各机型更新后空白气泡应显示三种占位之一（丢失/加载失败/内容为空），占位文字即失败类型，据此清理对应字卡分组】 |
| 206 | 多机型续报（2026-09-05 Oppo A5 Pro + Via 诊断报障，用户明说其他机型也有；与机型无关）：联系人发的表情包「出现重复，过一会变成空白方框」；不发表情包时也会「跳出一段乱码然后变成空白方框」。根因（#180 尾巴日志 × #142 媒体令牌化的交互缺陷）：chatTailAppend 对 sticker 型消息照收——text 就是媒体本体（base64/令牌），收录时截断到 1000 字符且丢弃 type；随后 mediaNormalizePass 把该条 text 令牌化改写为 @@m:令牌，日志里留存的旧文本签名随之漂移，下次启动 chatTailMerge 按签名判其为「未落盘新消息」回放成无 type 的截断 base64 文字条＝同一表情包旁多出乱码复制（重复+乱码）；normCell 归一化又见「text 以 data:image/ 开头且无 type」按历史数据迁移成 type:image→截断 base64 解码失败＝坏图空白方框（用户看到的「乱码过一会变空白」） | 双端收口：①chatTailAppend 拒收一切回放必失真的消息——type 为 sticker/image/voice、带 parts（含令牌短 parts，防回放丢图成半条）、text 非字符串或超 CHAT_TAIL_TEXT_MAX 的一律不进日志（宁可不兜底，绝不回放坏数据）；②chatTailMerge 回放端拦截旧版存量脏条目——data:/@@m: 开头却无 type 的条目直接跳过（随日志 60 条滚动自然淘汰），不再复活成乱码/坏图；不再新增（append 已拒收） | 哨兵 3 条（build.mjs，needle 均在 chat.js 内唯一）：append 端 type 拒收行、text 长度拒收行、merge 端存根拦截行。专项脚本 tools/verify-chat-tail.mjs 扩到 27 断言（A2 媒体型/超长/parts 消息不进日志；D1-D4 存量存根不回放、正常未落盘消息照常并回）。验证：node --check 过；--check-sentinels 全绿哑 0；verify-chat-tail 27/27、verify-quote-image 21/21、verify-media-pool 8/8。【真机:待验证】（Oppo A5 Pro/Via 及任意机型）：更新后联系人发表情包不再多出乱码/空白复制条；存量历史里的乱码/空白条为旧版已入库脏数据，长按删除即可且不会再新增 |
| 186 | iPhone 15 Pro + Safari 主屏幕（iOS 18.3.2/Safari 18.3 老内核）：底部白边/工具栏离底太远——独立应用 inner=793(=852−59 状态栏) 且 env(safe-area-inset-top)=0，页面铺不满 852 物理屏 | 根因（第三种 iOS 形态）：iOS 18.3 老内核 standalone 状态栏区域露白=页面没垫到 852（inner 只到 793，env=0 无法探知状态栏高度）。与 16 Pro 26.1「已避让形态」信号完全相同（env=0、diff=62/59>0）但需要**相反**处理（16Pro 不加 padding 正确；18.3 必须垫 59+高度 852）——JS 无法程序区分，唯有用户声明。修复：#185 顶部避让修正开关扩宽——force=1 时 safeTop=env 优先、env=0 回退用 diff（=系统保留的状态栏高度，20-160 过滤），高度公式 safeTop+inner 自然补满 852；默认关（16Pro 已避让形态维持 0 不回归） | 哨兵：#185 mobile-adapt needle 扩宽后仍唯一。验证：node --check 过；开关矩阵单测（15Pro-18.3 force=1→59/852、force=0→现状、14Pro-26.6→59/852、16Pro-excluded force=1→垫 62 需自查关闭）全过；--check-sentinels 418 全绿。【真机:待验证】（iPhone 15 Pro iOS 18.3 主屏幕）：开关开启后底部白边消失、工具栏贴底；顶部 Mochi 行在系统状态栏下方不重叠；关闭开关恢复现状 | <br /> | <br /> |
| 208 | 苹果17 自带浏览器（iOS 18.7 standalone 全屏模式，用户明说其他机型也有）：「聊天页底部输入栏下面一块白边不贴底、位置上移 UI 按钮点不到」；诊断基态全绿但错误环 21:32~21:37 连环五条「底部导航栏悬空（自动采集）」+一条「顶部重叠」 | 根因三层：①键盘收起视口未还原——iOS standalone 键盘收起时 WebKit 偶发不把可视/布局视口还原到基线（差 >60px），restoreKb 的「确已还原」门槛 `vv.height >= _fullVv-60` 永不满足 → kbActive 卡真、.phone 卡收缩高 → 输入栏整体上移、下方露 body 底色白带（#110 同症状家族在 #148 实测写入架构下的残留死锁）；②采集器盲区——聊天页打开时 tabs.js 给 .tabbar 挂 hidden（display:none）矩形全 0，5s 监视照判「底部导航栏悬空 860px」刷假错误环；③判定器盲区——保留形态下④按 inner 判「底部贴合」，视口真卡收缩时全绿漏报；另切后台回来瞬态 inner=整屏（diff=0）时 resStand 不命中 → 误报「顶部重叠」（iPad 全屏态状态栏 display:none 同样误报） | 修复（mobile-adapt.js + device.js，均 AI-B 域零跨域）：①healViewport 键盘分支加自愈兜底——失焦 >4s 且视口仍 <基线−60（物理上不可能还有键盘等输入）→ 强制 restoreKb（重吸基线+触发重排，1s 看门狗随后按实测重写 --mochi-ios-h）；原 60px 门槛与 250ms 轮询路径不动；②采集器 tabbar hidden/零矩形 → null（⑤b 跳过）；③判定器：⑤d 新增「布局视口未贴底」（保留形态 diff > envTop+24 且无键盘会话 → ✗，白带状态从此可诊断），③顶部重叠加 diff ≥ envTop−8 守卫（瞬态/iPad 不再误报，真覆盖设备 diff≈envTop 检出不变）。哨兵 3 条（build.mjs，逻辑锚点各自文件内唯一）。验证：node --check 过；tools/verify-ios-kb-stuck.mjs 26/26（自愈四场景+采集器三场景+判定器八场景含 iOS17/#199/iPad 防回归）；verify-ios-reserved-standalone 25/25、verify-fullscreen-ipad 25/25 复跑过。【真机:待验证】（苹果17/iOS18.7 及任意 iOS standalone 机型）：①聊天页开键盘打字收起后输入栏贴底无白带；②若再出现白带，屏幕适配诊断会出现「✗布局视口未贴底」条目（发给开发者即可对号）；③聊天页停留时错误环不再新增「底部导航栏悬空」；④切后台回来不误报「顶部重叠」 |
| 209 | 红米 K70 + Edge（2026-09-05 诊断报障 v3.26.475 ts=1788613665382，用户明说其他设备型号也有；「输入框和网站底部有断截面（灰边，不贴合）」）：聊天页键盘收起后输入栏下方长期露一条 body 底色浅灰（#e9e9e9）横带，输入栏悬空不贴底；诊断基态全绿（残留在聊天页、切页/失焦即自愈，诊断时已复原故采不到） | 根因（mobile-adapt.js 安卓机器无稳态看门狗，#141「输入栏下方灰块」/#208「输入栏上移白带」同族第 N 次复发、多机型通病）：安卓返回键/手势收键盘**不派 blur**（activeElement 仍是输入框），叠加 #197 族 focusout 丢失时收键盘的复原路径（focusout 分支/vv 收起分支/250ms 轮询表停摆后）都可能被跳过——.phone 留着键盘期内联收缩高度/顶对齐 → 输入栏顶在残留高底部、下方露出 body 灰底。iOS 侧 healViewport 看门狗在 `if (isIOS)` 分支内、安卓完全不经过（无头实测 __mochiIosKb 未注册/html 无 ios-vv-fit 实锤）；安卓侧唯一巡检是 device.js 只读监视，不修复。无头 412×915 复现实锤：.phone 种 40px 内联高度残留即重现用户描述的灰边；healthy 态两页均贴底。修复（mobile-adapt.js 安卓 `if (!isIOS)` 机器内）：新增 1s 稳态残留清扫（安卓侧唯一视口看门狗，与 iOS 1s 看门狗同口径）——纯视口证据完全不看焦点（天然免疫 focusout 丢失）：①不在键盘会话（_aKb/_aProv 均假，收起动画期 _aKb 仍真故天然避开动画中途）；②vv.height 与 innerHeight 都距无键盘基准 ≤12px（与 _a 机器「收起动画完成」同阈值）＝键盘肉眼已不在场；③此时 .phone 内联 height/alignSelf 唯一写入方就是键盘停靠链（全文件 grep 佐证）必为残留 → 清空 + _aPanComp/kbUndockPanels 复原。悬浮键盘内核（_aProv 推定停靠，vv 全程无信号、物理键盘可能在场）不归本通道（有 2.2s 无活动自愈） | 哨兵 1 条（build.mjs，逻辑锚点 mobile-adapt.js 内唯一）：`if (_hNow <= 0 || _hNow < _aH - 12) return;`。行为断言 tools/verify-kb-residue-heal.mjs（10 断言：S0 锚点；S1 焦点保留+残留 ≤4s 自愈贴底=本 bug；S2 模拟键盘收缩态残留不被误清+视口还原后贴底；S3 无焦点残留照清；S4 健康态零写入）。验证：node --check 过；--check-sentinels 436 全绿哑 0；verify-kb-residue-heal 10/10；verify-ios-kb-stuck 26/26、verify-viewport-form 49/49、verify-fullscreen-ipad 25/25、verify-ios-reserved-standalone 29/29、npm run verify 10/10 复跑过（#208/#210/#185 相邻修复零回归）。【真机:待验证】（红米 K70+Edge 及任意安卓）：①聊天页打字后用返回键收键盘，输入栏贴底无灰边；②iOS 设备/悬浮键盘内核（X5/夸克）/全屏模式均不受影响 |
| 207 | OPPO R15 自带浏览器 HeyTapBrowser 40.10（Android 10/Chrome 115 内核，2026-09-05 诊断报障 v3.26.475 ts=1788613665382，用户明说其他设备型号也有；同族第三次：v3.15.x iPhone「嘟嘟嘟」、#190 OPPO Find X9「底噪/电流声」）：「后台保活有电流声，不是静音音频」——开保活即持续嗡声 | 根因（bg-keep.js 保活音频）：保活用 1 秒循环正弦波 <audio>（volume 0.05）维持「页面在出声」躲 Chromium 后台冻结，安卓频率一直是 220Hz——正落人耳最敏感低频段；#190 把幅度 0.02→0.006（数字 ≈-70dBFS）后老机型（R15 为 2018 年机型，功放底噪大）夜间安静环境实听仍是持续嗡声，降幅度已到头（再降跌破 Chromium audible 判定=后台冻结保活失效）。修复：安卓频率 220Hz → 18000Hz，幅度 0.006/volume 0.05/loop 等保活参数分毫不动——Chromium audible/无声节流按数字样本电平判定、与频率无关（18kHz 与 220Hz 同幅度 RMS 完全一致），保活零回归；人耳对 18kHz 基本无感 + 手机外放高频频响天然滚降 20~40dB（老机型更差），物理不可闻。18000×1s=整周期循环接缝无相位跳变；18000<22050 奈奎斯特且距 48k 重采样抗混叠滤波带有余量。iOS 维持 220Hz@0.002（v3.15.x 已收敛，bit 级不动）。不做机型白名单——全安卓一次性换根因，防「修 A 机型坏 B 机型」反复 | 哨兵 1 条（build.mjs）：`kaIsIOS() ? 220 : 18000`（频率分支逻辑锚点，bg-keep.js 内唯一）。行为断言 tools/verify-keep-audio.mjs（新增 18 断言，verify:all 自动纳入）：抽取真实生成函数解码 WAV——安卓 18000Hz±5/幅度 0.006/接缝相位连续/首样本 0/数字电平 0.0003>audible 安全线 0.00025/非零样本>95%/奈奎斯特余量；iOS 220Hz±2/0.002 防回归；loop/volume=0.05/playbackState='playing' 文本锚。验证：node --check 过；--check-sentinels 全绿哑 0；verify-keep-audio 18/18。【真机:待验证】（OPPO R15 自带浏览器及任意安卓）：开后台保活不再有电流声/嗡声（贴近扬声器也听不到）；保活不失效——切后台 1 分钟以上回前台，TA 消息/通知仍正常产生；iOS 设备无任何听感变化 |
| 185 | iPad Air 7 + Safari：①全屏模式左右露白（640px 限宽居中设计在全屏下左右各 ~90px 底色）；②主屏幕页面滑动「会飞、一弹一弹」（文档可滚+iOS 橡皮筋与自愈 pin 对打）；③屏幕适配诊断在聊天页误报「✗顶部重叠 状态栏顶位 0px」（全屏页隐藏状态栏 rect=(0,0) 被当成实测） | 修复：①base.css 新增 html.tablet.ios-fs-active .phone { width:100vw }——iOS 全屏模式全宽铺满，浏览器/非全屏保留 640 限宽居中（v3.7.x 平板设计不变，想要全宽可用 设置→手机布局（强制））；②#168 fs html/body 规则补 overflow:hidden 锁文档滚动（页面内滚动不受影响，橡皮筋弹跳消失）；③采集器状态栏改「隐藏跳过+相对 .phone 测量」（sbTop=sr.top−pr.top，文档滚动免疫），判定器对隐藏状态栏输出说明行不判 ✗ | 哨兵 3 条（build.mjs FIX_SENTINELS）：采集器相对测量 / fs 滚动锁 / 平板全宽。验证：node --check 过；--check-sentinels 428 全绿。【真机:待验证】（iPad Air 7）：①全屏模式左右铺满无白边；②主屏幕滑动不再弹跳/飞；③聊天页里跑屏幕适配诊断不再误报顶部重叠（显示「状态栏隐藏」说明行） | <br /> | <br /> |

| 132 | 功能增强（非 bug，2026-09-06 用户问「经期的温柔前缀字卡使用概率是多少」并要求【系统预设字卡】各页显示概率可自由调整、对齐【聊天默认字卡 30%/自定义 70%】模式全显示）：温柔前缀触发概率原为 period.js warmText 硬编码 25%（触发后 45% 拼前缀/35% 拼动作/20% 双拼）；摸鱼浮字 35%、吃饭追问关心 35%、花园悄悄话 40%、同频敲三下 60%、伸手摸到 55%、查岗回应 50%、喝水（ta浮层50%/进页催35%/定时0.22）、房间/此间/漂流瓶/音乐/存钱罐/互动回应 100（点击必有）全部硬编码散在各消费点，字卡库无任何概率入口 | 新键 `dcf-<分类>`（per-cid 随桌面命名空间），默认=各分类历史值（行为不变）。UI：字卡库【其他互动功能字卡】页 13 分类 +【联系人跨桌面查岗】页各一个「使用概率」stepper（步长 5，0=该分类字卡不触发）；default-cards.js 暴露 window.dcfGet(cat)（DCF_DEF 默认表+clamp 0-100+未知分类回退 100），mochi-wrj-heal 时随 dc-* 键重同步。接线 12 消费点：period.js warmText 概率行接 dcf-period（默认 25）；p2-features dcfP/dcfHit 助手——fish/eat/sync/reach 单值替换（35/35/60/55）非叠加、water 三处乘法门控（默认 100 不改内部节奏、0=全关）；garden 40；chat.js 查岗回应 _dkP（默认 50，拒绝分支原 50 不动）；room sayLine/cjian cjLine/drift poolLine/music taPauseSendCard 乘法门控（默认 100）+空串守卫（drift 回应瓶不落库/bubble 空不出/cjian 感知行跳过）。【聊天默认字卡】dc-overall-chat 30% 既有 UI 不动 | 哨兵 14 条（build.mjs：dcfGet 暴露行/period _warmP 行/p2 四分类/garden/_dkP/room/cjian/drift/music 门控行/模板 dcf-prob-period-val）；行为脚本 tools/verify-func-card-prob.mjs 19 断言（A 段 14 锚点：14 stepper UI+12 消费点接线+空串守卫；B 段 5 断言：vm 桩环境跑 default-cards.js 真实源码验 dcfGet 默认表 14 分类逐一/clamp/未知分类/设 0 关断；C 段概率行结构）；node --check 全过；--check-sentinels 472 全绿哑 0。【真机:待验证】字卡库→其他互动功能字卡页各分类下方出现「使用概率」并随 ± 更新；把「经期字卡概率」设 0 后 TA 经期回复不再出现温柔前缀/动作；查岗页设 0 后查岗回应不再拼字卡 |
| 211 | iQOO12 + Chrome 151（Android 16/V2307A，2026-09-06 用户报障，用户明说其他设备型号也有）：「打开聊天偶尔会闪动+对方回复消息会闪一下」——历史超过 200 条的桌面每收发一条消息整个消息区闪一次；历史 ≤200 条的设备同版本不闪（假象为机型相关，实为历史条数相关） | 根因（chat.js addRec）：渲染窗口超限判定 `msgs.length - renderStart > RENDER_MAX` 在每次钳位渲染后（renderStart=len−RENDER_MAX）只要再来一条消息就恒为真——历史 >RENDER_MAX(200) 条时每收/发一条消息都 renderWindow(false,true) 整窗重建 200 个气泡（img 节点全部重建重新解码=肉眼可见闪一下），无头 Chrome 种 600 条历史实测实锤（MutationObserver：来一条消息=rem 200+add 200）。修复两处：①判定收紧到 `> WINDOW_MAX` 硬上限（与 loadOlderIncremental→pruneWindowBottom 同口径），常规收发全部走 renderMsg 增量追加；仅当上翻加载旧消息使窗口真正越过 WINDOW_MAX(400) 时才重钳位，DOM 上限语义不变；②「打开聊天偶尔闪动」第二来源：后台分批归一化 runDeferredNormalization 的 finish 曾在发现任意改动且聊天页可见时无条件 renderWindow 整窗重建——改为改动全部落在当前渲染窗口之外（changedHi<renderStart）时跳过重建只落盘，屏上数据真变了仍重渲，sysNick 清扫/相邻删除保守整窗 | node tools/verify-chat-rebuild.mjs 13 断言（S0-S4 无头浏览器实测 9MB 大历史：打开静置/对方回复/自己发送零整窗重建+回复走增量追加；G1-G4 抽真实源码桩环境验渲染闸：窗口外迁移跳过重建且照常落盘/窗口内迁移重渲/结构删除与 sysNick 保守重渲）+ build.mjs 哨兵 2 条；真机：大历史桌面打开聊天与连收多条消息消息区均不再整屏闪 |
| 210 | iOS 屏幕适配定位效率（非单机型：#114/#148/#174/#179/#184/#185/#186/#199/#200/#208 多轮反复）；另修 #186 判定器两处缺陷（forced 设备自检必误报「底部超出/顶部双倍避让」、「用户已声明覆盖形态」分支在真实采集路径永不命中=死分支） | 修复：①形态判定收敛到共享纯判定器 window.mochiViewportForm（device.js，执行器 syncVvFit 与诊断 screenDiagJudge 共用=单一事实源，新形态只改一处，消两处手抄漂移；force 声明优先置 resStand=false、期望底边=屏高）；②异常抓拍补强：#176 监视器加事件沿捕获（resize/vv resize/旋转/回前台 1.2s 去抖补采，堵 5s 轮询漏瞬态）、错误环条目带事发现场数值（env/var/diff/inner/sb/scale）、屏幕适配报告尾附历史快照时间线（✗ 时刻随报告带出免复现）；③采集器拆出 collectFitInp 并把 safe-top-force 传入判定器；④全屏态页内全绿时输出「※ 全屏态·页外留白提示」行（#212 iQOO12 实证的判定器结构性盲区：挖孔/摄像头区 letterbox 在页面坐标系外测不到，提示引导关开一次全屏重新申请；已有 ✗ 时不出避免噪声） | tools/verify-viewport-form.mjs（真机信号台账 53 断言：15Pro/18.3、16Pro/26.1、14Pro/26.6+force、15Pro/18.3+force、iPad Air、荣耀50se 雨见、iOS17 防回归、env 超界、全屏提示行 3 场景）；verify-ios-reserved-standalone 重写 29 断言；哨兵改锚 6 条+新增 8 条；【真机:待验证】各机型跑「屏幕适配诊断」应全绿且报告尾出现「历史快照」段；全屏态诊断尾多一条「※ 页外留白提示」 |
| 212 | iQOO12 + Chrome（2026-09-05 诊断报障 v3.26.475 ts=1788613665382，用户明说其他设备型号也有）：「聊天全屏下顶端有留白」——屏幕适配诊断全 ✓（.phone 贴满/状态栏顶位 0/底部贴合），页面坐标系内无任何异常，但用户肉眼可见顶端一条空白 | 根因（fullscreen.js enterFs，Chromium issue 40723205）：挖孔屏上 Fullscreen API 默认 navigationUI:'auto' 不把全屏面铺到挖孔/摄像头区——空白条在**页面外系统层**（浏览器 letterbox），页面内任何 CSS/JS 测量都全 ✓，属判定器结构性盲区（本会话无头复现实证：模拟 423×941+陈旧 screen 361×801，.phone=941 贴满/chat-head 顶位 0，与真机诊断一致）。官方 workaround=requestFullscreen({navigationUI:'hide'})；本项目安卓路径 enterFs 恰是无参 requestFullscreen()，iOS 路径（iosTryNativeFs）反而早已带参 | fullscreen.js enterFs 补 `const fsOpts = { navigationUI: 'hide' }` 并经 `el.requestFullscreen(fsOpts)` 调用（doRetry/reenterFs 手势重入同走此函数自动生效）；webkit 前缀 API 与不认选项的老内核忽略参数零回归；iOS/桌面/ Via CSS 兜底路径分毫未动。哨兵 1 条（build.mjs，逻辑锚点 fullscreen.js 内唯一）：`const fsOpts = { navigationUI: 'hide' };`。行为断言 tools/verify-fs-nav-hide.mjs（8 断言，verify:all 自动纳入：安卓路径选项在位/无参调用清零/iOS 既有参数防回归/webkit 兜底保留/整文件编译）。验证：node --check 过；--check-sentinels 全绿哑 0。给判定器会话（#210 已收口）：本例暴露「页面外 letterbox」盲区，可考虑 fsActive 态加 ※ 提示行（空白条在挖孔区=页面外，关开一次全屏模式重申请）。【真机:待验证】（iQOO12 及任意挖孔屏安卓 Chrome/Edge）：更新后**关闭一次再重新开启「全屏模式」**（重新申请全屏）→ 聊天/桌面顶端不再有空白条；非挖孔屏、iOS、桌面全屏、CSS 兜底路径均不受影响 |
| 187 | 设计变更（用户决策 2026-09-05）：iPad/平板默认观感从「640/820px 限宽居中」（v3.7.x）改为**全宽铺满**（竖横屏均 100vw）——多台 iPad 用户反馈浏览器/主屏幕「四边不贴合、不居中」；此前仅 iOS 原生全屏（fs-active）全宽 | 实现（base.css 平板区块）：html.tablet .phone 宽度 min(640px,100vw) → **100vw**；横屏 @media(orientation:landscape and ≥1024px) min(820px,100vw) → **100vw**；内容阅读宽度由内部规则自行约束（横屏气泡 460px 封顶、弹窗 360px 保留）。影响所有 iPad/平板用户观感（回归风险：横屏大屏气泡行宽依赖既有 460px 封顶） | 哨兵：#148/#185 平板全宽相关锚点保留；新增无（纯宽度值变更，#148 fs 全宽锚点可覆盖该区块存在性）。验证：node --check 过；--check-sentinels 446 全绿。【真机:待验证】（iPad Air 7 竖/横屏）：主屏幕+浏览器均全宽铺满四边贴合；横屏聊天气泡 ≤460px 不过宽 | <br /> | <br /> |
| 215 | 华为 P50E + Edge（Chromium 151，2026-09-06 报障 v3.26.475 构建，用户明说其他设备型号也有）：「聊天里我发消息，我的气泡里没有文字，文字消失了」——点发送瞬间内核把输入栏未提交的组合文本整体撕掉（composition cancel：DOM 清空且不派发任何 input/beforeinput 事件），addMsg(input.innerText) 读空 → buildParts 为空 → 一条消息都不发出、用户刚打的字静默消失。#115 防复活守卫只管「发送后内核迟到写回」，不覆盖这一反向缺口。无头复现（362×764/DPR3.375/EdgA151 UA）：IME 提交文本→零事件清空 DOM→点发送＝消息 0 条 | 修复（src/js/chat.js，跨域改动已在 WORKLOG 声明）：新增 readSendText 供点击/Enter 两个发送入口取值——innerText/textContent 双口径读空、且最近输入快照 _mLastTyped 新鲜（真实编辑晚于上次清空且 <15s）才恢复；快照由捕获阶段 input 监听维护（手动全删即归空＝不幻影恢复），clearChatInput/切桌面同步作废；正常路径与防重发/防复活守卫零改动，全机型无感 | 哨兵 2 条（build.mjs，needle 均在 chat.js 内唯一）：快照捕获行、新鲜恢复条件行。行为脚本 tools/verify-chat-send-recover.mjs（11 断言：T1 正常发送/T2 撕文本恢复核心/T3 手动删空不幻影/T4 隔次发送不复活/T5 Enter 路径恢复/T6 #115 迟到写回语义保持+5 静态锚）。验证：node --check 过；--check-sentinels 全绿哑 0；旧产物上 T1+静态锚绿、T2-T6 红（修复未构建），构建后应 11/11。【真机:待验证】（华为 P50E+Edge 及任意机型）：打字点发送后消息照发、气泡文字不再丢失 |
| 216 | 一加Ace3（OPPO PJE110+Edge 152，2026-09-05 诊断报障 v3.26.475，用户明说其他设备型号也有）：「音乐封面丢失（新加的也是这样）」，错误环 page-music 三条 BodyStreamBuffer was aborted | 封面全链押在第三方单点 api.injahow.cn：存量封面存的就是 meting 图片代理 URL（302→网易CDN），代理一慢/挂=新旧封面一起丢；新加歌 fetchNeteaseCover 只走 meting type=song，8s 挂起即无封面（诊断三条 abort 与之吻合）。修复（四层，显示不再依赖第三方代理）：①resolveCoverDirect——meting pic 落库前 fetch 跟随 302 解析成网易 CDN 直链（两跳 ACAO:* 实测），失败原样回退代理；normNeteaseCoverUrl 网易域统一 https+?param=300y300；②meting 主源挂走 fetchNeteaseCoverFallback=fetchNeteaseInfo 多代理链 song/detail 的 album.picUrl（第二封面源）；③存量迁移：COVER_PROXY_RE 识别代理封面（仅 type=pic，不碰 type=url 播放链与 dataURI），打开音乐页按已渲染窗口+歌单+正在播放/切歌顺路解析直链写回，失败留待下次自愈，in-flight 用不落盘 Set；④迁移顺带同步历史/我的历史/TA收藏快照里同曲代理封面（只换 URL 不动快照结构）。abort 全部异步+cancel body（同 resolveNeteaseDirectUrl 口径，不再刷未处理 rejection） | 哨兵 4 条（build.mjs）：COVER_PROXY_RE 正则行 / pic 落库前解析直链行 / fetchNeteaseCoverFallback 函数行 / syncSnapshotCovers 函数行。行为断言 tools/verify-music-cover-direct.mjs 17 断言（本地桩模拟 meting/CDN/兜底源端到端：存量迁移/新歌解析/主源挂双源兜底+param 归一/dataURI 与 type=url 防误伤负例/三类快照同步/列表图标渲染/零未捕获错误）。验证：node --check 过；临时副本全量构建哨兵 444/444 哑 0（pwa 产物哨兵新路径实测生效）；相邻回归 dur-cover 9/9、history-cover 8/8、ta-fav-keep 10/10、bg-resume 12/12、single-audio 15/15。【真机:待验证】（一加Ace3+Edge 及任意机型）：更新后打开音乐页存量封面逐批恢复、新加网易云歌封面直接以网易 CDN 直链显示、听歌记录/TA收藏/歌单封面同步恢复；播放（type=url 代理链）分毫不动 |
| 214 | 一加Ace3（OPPO PJE110+Edge，2026-09-05 诊断报障 v3.26.475=已含 #201 修复仍黑边；#201 OPPO Reno14+Edge 同症状第二例）：「顶部有多余黑边」，诊断全绿、diff(screen−inner)=40px 即页面顶上被涂黑的系统区，布局链无病 | #201 只改了页面 <meta theme-color>，但该机 display-mode: standalone=true——安卓 Edge/Chromium standalone 形态状态栏涂色取 manifest 的 theme_color 而非页面 meta，manifest.json 仍写死 #111111=黑边残留唯一深色源。修复：src/pwa/manifest.json theme_color #111111→#e9e9e9（与 #201 meta 静态默认一致）；深色模式由 #201 的 meta 动态同步兜底。布局链零改动。配套：build.mjs 哨兵检查器扩 artifactText——pwa/ 产物不进 index.html，产物核对改读根目录对应文件（非 js/css 不走压缩比对） | 哨兵 1 条（build.mjs）：pwa/manifest.json `"theme_color": "#e9e9e9"`（检查器 #214 扩展支持 pwa 产物文件）。验证：临时副本全量构建 444/444 全绿哑 0；verify-music-cover-direct.mjs 内含 manifest src/产物双断言。【真机:待验证】（一加Ace3+Edge 安装/添加到主屏幕形态）：SW 更新生效（可能需两次打开让新 sw 接管）后顶部黑边变浅色；深浅主题切换该区随 #201 meta 链路；浏览器内打开形态与 iOS 不受影响 |
| 211 | iPhone 17 Pro + Safari 主屏幕全屏：顶部 Mochi 行与灵动岛融为一体点不动、底部输入栏悬空——屏幕适配诊断却全 ✓（保留形态被当正确，用户不知有开关可修） | 根因（#175 诊断工具盲区）：保留/覆盖两形态 JS 信号完全相同（env≈diff>0，env=0 与否皆可能），程序不可分——判定器对保留形态输出全 ✓，用户不知道【顶部避让修正】开关能自服（iPhone 17 Pro 实为覆盖形态：env=62/diff=62/顶栏融入灵动岛） | 修复（#210 引导行）：判定器对「系统保留形态（force 未开）」追加说明行「歧义形态提示：若顶部 Mochi 行与灵动岛/时间重叠或点不动 → 开启上方【顶部避让修正】开关（自动刷新即修）；若底部白带则保持关闭」——报告本身引导用户自服；force 开启后提示消失（不骚扰已修好的设备）。#209 共享判定器架构已由并行会话就位，本条仅补引导可发现性 | 哨兵：#210 引导行 needle（device.js 歧义形态提示）。验证：node --check 过；判定器四场景单测（保留态引导出/force 后消失/iPad 形态不引导/16Pro 已避让不引导）全过；--check-sentinels 全绿。【真机:待验证】（iPhone 17 Pro 主屏幕）：报告出现歧义提示 → 按提示开【顶部避让修正】→ 顶栏脱离灵动岛可点、输入栏贴底；报告显示「覆盖形态（用户已声明）」 | <br /> | <br /> |
| 212 | iPhone 15 Pro + Safari 主屏幕（iOS 18.7，force 声明覆盖形态）：开启【顶部避让修正】后只要发消息就复发底部大白边+整个页面上移，只能重退再进恢复 | 根因（force 形态的键盘周期回归）：force 声明下 .phone 高度=852（=envTop59+inner793，超出布局视口 59px 属预期），发消息键盘开合周期里 WebKit 偶发把滚动位置/内联高度打回预声明前状态（文档被拽上移 59px → 内容上移+底部露 body 白带；重退再进触发完整重排才恢复）。此类瞬态回归无法在源头堵死（WebKit 行为），唯一稳妥方案是看门狗自愈 | 修复（mobile-adapt healViewport #212 看门狗）：standalone+fs+无键盘稳态+force 键开启时，实测 .phone 底边距屏底短缺 >8px → 直接重写判定器期望值（--mochi-safe-top/--mochi-ios-h + 清键盘期内联高度 + scrollTop 归零），1s 看门狗周期内自愈，不再依赖用户重进。表单经 window.mochiViewportForm 现算（healViewport 作用域取不到 syncVvFit 局部 _f） | 哨兵 1 条（build.mjs）：mobile-adapt `if (_short > 8) {`。验证：node --check 过；--check-sentinels 458 全绿；产物含看门狗代码（_short>8 锚点在位）。【真机:待验证】（iPhone 15 Pro force 开启）：发消息/键盘开合后底部不再出白边（即使 WebKit 打回，1s 内自动复位）；关闭开关设备行为不变 | <br /> | <br /> |
| 219 | 小米15Pro + Chrome 151（Android 16/2410DPN6CC，2026-09-06 报障 v3.26.487，用户明说其他设备型号也有）：手机美化→背景，【背景模糊】【背景遮罩】调整后背景无变化 | 根因（home.css，#147 回归）：#147 把壁纸从直写 .phone 改画到常驻图层 #phone-bg-layer（personalize.js 注入，z-index:1，插在 .phone 最前），而模糊/遮罩层 .phone-bg-mask 仍是 z-index:0（#147 之前它是背景元素里最前的，z:0 够用）——壁纸图层把遮罩层压住：白色遮罩被盖=调遮罩无感；backdrop-filter 向下采样不含壁纸=调模糊无感。与机型无关，z 序永远 z1>z0，多机型通病 | 修复（home.css 一行）：.phone-bg-mask z-index:0→2——重新盖在壁纸图层(z1) 之上；与 .page/.statusbar/.tabbar(z-index:2) 同值但 DOM 序靠前、同值后到者胜不影响页面内容；blur=0 默认态仍走 .blur-on 门控（iOS 常驻栅格化红线不变） | 哨兵 1 条（build.mjs：home.css `position:absolute; inset:0; z-index:2; pointer-events:none;`）；tools/verify-desk-beauty.mjs 追加 M1~M3 断言（遮罩层 zIndex=2、blur-on 挂上且计算 backdropFilter 非空、遮罩预览随滑杆变化）。【真机:待验证】（小米15Pro/Chrome 151 及任意机型）：手机美化→背景→设一张自定义壁纸→拖【背景模糊】背景变模糊、拖【背景遮罩】背景变淡；预设/纯色壁纸下同样生效 | <br /> | <br /> |
| 220 | 小米15Pro + Chrome 151（2026-09-06 报障 v3.26.487，用户明说其他设备型号也有）：点开聊天界面，联系人聊天记录的消息有时先跳动一下才显示正常 | 根因（chat.js，#211 第三条漏网路径）：enterChat 一直「无条件 renderWindow 整窗重建」；权威读库收尾（loadMsgs 的 idbGet 回调）在 changed=true 时同样无条件整窗重建。小历史桌面（LS 快照=全量）打开聊天=渲染两次：第一次快照内容、第二次权威内容——气泡全部重建重新解码=肉眼跳动一下（快照与权威几乎总有小差异：_lsLite 残留剥离/已读回执 idle 占位→真实文本/尾巴合并，changed 几乎必为 true）。#211 修了收发(addRec)与归一化收尾(finish)两条路，本条补齐最后一条：重开+权威到达 | 修复（chat.js 五点，inplacePatchIfSameWindow 同窗原地补丁）：①renderWindow 登记「屏上由哪份 msgs 渲染」（windowRenderedN/windowRenderedPrefix/windowStale）；②addRec 增量追加后登记条数对齐屏上最大 idx+1；③enterChat 重开同窗同貌（同桌面/同条数/未裁剪 renderStart=0/归一化未改过窗口）则跳过整窗重建只补滚底三连；④权威读库收尾与 mochi-restore-done 改走「同窗则原地补丁（逐条核对 data-idx 序+已读回执占位替换真实文本），不同窗才整窗重渲」；⑤后台归一化改动落在窗口内时 windowStale 置真=补丁让位给重渲。clearChatHistory/chatImportMsgs 整包替换凭据复位。门槛 len≤RENDER_MAX(200)+renderStart=0：大历史桌面窗口本就裁剪态（renderStart>0）不进补丁路径、行为不变 | 哨兵 5 条（build.mjs：chat.js 锤点）；tools/verify-chat-rebuild.mjs 追加 S5（种入 300 条大历史冷启动→enterChat→静置 10s：全窗重建事件只允许首屏一次，读库收尾/归一化收尾不得再触发第二次 rem≥50+add≥50）。【真机:待验证】（小米15Pro/Chrome 151 及任意机型）：聊天页退出再重开、冷启动直接打开聊天、正看聊天时收到新消息——消息区均不再跳动闪一下 | <br /> | <br /> |
| 221 | 多机型反馈「贪吃蛇手机端不好操作」（2026-09-06 用户复述多名玩家意见；AI-A 域 src/js/snake-game.js + src/css/chat-pages.css） | 三根因（无头 390×844 触控仿真逐一复现）：①**单槽 nextDir 吞转向**——一个 tick（normal 150ms）内连给两个转向（急转弯 上→左），后给的覆盖先给的，第一下被吞=「按了没反应」撞墙；②**滑动粘性轴锁**——一次触摸锁死横/竖轴后 L 形拖动（先右后上）另一轴偏移再大也不响应，必须抬手重滑；且同向重复滑动 return 时不重置 touchBase，位移在旧基点累积，拐弯判据（另一轴偏移 >1.5× 累计位移）永远不成立（无头实测 200px 横移+36px 上移仍不转向）；③**方向键 click 依赖 touchend 合成**慢一拍、快速连点丢次；dpad 无 touch-action 另有双击缩放等待 ~300ms | 修复（snake-game.js 三处 + chat-pages.css 两处）：①双槽输入队列：setPlayerDir 入队 nextDir/nextDir2（队尾方向判回头/同向去重），applyDir 每步只消费头一格、nextDir2 顶上——两个紧凑输入=两步各转一次；②轴锁可解锁：锁定轴正常响应；另一轴偏移 ≥TH 且 >当前轴位移×1.5 时改锁并转向；**每次有效 move 都把 touchBase 跟到当前点**（无论是否转向，防同向滑动累积位移堵死后续拐弯判据）；③dpad pointerdown(touch/pen) 即时转向 + click 兜底（鼠标路径防双触发），snake-dp/snake-btn 补 touch-action:manipulation | 哨兵 5 条（build.mjs：snake-game.js ×4 逻辑锚点 + chat-pages.css touch-action ×1）；行为断言 tools/verify-snake-touch.mjs 11 断言（无头 390×844 CDP 触控仿真：A 开局面板/B 紧凑两次滑动两步各转一次/C L 形拖动不抬手转向/D dpad pointerdown 单独到达即入队/E touch-action 生效/F 源码逻辑锚点 ×5）。验证：node --check 过；--check-sentinels 483 全绿哑 0；临时副本全量构建 477/477 哑 0；verify-snake-touch 11/11；存量回归 verify-snake-fs-result 41/41、verify-snake-features 8/8、verify-snake-smooth 11/11（SERVE_DIR 指临时构建）。【真机:待验证】（任意手机）：①急转弯（如贴墙先上后左连滑）不再吞第一下转向；②一次触摸画 L 形（先横走再拐弯）不抬手即可转向；③方向键点按即时转向、快速连点不丢；④穿墙/安全/暂停等原有行为不变 | <br /> | <br /> |
| 213 | 屏幕适配诊断增强（定位能力收尾）：瞬态过程（键盘开合/工具条伸缩/白带出现）无法回放——监视只存状态沿快照，发生前后的连续视口变化丢失；报告缺系统版本行（形态判定强依赖 iOS/Safari 版本映射，#184/#200/#203 各形态按版本分布） | 实现：①mobile-adapt 新增视口时间线环形缓冲——每秒 1 拍记录 inner/vv/scale/键盘/全屏（保留 60 条≈1 分钟），暴露 __mochiVvTimeline()；②屏幕适配报告尾部新增「近 60 秒视口时间线」回放段（每行含 Δ 高度差，键盘 +350/白带 -59 一目了然），非 iOS 环境自动省略；③报告基础节新增系统版本行（iOS x.x / Safari y.y），形态判定与系统版本映射从此可对号 | 哨兵 4 条（build.mjs FIX_SENTINELS）：mobile-adapt vvLogPush + device.js 时间线回放段/系统版本行/template minimum-scale（已有）。验证：node --check 过；--check-sentinels 486 全绿。【真机:待验证】（任意 iOS 机型）：屏幕适配诊断报告尾部出现时间线，打字/开关键盘后时间线可见 kb:1→0 与 Δ 高度跳变 | <br /> | <br /> |
| 214 | 屏幕适配诊断增强（用户点名聊天/主页两处问题集中地）：文档级数据有了但页面级盲区——聊天输入栏贴没贴底、消息区溢出、桌面缺的图标/组件在哪、页数对不对，全无数据 | 实现（device.js collectScreenDiag 页面专项）：①聊天页——可见性/消息节点数/内容高与可视比/输入栏底边与宽；聊天页可见且键盘已收时判定「输入栏未贴底 Xpx ⚠」「超出可视区 ⚠」；②主页——页数/桌面图标数/池内组件数与名单（桌面缺图标/组件即在此处，装修模式可加回）/tabbar 底边；报告新增 == 聊天页 == 与 == 主页 == 两节 | 哨兵 2 条（build.mjs FIX_SENTINELS）：聊天输入栏行 + 池内组件行。验证：node --check 过；--check-sentinels 488 全绿；CDP 实测两节输出（无头环境含 5 个池内组件清单）。【真机:待验证】（任意机型）：报告出现两节页面专项数据；桌面缺组件时池内组件名单直接指出缺什么 | <br /> | <br /> |
| 223 | 荣耀畅玩40 Plus + 夸克（Android 12/RKY-AN00，2026-09-06 诊断报障 v3.26.491，用户明说其他设备型号也有）：「群聊的聊天框颜色改不了，一改就恢复」——美化里选气泡/文字颜色后立刻弹回旧色 | 根因（group-chat.js pickGcColor，v3.9.x 对比度保护设计缺陷）：选色回调里 `gcBeautySet(key,color)` 后立即查 `gcColorPairBad`（阈值 2.2），低于即回滚旧色。从默认组合（黑气泡#111111+白字#ffffff）出发：粉/浅色气泡对白字对比度仅 1.1~1.5 全被拒，深色文字对黑底同样被拒——两步互锁，用户无论先改哪个都被弹回（三步中转绕路无提示无人能想到）。与机型无关纯逻辑 bug。单聊同功能在 v3.26.x 已改「自愈」方案（chat-settings._ensureBubbleContrast：保留用户配色，低对比时注入高优先级文字色覆盖保证可读），群聊漏改 | 修复（group-chat.js 两处，方案对齐单聊）：①pickGcColor 删除选色后回滚分支，用户所选颜色直接生效；②applyGcBeauty 尾部新增 gcEnsureContrast 自愈——out/in 两组文字/背景对比度 <1.5 时注入 `#gc-contrast-fix` 样式（`#page-group-chat .msg-out/.msg-in .msg-bubble.msg-bubble{color:黑或白!important}`，按背景亮度选黑/白），组合改回可读时自动移除；深浅主题切换/恢复完成等 applyGcBeauty 重入路径均自动重算；设置面板低对比警告行文案同步改述新行为 | 哨兵 2 条（build.mjs）：自愈规则表达式 + dcfPFish 助手（#224）。行为断言 tools/verify-gc-color.mjs（无头真实产物端到端：真实 UI 走 群聊设置→美化→我的气泡颜色 弹窗选樱花粉→确定——颜色保存生效不再弹回；低对比组合自愈样式注入且强制文字可读；恢复默认组合自愈样式自动移除；方案导入路径同样被自愈兜底）。【真机:待验证】（荣耀畅玩40 Plus+夸克 及任意机型）：群聊→右上设置→美化聊天→改「我的气泡颜色/联系人气泡颜色」为任意色板色——改完不再弹回旧色；配成极低对比时消息文字仍清晰可读 | <br /> | <br /> |
| 224 | 荣耀畅玩40 Plus + 夸克诊断日志（2026-09-06 v3.26.491，存量自 #132 起所有版本）：「最近错误」每分钟一条 `Uncaught ReferenceError: dcfP is not defined at chk`（page-group-chat/page-setting），报错点 index.html:60410 | 根因（p2-features.js #132 接线时作用域错）：dcfP 定义在「同频/伸手」IIFE（2083-4304 行）内，摸鱼抓包检查器 chk 在另一个 IIFE（4312-4375 行）里引用 `dcfP('fish',35)`——作用域不通必抛 ReferenceError。连锁：chk 抛错后尾部 `lastTa=cur` 不执行 → lastTa 停留旧值 → delta 恒 >0 → 每分钟继续抛（TA 摸鱼值上涨时必现）；该错误还会污染用户诊断报告的「最近错误」环，干扰其他问题定位 | 修复（p2-features.js 两处）：chk 所在 IIFE 内自备同语义助手 `dcfPFish(def)`（走 window.dcfGet('fish')，dcfGet 内部含内置默认表回退，语义与原 dcfP('fish',35) 完全一致），chk 改调 dcfPFish(35)。#132 哨兵 needle 由 `dcfP('fish', 35)` 同步改锚 `dcfPFish(35)`（原锚即作用域 bug 本体，改回即复发） | 哨兵 2 条（build.mjs）：#132 摸鱼概率锚更新为 dcfPFish(35) + #224 助手函数行。验证：node --check 过；--check-sentinels 490 全绿哑 0（含改锚后）；verify-gc-color.mjs 无头实测页面零 dcfP 报错。回归面：#132 其余 13 条消费点（period/garden/chat/room/cjian/drift/music）未触碰，哨兵全绿 | <br /> | <br /> |
| 225 | 用户报障（2026-09-07，未给机型——纯逻辑问题全机型通用）：「网站更新的问题，顶部的更新一直重复提醒」。v3.26.x 按版本 ack（281e0ec「更新条按版本免打扰收口」）之后复发 | 残留两洞（pwa.js）：① ack 只在点「刷新使用新版/稍后」时写——用户看到条不点（杀掉重开/切走），ack 不存在 → 同一版本每次打开都弹；② SW 通道拉 version.json 失败时 showVerBar() 无 ts 照弹，「宁多勿漏」整体绕过 ack（GitHub Pages 弱网常态）。v1 修订曾引入 24h 时间窗，站点主否决（本站一天可部署十几次，按时间压制新版本提醒不成立）已废 | pwa.js showVerBar（两通道唯一入口）弹条门改 verSeen 一版一弹（v2）：新键 ver-update-notify 弹条即记「ts|时刻」、记录永久有效不按时间过期——同一版本只弹一次（无论点没点按钮、隔多久重开）；更新的版本（ts 更大）立即照弹、无任何时间限制；ts 未知（拉版本失败）只在从没弹过条时照弹（宁多勿漏只留全新用户，弱网绕过口子堵死，真正新版本由轮询通道网络恢复后立即提醒不漏）；按版本 ack 机制保留 | 哨兵 1 条（build.mjs FIX_SENTINELS）：gate 表达式 `!verShouldNotify(onlineTs) || verSeen(onlineTs)`（js/pwa.js 内唯一）+ 专项脚本 tools/verify-ver-update-snooze.mjs（vm 跑真实源码 19 断言：同版本 25h 前弹过不再弹/新版本立即弹无时间窗/落后十几版立即弹/弱网无 ts 不绕过/全新用户保留/ack 兼容/稍后刷新按钮行为）。【真机:待验证】（任意机型）：①同一条提醒不点按钮杀掉重开不再弹；②新部署后打开立即弹一次；③点稍后当天再部署仍会弹（不压制） | <br /> | <br /> |
| 226 | 多机型复发（2026-09-07 用户报障「好多手机型号，刷新后丢失美化、丢失数据」；与 #82/#88 荣耀 200 Pro「设置开关退出重进变回去」同族，#166（2026-09-05 构建）后复发）：美化/设置/近期小数据改完刷新就回退，表情图片也可能丢——浏览器杀进程回滚 localStorage 后，写日志的第二道自愈防线（IDB 每键时间戳标记）整层失效 | 根因（idb.js，#166 微批化回归）：#166 把 wrj 标记落库从逐键 idbSet（4s 超时+连接重建+重试）改成 idbSetAll 150ms 微批单事务，但 idbSetAll 无超时骨架——真我/荣耀/小米 Edge 等挂起内核上事务既不 oncomplete 也不 onerror，Promise 永不落地：①wrjMarkFlush 的「返回 false 退回逐键 idbSet」兜底永不触发 → 标记静默丢 → LS 回滚后 wrjMergeFromIdb 找不到新标记、自愈失效，idbRestore 又以「LS 有值即最新」为准 → 最近的改动整体回退（LS 日志第一道防线与 LS 值同批回滚，救不了）；②media-pool mochiMediaFlush 同样永久挂起：writeBuf 已 splice 却既不写成功也不回队 → 表情/图片令牌静默丢。修复（idb.js 一处）：idbSetAll 补与 idbSet 同款超时骨架（按值体积放大 4s~30s，超时置空连接 + resolve(false)），两个调用方的既有 false 兜底即刻可达；健康内核路径零变化（#166 微批优化保留） | 哨兵 1 条（build.mjs）：idbSetAll lim 超时表达式（js/idb.js 内唯一）。行为断言 tools/verify-idb-setall-timeout.mjs 13 断言（纯 Node 桩 IDB 跑真实 src/js/idb.js+media-pool.js：T0 静态锚/T1 健康内核标记微批落库不回退/T2 挂起内核 4s 判 false+兜底逐键落标记/T3 媒体池 flush 有界返回+写恢复后令牌落库）。红绿对照：git stash 修复前源 T2/T3 永久挂起=红（精确复现回归现场），修复后 13/13=绿。同库复跑：verify-ver-update-snooze 20/20（#225 并行批次）、verify-storage-opt 22/9 红为存量（stash 对照 HEAD 同红，TASKS #130 在册）。【真机:待验证】（真我/荣耀/小米 Edge 家族及任意机型）：改美化/设置开关→立刻杀掉浏览器重开→改动保留不再回退；表情/图片刷新后不再丢 |
| 215 | 屏幕适配诊断「历史对比」虚假变化项：两次连续诊断显示「ori: 竖屏 → undefined；fs: false → undefined」（用户实测报告） | 根因（device.js sdHistCompare 字段名错位）：快照存档键叫 ori/fs，但对比时当前值直接读采集器的 orientation/fsActive（cur[p[1]] 键名不匹配）→ 这两项恒判「变化」恒显 undefined，历史对比失去可信度 | 修复：对比表改为「快照键↔采集键」成对映射（PAIRS 15 组），并在采集器补 ori/fs 别名字段——两次一致诊断正确显示「各项一致」 | 哨兵：#175 对比 PAIRS needle 同步（sdHistCompare PAIRS 表）。验证：node --check 过；--check-sentinels 492 全绿；CDP 端到端两次连续诊断「各项一致」无 undefined。【真机:待验证】（任意机型连续跑两次屏幕适配诊断）：第二次报告历史对比应显示「各项一致」 | <br /> | <br /> |
| 227 | 诊断导出升级（2026-09-07 用户需求「【屏幕诊断】新增也可以导出txt，不过能不能导出docx；信息诊断可以修改为导出docx吗」）：信息诊断「导出txt」按钮改「导出docx」；屏幕适配诊断弹窗此前只有自动复制、无文件导出入口，补「导出docx」按钮（文件名前缀 mochi-screen-diag-） | 方案（device.js，AI-B 域）：docx=ZIP 容器的最小 OOXML，零依赖手写「存储式 ZIP（全不压缩，解压兼容面最大）+表驱动 CRC32」打包 [Content_Types].xml / _rels/.rels / word/document.xml 三件套；正文一行一段落（Consolas+雅黑、xml:space 保留空格、XML 转义、sectPr 收尾），Word/WPS 手机端直接打开转发；不引第三方库保持单文件构建。旧 exportTxt 整体移除（无死代码），两条旧 txt 哨兵随迁 docx 口径（「建议优先【导出docx】」「exportDocx(c ? c.text() : cur)」——#113 取消自动复制锚点语义不变，仍锚「手动导出」调用点） | 哨兵 1 条（build.mjs）：docx 生成 ZIP 本地头签名写入 `setUint32(0, 0x04034b50`（js/device.js 内唯一逻辑锚）。行为断言 tools/verify-docx-export.mjs 24 断言（vm 抽真实 crc32/buildDocxBlob 源码生成真实 docx 字节：ZIP 逐字段解析+独立重算 CRC+document.xml 内容/转义/中文断言+两处按钮接线静态断言）；另经系统 unzip -t 三件套校验 OK。构建 sw mochi-mtq2ttn9 哨兵 493/493 哑 0。【真机:待验证】（任意机型）：设置→诊断信息→【导出docx】、屏幕适配诊断→【导出docx】——下载的 docx 用 Word/WPS 打开不乱码、报告数值不错行 | <br /> | <br /> |
| 229 | 多机型「部分数据丢失」残余洞收口（2026-09-07 用户再报「手机出现数据丢失，不是全量丢失，其他机型也有」；#82/#88/#226 同家族第三层）：#226 修好「标记写不进去」后，自愈第二道防线的**读取侧**仍有一击即溃点——合并恰逢 IDB 挂起窗口时整个会话放弃自愈 | 根因（idb.js wrjMergeFromIdb）：①入口即置 `_wrjMerged=true`（防重入），失败也永不重试；②走 `idbGetAllKeys`——它把「清单读取失败(null)」折叠成「空数组」，与「库里确实没有标记」不可区分。真我/荣耀/小米 Edge 等挂起内核上，启动合并若撞上 IDB 挂起窗口（iOS 挂后台杀 IDB 服务后回前台、Edge 回收后台进程后首读）：清单读失败被当成「没有标记」→ 空转一次 → 会话剩余时间 LS 被杀进程回滚的美化/设置/近期小数据再无第二道防线（第一道 LS 日志与 LS 值同批回滚救不了）→ 用户看到「刷新后部分数据丢失」。标记/新值其实都幸存在 IDB 里，只差读回来 | 修复（idb.js 一处）：合并改走严格三态 `idbListKeys`（null=读失败≠没数据）；**合并真正走完（或确认无可修）才置 _wrjMerged**；读失败 → 有界重试 10s×5（`wrjMergeRetry`，busy 解锁统一收口防重入叠加）；有标记却读不到任何有效时间戳（idbGetMany 失败折叠 undefined→cand 空）同样重试，「标记都在但都不比已知写入新」才是真无可修；时间戳守卫（t > _wrjTimes）保证迟到的重试合并永不覆盖本会话新写入。健康路径行为零变化（该修照修、无标记照旧一步结束） | 哨兵 1 条（build.mjs）：`if (!keys) { wrjMergeRetry(); return; }`（js/idb.js 内唯一逻辑锚——删掉重试改回一次性放弃即消失）。行为断言 tools/verify-wrj-merge-retry.mjs 17 断言（纯 Node 桩 IDB 跑真实 src/js/idb.js，场景=回滚世界「IDB 新值+新标记幸存、LS 残留旧值」：U0 静态锚/U1 健康内核启动合并直接自愈+派发 mochi-wrj-heal/U2 清单读失败（毒 restore+合并首轮）→10s 重试后自愈+listCalls≥3/U3 标记时间戳读失败（idbGetMany 折叠 undefined）→重试后自愈）。红绿对照：git stash 修复前源 7/17（U2/U3 行为断言全红=LS 永远停留旧值，精确复现回归现场），修复后 17/17=绿。【真机:待验证】（真我/荣耀/小米 Edge 家族及任意机型）：改美化/设置开关→立刻杀掉浏览器重开→改动保留；若仍遇回退，第二次刷新应能自动恢复（重试自愈在会话内 10s~60s 间落地） | <br /> | <br /> |
| 228 | OPPO Reno6 5G + 雨见浏览器（Gecko/Firefox 152 内核，安卓 13；2026-09-06 诊断报障 v3.26.486：「发送语音点结束还是显示输入中，发不出去语音」，用户明说其他设备型号也有；#169 同机续报） | 根因（chat.js 录音停止链路，四个静默卡死洞）：①雨见等慢壳 ondataavailable/onstop 迟到或丢失——stop() 后收不到结账事件，面板永远停在「正在录音…」、试听/发送键永不出现（诊断错误环零语音异常=静默卡死实证）；②录出空数据（isTypeSupported 谎报的容器）时 onVoiceRecStop 对空 blob 静默 return，同样永久卡「正在录音…」零提示；③getUserMedia 在壳权限委托异常时可能既不 resolve 也不 reject 永久挂起——#169 的 voiceStarting 闸门随之永不复位，之后每次点「开始录音」都被静默忽略=面板看似点不动；④停止结账期间连点会启动新录音机并被旧结账偷走句柄。修复（mime 三分支与 #169 闸门原样保留，只补兜底不换路）：①停止结账统一收口 voiceFinalizeStop + voiceStopSettled 幂等闩（onstop/看门狗/异常三路只结一次账）+ onstop 3s 看门狗用已到分片自行收尾；②空数据改可见失败态「没录到声音数据，请重试」+清残留可发送状态防旧录音误发+置 voiceMimeFallback 下次换浏览器默认容器，录到数据自动清除；③acquireVoiceStreamGuarded 15s 看门狗，超时报「麦克风无响应」复位闸门、迟到的流停轨防麦克风常驻占用；④voiceStopping 防重入（toggle/启动双向）。 | 哨兵 4 条（build.mjs，全逻辑锚点、chat.js 内唯一）：看门狗武装表达式 / voiceMimeFallback=true 空数据兜底 / mic timeout TimeoutError 挂起看门狗 / voiceStopSettled 幂等闩。验证：node --check 过；新增 tools/verify-voice-send.mjs（静态 7 断言 + 无头真实产物端到端 17 断言：桩 MediaRecorder 精确复现 onstop丢失/空数据/onstop迟到连点/中途关面板/getUserMedia挂起五故障形态+正常录音发送链路不回归）。【真机:待验证】（OPPO Reno6+雨见 及任意机型）：①发语音点停止→出现试听+「发送到聊天」→能发出语音气泡；②个别壳若仍录不出，面板显示「没录到声音数据，请重试」不再永远转圈，再录一次自动走默认容器；③权限异常/挂起时 15 秒内出「麦克风无响应」提示且之后仍能重试；④录音中快速连点不丢试听态 | <br /> | <br /> |
| 230 | 任意机型（2026-09-07 用户报障「领取红包会闪屏」，明说其他设备型号也有；#211/#220 同根因家族）：点领取/长按退回红包瞬间整个消息区闪一下——每次必闪、与机型和历史条数无关 | 根因（chat.js）：领取/长按退回/TA领取/TA退回/自动领取五处红包状态流转一律 renderWindow 整窗重建——body.innerHTML='' 后全部气泡（img 节点全部重建重新解码）＝肉眼整屏闪一下；#211（addRec 窗口闸）/#220（重开同窗补丁）修的都是别的触发路径，红包状态流转是同族最后一条未收口路径——领红包必经此处＝所有机型每次必闪，#211/#220 的窗口闸拦不到它（无头实测点击即 add31/rem31） | 修复（chat.js 一助手五守卫）：rpStatusCls 后新增 rpPatchStatusInPlace——按 data-idx 定位屏上红包卡，只更新该卡 opened/expired class 与状态文案（CSS 状态类只改边框/透明度/字色，卡片尺寸不变＝无布局跳动，childList 零变动＝零闪动）；五处调用点改「先试补丁、返回 false（卡片被裁剪出渲染窗口/数据异常）才回退原 renderWindow」，回退分支与旧版行为逐字一致；#211/#220/#228 已修路径一行未动 | 哨兵 2 条（build.mjs，chat.js 内唯一逻辑锚）：补丁表达式 card.classList.remove('opened', 'expired') + 领取路径守卫 if (!rpPatchStatusInPlace(rpIdx)) renderWindow(true, true)。行为断言 tools/verify-rp-claim.mjs 11 断言（无头真实产物：种入 30 条文字+1 张待领取红包→点击领取——状态文案=已领取/卡片节点身份保持 __rpMark/同批增删=0 批/回执增量追加+四条静态锚）。红绿对照：修复前（HEAD 产物）6/11——卡片节点被重建+同批增删 31/31=报障闪屏精确复现；修复后 11/11。家族守卫 verify-chat-rebuild 16/16（首跑 15/16 为计时型断言抖动，复跑两轮全绿）、同文件相邻 verify-voice-send 24/24、布局 verify 10/10、构建哨兵 500/500 哑 0（sw mochi-mtq55x3h）。【真机:待验证】（任意机型）：①点 TA 发的红包→变「已领取」、消息区不再整屏闪、随后追加「你领取了红包」回执；②长按退回同样不闪；③正看聊天时 TA 领取/退回我发的红包同样不闪 | <br /> | <br /> |
| 231 | 红米 Note12 Turbo Chrome（2026-09-07 诊断报障 v3.26.492：「自用完整外观方案、聊天美化方案……保存后再刷新网页又恢复初始状态」，用户明说其他设备型号也有；与机型无关的迁移漏键）：完整外观方案（full-beauty-schemes）保存后刷新，方案管理页清空回「还没有保存的完整方案」；美化撤销栈、更新条「一版一弹」记忆（ver-update-ack-ts/ver-update-notify，#225v2）同样每刷新清空 | 根因（contacts.js EXCLUDE 漏登记）：personalize.js 的 full-beauty-schemes（v3.27.x 新增）/beauty-undo-stack 与 pwa.js 的 ver-update-ack-ts/ver-update-notify（#225v2 新增）都是全局根键，但没登记进 contacts.js EXCLUDE——migrateLegacy 每次刷新把它们当旧顶层业务键迁进 default 并删 LS 根键（IDB 根键保留→idbRestore 每次回填→再被迁走，循环）；方案列表/撤销栈/已弹版本记忆的读取方都读根键 → 刷新即「恢复初始」。与 #121 时代 feed-*/pomo-*/beauty-schemes 漏排除同族——新功能加全局根键忘记同步 EXCLUDE | 修复（contacts.js 三处）：①EXCLUDE 补登记四键；②migrateLegacy 存量回收清单并入 full-beauty-schemes/beauty-undo-stack——修复前已被迁走的存量方案副本（报障用户的「自用」方案数据就滞留在 default: 副本）一次性写回根键；③idbRestore 回填与回收清单双保险下，IDB 根键与 default 副本任一幸存即可找回。注：聊天美化方案（chat-beauty-schemes）本就在 EXCLUDE（v3.26.x 已补），其「回退」体感主要来自 #226/#229 回滚家族（修复随 7552990 已入库）与本键方案列表清空的叠加观感 | 哨兵 2 条（build.mjs）：EXCLUDE 四键整行锚 + 存量回收清单尾锚（'full-beauty-schemes', 'beauty-undo-stack']，同时接替被本批改动打破的 desk-freq-mode 旧锚）。行为断言 tools/verify-exclude-feed-schemes.mjs（32 断言，纯 Node 桩 IDB 跑真实 idb.js+contacts.js+feed.js）：V1a 根键 migrateLegacy 后仍在+滞留副本回收/V1b IDB 缺失时从 default 副本找回存量方案/V1d 真旧顶层键迁移不被误伤。红绿对照：git stash 修复前源 11/32（方案根键被迁成 null=报障原文复现），修复后 32/32。【真机:待验证】（任意机型）：保存完整外观方案→刷新→方案管理页方案仍在；保存过的存量方案刷新一次后找回 | <br /> | <br /> |
| 232 | 红米 Note12 Turbo Chrome（2026-09-07 同一诊断报障：「联系人的朋友圈头像、昵称保存后再刷新网页又恢复初始状态」，多机型同发；与机型无关）：朋友圈好友列表页给联系人/自己设的朋友圈头像、昵称，保存后刷新即回「未设置/旧值」 | 根因（feed.js feedRootRescue 回收方向过时）：该 IIFE 为修复 v3.13 前「feed-* 根键被误迁 default」而设，逻辑是「根键有值→删 default 副本；根键没值→把 default 副本搬上根键再删本地」。但 v3.8.x 朋友圈好友列表起，身份/封面六键（feed-cover-bg/feed-ta-cover/feed-ta-name/feed-ta-avatar/feed-user-name/feed-user-avatar）已改按桌面独立存取（所有读取方 per-cid 优先、根键只是旧版回退，好友列表更是只读 per-cid）——于是：首次刷新把 per-cid 值搬上根键并删本地，之后每次刷新「根键有值→删 default 副本」把用户编辑删回旧全局值＝每次编辑都活不过下一次刷新 | 修复（feed.js 一处）：六键从 KEYS 拆出到 DESK_KEYS，改收养式回收——per-cid 有值一律不动（不删不搬）；per-cid 为空且根键有旧全局值时，先向 IDB 三态确认（idbHasKey false 才收养——>200KB 大值只存 IDB，def.get 看不到≠不存在，防旧全局小值盖掉大头像/封面）再收养进 default，根键保留作回退；幂等。feed-notices/feed-app-unread 两真全局键旧行为原样保留 | 哨兵 2 条（build.mjs）：DESK_KEYS 拆分行锚 + idbHasKey 三态守卫锚（js/feed.js 内唯一）。行为断言同 tools/verify-exclude-feed-schemes.mjs：V2 根键有旧值时 default 现行头像不被删（旧代码必红=报障复现）/V3 收养路径/V4 守卫防覆盖大值/V5 全局键旧行为保留。红绿对照：旧源 V2/V3 红，修复后 32/32。【真机:待验证】（任意机型）：朋友圈好友列表改联系人朋友圈头像/昵称→刷新→仍是新值；旧全局用户（从没设过 per-cid）刷新一次后好友列表显示出旧全局值 | <br /> | <br /> |
| 233 | 多机型 LS 回滚家族第四层（2026-09-07 红米 Note12 Turbo Chrome 诊断随报；#82/#88/#226/#229 同家族——前三层修的都是「自愈逻辑本身」，本层是自愈的弹药库每次刷新被人拆掉）：无独立症状，但美化/设置回退类报障在 #229 修复后仍可能复发 | 根因（contacts.js isExcluded 漏挡无冒号系统键）：idb.js 的 __wr-journal（写日志＝LS 回滚自愈第一道防线）、__ls-dirty（LS 脏键索引）、__big-idx（大键索引）都是 xy-home-v2: 根命名空间无冒号键，不在 EXCLUDE——migrateLegacy 每次刷新把它们当旧顶层业务键迁进 default 并删根键：写日志历史每刷新清零（init 回放只剩本会话写入）、大键/脏键索引反复丢失（OOM 防线与回填避脏降级），自愈防线被持续削弱 | 修复（contacts.js 两处）：①isExcluded 加兜底规则——remainder 以 __ 开头＝系统键，一律不迁移（现有一键三键+未来新增系统键自动受保护）；②migrateLegacy 加 default:__ 滞留副本清扫（LS+IDB 双删，防 idbRestore 回填复活），清掉历史上已被误迁的死副本（纯占 LS 配额）。标记键 __wr-j:* 因含冒号本就走「保守视为命名空间键」分支，不受影响 | 哨兵 2 条（build.mjs）：__ 兜底规则行锚 + default:__ 清扫锚（js/contacts.js 内唯一）。行为断言同 tools/verify-exclude-feed-schemes.mjs：V1c 根 __wr-journal/__big-idx 迁移后仍在+default 滞留副本 LS/IDB 双删（旧源 __big-idx 根键被迁走=红）/V1d 真旧顶层键照常迁移（防兜底规则过度拦截）。红绿对照：旧源 11/32，修复后 32/32。已并入设备索引「多机型 LS 回滚家族」行（82、88、226、229、233） | <br /> | <br /> |
| 234 | 红米 Note12 Turbo Chrome（2026-09-07 同一诊断的「开关持久化体检」输出 cs-voice-send：LS="1" 读取=缺失——装置性误导，非数据问题）：诊断「开关持久化体检」的「读取」列恒为缺失（无论键是否存在） | 根因（device.js 探针键位拼错）：xyStore(prefix) 的前缀参数不带尾冒号（内部自拼':'），探针却把带尾冒号的 P（xy-home-v2:default:）传进去，读的是 xy-home-v2:default::cs-xxx 双冒号键——恒 null。LS 直读列与 IDB 权威列键位正确，唯独「读取（内存优先）」列全错，导致据此体检判读「应用读不到 LS 里的值」会得出错误结论（本会话就曾被它误导） | 修复（device.js 一处）：另备前缀 SP，「读取」列改走 xyStore(SP).get(short)；LS/IDB 两列键位不动。**2026-09-11 复发修正（小米17Pro+Edge152 诊断实证，vivo/荣耀等多机型同现——装置性错误与机型无关）**：首修把 SP 写成 `G + ':' + cid`，但本文件 G='xy-home-v2:' 已带尾冒号，拼出的仍是 xy-home-v2::\<cid\>:: 双冒号，「读取」列恒缺失原样复发。正确拼法 = `SP = G + cid`（等价 contacts.js 家族的 `xyStore(GNS + ':' + cid)`，GNS 不带尾冒号）。教训：修复验证当时用「同一病句改写」的静态断言钉死的是笔误形态，非行为 | 哨兵 1 条（build.mjs）：window.xyStore(SP).get(short)（js/device.js 内唯一；任何额外冒号拼法回归即消失）。行为断言：verify-exclude-feed-schemes.mjs V6 静态断言（SP=G+cid 在位 / 首修病句 G+\':\'+cid 清零 / xyStore(P) 病句清零 / 读取列走 SP / IDB 列键位不受影响） | <br /> | <br /> |
| 235 | 形态判定收敛（#209 共享判定器 mochiViewportForm）补 Safari 版本门：26.x 内核（16Pro/17Pro Safari 26.1/26.6，env≈diff>0）实为「覆盖形态」却被判「保留」→ 顶部避让漏加（顶栏贴灵动岛）+ 高度少算 env 段（底部白带）；18.3 老内核才是真「保留」 | 实现（device.js mochiViewportForm）：解析 Safari 主版本（Version/x.y）入 sig.safMajor（执行器 _sig0/判定器/采集器三处同源透传），保留判定 resStand 加 `safMajor > 0 && safMajor < 26` 门——Safari≥26 同信号一律按覆盖形态处理（safeTop=env、expBase=min(屏高,envTop+inner)）；Safari<26 维持保留判定（#203 15Pro/18.3 实证不变）；force 声明优先级不变。无 safMajor 的旧调用方兜底按旧逻辑（0<26 恒真）零回归 | 哨兵：#209 共享判定器 needle 随新门同步（build.mjs 既有锚点行更新）。验证：node --check 过；形态矩阵三机型单测（26.6→covered/18.3→reserved/iPad→ipad）全过；--check-sentinels 全绿。【真机:待验证】（iPhone 16 Pro/17 Pro Safari 26.x 主屏幕）：顶部避让自动生效（Mochi 行在状态栏下方）、高度铺满可视区；15 Pro/18.3 回归不变形 | <br /> | <br /> |
| 236 | OPPO K13 Turbo Pro + HeyTapBrowser（2026-09-07 诊断报障 v3.26.490，「屏幕下方大片空白」，用户明说其他设备型号也有；诊断三 ✗：顶部重叠+底部少填 108px+底部导航栏悬空 126px，SIG form=covered/safeTop=0/期望底=760/.phone=652/kbActive=true）：①桌面状态栏顶位恒 0 钻进系统状态栏区；②收一次键盘后 .phone 内联高锁死 652=底部 108px 空白+tabbar 悬空，杀掉重开前不复原 | 根因双洞（mobile-adapt.js）：①covered 形态的执行侧（写 --mochi-safe-top / 挂 mochi-cover-top）整体在 isIOS 分支（syncVvFit），安卓永远没人执行——该壳 viewport-fit=cover 生效、页面画进系统状态栏下方（env-top=40），base.css 后加载的 .statusbar{padding:4px} 压死 @media 的 env() 避让（#114 同根因安卓版；判定器早判出 covered/safeTop 应=40，但安卓无执行器落地）；②该壳收键盘后 vv.height 恒停在 652=inner(720)−底栏(68) 不回基准 → syncAndroidKb 的 open=h<_aH-60 恒真 → _aKb 卡真（含无聚焦被纯 vv 读数置位的会话）、.phone 内联高锁死 vv 值；#209 清扫因 _aKb 真被跳过、focusout 400ms 复原因 652<_aH-60 不达、250ms 轮询等 vv 回 708 也不达——四条复原路全被这一个残留读数堵死 | 修复（mobile-adapt.js 两块+device.js 判定器配套）：①安卓块新增 _aSyncCoverTop 执行器：env 探针（按横竖屏缓存）→ 共享判定器 → safeTop>0 写 --mochi-safe-top+挂 mochi-cover-top（base.css 既有 html.mochi-cover-top .phone .statusbar 规则抬状态栏），否则摘除；高度侧刻意不动（浏览器形态布局视口=inner，#199 同款语义）；判定器 coverBrowser 扩 `!!sig.andr`（安卓壳带底栏 diff>2 也入浏览器覆盖形态→期望底边=inner 不再误报少填 40），iOS 不传 andr 维持 #199 原判式零回归；诊断③对 coverBrowser 改用有效顶位=元素顶+实测 padding-top（该形态 .statusbar 靠自身 padding 抬升）；②1s 看门狗新增键盘会话卡死自愈：真键盘证据=vv 缩幅≥min(_aIH,_aH)×22%（真键盘>200px）或 inner 同缩（resizes-content）——都不成立而缩幅落残留带（13~22%）+会话超 1.5s+vv 读数稳 1.2s（避开收起动画）→ 清键盘态+置 _aVvStale 闩抑制纯 vv 再触发（触摸/聚焦/回基准解除），防 652↔720 抖动来回抽 | 哨兵 4 条新增+#199 判定器锚同步（build.mjs，各自文件内唯一逻辑锚）。行为断言 tools/verify-viewport-form.mjs 扩至 76 断言：HeyTap 信号台账（cover-browser/safeTop=40/expBase=720）+iOS 不传 andr 零回归闸+安卓 env=0 不误伤+screenDiagJudge 端到端（修复后稳态全绿/报障现场三 ✗ 精确对号）。红绿对照：git stash 修复前源 72/76（HeyTap 台账 3 红+稳态全绿 1 红=报障复现），修复后 76/76。家族守卫：verify-kb-residue-heal 10/10（#209 看门狗不回归）、verify-fullscreen-ipad 25/25、布局 verify 10/10；verify-ios-kb-stuck 3 红/verify-ios-reserved-standalone 7 红/verify-screen-diag-opt 4 红经 stash 对照 HEAD 基线逐项同红=存量（#235 批 Node 环境 fixture 口径，归属其会话），本批零新增回归。构建哨兵 512/512 哑 0（sw mochi-mtq84lcq）。【真机:待验证】（OPPO K13 Turbo Pro+HeyTapBrowser 及任意安卓壳）：①更新后桌面状态栏（Mochi 行/时间）自动落到系统状态栏下方、不再重叠；②聊一次天（键盘弹出再收）→底部不再残留大片空白，屏幕适配诊断全 ✓；③诊断报告形态行显示「浏览器覆盖壳（#199/#236）」且无 顶部重叠/底部少填/导航栏悬空 | <br /> | <br /> |
| 237 | 多机型（2026-09-07 用户报障）：备忘录添加备忘后聊天里联系人零反应——期望「添加备忘触发 TA 在聊天里提问/回应」。排查定性：非回归，链路本就不存在——memo-app.js 新增路径只做本地保存+备忘页内 25% 概率安慰语（memoShowMsg 仅备忘页内渲染）；聊天侧仅两通道=勾选完成（memo-app-send 开关默认关）/单条手动分享；BUGS/FIX-REGRESSION/notice.json 无在册记录，git 全历史也无此功能 | 修复（memo-app.js 两处，最小改动）：①新增 DEF_MEMO_ASK 追问语料 4 条（{m} 占位）；②memoAddFromInput 保存成功后经 window.chatAddIn 即时发一条「回应+追问」（{m}=memoClip(v,16) 截断），带 { tag: '备忘' } 来源 chip（与喝水/吃饭提醒同模式：自带未读数+桌面横幅/后台通知联动，不在聊天页也能被提醒）；完成/手动分享通道不动，备忘页内安慰语保留；随批更新功能介绍页文案（template.html 生活工具·备忘录行，顺带删去已下线的「桌面横幅实时显示待办数」过期描述=v3.15.x 已按用户要求删除） | 哨兵 1 条（build.mjs）：window.chatAddIn(memoPick(DEF_MEMO_ASK).replace('{m}', memoClip(v, 16))（js/memo-app.js 内唯一逻辑锚，删调用即红）。验证：node --check 过；--check-sentinels 全绿哑 0（构建后）。【真机:待验证】（任意机型）：备忘录添加任意备忘→切聊天页，TA 立即有一条带「备忘」chip 的追问消息、可正常回复续聊；连加多条每条各有一句；聊天统计/防重复不受影响 | <br /> | <br /> |
| 238 | 同用户（2026-09-07，#237 追加需求）：备忘需要「概率触发」联系人在聊天里发提醒——即 TA 主动催办备忘（此前 #237 只覆盖「添加当下回应追问」，待办挂着时聊天侧零提醒，原催办只在打开备忘录页时页内显示） | 实现（memo-app.js，完全复刻吃饭提醒模式）：①新增 DEF_MEMO_REMIND/_DUE/_OVER 三组话术（{m}=memoClip 16 截断、{n}=过期天数）；②配置单键 JSON `memo-app-remind`（en 默认开/prob 默认 2 同吃饭/last=上次提醒时刻），存根命名空间随 memo-app-* 全局共享，键名登记 contacts.js EXCLUDE（跨域 1 行）防 migrateLegacy 误迁；③memoRemindTick：每 4 分钟一掷（+启动 60s+回前台 mochi-fg-resume 2~6s 补触发，同 ta-ask 通道），守卫=开关/概率>0/23:00–06:00 静默/命中后至少隔 2 天；④memoRemindFire 按紧急度挑事项（过期>今日到期>积压>2 天>普通待办）组话术经 chatAddIn 发 { tag:'备忘提醒' } chip（自带未读+桌面横幅/后台通知），震动 [80,60,80]，发出即写 last 时刻；⑤备忘录页 manage 行新增「备忘提醒：开/关」「提醒概率 N%」两钮（openModal 输 0-100），memo.css .memo-manage 改 flex 换行；⑥暴露 window.memoRemindTickNow 供手动/回归触发（同 triggerTaInviteNow 惯例）；template.html 功能介绍行同步 | 哨兵 2 条（build.mjs）：`window.chatAddIn(text, { tag: '备忘提醒' })`（发送锚）+ `if (Date.now() - c.last < 2 * 86400000) return;`（间隔闸）。验证：node --check 过；--check-sentinels 517 全绿哑 0；playwright 冒烟（prob=100+clear done+有未完成备忘→memoRemindTickNow→聊天出现带「备忘提醒」chip 催办、标记落位、同日二次触发不再发）。**同日 2 轮（用户反馈「也不用提醒太频繁」）：每日一次闸升级为 2 天间隔闸**——配置 done(日标记)改 last(时间戳)，v1 存量 done='YYYY-MM-DD' 自动迁移为该日 23:59:59 时间戳不丢间隔起点；默认 2% 概率+2 天间隔≈两三天最多催一次，概率调 100% 也受间隔闸约束。【真机:待验证】（任意机型）：备忘录页底部「备忘提醒：开、提醒概率 2%」可见可调；挂一条今天截止的备忘等到概率命中（或把概率调 100 等一轮）→聊天收到带「备忘提醒」chip 的催办，之后至少隔 2 天才会有第二条；23 点后不催 | <br /> | <br /> |
| 216 | 屏幕适配诊断聊天页盲区补齐：键盘打开期间的专项数据（聊天「界面上移/输入栏被键盘盖住」发生在键盘期，此前报告仅键盘收起后可采，发生瞬间的输入栏位置/键盘高度无数据） | 实现（device.js collectScreenDiag 聊天块）：新增键盘期采集——键盘高度（基线 inner − vv）、键盘期输入栏底边（应 ≤ 键盘上沿）；报告聊天页节键盘打开时输出「键盘期：键盘高度≈Xpx 输入栏底边=Ypx」行。配套上轮时间线（kb 跳变回放）构成键盘期完整定位链 | 哨兵 1 条（build.mjs FIX_SENTINELS）：inp.chat.kbActive。验证：node --check 过；--check-sentinels 508 全绿；CDP 实测无键盘环境不输出键盘期行（判定正确）。【真机:待验证】（任意机型聊天页键盘打开时跑屏幕适配诊断）：报告出现「键盘期：键盘高度≈X 输入栏底边=Y」行，输入栏被盖/上移时 Y−键盘上沿差值直接定位 | <br /> | <br /> |
| 239 | 小米 15 Pro + Chrome（2026-09-07 报障 v3.26.491：「其他互动功能字卡看不到了，点击没有任何内容」；诊断交互轨迹=fc 页点分组 chip×3+搜索两次后退出）：打开「其他互动功能字卡」页满屏都是「使用概率」stepper，分类 tab/分组条/搜索框/字卡列表全在屏幕外 | 根因（template.html + chat-pages.css，#132 引入布局回归）：#132 在 fc 页头部插入 13 行「使用概率」stepper（dcf-prob-box 实测 794px），而 .card-list{flex:1;overflow-y:auto} 的 overflow 非 visible 使 flex 自动最小尺寸归 0——头部一大列表就被压扁：384x808 无头实测 #fc-list 高 6px、首条字卡 top=1271（视口 808）=用户首屏只见概率行，点 chip/搜索「没有任何内容」（列表在屏外）。dc 页（无概率框）正常，所以只有 fc 页报障 | 修复两处：①template.html 概率框整块移到 #fc-list 之后（字卡列表回首屏；stepper 绑定走 getElementById 与位置无关），说明文案「每个分类下方」同步改「页面底部」；②chat-pages.css 给 #page-fun-cards/#page-deskcheck 照搬 v3.26.x #page-default-cards 同款整页滚动规则（flex:0 0 auto+overflow:visible）——未来头部再插高块列表也不会被挤没 | 哨兵 1 条（build.mjs）：fc-list 整页滚动规则锚。行为断言 tools/verify-fun-cards-layout.mjs（无头 384x808 真实产物：fc 页打开首屏内存在可见字卡条目+概率框位于列表下方+点分组 chip 列表内容变化+列表不再 6px 塌缩）。红绿对照：修复前（概率框在头部+无滚动规则）首屏零字卡=报障复现。回归面：dc 页规则未动，虚拟窗口滚容器判定（clipsContent 找 .page）对三页一致。**同日复发定性（v3.26.513 报障批，非回归）**：OPPO Reno6 5G/雨见 报「【其他功能字卡】页面下面无法滑动显示字卡」——该机 v3.26.513 构建于 04:19（#239 修复 05:11 的 516 之前），git worktree 检出 9fe669d 产物红绿对照 4/10（S1 inVp:0/S2 手势滑 335px 后 visN:0/列表 6px=报障原文复现），HEAD 10/10；=**同根因跨机型残留旧版，非修复丢失**。verify 脚本同批加固：SERVE_DIR 支持旧产物对照+S 段 360×658（OPPO 同款视口）断言 S1 首屏有字卡页面可滚/S2 CDP 触摸手势滑动 scrollTop 推进+滚出屏外新字卡/S3 概率框可滚入视口。【真机:待验证】（小米15Pro/Chrome 及任意机型）：字卡库→其他互动功能字卡——打开即见摸鱼字卡列表，切 tab/点分组/搜索即时出内容，概率设置在列表滑到底部；旧版设备（≤513）更新到 516+ 后同页正常 |
| 240 | 小米 15 Pro + Chrome 151（2026-09-07 报障 v3.26.491，同用户同日：「背景模糊和背景遮罩功能用不了，调整了数据但是背景没有变化，还是很清晰」；#219 已随该版本上线=修复后残留）：桌面美化里调背景模糊/白色遮罩滑块，桌面壁纸始终清晰无变化 | 根因（home.css 模糊载体缺陷，真机内核差异）：#219 已把 .phone-bg-mask 提到 z2 正确盖住壁纸，无头实测计算样式（z2/blur(12px)/rgba 白 50%）与像素 diff 全部正常——真机 Chrome 151 的 backdrop-filter 对 z1 兄弟常驻图层（#phone-bg-layer）的采样/合成不生效（该内核真机特有，无头无法复现）；白色遮罩无感的观感同源（mask 层依赖 backdrop 采样链路的合成级联） | 修复（home.css + personalize.js 各一处）：模糊载体从 mask 的 backdrop-filter 改为壁纸层自滤 filter——blur>0 时 .phone 挂 .desk-blur-on，`#phone-bg-layer` 直接 filter:blur(var(--desk-bg-blur)) 且四边外扩 24px（≥最大 blur 半径 20px）防边缘发虚（!important 覆盖内联 inset:0）；blur=0 不挂类不激活（iOS 卡顿红线语义不变：#147 壁纸常驻合成层静态 blur 纹理可缓存，成本不高于原 backdrop 每帧采样）。白遮罩层 rgba(var(--desk-bg-mask-op)) 与 #219 z2 结构原样保留（#219 哨兵锚不破） | 哨兵 1 条（build.mjs）：.desk-blur-on 规则锚。行为断言 tools/verify-desk-blur-layer.mjs（无头 390x844 真实产物：种壁纸+blur12+mask50→.desk-blur-on 挂上+壁纸层 computed filter=blur(12px)+四边 -24px+模糊开关两次截图像素 diff 显著；blur=0 时类摘除+filter 归 none=红线不激活）。回归面：verify-desk-beauty 全绿（#147 图层/#219 z2/#151 重应用不回归）；#219 backdrop 锚已由新载体替代（真机等效路径）。【真机:待验证】（小米15Pro/Chrome 151 及任意机型）：设置→手机美化→背景模糊调 12px、背景遮罩调 50%——回桌面壁纸变模糊、整体变淡，调整即时可见 |
| 241 | 小米 15 Pro + Chrome（2026-09-07 报障 v3.26.491，同用户同日：「点开聊天界面，联系人的聊天消息有时会先跳动一下，然后才显示正常」；#220 已随该版本上线=同症状残留路径）：打开聊天时消息区偶尔整屏跳一下再恢复 | 根因（chat.js，#220 原地补丁的条件洞）：#220 只覆盖「重开时同窗同貌」（windowRenderedN===len）；LS 快照与 IDB 权威**条数不同**（快照缺上次会话尾条/对端新消息只在 IDB，用户 chat-msgs 3.4MB 大历史常踩）时 `windowRenderedN !== len` 直接放弃 → 权威读库收尾整窗清空重画=肉眼跳动。无头复现：快照 1 条+权威 3 条打开聊天，observer 实录 rm3+add8 整窗重建 | 修复（chat.js 两处）：①inplacePatchIfSameWindow 放宽——`grown = len - windowRenderedN`，屏上是权威**前缀**（grown≥0）且前缀段无 lite 残留+DOM idx 齐整时不再放弃；②grown>0 时调 loadNewerIncremental(len) 尾部增量追加（复用滚动加载增量路径：已有节点零重建、锚定滚动、prune 照旧），追加没补齐返回 false 走原整窗兜底；屏上比权威多（grown<0，数据回滚/裁剪）仍整窗。loadNewerIncremental 加可选 targetLen（不传=滚动加载语义逐字节不变）。#220 同窗路径（grown===0）/#230 红包补丁一行未动 | 哨兵 1 条（build.mjs）：增量追加循环逻辑锚（chat.js 内唯一）。行为断言 tools/verify-chat-jump-incremental.mjs（无头真实产物：种 3 条→LS 快照回滚 1 条→reload 打开聊天——observer 零整窗清空（无 rm 批）、只增量追加尾部 2 条、最终气泡=权威 3 条；重开场景零重建 #220 不回归）。红绿对照：修复前同场景 rm3+add8=报障跳动精确复现。回归面：verify-chat-rebuild 全绿（#220/#230 家族）、verify-rp-claim 全绿。【真机:待验证】（小米15Pro/Chrome 及任意机型，大历史桌面）：收到新消息后重开聊天/退出再进——消息区不再整屏跳动，新消息平滑追加在尾部 |
| 242 | 自查发现（2026-09-07 代码核查，缺陷可达性随 v3.26.x 多群聊分组引入——此前仅内置一群无「切群」可言）：群聊成员回复/撤回是 setTimeout 异步链，发消息后切到别的群，在途回复在执行时刻才读当前 curGid——A 群的回复写进 B 群消息数组并落盘到 B 群存储键（串群），A 群永久丢回复；撤回定时器的 myIdx 同样可能指向新群消息把无关消息标成已撤回；「继续说」按钮同链路同病 | 根因（group-chat.js）：scheduleReply/gcContinueSay 调度时未捕获来源群，memberReply 内 msgs.push/saveMsgs/renderMsg 全在定时器执行时刻读模块级 curGid（switchGroup 会整体换掉 msgs 数组与存储键）；打字指示 show/hide 也不分群（A 群定时器会隐藏 B 群的指示器） | 修复（group-chat.js，逻辑最小改动）：调度入口捕获 `const gid = curGid` 并穿透 memberReply(cid, quoteText, gid)；新增 gcDeliverReply(gid, rec, sfx) 统一投递——同群走原路径（push/save/render/贴底/音效），跨群走 gcReadGroupKey/gcWriteGroupKey 读改写来源群存储键（LS+IDB 同 gcWriteMsgs 口径，不触碰当前 msgs/DOM），返回来源群内下标供撤回定位；群已删除（gcGroupAlive=false）直接丢弃（同「删除群聊连消息一并删除」语义）；retractGcMsg(idx, gid) 跨群时落到来源群存储；打字指示 show/hide 加 gid===curGid 守卫；enterGroupChat/switchGroup 补 hideTyping 清共享指示器残留 | 哨兵 2 条（build.mjs）：`retractGcMsg(myIdx, gid)` + `memberReply(cid, userText, gid)`（js/group-chat.js 内唯一逻辑锚，去掉 gid 穿透即红）。验证：node --check 过；--check-sentinels 全绿；verify-gc-send/verify-group-chat-fixes 等群聊家族脚本+布局 verify 过。【真机:待验证】（任意机型）：A 群发消息立刻切到 B 群→成员回复只出现在 A 群、B 群无串入；切回 A 群回复都在 |
| 243 | 自查发现（2026-09-07，同 #242 核查）：群聊 loadMsgs 的 IndexedDB 异步回填无 key 复核——切群/快速重进后旧群的 idbGet 回调才 resolve，`a.length >= msgs.length` 用旧群整包覆盖当前群 msgs 并 renderAll；下次保存再把 A 群历史回写进 B 群存储键（数据交叉污染）；慢 IDB 手机（idbHydrateKey 最坏 6s+8s）窗口更大 | 根因（group-chat.js loadMsgs）：idbGet 回调闭包只比较数组长度，不校验 key 是否仍是当前群的键；switchGroup/enterGroupChat 都会再次 loadMsgs，旧回调悬挂期间 curGid/msgs 已换人 | 修复（group-chat.js 一行守卫）：idbGet 回调首行加 `if (key !== groupMsgKey(curGid)) return;`——key 不再是当前群的键就整包丢弃 | 哨兵 1 条（build.mjs）：`if (key !== groupMsgKey(curGid)) return;`（js/group-chat.js 内唯一）。验证：node --check 过；--check-sentinels 全绿。【真机:待验证】（任意机型，两个消息量悬殊的群）：两群间快速连续切换 5~10 次再各看一遍——消息不串群、条数不异常膨胀 |
| 244 | 自查发现（2026-09-07，同 #242 核查）：群聊撤回消息「点击查看原文」innerHTML 直出原始 rec.text——多行丢换行；图片/表情/语音回复点开是整屏 base64 或「语音 X″|||data:audio…」原文；自定义字卡内容含 HTML（导入的共享字卡包可携带）会被浏览器当标签执行（注入面）。单聊 chat.js retractMsg 早改为存渲染快照 orig，群聊 v3.9.x 撤回上线时未对齐 | 根因（group-chat.js）：retractGcMsg 只标 retracted 不存 orig；渲染分支 `b.dataset.orig = rec.orig || rec.text` 的兜底值是未转义原文，点击 onclick `b.innerHTML = b.dataset.orig` 直出 | 修复（group-chat.js 两处，对齐单聊语义）：①retractGcMsg 撤回前先抓 DOM 渲染快照 `rec.orig = el ? el.innerHTML : gcRetractFallbackHtml(rec)`（气泡被 DOM 窗口裁剪/跨群撤回时无 DOM，走安全回退）；②新增 gcRetractFallbackHtml(rec)——媒体类型给 [图片]/[表情包]/[语音] 占位、文本走 escTxtBr 转义；渲染分支改 `b.dataset.orig = rec.orig || gcRetractFallbackHtml(rec)`，旧版本落盘的存量无快照撤回记录同样走安全回退 | 哨兵 1 条（build.mjs）：`rec.orig = el ? el.innerHTML : gcRetractFallbackHtml(rec);`（js/group-chat.js 内唯一）。验证：node --check 过；--check-sentinels 全绿。【真机:待验证】（任意机型）：群聊回复设置撤回概率拉满→成员撤回后点「撤回了一条消息」——文本带换行正常还原、表情/图片点开是图不是 base64 铺屏、含 HTML 的字卡不会被当标签执行 |
| 245 | 小米15Pro Chrome（同机对 #241 的复发报障）：打开聊天页依旧「闪屏+弹一下后恢复正常」。排查实锤两条根因（均无头红绿复现）：①大历史 LS 兜底快照被 liteSnapArray 必然剥负载（img/voice→''+_lsLite），权威回读后旧逻辑「见残留即整窗重画」；②预权威保存（启动期签到/TA 主动消息一保存）把 LS 快照**整包覆盖**成会话新消息=首渲缺历史、权威合并重排后前缀凭据失配=**同一消息屏上两份**+权威收尾整窗重画（「恢复正常」全靠相邻重复归一化兜底）；③副产品：冷启动早期 idbGet/HasKey 对已存在大键误报「无」（登记 TASKS #133，AI-B 域） | 根因（chat.js）：①inplacePatchIfSameWindow 的 lite 残留扫描在权威合并后的 msgs 上进行，扫不出「屏上渲的是精简」且见残留即放弃；②saveMsgs/saveMsgsNow 预权威分支 writeLsSnapshot(msgs) 覆盖写；③loadMsgs LS 解析门 !persistTimer&&!msgs.length 被启动期新增双双跳过 | 修复（chat.js 四处）：①renderWindow 渲染时刻登记 windowRenderedLite（窗口内精简残留下标），inplacePatchIfSameWindow 对这些下标 renderMsg+replaceChild **原位换节点**（batchRendering 抑制入场动画/滚动副作用、草稿 collect/restore、贴底保持/高度差补偿、节点缺失仍整窗兜底）；②新增 mergeLsSnapshotWith——预权威 LS 保存改与既有快照按 ts|side|text 去重合并（两处调用点替换）；③loadMsgs 权威未就绪即解析 LS 并与内存新增去重合并（首渲含完整历史=屏上与权威天然前缀对齐）；④#90「确认空库」分支加账本矛盾守卫（账本 n>0 与 isMiss 矛盾=探测说谎，按读取失败重试，绝不拿 LS 会话快照回写 IDB） | 哨兵 1 条（build.mjs）：`old.parentNode.replaceChild(nu, old);`（js/chat.js 内唯一）。验证：tools/verify-chat-lite-upgrade.mjs **9/9**，红绿对照 HEAD 旧产物 **5/9**——红项恰为 J2(升级批缺 rm1+add1)/J3(图片未补真身)/J3b(种子消息×2)/J6(锚点缺失)，修复后全绿；J1 红绿均绿=旧路径本轮未走整窗重画（走的是失配追加=J3b 红的成因），判据保留防整窗重画回归。【真机:待验证】（小米15Pro 及任意机型）：打开有图片/语音历史的聊天页不再整屏闪+弹、消息不重复、历史不再先缺后补 |
| 247 | 自查发现（2026-09-07，接 #242-244 结构核查；编号 246 让渡备注：246 留空防与并行批次撞号）：群聊媒体存储膨胀——我发表情/图片/组合消息与成员回复的表情图片整段 base64 内联进消息数组（单聊 #142 已改走媒体池 @@m: 令牌，群聊漏对齐）；消息数组无上限+每次落盘全量 JSON.stringify 重写 LS+IDB，长期使用数组一次比一次大，LS 配额超限后 try/catch 静默丢写只剩 IDB 异步 fire-and-forget | 根因（group-chat.js）：gcWriteMsgs 直接 stringify 原始 msgs，无令牌化归一；撤回快照也会把已展开的 base64 再存一份 | 修复（group-chat.js，复用 media-pool 全套设施）：①新增 gcNormalizeMedia——落盘前把 msgs 里所有 data:image（≥1KB）载荷（sticker/image 的 text、parts 图片、quote 图）批量 tokenize 换 @@m:hash（内容寻址去重同一张图全站一份），渲染由 media-pool 文档级观察器自动解图（群聊零渲染改动）、池 GC 引用扫描已含群聊键（#186）；②gcWriteMsgs 落盘顺序=先 normalize→池 flush→再写消息（#142 同款顺序保证）；③调度器兼容异步 writer，saveNow（离页/切群）走 quick=true 跳过令牌化直接同步落盘防 unload 窗口丢写（池有独立 hidden flush 兜底）；④gcPlayVoice 加 mochiMediaExpand 兜底（池 tokenize 现仅收 data:image，语音保持内联，为日后入池预留）；⑤撤回快照防臃肿——媒体类记录/超 20KB 快照走 gcRetractFallbackHtml 占位（#244 needle 随本批同步更新为带长度守卫版） | 哨兵 2 条（build.mjs）：tokenize 批量应用锚+mediaish 快照守卫锚（js/group-chat.js 内唯一）。验证：node --check 过；--check-sentinels 全绿；群聊家族脚本+布局 verify 过。【真机:待验证】（任意机型）：群聊发几张表情/图片后 group-chat-msgs 键体积不再随图片数线性膨胀；重进群聊表情/图片正常显示（令牌解图） |
| 248 | 自查发现（2026-09-07，同上）：群聊历史只进不出——进群只渲最近 RENDER_MAX=200 条，DOM 窗口 400/320 只裁不加，「查看更早」入口不存在，更早的消息存了但界面永远看不到（单聊有滚动加载，群聊漏对齐） | 根因（group-chat.js renderAll）：渲染窗口起点固定 max(0, n−200) 后无任何向上续载机制 | 修复（group-chat.js+group-chat.css）：①新增 gcRenderStart 渲染窗口起点，renderAll 时列表顶部挂「查看更早的消息（还有 N 条）」入口；②点击按 GC_EARLIER_CHUNK=150 条向顶部补渲（renderMsg 新增 beforeEl 锚点前插、gcPlaceMsg 统一放置，拍一拍/系统消息同样支持），按 scrollHeight 差值回补 scrollTop 视口不跳；③加载完自动更新剩余条数/到顶移除入口；④DOM 窗口裁剪跳过分页按钮不误删 | 哨兵 2 条（build.mjs）：渲染窗口起点锚+滚动位置保持锚（js/group-chat.js 内唯一）。验证：node --check 过；--check-sentinels 全绿；群聊家族脚本+布局 verify 过。【真机:待验证】（任意机型，老群聊）：进群聊点顶部「查看更早的消息」——历史按块补出、视口不跳、翻到顶按钮消失；底部收发消息贴底跟随不受影响 |

| 249 | 收口重构（2026-09-07，非新 bug：设备判定收口第二批，防「同一事实多出处修一半」）：UA/平台特判散落 4 文件各写一套（chat.js 语音 WebView 18 壳大正则、data-backup.js huawei\|quark 分享黑名单、music-player.js 两处 QQ\|Quark 拦截提示、bg-keep.js miui\|xiaomi 通知提示）+ iOS 判定复刻两份（bg-keep.js kaIsIOS 自拼正则+iPadOS 伪装分支、idb.js armFgIdbReset 复刻 touchMac 检测——v3.16.x 收口漏网角落，device.js 判定规则升级时这些复制品会被漏掉） | 根因：mochiDevice 自 v3.16.x 就是唯一判定源，但能力型判定（WebView/分享黑名单等）无处安放，各文件只能自己嗅探 | 修复（device.js+4 消费端，逻辑零变化）：①device.js mochiDevice 新增 env 能力层——isAndroidWebView（空 UA 保守 true 原语义）/brokenFileShare/apiBlockedHint/notifyQuirk 四布尔标记，UA 嗅探唯一处，以后加新壳特征只改本文件；②chat.js isAndroidWebView/data-backup.js brokenFileShare/music-player.js 两处提示/bg-keep.js 通知提示全部改读 env 对应字段；③bg-keep kaIsIOS 改薄壳读 mochiDevice.isIOS（函数名保留=#207 哨兵与 verify-keep-audio 抽取契约不变）；④idb armFgIdbReset gate 改读 mochiDevice.isIOS（device.js isIOS 本身含 #144 Macintosh 伪装分支，行为语义等价：伪装 iPad 仍回前台重建 IDB；#144 哨兵 needle 同步为消费表达式） | 哨兵 4 条新增（build.mjs：device env 挂载锚/chat 消费锚/data-backup 消费锚/bg-keep 薄壳锚）+#144 needle 同步 1 条。验证：tools/verify-device-env.mjs **27/27**（A 段 device.js 真实 env 表达式 13 场景求值：微信/三星→WebView、标准 Chrome→否、华为/夸克→brokenFileShare、QQ→apiBlockedHint 且 brokenFileShare 不误扩、iOS→notifyQuirk 否、空 UA 保守 true；B 段消费端收口+旧正则清零 9 条；C 段 kaIsIOS 薄壳行为 4 条）；verify-keep-audio **18/18**（抽取壳适配 window.mochiDevice 桩，iOS 220Hz/0.002 与安卓 18kHz/0.006 行为断言不回归）；verify-fullscreen-ipad **25/25**（#144 isIOS 五场景行为零变化）；--check-sentinels 533 全绿哑 0。【真机:待验证】（等价性重构，重点回归四条既有链路）：①标准安卓 Chrome 语音录制（应走 webm/opus 无爆音）与任意 WebView 内录制（应有声可播）；②华为/夸克导出备份仍直接走「确定后下载」不卡分享面板；③小米/红米后台通知测试提示文案仍在；④iPad 伪装 UA 场景 #144 原清单复测 |
| 250 | 用户报障「切换桌面联系人网站会卡死」（2026-09-07，prof-contact-switch.mjs 实测归因：切换一次同步扇出 79 个 contact-switched 监听器 ≈75ms 主线程，真机叠加 MB 级图片重新解码=卡死体感） | 根因三处冗余重活叠加：①chat.js 切换监听对全局键 my-emoji-groups（#172 实例 34.93MB）无条件 myEmojiLoad()+reloadMyEmojiFromIdb()——切桌面既不改变该键也不清 myGroups，每次切换整包 JSON.parse×2+35MB 级 idbGet 事务；②group-chat.js 切换监听无条件 renderAll() 整窗重渲最近 200 条——群聊数据全局共用、切换时群聊页必是隐藏态（setActiveContact 强制回主页），纯浪费；③personalize.js applyAllCardBgs/applyAllWidgetTexts/applyAllWidgetOpacities 三个独立切换监听器与综合监听器 refreshDeskVisuals() 重复执行同一批卡片背景重应用，且 applyCardBg/applyPageBgs/restoreAppIcons 对 MB 级 dataURL 无条件重赋值（赋同值=浏览器作废已解码位图重新解码） | ①切换监听只保留 loadEmojiPref（按桌面偏好落位）+面板开着才重绘，myGroups 模块态与桌面无关不再重读；②切换监听按可见性分流：pageVisible 才 updateGroupName+renderAll+面板刷新，隐藏态置 gcSwitchDirty 挂起、enterGroupChat 全量重建时清账（成员名随联系人改名由进群重建保证，页面隐藏时面板刷新一并跳过）；③删三个独立监听器（语义全保留在综合监听器一处）+三处赋值恒等跳过（值变才写，壁纸层 #147/#151 同款既有语义） | 哨兵 3 条（build.mjs：chat 全局键守卫锚/gc pageVisible 锚/pz applyCardBg 恒等锚）；tools/verify-contact-switch-perf.mjs **9/9**（静态 J1-J5+行为 B1 隐藏态连切 3 次群聊 DOM 零重渲/B3 桌面功能在位；stash 红绿对照 HEAD 3 通过 6 失败）；prof-contact-switch.mjs 复测同步中位 **74.8ms→43.3ms（-42%）、长任务 ~100ms→~55ms**；--check-sentinels 536 哑 0。【真机:待验证】（任意机型，重度表情库/多群聊设备优先）：连续切换联系人 5~10 次无卡死/转圈；切换后桌面壁纸、卡片背景、图标、群聊消息、表情包面板均正常 |
| 252 | 用户报障「有的手机选择，深色模式里的浅色，颜色依旧不对」（2026-09-07；#107 同症状复发：系统深色的安卓机上 app 选浅色仍被压黑/颜色不对） | 两条根因叠加：①base.css :root 只写了 `color-scheme:light`——裸 light 只是「支持浅色」的偏好声明，**不是** Chrome Auto Dark 的退出开关；安卓 Chromium/WebView（含 Edge/各壳）在系统深色下仍会把网页配色整体压黑（「颜色依旧不对」且换啥色都偏黑、仅部分机型=UA 相关）。文档退出开关必须带 only 关键字=「网页自述完全不支持深色，禁止 UA 代客压黑」；②template.html 头部启动脚本只认 `theme-mode==='dark'`——三档制的 auto 档（跟随系统）用户在系统深色下，首帧到 personalize.js 执行前属性缺失=开屏白闪（FOUC）；且浅色档依赖「属性本来就不在」，历史残留的 data-theme 属性无法被纠正 | ①base.css `color-scheme:light`→`color-scheme:only light`（[data-theme=dark] 处保持裸 `dark` 覆盖，原生控件仍随深色）；②template.html 头部脚本升级三档语义：dark 或（非 light 且系统 prefers-dark）→ 挂属性，否则 removeAttribute（浅色档强制清残留）；③personalize.js applyThemeMode 同款显式 removeAttribute 双保险（原实现已支持，加注释固化语义） | 哨兵 3 条（build.mjs）：#107 needle 同步为 `color-scheme:only light` + template 头部三档脚本锚 + personalize applyThemeMode 显式 remove 锚（按产物压缩后无缩进形态）。验证：node --check 过；构建哨兵 **540/540 全绿哑 0**；tools/verify-dark-mode.mjs **29/29**（D 段适配三档真实用户路径：行点击开弹窗→点「浅色」胶囊→D5 观测到 `light only`）+npm run verify 布局 **10/10**+verify-device-env **27/27**+verify-contact-switch-perf **9/9**。【真机:待验证】（报障机型优先，任意系统深色的安卓机）：系统深色+app 选「浅色」→界面真回浅色、气泡/文字颜色正常不再发黑；「跟随系统」档开屏不再白闪；app 内切「深色」仍正常；浅色下群聊换色不再被压黑（#107 清单复测） | <br /> | <br />
| 253 | 华为 Pro70（ADY-AL00，Android 12）+ Edge 151（2026-09-07 诊断报障 v3.26.521 ts=1788784296244，用户明说其他设备型号也有）：「公用字卡和专享字卡无法导入 milk 网站的字卡库 json」；诊断「启动文件异常」三条 `[字卡导入] applyImportData 异常 fromPubFallback is not defined`（reply-library-replies 7KB / reply-groups-3+动作 16KB，均为 milk 导出格式） | 根因（chatcard.js applyImportData，#139 引入的作用域缺陷，**机型无关**）：`const bag = {}` 与 `let fromPubFallback = false` 声明在「全量备份提取」分支块（ls/idb 备份格式识别）**内部**，而函数尾部的 #139 防复制守卫 `if (fromPubFallback && ccScope === 'own')` 和 `bag[PUB_PREFIX + ':' + PUB_KEY]` 在函数作用域读这两个名字——块级作用域不可见 ⇒ **任何格式**（milk/星言/本应用导出/全量备份）只要成功解析出字卡、走到尾部守卫必抛 ReferenceError，saveGroups 永不执行（#171 的 catch 把它提示成「导入处理失败」+ 写入诊断，崩溃发生在写入前=库不受损）。#139 落地以来所有导入全被这一行拦死（milk 是受害者之一，#171 修好的解析分流本身无恙）；哨兵只锚守卫文本在位、#171 verify 的 apply 被 mock，均证不出作用域错误，与 #224（dcfP 作用域）同族第二例 | 修复（chatcard.js 两行声明提升，零语义/零设备分支）：`bag`/`fromPubFallback` 声明提升到 applyImportData 函数作用域（非备份格式 bag 恒空对象、标记恒 false，守卫语义不变）；备份分支内 `const bag = {}` 改赋值、块内重复声明删除。不触碰 #171/#182 的解析自救链与设备兼容逻辑，不新增任何机型分支 | 哨兵 2 条（build.mjs）：提升声明两行（逻辑锚=声明表达式，挪回分支块内即消失）。行为断言 tools/verify-cc-import-apply.mjs（抽真实 applyImportData+writeImport 源码桩环境端到端 **21/21**：milk 真实导出结构 merge 7 张不抛错落位/专属页本应用格式导入/#139 守卫「合并=公用库」跳过写入保持/备份专属键提取+（全量备份提取）标签/非法媒体白名单/空数据不写盘/replace 整库替换全键归位；红绿对照 HEAD 全红 ReferenceError 恰为修复点）。验证：node --check 过；--check-sentinels **542/542 哑 0**。【真机:待验证】（华为Pro70+Edge 及任意机型）：milk 导出 json 在 公用字卡/专享字卡 两入口按 追加/导入到当前分类/替换 三模式导入均成功 toast，字卡落位对应分类；全量备份 json 经字卡入口导入仍走「（全量备份提取）」且防复制守卫不回归 |
| 254 | 用户报障「华为nova8pro 浏览器edge【音乐】里的去除vip歌曲功能失效……这个问题其他设备型号也有出现」（2026-09-07 诊断实证：音乐页点【清理会员歌曲】后 proxy.cors.sh 三连「网络错误/断网」） | 根因（music-player.js fetchNeteaseFees，v3.14.x 引入，机型无关）：「去除VIP歌曲/清理会员歌曲」依赖两个第三方公共 CORS 代理查网易云官方 fee 字段——实测 proxy.cors.sh **域名 DNS 已注销**（ENOTFOUND，请求根本发不出去）、api.allorigins.win 返回 522、corsproxy.io 401 更早已删。公共代理是持续死掉的消耗品，歌单导入（fetchV6Durations/fetchNeteasePlaylist）与单曲识别（fetchNeteaseInfo）同链路同病：任何机型点击必走「检测失败：网易云查询服务暂不可用」，功能整体失效 | 修复（music-player.js，消除独立死点）：fetchNeteaseFees 整体重写为 **meting 播放同源通道逐首探测可播放性**（与播放/时长探测同一依赖面：meting 活着播放才活着，本功能跟着活）——免费歌 meting ?type=url 302→网易 CDN（r.redirected）或响应本身 audio/*；VIP/失效歌返回 200+空正文 text/html 且无跳转（resolveNeteaseDirectUrl 同判据）；判不可播记 fee=1 沿用既有过滤语义；**探测失败（网络断/超时）不计账，绝不把「没探到」当「免费」误删**；并发 8 条 worker pool 防 ERR_INSUFFICIENT_RESOURCES，30s 全局兜底收口；openVipClean 加 vipCleanBusy 防重入（大库探测数十秒防连点叠加）；面板文案同步改为「经播放探测不可用（会员/付费或链接已失效）」。响应头即判定+异步 abort/cancel body（防 BodyStreamBuffer was aborted 刷诊断错误环，同 resolveNeteaseDirectUrl 口径） | 哨兵 1 条（build.mjs）：`playable ? 0 : 1` 记账判据锚（music-player.js 内唯一）。行为断言 tools/verify-music-vip-clean.mjs **8/8**（T1-T3 合并/复活既有用例不动；T4/T5 桩改造为新 meting 通道；新增 T7 探测全失败不弹面板不误删、T8 部分探不到只移除判死歌曲）。验证：node --check 过；--check-sentinels 543 全绿哑 0。【真机:待验证】（华为 nova8 Pro/Edge 及任意机型）：音乐页设置内点【清理会员歌曲】→ toast「正在探测 N 首…」→ 数秒后弹出/提示「未发现会员/付费歌曲」，不再报「检测失败」；库里有 VIP 歌时确认移除后列表消失 | <br /> | <br />

| 255 | 用户报障 iPhone 16 Pro / iOS 26.1 Safari（2026-09-07，明说其他设备型号也有）：①公用/专享字卡【批量导入】弹窗打字时输入框往上滑、显示不正常 ②「批量导入功能打开的页面太小了，可以放大一点」 ③随附诊断附带现行报错 ReferenceError: Can't find variable: floorPick ×6（page-room，装扮选墙纸确定即抛） | ①居中容器通病（机型无关）：键盘开合动画与输入法候选条显隐使 .phone 高度变化，.modal-mask{align-items:center} 的居中弹窗每次重新取中=输入框跟着上滑，WebKit caret 显现滚动再叠加位移；②.modal 默认宽 272px + 原生 textarea rows=3（iOS 不做 ce-box 转换，rows 决定实际高度），批量导入长文本局促；③room.js decoFlow 装扮墙纸确定后 setTimeout(floorPick,0) 而 floorPick 函数整体从未定义（地板第二步从未实现）=必抛 ReferenceError，与 #224(dcfP)/#253(fromPubFallback) 同族「引用了不存在的名字」 | ①mobile-adapt.js iOS 键盘会话（_kbActive/_iProv）给 #modal-mask 挂 modal-kb-dock 顶对齐——开启/restoreKb/推定停靠/推定清除/250ms 轮询兜底五处接线，收键盘摘除；base.css 顶对齐规则 + margin-top max(10px, env(safe-area-inset-top))；安卓/桌面不挂类零影响；②chatcard.js 批量导入弹窗走 opts.big（.modal--big 330→420px/94vw）+ textareaRows:8；③room.js 补齐 floorPick（FLOORS pills 弹窗、🔒Lv 锁级过滤与墙纸同款、写 d.floor 由 renderScene floor-<id> 消费） | 哨兵 4 条（build.mjs：floorPick 定义锚 / modal-kb-dock 开关锚 / modal-kb-dock CSS 锚 / textareaRows:8 放大锚）。行为断言 tools/verify-modal-kb-dock.mjs **13/13**（SRCDIR 红绿对照 HEAD 4/13，9 红恰为修复点）；node --check 过；--check-sentinels 543 全绿哑 0。注：②的 chatcard.js 两行放大因并行 #253 会话提交时显式 add chatcard.js 一并随 b761bb9 入库（其构建产物未含，待下次构建接入）。【真机:待验证】（iPhone16P Safari 及任意机型）：批量导入弹窗打字时输入框位置稳定不再上滑、弹窗明显变大（宽版+8 行）；小屋→装扮→选墙纸确定后正常弹出地板选择不再报错 | <br /> | <br /> |

| 256 | 小米15 Pro + Chrome（2026-09-07 用户报障，明说其他设备型号也有）：「联系人发表情包，聊天里联系人发送的表情包消息会重复一条，然后其中有一个刷新几下会消失，变回正常的一条」——即 TA 表情包气泡短暂成对出现、刷新后又被归一化删掉一张的「翻饼」 | 相邻重复判定三套口径不一致+媒体令牌化签名漂移叠加：①addRec 实时去重窗 1200ms，而多字卡回复条间隔 randInt(1200,2800)ms 整体落在窗外——表情包池小/表情包概率拉满时一批两回复抽中同款表情包＝屏上两张；②刷新归一化（collapseRapidDups/normCollapseRange）只删 ≤2500ms 且两份文本形式一致的对——#142 令牌化把 text 从 data:base64 异步改写为 @@m:令牌，跨形式对文本直比不等＝永久漏判，要等两次令牌化收敛后再一轮归一化才消（体感「刷好几下才好」） | 修复（chat.js，AI-A 域）：①新增 dupGapMs 唯一窗口源（文本 2500ms 不变；img/voice/special 字段型沿用 60000ms；text 即媒体的 sticker/image/voice 型仅收件侧扩到 60000ms——发件侧是人为重发不吞）②新增 mediaTxtEq 跨形式比对（池令牌 mochiMediaExpand 展开即原数据再比）③addRec 实时去重与两处归一化全部改走 dupGapMs/mediaTxtEq＝屏上所见即刷新后所见④dupSig 的 x 字段令牌展开归一。逻辑零设备分支，跨机型行为一致 | 哨兵 3 条（build.mjs FIX_SENTINELS，因并行会话在途占用 build.mjs 未登记，**构建者收口时必须补登**）：{ name:'chat-dup-addrec-gap', file:'chat.js', needle:'dts <= dupGapMs(rec)) { saveMsgs(); return null; }' }/{ name:'chat-dup-mediatxteq', file:'chat.js', needle:'function mediaTxtEq(a, b) {' }/{ name:'chat-dup-dupsig-expand', file:'chat.js', needle:'const ex = window.mochiMediaExpand(x); if (ex) x = ex;' }。验证：node tools/verify-sticker-dup.mjs 32/32（dupGapMs 七窗口口径/mediaTxtEq 跨形式五断言/dupSig 展开/归一化行为九场景/addRec 静态锚七条）；node --check 过；--check-sentinels 543 全绿哑 0。【真机:待验证】（小米15Pro 及任意机型）：联系人连续回复表情包不再出现同款两条；刷新后条数与刷新前一致不再「弹没」一条；自己 60s 内重发同款表情包仍正常显示两条 |
| 257 | 小米 MIX 4 + Edge 151（EdgA/151，Chromium 151，standalone，2026-09-07 诊断报障 v3.26.520 ts=1788740934736，用户明说其他设备型号也有）：「点进聊天页面里动不了，想点哪个按键都点不了，退也退不出来，什么按钮都点不了」——整页触摸无响应只能杀进程 | **死点击**（触摸事件活着、click 事件死）。用户诊断交互轨迹实锤关键签名：卡死时段（20:31:22-20:32:02 长任务 385/133/139/160ms+前后台切换窗口）用户疯狂触摸但 click 轨迹整段空白，前后（19:19 设置/20:32:20 psync 开关）点击轨迹正常。成因族（残留 scroll-lock / 键盘会话 .phone 内联残留 / 焦点被劫持 / 合成层输入管线陈旧）在用户重启后取诊断时现场已洗掉，无头空库复现不出（#148「页面突然上移什么都点不动」同族），单根因无法实锤——按仓库「证据门控、不猜机型」原则补与成因无关的逃生门+现场采集 | 修复（mobile-adapt.js + device.js，AI-B 域，零机型分支）：①**死点击逃生门**——同一位置 3 次快速轻点（1.6s 窗口/相邻<650ms/单击<350ms/位移<12px）均无任何 click 跟随（点得动的页面 click 必然同步派发）=判死点击→按证据逐层复位（残留滚动锁/.phone 键盘期内联 height/alignSelf/top[安卓，iOS 由 healViewport 自治不越界]/失焦）+ #cc-toast 播报 + LS 落现场（`xy-home-v2:__diag-stuck` 环形 3 条，跨重启可读）+ `window.__mochiStuckProbe()` 只读探针；浮层打开期（正在操作弹层）、键盘会话期（__mochiAndroidKb/__mochiIosKb kbActive/prov）、长按（气泡菜单路径）、滚动手势一律不计数，能点动的设备永不满足全部条件=零行为变化；②**诊断现场补齐**（防「报障拿不到现场」重演）：device.js 新增触摸轨迹采集（`__diag-touch` 环形 6 条，与交互轨迹并排读=触摸有 click 无即死点击实锤）+「键盘/锁残留」行补输出浮层开清单（vg.lock.open 此前收集了但从未打印）与逃生探针 + 诊断文本新增「触摸轨迹」「卡死逃生记录」两段 | 哨兵 2 条（build.mjs）：`if (_escLastClickAt >= tapEndAt)`（mobile-adapt.js 判定锚）+ `'xy-home-v2:__diag-touch'`（device.js 采集锚）。行为断言 tools/verify-stuck-escape.mjs **28/28**（S1-S10 静态接线+B1-B11 行为：健康点击不触发/死点击 3 连触发 toast+落盘/残留锁与 .phone 内联与失焦复位明细/长按滑动浮层键盘四豁免/第 3 击 click 迟到误报退出/间隔>650ms 不触发/探针字段/iOS 不越界清 .phone；R-HEAD 旧源死点击零响应=判定力证明）。验证：node --check 两文件过；--check-sentinels **545 全绿哑 0**。【真机:待验证】（小米 MIX 4/Edge 及任意机型）：复现点不动时同点快速轻点 3 次→toast「已尝试恢复（明细）」且页面恢复可点；恢复动作与现场在 设置→诊断 报告「卡死逃生记录/触摸轨迹/浮层开」三处可读；正常使用（弹层内/打字中/长按菜单/滚动）永不弹恢复提示 |
| 258 | 用户报障「深色模式 聊天打开拍一拍功能的页面，里面的拍一拍字卡全是白的，看不见文字」（2026-09-07；新内核无头复测正常=老内核专属病） | dark.css 全部深色组件覆盖（749 条规则）写成 **CSS 原生嵌套**（[data-theme="dark"] { .foo {} } 形态）。嵌套语法需 Chromium 112+ / iOS 16.5+ / Firefox 117+；老内核（夸克/华为/UC/旧 Edge/旧 WebView、iOS 16.4-）把嵌套规则**整段丢弃**：①浅色白底规则是扁平的照常生效（#poke-list .cc-item 白底 rgba(255,255,255,.92)）；②文字色继承 var(--ink) 被扁平的 [data-theme] 变量覆盖成 #f0f0f0 亮色 → **白卡白字看不见**。新内核无头验证全绿所以历次构建/回归都拦不住（verify 跑在 Chrome 新内核上）——「有的手机」类深色小毛病（桌面 tabbar 白条/各页组件回白）同源。副产品：chat-main.css 拍一拍 tab 段有一个不配对的多余 `}`（浏览器容错跳过，无行为影响，顺手清除） | dark.css 749 条规则机械展平为带完整 [data-theme="dark"] 前缀的扁平选择器（**语义等价**：特异性/级联顺序不变，garden/memo 镜像链 +1 特异性保持；变量声明块功能不变）；文件头加「禁止 CSS 原生嵌套」铁律注释；chat-main.css 删多余 `}` | 哨兵 1 条（build.mjs）：`[data-theme="dark"] #poke-list .cc-item`（css/dark.css，展平形态；回退成嵌套即断）。verify-dark-mode.mjs 扩 E1-E3 静态断言（产物含展平拍一拍规则/产物无嵌套深色块/src dark.css 括号平衡零嵌套）**32/32**；构建哨兵 550/550 哑 0；布局 verify 10/10。【真机:待验证】（老内核机型优先——夸克/华为/UC/旧 Edge/iOS≤16.4）：深色模式下拍一拍面板字卡为深底亮字；桌面 tabbar/聊天页顶栏/设置页各组件深色正常不再回白；新内核机型零变化 | <br /> | <br />
| 259 | 用户反馈（2026-09-10）：①「为什么【输暗号 990915，本机永久跳过问答】在页面直接显示出来了，没有隐藏」；②明确要求「没有输入暗号时，2 个问答题每次打开都出现，不回答无法关闭」 | 两处根因（applock.js / template.html，AI-B 域）：①**暗号明文上屏**——设置页静态说明（template.html #applock-qa-sub）+ 开关开/关两态动态文案（syncQaUi）+ 问答屏底部入口 label，共三处硬编码 990915，旁人翻到设置页即知后门＝问答门形同虚设；②**问答门共用密码锁的会话豁免**——evalLock 开头 `if (sessOk()) return` 让同一标签刷新不再问答（sessionStorage mochi-applock-ok 本是密码锁「仅打开时锁」语义），与「每次打开都出现」不符 | 修复（零机型分支）：①三处文案去掉暗号数字（常量 QA_SKIP_CODE 保留，仅不再上屏）；②evalLock 拆分豁免——needQa 分支前置且不读 sessOk＝每次页面加载（冷启动/新标签/刷新/更新 reload）必重答全部题目；密码锁仍在 sessOk 时短路（同标签刷新不重锁原行为不变），问答答对后 `needPin && !sessOk()` 才进密码屏；问答屏本就无取消入口（cancel:false / 点空白不关 / 无 ESC 与返回键处理）＝不答无法进入 | 验证：哨兵无变化（不锚文案）；tools/verify-applock.mjs I5 断言反转为「同标签刷新仍问答（1/2）」+ 新增 I5b（sess=1 仍问）**64/64**，D1 密码锁不重锁仍绿；node --check 两文件过；构建哨兵 555/555 哑 0；产物 grep「990915」仅剩常量 1 处。真机:待验证（任意机型）：设置页与问答屏不再出现 990915；未输暗号时刷新仍要重答两题、不答无法进入；输对暗号后本机永久不问；数字密码锁同标签刷新仍不重锁 | <br /> | <br />

| 260 | vivo X200s + Edge 152（EdgA/152，Chromium 152，standalone，2026-09-09 诊断 v3.26.529 ts=1788796421477，用户明说「这个问题其他设备型号也有出现」）：「浏览器后台依然保活失败，浏览器弹窗收不到消息，以前可以」——#153 已修的后台冻结在内核升级后复发 | 根因（bg-keep.js，通用根因零机型分支）：保活机制只押「正在播放音频」这一个页面冻结豁免信号，豁免成立与否取决于内核把音频判为 audible——#190 后数字电平 0.006×0.05=0.0003，距 audible 判定线仅 20% 余量；Edge 151→152 内核一收紧（按响度/频带判定）豁免即丢 → Chromium 139+ 的 1 分钟后台冻结直接生效（#153 的隐藏期补播钳制只保证「音频被认定在播时」静默窗口短，豁免本身丢了它救不回）→ 定时器全停=消息不产生+通知不弹；「以前可以」时代正是 0.02×0.05=0.001 电平（#190 之前）。底噪线历史：#190（220Hz@0.02 嗡声→降 0.006）、#207（220Hz@0.006 仍嗡→换 18kHz）——底噪根因在 220Hz 频率不在幅度 | 修复（bg-keep.js + device.js，零机型分支，#190/#207 听感修复全保留）：①**幅度 0.006→0.02 恢复 4 倍电平余量**（0.001），18kHz 频率不动——底噪根因在频率，18kHz 上线后无底噪投诉、外放高频滚降 20~40dB 下 0.02 仍物理不可闻；iOS 220Hz@0.002 bit 级不动；②**WebRTC 第二冻结豁免锚**——页内一对回环 RTCPeerConnection + 数据通道（host candidate 本机回环，无 STUN/无外发流量/无音频焦点/无声），与音频互为双锚任一失效另一个仍在；断线 30s 静默重建，start/stop/heal 三处接线；③**后台心跳冻结取证**——页面隐藏期每 30s 写 IDB（xy-home-v2:__ka-hb，计数+最近 8 拍轨迹），回前台补记 resumed；device.js 诊断新增【保活现场】行（保活/通知/音频/媒体条/WebRTC/心跳），相邻拍间隔>90s 直出「冻结证据：心跳断流Xs」——下次同类报障不再靠口述猜 | 哨兵 4 条（build.mjs）：#190 哨兵 needle 同步改 `kaIsIOS() ? 0.002 : 0.02` + 新增 `p1.createDataChannel('mochi-ka');` / `setInterval(kaHbTick, 30000);` / `window.__kaProbe = function () {`（bg-keep.js）+ `window.__kaProbe === 'function'`（device.js）。verify-keep-audio.mjs 幅度断言同步 0.02 + 新增双锚/心跳/探针锚点断言；contacts.js EXCLUDE 登记 `__ka-hb`（IDB 全局根键防 migrateLegacy）。【真机:待验证】（vivo X200s Edge 152 及任意机型）：更新构建后挂后台 10 分钟以上 → 通知栏照常弹 TA 消息；若仍失败，复制诊断里【保活现场】行的心跳断流秒数即冻结实锤（直接判定修复是否生效、别再盲改） |

| 261 | 用户报障 荣耀畅玩40 Plus + 夸克（2026-09-10 诊断 v3.26.529 ts=1788796421477，viewport 423×853、ce-box 转换生效、无相关 JS 报错）：「桌面的今日情话右边出现了【全选】的黑色按钮，退出重进后还是一样没有消失」；用户明说「这个问题其他设备型号也有出现」并要求「不要覆盖修改导致不同型号设备浏览器的bug反复出现」 | 黑色【全选】不是本项目 DOM（全站扫过：只有各批量管理条里的 .fb-btn/.ti-batch-btn，都不在桌面且不随页面 hidden 渲染），它是**浏览器原生文字选择工具条**（ActionMode 浮层），宿主＝文档选区／文本控件选区。本项目有两处造选区而没人收：①复制兜底路径（device.js copyText/sdCopy/fCopy）用隐藏 textarea `select()` 全选 → `execCommand('copy')` 后选区原地留着，节点 800ms 后才 removeChild；②桌面 home.css 虽 `user-select:none`，部分内核照样无视并自造选区。**实测修正（关键）**：Blink 移除选区宿主时不产生「孤儿选区」，而是把选区**重挂到 document.body 且仍然活着**→ 内核收不到「结束选择」通知，浮层就永久停在屏幕原位（浮层归浏览器所有，切后台/重进都不销毁，故用户「退出重进」无效） | 修复（device.js + mobile-adapt.js，AI-B 域，**零机型分支**）：①**主防线**——新增 `window.mochiKillCopySelection(ta)`（`setSelectionRange(0,0)` ＋ 仅当它是 activeElement 时 blur ＋ `getSelection().removeAllRanges()`），三处复制站点在 `execCommand('copy')` 之后、延迟 removeChild 之前**当场塌选区**；调用带 `&&` 守卫（helper 所在 IIFE 若早退不得把已成功的复制抛成「复制失败」）。②**兜底**——mobile-adapt.js 新增全局死选区回收器 `reapDeadSelection()`（活选区仅当「非编辑区 **且** 在 #page-phone 桌面内」才收，编辑区/桌面外一律不动，避免吃掉用户正当的整页全选）＋ `window.__mochiReapSelection` 只读探针，接线 pointerdown/touchstart（捕获）＋ selectionchange ＋ visibilitychange（正对「退出重进」路径）。未动既有设备防线：execCommand 优先于 clipboard API（v3.26.x 防部分 WebView 权限弹窗/整页重载）、不重新引入 `ta.focus()`（v3.27.x 防弹键盘）、不扩全站 user-select | 哨兵 3 条（build.mjs）：`ta.setSelectionRange(0, 0)`（device.js 源头塌选区）／`if (editable || !desk || !desk.contains(host)) return false;`（回收器判定范围，放宽即成新 bug）／`document.addEventListener('selectionchange', reapSoon)`（接线，删则回收器＝死代码）。行为断言 tools/verify-copy-selection.mjs **23/23**（S1-S8 静态站点全覆盖＋产物接入；A1-A4 复制点塌选区且每条先立「选区确已建立」前置双向咬合；B1-B6 回收器含 B3a 旧路径对照组＋三条「不误清」反向断言；C1 桌面禁选 CSS 未被顶掉；C2 零未捕获错误）。反向对照：`git show HEAD` 的 device.js/mobile-adapt.js 中 mochiKillCopySelection／reapDeadSelection 命中 0＝红方向成立。跨域待处理：**divination.js copyFallback** 同族残留（AI-A 域未改，verify S2 已登记为「已知跨域站点」，对方修好后自动转全绿）。【真机:待验证】（荣耀畅玩40 Plus/夸克及任意机型）：桌面任意处轻点一次、或切后台再回前台 → 黑色【全选】消失不再卡屏；设置页诊断复制与字卡/备份等任一「复制」按钮仍复制成功且不再弹出【全选】 |

| 262 | 用户报障 iPhone 17 + 苹果默认 Safari（PWA 独立模式，2026-09-10 诊断 v3.26.529 ts=1788796421477，my-emoji-groups 4.02MB／default cc-groups 39.27MB／回复字卡池 sticker 66）：「消息出现"表情包为空"提示，但字卡库中无可清理表情包」；用户明说「这个问题其他设备型号也有出现」并要求「不要覆盖修改导致不同型号设备浏览器的 bug 反复出现」 | 屏上原文是 #205 的「表情内容为空：这张字卡图本身没有画面，可长按撤回或到字卡库清理该分组」。根因不在阈值而在**判空对象**：#205 用 `canvas.drawImage(img)` + 24×24 采样 alpha 全 0 判空，而 **canvas 画动画图只画得出第一帧**；表情包 GIF 的首帧经常就是整幅全透明的清屏帧（disposal=2，微信/QQ 表情常见），且字卡库对 GIF **直存原图保留动画**（chatcard.js v3.7.x 跳过 canvas 重编码）→ 一张正在正常播放的动图被整块换成占位文案。图本身健康，所以按提示去字卡库当然「无可清理」。**无头 Chromium 与 WebKit 实测同果＝与机型/内核无关的通用误报**（iQOO／红米等共用同一批字卡库的设备一起中招） | 修复（chat.js `bindMediaFailPlaceholder`，AI-B 域，**零机型分支**，三道防线）：①**静态图门禁** `alphaCheckable(src)`——只有先证明「这是静态位图」才允许采样：仅接受 `data:image/{png,gif}` base64 且载荷 ≤96KB（真空白图压完必然极小，超阈值直接放行＝大动图零 atob 成本），GIF 数图形控制扩展 `21 F9 04` ≥2 即多帧不判，PNG 走块表见 `acTL`（APNG，规范保证在首个 IDAT 前）即不判，远程图／`@@m:` 媒体池令牌／webp·svg·未知格式／结构异常一律不判；②**二次采样**——首采全 0 后隔 350ms 独立复采仍全 0 才下结论，兜未知内核「解码未就绪画成空」的时序差；③**可恢复出口**——占位整行可点「点此恢复显示」把原 `<img>` 换回并置 `alphaChecked` 不再复判（误判也能自服），文案不再单方面把用户指向字卡库。口径同 #186/#247：**宁可不报，绝不误报**——漏判只是回到 #205 之前的空白气泡，误判却把坏图帽子扣在正常表情上并引导用户去清理根本不存在的坏分组。#205 真阳性（全透明静态 PNG／单帧空白 GIF）与 #202 加载失败、#186 令牌缺池占位全部保留 | 哨兵 5 条（build.mjs，均为逻辑锚点、chat.js 内唯一）：门禁接线 `if (!alphaCheckable(im.getAttribute('src') || '')) return;`／GIF 数帧 `if (n >= 2) return false;`／APNG `if (type === 'acTL') return false;`／尺寸上限 `if (!b64.length || b64.length > EMPTY_SNIFF_MAX_B64) return false;`／二次采样 `if (!alphaSampleEmpty(im)) return;`。行为断言 tools/verify-media-empty.mjs **chromium 24/24 + webkit 24/24**（S0-S12 纯逻辑门禁直接从 src 抽取执行；A1/A2 真空白静态 PNG·单帧 GIF 仍出占位＝#205 不失守；A3/A4 首帧透明的多帧 GIF 与 APNG 必须保留 `<img>`＝本 bug；A5 正常图不受影响；B1/B2 #202·#186 家族不回归；C1/C2 恢复出口复位且不复判）。**红绿双向对照**：回退 chat.js 到 HEAD 重新构建 → 同一脚本 **10/24**，A3/A4 如实复现用户屏上原文「表情内容为空…到字卡库清理该分组」；`--check-sentinels` 5 条 #262 锚点全部报缺＝哨兵有牙。家族回归：verify-sticker-retract 12/12、verify-media-pool 8/8、verify-sticker-dup 32/32、布局 verify 10/10。node --check 过。【真机:待验证】（iPhone 17/Safari 及任意机型）：会动的表情包不再显示「内容为空」；真正的全透明空白图仍提示且点提示可恢复显示；表情长按撤回／操作面板不受提示影响 |

| 263 | iPhone 14 Pro + Safari（PWA standalone，2026-09-10 诊断 v3.26.520 ts=1788740934736，歌单 18366934337 共 62 首）：音乐→【链接导入】粘贴网易云歌单链接「无法导入完整歌单，只能导入 10 首」；用户明说「这个问题其他设备型号也有出现」并要求「不要覆盖修改导致不同型号设备浏览器的 bug 反复出现」。诊断另见 `proxy.cors.sh/...api/v6/playlist/detail?id=18366934337&n=1000&s` 反复网络失败，以及一条 `proxy.cors.sh/https://music.163.com/song?id=18366934337`（拿歌单 ID 去请求单曲） | 根因与机型/内核/网络无关，纯上游数据面 + 取源策略（music-player.js）：① 网易 `playlist/detail` 的 `tracks` 对**用户自建歌单只给首屏 10 首**（全量在 `trackIds`/`trackCount`，官方榜单才全给），只读 `tracks` 的 meting 实例必然回 10 首；旧 `fetchNeteasePlaylist` 串行问源且**第一个非空源即收口** → 排在首位的 injahow（播放同源实例）永远以 10 首抢收，后面的全量源根本没被问到。实测同一张 18366934337：qijieya 62 / injahow 10。② `extractPlaylistId` 的分隔符类不含 `#`/`!` → 移动端「复制链接」形态 `m/playlist#!?id=xxx` 漏判，整张歌单落到 `extractNeteaseSongId` 当一首歌导入（诊断里 `song?id=<歌单ID>` 即实证，与 ①是两条独立失血路径）。③附带隐患：各列表实例返回的 `pic` 代理 URL 原样入库，#216 封面迁移链（`COVER_PROXY_RE` 只认 injahow）认不出 → 新第三方单点，实例哪天挂了这批封面一起丢 | 修复（music-player.js，**零机型分支、零 UA 判定**，verify 有 S8 反向断言盯）：① 取源策略改「**多源并发 + 取最长**」（同数取靠前者）：三个 meting 实例（qijieya 按 trackIds 补齐全量 / injahow 播放同源 / i-meto 镜像）+ 官方 v6 详情两条代理兜底**排在 meting 之后**（同源数不抢既有链路，只有 tracks 未被截断给出更长列表时才胜出，顺带自动吃到导入时 `fee` 前置过滤与 `dt` 时长）；② 「曲目数恰为 10」＝**截断签名** `NETEASE_TRACKS_TRUNC`：见到就不收口、给慢源 1.5s 宽限，全量数凑齐即刻提前收口，8s 硬兜底＋每路 7s AbortController（旧代理死亡不再拖长导入）；③ `trackCount − 实取数 = miss`，两处导入面板 toast 如实报「数据源受限，另有 N 首未取到，稍后重导同一链接可补齐」（去重只跳已有 ⇒ 重导真能补齐）；④ 歌单正则分隔符类补 `# ! & \s`（不含 `=`，`playlist=123` 仍不当歌单＝旧行为保持）；⑤ `canonicalMetingPicUrl` 把各实例 `pic` 归一到 injahow 图片代理接回 #216 迁移链，播放直链一律 `neteaseMetingUrl`。既有资产全部未动：meting 播放链、#254 可播放性判据、#216 封面迁移、#157 窗口化时长探测、`fee` 过滤语义 | 哨兵 6 条（build.mjs，均 js/music-player.js 内唯一逻辑锚）：取最长比较 `r.list.length > best.list.length`／截断签名宽限 `if (n !== NETEASE_TRACKS_TRUNC && !graceTimer) graceTimer = setTimeout(finish, 1500);`／全量实例接入 `https://api.qijieya.cn/meting/?server=netease&type=playlist&id=`／缺口记账 `const miss = Math.max(0, (totalKnown \|\| 0) - tracks.length);`／歌单正则 `line.match(/playlist[\/?#&!\s]*(?:id=)?(\d+)/i)`／封面归一 `cover: canonicalMetingPicUrl(t.pic),`。行为断言 tools/verify-music-playlist.mjs **23/23**（S1-S9 静态接线含 S8「函数体内零 UA/机型判定」反向断言；P1-P14 走真实「链接添加」UI 路径端到端：62 首歌单全量入库、全量源死亡时截断源仍 best-effort、两源各 10 不误伤、官方榜 200 首快速收口、直链/封面归一形态、移动端分享链、官方兜底 fee 过滤+时长补全、缺口如实提示、重导补齐 52 且零重复、单曲/mp3/纯数字反向不误判、全源失败无未捕获异常、混合批 63）。**红绿对照**：`SRCDIR=<HEAD src 快照>` 同脚本 **9/23**——P1 直出用户屏上原文「导入 10 首」、P7「链接音乐已添加」1 首、P5/P6 单实例一挂即整单失败。家族回归：verify-music-vip-filter 6/6（其桩同步放宽为 host 无关，保证零真网络）、vip-clean 8/8、dur-cover 9/9、cover-direct 17/17、history-split 14/14、filter 15/15。node --check 过；`--check-sentinels` 573 全绿哑 0。【真机:待验证】（iPhone 14 Pro/Safari 及任意机型）：贴 62 首歌单链接 → toast「已导入 1 个歌单 / 62 首」；手机端「复制链接」的 `m/playlist#!?id=` 链接同样整单导入（不再只进 1 首）；取不全时如实提示「另有 N 首未取到」且稍后重导同一链接能补齐不重复；官方榜单/带 VIP 歌单导入行为不变 |
| 264 | 用户报障 iPhone 12 Pro + 自带 Safari（诊断 v3.26.529）：多角色、【联系人跨桌面查岗】与【联系人跨桌面打电话】一直开启、频率=标准，「最近一星期多了一次都没有过」，并说明其他设备型号同现、要求「不要覆盖修改导致不同型号设备浏览器的 bug 反复出现」。根因（无头复现 + HEAD 红绿对照实证，与机型/内核无关，故零 UA 分支）：跨桌面请求队列存 localStorage 而前台弹窗只活在投出它的那个页面会话里——刷新 / iOS 内存回收重载 / 返回键（tabs.js popstate）/ 被别的 openModal 顶掉 / #257 逃生门复位，任何一条不走弹窗自身回调的路径都会留下永不应答的 `status:'pending'`，`hasPending(cid)` 从此挡死该联系人的一切跨桌面触发（查岗＋来电＋求聊天全灭）；一周下来每个非激活桌面各留一个孤儿＝用户视角「一次都没有」。放大器：只在页面活着且前台时掷骰（iOS 后台冻结定时器）、首查延迟 30~90s 让「开一下就走」的短会话一次都掷不到、多联系人同轮投进全站唯一的 `#modal-mask` 互相顶掉（自造孤儿）。 | 全部改在通用调度层（`incoming-requests.js` + `device.js` 诊断），零机型/UA 分支；概率与冷却口径、后台系统通知路径（#159/#204）、两段式「确认」弹窗（v3.20.x）、`avFixed` 头像权威零改动：① 投递写 `sid` 会话归属 + 跨会话孤儿自愈（`PENDING_TTL_MS`=10min，`queue()` 把超龄 foreign pending 转 seen 并留释放日志）；② `liveModals` + `reconcileLiveModals()` 每轮对账「遮罩在且标题仍是当初投出的那个」，弹窗被顶掉/关闭当场释放、不必干等 TTL；③ 三态闸门 `hardLocked`（#applock-mask，#259 冷启动默认开，投进去只会压在锁底下）/`typingBusy`（抢焦点会吞 IME 组合中的字）/`layerBusy`（`modal-mask`+`tc-mask`+`qa-mask`+`call-mask`），拒绝即不入队、不写冷却，下一轮照样有机会；④ 让路上限 `BUSY_ESCAPE`=3 轮后动用「一次性逃逸额度」照投、投成功即收回（防软互斥变成新的永不触发，也防逃逸轮同轮投 2 个弹窗再造孤儿）；⑤ 首查 30~90s→12s + 回前台 3s 补掷（visibilitychange，iOS 冻结后切回来不必白等一整分钟）；⑥ 每轮至多一个弹窗（`deliver` 返回布尔）；⑦ 诊断信息新增「跨桌面来消息体检」行：轮询次数/闸门/前后台/档位概率冷却/各联系人下次可掷/近期释放，下次报障有第一手现场。 | 哨兵 10 条（build.mjs，均为登记文件内唯一的逻辑锚、压缩后仍在产物）：孤儿自愈判据 `x.sid !== SESSION_ID && now - (x.ts \|\| 0) > PENDING_TTL_MS`／归属标记 `req.sid = SESSION_ID;`／对账比对 `titleEl.textContent === liveModals[cid]`／互斥覆盖面 `'modal-mask', 'tc-mask', 'qa-mask', 'call-mask'`／锁屏+打字硬挡／`force` 逃生口／让路上限照投 `busyTicks = 0; escape = true;`／逃逸额度一次性消费／首查 `setTimeout(startIncomingTick, 12000)`／诊断出口 `ip.ticks + ' 次 闸门=' + ip.gate`。行为断言 `tools/verify-desk-incoming.mjs` **39/39**（T1 自动投递＋同轮单弹窗＋未投者不烧冷却＋sid 落库；T2 两天前孤儿自愈＋新 foreign pending 不误清；T3 被别的弹窗顶掉当场对账释放、live 归零；T4 浮层占屏先让路≤3 轮到上限照投且让路期不消耗冷却；T5 打字期挡（含「焦点确已建立」前置）；T6 锁屏期挡、解锁后恢复且只 1 条 pending；T7/T8 手动触发→「稍后＋确认」释放后可重投、pending 期不重复；T10 静态接线含「零 userAgent/iPhone/iPad 判定」反向断言；T11 零未捕获异常）。**红绿双向**：`SRCDIR=<HEAD src 快照>` 同脚本 **11/39**，红项恰为本批逐条行为。node --check 三文件过；`--check-sentinels` 583 全绿哑 0。⚠️ 产物落后：17:03 那次构建含本批前 9 条锚，缺「逃逸额度一次性消费」（src 17:04 才补）→ 收口必须先重新构建再提交，`PRODUCT=1 node tools/verify-desk-incoming.mjs` 可验产物。【真机:待验证】（iPhone 12 Pro/Safari 及任意机型）：多桌面常开、频率=标准下 1~2 天内应能遇到跨桌面查岗/来电；刷新或切走再回来后仍会正常触发（不再被旧 pending 永久卡死）；诊断信息「跨桌面来消息体检」行可读轮询次数/闸门/待释放。 |
| 265 | 用户报障 小米13 + Edge（2026-09-10）：装修模式里手动拖动图标改桌面布局，退出浏览器再打开就还原初始布局；并说明「这个问题其他设备型号也有出现」、要求「不要覆盖修改导致不同型号设备浏览器的 bug 反复出现」。根因（无头 CDP 多实例复现 + TEMP 隔离副本红绿对照实证，与机型/内核/UA 无关，故零机型分支）：`personalize.js` 启动期的「自定义图标大键 IDB 补读块」对当前命名空间下所有 `app-icon-*` 键（含 `app-icon-order-*`／`app-icon-opacity-*` 这类本该在 LS 里的小键）无条件 `store.set` 回写——而 `idbSet` 是异步 fire-and-forget，Edge/真我/荣耀/小米 在杀进程或事务挂起时会丢掉这一笔写，IDB 落后于 localStorage（正是 `idb.js` retainValue 与 #40/#226/#229 写自愈所依据的同款判据），于是启动补读把「陈旧 IDB 值」反向写进内存缓存 + localStorage，覆盖用户刚保存的新顺序。本次会话 DOM 早已按新值排好（用户当场看不出异常），下一次启动读到的就是被覆盖的旧值 = 「退出浏览器后还原初始布局」。相邻两缺口同族：图标顺序只在脚本加载期应用一次，`mochi-restore-done` 之后不重放（LS 副本缺失/配额满的设备整会话停在默认排布）；`contact-switched` 重应用清单有 `restoreAppIcons`（图标图片）却没有 `restoreAppIconOrder`（顺序）→ 切桌面顺序互相串。 | 三条根因全部改在 `personalize.js`，零机型/UA 分支；#140/#151 小组件卡片（`desk-layout`）重放链、#40/#226/#229 写自愈（`__wr-journal`／`__wr-j:<key>`／`mochi-wrj-heal`）、`idb.js` 存储层与备份导入链路零改动：① R1 补读只填「现在读不到」的键（`store.get(rel) !== null` 即 return），把「LS 比 IDB 新鲜」这条项目自家优先级重新压回补读路径；大图键超 200KB 不进 LS 的正常补读场景不受影响（T4 反向对照专防此处被过度收窄）。② 补读前缀只取一次 `activePrefix()`（filter 与 slice 共用同一个 `iconPfx`）——此前两次调用之间 #88/#90 的异步 `correctCidFromIdb` 可能改了 cid，前缀与截取长度对不上会把别的桌面的键搬进当前桌面（与 #151 同族串桌面）。③ R2 `mochi-restore-done` 重绘清单补 `restoreAppIconOrder`（重排幂等、节点移动不重建，多跑一次无副作用）。④ R3 `contact-switched` 补 `restoreAppIconOrder`，与 `applyHiddenIcons` 成对。未改动 `restoreAppIconOrder` 自身实现；曾评估给 `buildDeskPages` 的「布局收缩」分支加持久化守卫，实测该分支启动期不可达，按最小改动原则不加。 | 哨兵 5 条（build.mjs，均为登记文件内唯一的逻辑锚、非名字）：只填空键守卫 `if (store.get(rel) !== null) return;`／前缀单次取值 `const iconPfx = window.activePrefix() + ':';`／restore-done 重排 `try { restoreAppIconOrder(); } catch (e) {}`／切桌面重排 `document.addEventListener('contact-switched', restoreAppIconOrder);`／补读收尾同调两个 restore `restoreAppIcons(); restoreAppIconOrder(); }`。行为断言 `tools/verify-desk-icon-order.mjs`（已纳入 `npm run verify:all` 自动发现清单）**15/15**：T1 跨重启覆盖链（boot1 落 IDB=OLD→注入持久化 IDB 丢写并改 NEW→boot2 屏上新顺序但存储被陈旧 IDB 回写→boot3 还原，即用户症状）／T2 LS 恒抛 QuotaExceededError 且写自愈 journal 也被遮掉的设备，回填后按 IDB 权威值重排／T3 切桌面顺序跟随／T4 只在 IDB 的 250KB 大图标键仍能补回渲染（不误伤原意图）。**红绿三向**：① `SERVE_DIR=<HEAD 源构建的隔离副本>` 跑同脚本 **9 通过 6 失败**，红项恰为 T1-2/T1-3/T1-4/T2-3/T3-1/T3-2 六条本批行为；② **定点摘锚**＝在已构建好的修复产物上把 R1/R2/R3 三处锚逐条删掉复跑，仍 **9 通过 6 失败**（红项 T1-1/T1-2/T1-3/T1-4＋T3-1/T3-2）＝单锚失效当场报红，不靠整批回退；③ 摘锚副本 T4 三条仍绿＝反向对照未把「大图标键只存 IDB 也能补回」的原意图一起弄坏；首版 T1 曾因页内 stub 随导航消失而被写自愈机制治好＝假绿，已改成 LS 布防的持久化注入（脚本注释留档）。相邻回归 `verify-desk-persist`／`verify-desk-incoming` 等同族脚本绿（两条既有红项经 HEAD 与仓库产物复跑字节一致，归属其 owner，非本批引入）。`node --check` 过；`node build.mjs --check-sentinels` 588 条全绿、哑哨兵 0。⚠️ 本批未构建（并行会话声明构建者且有在飞 src），收口须构建后 `node tools/verify-desk-icon-order.mjs` 复验产物。【真机:待验证】小米13/Edge 与其他安卓内核：装修模式拖动图标→彻底杀掉浏览器→重开，顺序保持；备份导入后当场重排不必等重启；切换联系人时桌面顺序各自跟随。 |
| 266 | 用户报障「导入了好多次字卡都是过一段时间就没了，刷新就消失字卡」（iPhone iOS 13 基础 + Chrome/Edge 双软件，用户明说其他设备型号也有、要求「不要覆盖修改导致不同型号设备浏览器的 bug 反复出现」）：字卡库导入/编辑后过段时间、刷新页面就消失 | 根因（chatcard.js，f143621 引入的**死代码**，与机型/内核无关，故零机型分支）：字卡库列表页（page-chatcard）显示时的「重算角标 + 大键挂起按需取回」兜底是一段 IIFE，2026-08-30 收口提交 f143621 把结尾 })(); 误改成 }); ——语法完全合法、node --check 过、原有哨兵查的「文本锚点还在」也过，但整段变成永不执行的死代码：iOS 启动回填被打断（挂后台杀 IDB 连接/大库超预算被挂起 __xyIdbDeferredKeys）后，内存与 localStorage 两路读空、只剩 IndexedDB 有权威数据，此时唯一会在用户查看时点把库拉回来的防线（hidden 观察者 → hydrateLibScopes(['public','own'])）就从此断掉＝字卡库读出空＝「过一段时间没了、刷新就消失」。与 #262/#265 同族「名字在、逻辑没了」，只靠行为断言/逻辑锚拦得住。修复（chatcard.js 恢复 })(); ，零语义/零设备分支）：把该 IIFE 结尾从 }); 改回 })(); 使其立即调用，观察者随之生效；另手工核对同类隐患（findDeadIifes 横扫 src 全量 62 个 JS，全绿）。其余取回链/hydrateLibScopes/refreshLibCounts 行为零改动 | 哨兵 1 条（build.mjs FIX_SENTINELS，js/chatcard.js 内唯一逻辑锚、含 })(); 硬调用括号、压缩后仍在产物）：observe(libPage, { attributes: true, attributeFilter: ['hidden'] }); + } + })() 。验证：node --check 过；tools/verify-cc-lib-hydrate.mjs **5/5**（A3 用 matchBrace 结构配对断言 IIFE 结尾紧跟 )(，防「注释里带 })(); 假绿」）；tools/verify-cc-list-hydrate.mjs（产物行为断言：构造「两键只在 IDB、被挂起、LS 与内存读空」现场→进列表页→角标 0 恢复为 2/3+toast+移出挂起名单+内存真实填上）；--check-sentinels 全绿哑哨兵 0。【真机:待验证】（该 iPhone iOS13+Chrome/Edge 及任意机型）：导入/编辑字卡后过一段时间或刷新，字卡仍在列表中不再消失；大库设备进入字卡库列表页短暂提示「字卡较多，正在加载…」后角标与内容齐全 |
| 267 | 荣耀 X50（ALI-AN00，Android 15）+ 自带 HonorBrowser/Chrome116（2026-09-10 诊断 v3.26.529 ts=1788796421477，viewport 370×816、inner 370×664，用户明说其他设备型号也有）：①「点开键盘没有输入框」②「点开输入框会出现一部分空白」③「输入法弹窗完全遮挡输入栏这一行，无法正常打字发送」。诊断签名：键盘弹出时 visualViewport.height 不缩（664 与 254 两种读数在同一会话交替出现）、gap=274 空白、kb=1 常驻。 | 两根因与机型无关（任何「靠平移而非收缩露焦点」的安卓内核都命中，荣耀 X50 自带浏览器即代表）：R1 `syncAndroidKb` 的 `open && !_aKb` 接管时不清 `_aProv` → `_aKb` 与 `_aProv` 并存，看门狗 `if (_aKb && !_aProv)` 与 #209 清扫 `if (_aKb || _aProv) return` 全被堵死、四条复原路一条都走不到 → `.phone` 永久停在键盘期内联收缩高＝输入栏下方一整块空白（用户报②）；且该类内核收键盘后不再派 `visualViewport.resize`、焦点也丢 → 250ms 轮询停表，收缩态无人复检（诊断 kb=1/vv=664 基线/gap=274 即此态）。R2 该类内核唯一的键盘高度证据是「浏览器为露焦点平移了多少」（vv.offsetTop/scrollY），而 `_aPinPan` 每次归零前把它丢掉 → 保底停靠只能盲猜 58%：IME 实占约 62%（664→254）时停到 58%=385 后输入栏整行仍在键盘下面＝看不见打不出发不了（用户报③）；IME 偏矮时 58% 又多缩出一大片空白。修复（mobile-adapt.js + device.js，AI-B 域，零机型/UA 分支，X5/旧夸克这些纯悬浮又平移的内核零行为变化）：① `_aPinPan` 在 `_aKb`/`_aProv` 均未接管时把平移量记进 `_aPanSeen`（取最大值、focusin 重置、带时间戳）；② `_aProvDock` 有新鲜实测（≥80px 且 1.5s 内）就按 `base−实测平移` 停靠，把输入栏正好压在键盘上沿之上，无实测仍走 58% 保底；③ `syncAndroidKb` 接管时补 `_aProvClear()` 防 R1 并存；④ 1s 看门狗补两条卡死自愈——视口侧（vv+inner 双回基准且活焦点不在文本框立即复原）＋焦点侧（软键盘必依附焦点：kb/prov 任一在顶 + 活焦点不在文本框 + 静默 >2.2s + vv 读数已稳 → 复原并按需置 #236 残留闩，防 254↔664 抖动把 `.phone` 来回抽）；⑤ device.js 诊断新增「本键盘会话实测平移」「焦点框被挡」行 + 错误现场 `i=/p=/cov=`，下次陌生内核报障一次粘贴即可分案。 | 哨兵 8 条（build.mjs，均为登记文件内唯一逻辑锚、压缩后仍在产物）：实测平移记档 `if (_panPx > 8) {`／停靠实测门槛 `_aPanSeen >= 80 && Date.now() - _aPanSeenAt < 1500`／接管即清推顶 `kbDockPanels(); _aProvClear(); }`／视口侧自愈 `if (_vN > 0 && _vN >= _aH - 12 && _iN >= _aIH - 12 && !_aIsText(document.activeElement)) {`／焦点侧自愈 `if ((_aKb \|\| _aProv) && !_aIsText(document.activeElement) && Date.now() - _aLastAct > 2200 && Date.now() - _aVvChgAt > 1200) {`／探针导出 `panSeen: Math.round(_aPanSeen)`（mobile-adapt.js）+ 诊断 `out.focusCovered = ar.bottom > (vv.offsetTop \|\| 0) + vv.height + 2 ? 1 : 0;`（device.js）。行为断言 `tools/verify-kb-pan-dock.mjs` **42/42**（S0 逻辑锚在产物/S1 双探针；M1 平移不收缩内核按实测停靠且输入栏落在键盘上沿之上、M2 无焦点纯 vv 收缩被视口侧自愈 ≤1.8s 清掉、M3 纯悬浮不平移仍 58% 保底 panSeen=0、M4 迟到真实收缩接管且 prov 必须已清、M5 vv 滞留收缩读数 + 焦点交回被焦点侧自愈 ≤4.2s 清并闩残留读数、M5b 再触摸解除闩重新停靠不自愈锁死、M6 真在打字与读数仍称有键盘时绝不误清、M7 40px caret 微滚不入尺仍 58%）。家族回归：verify-kb-overlay-kernel 10/10、verify-ios-pwa-kbd 18/18、verify-kb-residue-heal 10/10、verify-ios-typing-flash 9/9、verify-stuck-escape 28/28、verify-copy-selection 23/23；`npm run verify` 布局 10/10；`node build.mjs` 哨兵 595/595 哑 0。收口已构建：**本次构建者＝本会话（AI-B 域）**，sw mochi-mtvepm84。【真机:待验证】（荣耀 X50/HonorBrowser 及任意安卓内核）：点输入栏弹键盘 → 输入栏整行浮到键盘上沿之上、可正常打字发送、下方无空白；收键盘后 `.phone` 贴回视口底、输入栏悬空/空白消失；再弹键盘仍正常（不被自愈闩锁死下一次会话）。 | <br /> | <br /> |
| 268 | 用户报障「分页加载，上划无法查看以前的聊天记录；搜索提交了也无法跳转到位置」（聊天会话，用户明说其他设备型号可能同现） | 根因（无头复现+HEAD 对照实证，与机型/内核无关，零 UA 分支）：聊天长历史走渲染窗口（renderStart..renderEnd，RENDER_MAX=200/WINDOW_MAX=400）。用户上翻讲旧消息时 `loadOlderIncremental` → `pruneWindowBottom` 从**下界**裁剪 `renderEnd`，于是会有「目标消息已被渲染出去」的区间；而 `jumpToMsg`（**搜索跳转**与**点击引用块跳原文**共用）只处理 `idx < renderStart`（上界外扩窗），**对落在被裁剪下界之外的 `idx >= renderEnd` 完全不处理** → `body.querySelector('.msg[data-idx=..]')` 查不到 target、函数 return false → 搜索点了不高亮不滚动、引用点了没反应。分页本身是好的：增量上翻能一路加载回最老消息（diag 实测 firstIdx≈0），用户「上划看不到历史」实为「想搜索某条老消息却跳不动」。修复（chat.js `jumpToMsg`，AI-B 域，零机型/UA 分支）：target 不在 DOM 时，除原有 `idx < renderStart` 上界外扩窗外，补 `idx >= renderEnd` **下界外扩窗**——`loadNewerIncremental(Math.min(idx + JUMP_VIEW + 1, len))` 循环分批向下展开直到 `renderEnd > idx`（目标进窗），再重新 querySelector → `scrollIntoView({block:center})` 居中 + highlight。增量展开会触发 `pruneWindowTop` 顶上裁剪，目标位于展开后窗口内即可居中定位，无副作用；原有上界外扩窗、scrollIntoView、highlight、引用快照 resolveQuoteTarget、倒序展示逻辑零改动 | 行为断言 `tools/verify-chat-pgjump.mjs`（可提交、verify-suite 自动发现）**6/6**：种 841 条真实复现——①首屏只渲染最近 ~200 条（分页生效）②持续上滑（拖到最顶）`firstIdx` 一路压回 0＝分页能加载回最老消息 ③搜索点最老区 idx=40→`hasHl:true/hlIdx:40/visible:true` ④搜索点近最新区 idx=820（下界裁剪区）→`hasHl:true/hlIdx:820/visible:true`＝下界外扩生效。家族回归：verify-quote-jump（引用跳转复用 jumpToMsg）不回归；node --check chat.js 过；`--check-sentinels` 各登记锚在产物。收口已构建：**本次构建者＝本会话（AI-B 域）**，sw mochi-mtvf13m1。【真机:待验证】（任意机型）：聊天历史翻到很靠前，再搜一条老消息/点一个引用块 → 视口自动居中定位到目标气泡并闪烁高亮，之后可继续上下翻；长历史桌面翻动不闪烁、不卡顿 |
| 269 | 手机端【帮我决定】/【多人决定】「自定义选项」选项输入框一直跳且拉不上去（安卓 contenteditable .ce-box 随内容无限增高，把下方控件顶出屏、整列无法上划） | 根因（css/chat-main.css）：#chat-decision-body 的 dec-opts 早已限高框内滚动，但 #chat-gdecision-body 的 gd-opts 漏了同款——安卓 gd-opts 被转成 ce-box 后随输入行数无限增高，把思考时间/最多选几个/按钮顶出屏幕、整列无法上划=「一直跳且拉不上去」。修复：给 #chat-gdecision-body .dec-inp-wrap .ce-box[data-for="gd-opts"] 补同款 max-height:176px + overflow-y:auto + overscroll-behavior:contain（与 #chat-decision-body dec-opts 完全对齐） | 哨兵 `#chat-gdecision-body .dec-inp-wrap .ce-box[data-for="gd-opts"] {` + `max-height:176px;` 两段（build.mjs FIX_SENTINELS）/ node build.mjs --check-sentinels 598 全绿哑 0。【真机:待验证】（任意安卓）：多人决定→自定义选项→选项框输多行→框内滚动查看、下方控件不顶出屏、可正常上划 |
| 270 | 用户纠正定案（2026-09-10）：开屏问答门「无法被别人编辑，是固定的 2 个问题」——原 v3.31.x 实现把题目做成可增删改（设置页「编辑问答题」入口 + 管理面板） | 定案不符根因（applock.js / applock.css，实现回写）：开屏问答门的题目被设计成「可编辑题库」，违背「固定 2 个问题」的产品定案——管理面板入口在设置页正文，任何人（含被锁设备持有者之外的人）经 qaGuard 身份验证（有密码输密码、无密码输暗号 990915）即可增删改题目；即使视为「防顺手」，也给了题目被改动的通道。修复（零机型分支）：① **删除全部编辑机制**——`kind:'qalist'` 管理面板渲染、`onQal`/`qaEditItem`/`qaRerender`/`qaOpenEdit`/`qaPanel` 整套逻辑、`data-qal` 增删改按钮及点击分发、`K_QA_LIST`(applock-qalist) 常量与读写、设置页「编辑问答题」入口、`.al-qa-*` 样式全部移除；② `qaList()` 不再读任何已存储的自定义列表（旧版本遗留的 applock-qalist 数据天然失效，不迁移不删除、留在原键位对行为零影响），恒返回 `DEFAULT_QA` 固定 2 题（mj→梦角 / 知晓→是），答案仍只存 cyrb53 摘要不落明文；③ 设置页文案改为「2 道固定问答题」，锁屏问答屏入口「编辑问答题」消失；④ 验证钩子 `__applockQaTest` 只留 `getList()`（恒 2 题） | 哨兵 2 条（build.mjs FIX_SENTINELS）：`return DEFAULT_QA.map(function (it) { return { q: it.q, h: h53(String(it.a).trim()) }; });`（applock.js 固定列表逻辑锚，回改读存储即断）+ `data-qal`（`absent:true`，产物出现即编辑面板回流）。tools/verify-applock.mjs：删除 L 组（题目管理 12 断言），K5/K6 反转为「产物不含 data-qal / .al-qa-row」，新增 K7「产物含固定 2 题梦角」；node --check 过；`--check-sentinels` 600 全绿哑 0。【真机:待验证】（任意机型）：设置→应用锁→开屏问答门 无「编辑问答题」入口；每次打开仍固定先答 mj/知晓 两道题；改过的旧数据（曾编辑过题目的设备）刷新后仍回到固定 2 题 |
| 271 | 用户反馈【应用锁】→设置安全问题，设置完问题点【完成】无反应（对话框停在答案屏不关闭、看不出已保存） | 根因（applock.js askQaSetup，无头复现实证）：答案屏 onSubmit 只调 done(q,a)（内部 saveQa+toast+syncUi）、**从不隐藏遮罩**——面板一直滞留在此屏（点「完成」后再等 400ms 仍 shown:true、title 仍「设置答案」、答案仍在输入框），用户看不到已保存＝「点完成无反应」。数据其实已落库（applock-qa 已写）。修复（applock.js askQaSetup 答案屏 onSubmit，最小改动、零机型分支）：done(q,a) 后补 cur=null; buf=''; maskEl().hidden=true; 即关闭面板——两个调用方（flowQaEdit 修改安全问题 / 开启锁时引导设问答）共用此链，均期望完成后关闭。数字密码两阶段、忘密码问答重置、开屏问答门等其它路径不受影响 | 哨兵 1 条（build.mjs FIX_SENTINELS #271）：完成即关闭面板：此前只 save+toast、不隐藏遮罩，面板一直滞留在此屏，（applock.js 内唯一注释逻辑锚，删完成关闭块即红）。node --check 过；--check-sentinels 琥珀：src 锚在位、产物未重建（待构建者收口）。【真机:待验证】（任意机型）：设置→应用锁→修改安全问题→输问题→下一步→输答案→点「完成」→对话框立即关闭；重新进入能看到新问题；忘密码答对即重设 |
| 273 | 用户反馈「手机刷新总是进不去网页最新版」：缓存优先 + 后台静默刷新策略下，手动刷新先命中旧缓存 index.html（秒开）、后台拉最新 index 弱网易超时（GitHub Pages 国内 4MB+ 产物常 >3.5s），导致反复刷新仍停留在旧版 | 根因（src/js/pwa.js）：导航「缓存优先 + 后台静默刷新」，普通刷新立即拿到缓存旧版、新版需后台异步拉完下**一次**刷新才生效；弱网下后台拉取（INDEX_NETWORK_TIMEOUT=30s）仍可能超时失败→下次刷新又回退旧缓存＝用户「每次都进不去」。修复（零机型分支）：新增 `tryAutoUpgrade()`——冷加载（普通刷新/重进/standalone 启动）`pageshow(persisted=false)` 时对比 `./version.json`，云端 ts>页面 `data-build-ts` 且本会话未尝试过（`sessionStorage` 键 `xy-home-v2:auto-upgrade-session` 守卫）则直接走 `refreshNow()`（PRECACHE_NOW 预取最新 index 落盘再 reload）一跳进新版；预取异常刷新后仍回旧版时守卫已置位→退回收 `showVerBar()` 常驻更新条，绝不无限循环。周期轮询（15s/5s）与 SW `updatefound` 通道不动、只弹条不自动刷新，避免打断会话中途操作 | 哨兵 1 条（build.mjs FIX_SENTINELS #273，js/pwa.js 内唯一逻辑锚）：`tryAutoUpgrade`。node --check 过；未构建（见 WORKLOG，本次构建者待定，收口时跑 `--check-sentinels`）。【真机:待验证】（任意机型）：①云端部署新版后，手机普通刷新一次（或重进/从桌面打开）应自动进新版，不再反复刷；②弱网下预取失败的极端场景，应出现常驻更新条、点「刷新使用新版」仍可到达最新版；③同一版本不会反复自动刷新（session 守卫） |
| 273b | 用户反馈「网页顶部刷新按钮经常不出现」→进一步定位：弹条只在「线上 version.json ts > 页面 data-build-ts」时弹出，页面常驻最新版时**永不在位**（这是对的）；真正会让「刷新入口消失」的是弱网拉 version.json 失败时 catch 只 failCount++、**静默跳过不弹任何提示**＝用户整个会话看不到顶部刷新按钮 | 根因地位（src/js/pwa.js checkVersion）：`.catch(function(){ failCount++; })` 弱网失败无任何 UI 反馈；加装 `maybeNetHint()` 弱网兜底——连续失败 ≥2 次（弱网持续、非单次抖动）即复用更新条（`ver-update-bar`）显示「网络异常，未能确认最新版本」+「重试刷新」，点击复用 `refreshNow()`（PRECACHE_NOW 预取最新 index 落盘 + reload），弱网也尽量够到最新版；内存守卫（每页面加载只提示一次，不随 5s 轮询反复闪）；网络恢复且真有新版时正常 then 分支的 `showVerBar` 会覆盖本文案，不与新版本提醒打架。**注**：若用户本页已是最新版，正常无提示属正确行为，并非故障 | 哨兵 1 条（build.mjs FIX_SENTINELS ~#273 弱网兜底，js/pwa.js 唯一字符串锚，压缩后仍在产物）：`'网络异常，未能确认最新版本'`。node --check 过；`--check-sentinels` 哑哨兵 0；未构建（待构建者收口）。【真机:待验证】（任意机型）：①弱网（开飞行/切 3G 弱信号）下打开页面，顶部出现「网络异常，未能确认最新版本」+「重试刷新」，点重试能刷新；②弱网期间只提示一次不反复闪；③正常联网且页面已最新时顶部无提示（正确）；④联网且有新版时出「刷新使用新版」 |
| 274 | 用户反馈「设置了应用锁，但刷新/重新打开网站锁被关了」（安卓手机同一标签页刷新：既不弹锁屏、设置页「应用锁」开关也显示成关闭） | 根因（applock.js evalLock 的同步自愈，AI-B 域）：`if (enabled() && !pinHash()) setEn(false)`——只要本次同步读到「en=1 但读不到密码」就当异常态把锁置 0。安卓「数据主要在 IndexedDB、localStorage 仅快照」下刷新首帧 applock-pin 常还没从 IDB 回填（LS 里只有 en）、或 LS 因配额静默写失败只剩 en → 首帧自愈即误关锁＝门户大开，用户视角「刷新后锁被关了、开关也变关」 | 修复（selfHealChecked，零机型分支）：读到 en=1 但同步读不到密码时不再立即关锁，改先异步 `window.idbGet('xy-home-v2:applock-pin')` 确认——IDB 有密码就 `gSet(K_PIN, v)` 回填本机并重新 evalLock 正常锁屏；仅当 LS+IDB 双端都确认无密码才 `setEn(false)` 自愈关锁（防「忘密码锁死」初衷保留）。正常开启/双写一致场景零行为变化 | 哨兵 1 条（build.mjs FIX_SENTINELS）：`gSet(K_PIN, v); evalLock();`（js/applock.js 内唯一逻辑锚＝IDB 有密码先回填不放关锁；删则退回同步置 0=门户大开）。tools/verify-applock.mjs：新增 R1/R2（LS 缺密码、IDB 有密码→刷新仍锁屏且开关不被误关、密码可正常解锁）+ H7 静态锚；既有 A/F 回归保持不变。【真机:待验证】（安卓手机）：设好密码锁后普通刷新，应仍要求输密码、设置页开关保持开启，问题不再复现 | <br/> | <br/> |
| 277 | （编号注：本条原拟 #275，该号已被并行在途的媒体池腐蚀批占用，让位改登 #277）用户报障 iPhone 17 + 苹果自带 Safari（PWA standalone，iOS 18.7/WebKit 26.6.1，诊断 v3.26.529 ts=1788796421477，「未知 bug」随报附诊断）：错误环 9/8~9/10 反复自动采集同一签名「[屏幕适配] --mochi-ios-h、底部导航栏悬空、底部少填｜env=62 var=0 diff=62 inner=894 phone底=894 sb=62 全屏」，全部落在「回到前台」之后；用户明说其他设备型号也有、要求「不要覆盖修改导致不同型号设备的 bug 反复出现」 | 根因（mobile-adapt.js syncVvFit 的 env 探针缓存，AI-B 域，证据链：①诊断 env 探针独立缓存实测 env=62＝真实覆盖形态；②应用侧 1s 常驻自愈循环在坏态期间每秒都在跑却没修正＝自愈用的值和诊断实测值不同源；③三处 --mochi-ios-h 写点全部经 mochiViewportForm 判定器，写 894 只能来自 envTop=0 的判定入参）：env(safe-area-inset-top) 探针缓存 `_envTopCache` 只在 orientationchange 时失效——standalone 冷启动早帧（覆盖布局未稳定）探到 0 被永久缓存，稳定后实为覆盖形态 env=62；stale envTop=0 令 expBase 少算 env 段（min(956, 0+894)=894），切后台回来 inner 短报 894 时 --mochi-ios-h 写 894＝.phone 底部 62px 白带/tabbar 悬空；而 1s 自愈循环每次都用同一个中毒缓存算出同值、「确认」坏态永不自愈，直到某次 inner 回报整屏（diff=0 时中毒值恰好无害）才「假愈」，下次切后台复发——iPhone 14/15/16 Pro 等 WebKit26 standalone 同族机型同理 | 修复（syncVvFit，零机型分支，#148 探针/#179 公式/#189 迟滞/#208/#235 判定线全保留）：矛盾信号自愈——「布局视口顶部确实缺了一段（screen−inner≥20）而缓存却说没有顶部 inset（=0），或缓存值与缺口对不上（差>8）」即判定缓存中毒，节流 5s 重探一次（`_envTopCacheAt` 时间戳节流，防 1s 自愈循环频繁建探针 DOM）；重探后走既有判定器链路：covered 形态 safeTop=62 → --mochi-safe-top=62px + expBase=956 → --mochi-ios-h=956，.phone 铺满整屏、tabbar 贴底。真「已避让」形态（env 实为 0）探回同值、缓存与行为零变化；旋转失效路径不变 | 哨兵 1 条（build.mjs FIX_SENTINELS）：`(_envTopCache === 0 || Math.abs(_envTopCache - _diff0) > 8)`（js/mobile-adapt.js 内唯一逻辑锚＝矛盾判定表达式本体；删/改条件即断）。行为断言 tools/verify-env-reprobe.mjs：S 组静态接线（矛盾条件/节流/探针时刻入账/旋转失效保留/#189 迟滞写保留）+ 产物接入断言。node --check 过。【真机:待验证】（iPhone 17/Safari 及任意 iOS standalone 机型）：切后台再回前台（或锁屏再解锁）→ 屏幕底部不再出现白带/灰带、桌面 tabbar 贴底不悬空；诊断页手动跑屏幕适配诊断全 ✓；反复切前后台不再新增「[屏幕适配] 底部少填」错误环条目 | <br/> | <br/> |
| 276 | 用户反馈「群聊里联系人撤回的消息的图片显示为【图片】的文字，无法查看撤回的图片的缩略图」 | 根因（src/js/group-chat.js）：#245/#247 防臃肿批规定媒体类消息撤回不存 DOM 快照（防 base64 把令牌化省下的空间吃回去），点击查看时走 gcRetractFallbackHtml 占位——图片只剩「[图片]」文字。但图片 src 本就持久化在 rec.text / rec.parts 里（含 @@m: 令牌形态），渲染时即时重拼即可显示，无需快照。修复：新增 gcRetractMediaHtml(rec)——image/sticker/parts 含图记录从 rec.text/rec.parts 重拼 img（data: 直显、@@m: 令牌由 media-pool 文档级观察器重写），renderMsg 撤回分支按「媒体即时视图 > 存量快照 rec.orig > 占位回退」取值（存量已撤回的占位老记录也能看图）；防臃肿语义不变（快照仍不存 base64，语音仍占位） | 哨兵 1 条（build.mjs FIX_SENTINELS #276，js/group-chat.js 唯一逻辑锚）：`gcRetractMediaHtml(rec) || rec.orig || gcRetractFallbackHtml(rec)`。node --check 过；`--check-sentinels` 本条在位（device.js #210 红为并行会话在途改动，非本批）。未构建（本次构建者待定，收口时构建+跑 verify）。【真机:待验证】：群聊让联系人发图片→撤回→点撤回提示应显示图片缩略图（再点收回提示文字）；表情包/图片+文字组合同理；语音撤回仍显示[语音]占位属正确 |
| 279 | 用户报障 iQOO12 Chrome「不清楚问题的bug」（用户明说其他设备型号也有出现、要求「不要覆盖修改导致不同型号设备浏览器的 bug 反复出现」），随附设置页诊断：交互轨迹 19:39:48 用户还在应用内点 tab 切页（当时无锁屏遮挡），19:39:56 却点了开屏「进入」——两步之间页面被重载过一次；随后 19:39:57~19:40:00 开屏问答门两题重新作答一遍（textAsk 屏 ce-box→applock-txt 同步成对出现、每步点击都有响应、无死点击、卡死逃生记录为空＝输入链路与点击链路本身无恙），异常即「会话中途页面自己重载回开屏」 | 根因（pwa.js，#273 冷加载自动升级的固有缺口，机型无关故零机型分支）：tryAutoUpgrade 命中后直接 refreshNow()——reload 在 PRECACHE_NOW 预取完成那一刻无条件落地，弱网（GitHub Pages 国内 3.6MB+ 产物）预取可耗时几十秒，正好砸进用户已开始的会话（打字/切页/正答问答门），被迫回开屏+问答门重答；与 v3.5.114 撤销「SW 通道检测到新版自动刷新」（同族问题：刚进入桌面就被打断回开屏）同一设计取向，#273 通道漏了同样的防打断约束 | 修复（pwa.js，通用根因、零机型分支）：①捕获级记录本页面首次交互时刻（touchstart/pointerdown/mousedown/keydown，只记时刻不记内容）；②refreshNow(auto, autoTs) 的 doReload 落地前最后一刻复核 autoReloadAllowed()——页面 hidden 放行（回前台即新版、重载无感）、用户未交互放行（保留 #273「冷加载一跳进新版」快路径）；用户已操作且前台时放弃自动重载、退回 showVerBar(autoTs) 常驻更新条由用户自选时机；③tryAutoUpgrade(autoTs) 入口先查活动，用户已操作直接 return false 让调用方弹条；手动点「刷新使用新版」（auto≠true）不受限，预取失败的 session 守卫退回更新条逻辑不变 | 哨兵 1 条（build.mjs FIX_SENTINELS #279，js/pwa.js 内唯一逻辑锚＝守卫表达式本体）：`auto && !autoReloadAllowed()`。行为断言 tools/verify-auto-upgrade-guard.mjs：P1 全新加载零交互 allowed()=true（快路径保留）/ P2 派发真实 mousedown 后 allowed()=false / P3 重新加载后派发 touchstart（真机主路径）仍拦截 / P4 产物接线静态锚。node --check 过；--check-sentinels 全绿哑哨兵 0。【真机:待验证】（iQOO12 Chrome 及任意机型/浏览器）：①部署新版后打开旧版页面放着不动 → 仍自动进新版；②正在操作（答问答门/打字/切页）时部署新版 → 页面不再自己重开，顶部出「刷新使用新版」条，点它才升级；③后台挂起时部署新版 → 回前台已是新版 | <br /> | <br /> |
| 278 | 用户报障（华为畅享70Pro + Chrome150，诊断 v3.26.520；OPPO 等多机型同报，原话「不要覆盖修改导致不同型号设备浏览器的bug反复出现。这个问题其他设备型号也有出现」）：信息诊断错误环反复自动采集「[屏幕适配] 底部超出／底部导航栏被裁｜diff=-535 inner=1331 phone底=1332」；现场实测 vv高=1332=.phone 高、底部空隙=0＝布局本身正常铺满，页面无可见异常 | 根因（device.js 共享判定器 mochiViewportForm 的期望底边公式）：该批安卓机 Chrome 的 screen.height 报数是坏值——screen=796 比实际可视区 inner=1331 还小（物理上屏幕不可能小于视口）；旧式 min(screenH, envTop+innerH) 取到 796 当期望屏底，.phone 贴 inner 铺满（底 1332）即被判「底部超出 535px」+「底部导航栏被裁」，自动采集每次都刷错误环；执行器 vh 同源，正常机型不受影响（screen≥inner 时 min 钳制语义不变） | 修复（mochiViewportForm，零机型分支）：min 钳制加 screenH≥innerH 门——screenH<innerH 视为坏值弃用、回退 envTop+innerH；forceCover 分支同款门（同坏值类）。执行器与诊断共用判定器，两处同源生效，无任何机型硬编码 | 哨兵 1 条（build.mjs FIX_SENTINELS #278，js/device.js 唯一逻辑锚）：`Math.min((screenH >= innerH ? screenH : 0) || (envTop + innerH), envTop + innerH)`；#210 force 分支哨兵 needle 随代码演进更新为带门表达式。tools/verify-viewport-form.mjs 新增 #278 台账 fixture（screen796<inner1331 → expBase=1331、形态 plain），80/80 通过；node --check 过；--check-sentinels 608 条全绿哑 0。未构建（见 WORKLOG，待构建者收口）。【真机:待验证】（报障机型优先，任意安卓）：诊断页跑屏幕适配诊断「底部贴合/底部导航栏完整」全 ✓；错误环不再新增「底部超出 diff=-535」条目；正常机型布局无变化 | <br /> | <br /> |
| 280 | 用户报障（华为畅享20 Pro + Edge/EdgA149，诊断 v3.26.536）「刷新出现黑屏卡顿，几分钟后恢复。目前出现十多次了」，用户明说其他设备型号也有出现、要求「不要覆盖修改导致不同型号设备浏览器的bug反复出现」；诊断实证：最近错误 3 条「资源加载失败 <img> https://…/mochi/@@m:<md5>」＝媒体池令牌裸路径被浏览器当相对 URL 发真实同源网络请求 | 根因（src/pwa/sw.js fetch 兜底链 + media-pool.js 渲染解析失败路径）：令牌解析未完成/失败（安卓 Edge 内核 IDB 事务挂起族高发）时 img.src 保持 '@@m:<hash>'，SW 接管后网络优先 3.5s 超时 → catch 链最终一次【无超时】fetch(req)——GitHub Pages 国内弱网下每个挂 30s~几分钟才 404；聊天历史里每张失败图刷新都重发，挤占同源并发连接拖累 version.json/notice.json，#202 失败占位也要等分钟级 error 才能上屏＝刷新后长时间卡顿黑屏的成分之一 | 修复（sw.js fetch 头部，零机型分支）：pathname 含 '@@m:' 的请求本地立即 404（该路径永远不可能是真实资源，真数据在 IDB media:<hash>）——error 毫秒级触发、#202 占位即时、网络零占用；池命中场景 media-pool 观察器照旧改写 src 不受影响 | 哨兵 1 条（build.mjs swNeedlesSrc + swNeedles 各 +1）：`u.pathname.indexOf('@@m:') >= 0`<br />tools/verify-refresh-perf.mjs B 组行为断言：SW 激活后注入 src='@@m:…' 的 img——本地服务器零请求命中 + error 事件毫秒级触发 | 【真机:待验证】（畅享20Pro/Edge 及任意安卓内核弱网）：刷新不再长时间黑屏卡顿；池缺失的失败图即时显示「图片丢失」占位而非长时间空白；正常表情图显示不受影响 | <br /> | <br /> |
| 281 | 同上一批报障（同用户同机、明说多机型同现）的启动侧成分：诊断 JS 堆 117.3MB、实测帧率≈32fps、IndexedDB my-emoji-groups=17.26MB（#172 实例 34.93MB）——每次刷新脚本求值即整包读+解析该大键 | 根因（chat.js my-emoji 启动恢复链）：IIFE 在脚本求值时立即 idbGet(my-emoji-groups 大键)+主线程 JSON.parse 整包，秒级长任务恰好压在开屏/桌面/聊天首屏渲染的启动关键窗口（#250 切桌面卡死已同口径治理 contact-switched，启动路径漏了）；且解析结果整会话驻留内存 | 修复（chat.js，零机型分支）：① 启动取回延迟到 mochi-restore-done 后 4s（表情面板/朋友圈插入面板打开本就 reloadMyEmojiFromIdb 现读权威＝#172 防丢主链不变，仅把「整包读+解析」挪出关键窗口）；② myEmojiSave 防盲写闸门由「在挂起名单」扩为「未应用过 IDB 权威值(__myeIdbApplied) 或 仍在挂起名单」，堵住延迟窗口内保存把空/小包顶掉 IDB 全量的新窗口（myeApplyIdb 成功/闸门内确认无键都置位） | 哨兵 2 条（build.mjs FIX_SENTINELS #281，js/chat.js 唯一逻辑锚×2）：`setTimeout(tryRestore, 4000)` 调度行 + `window.__myeIdbApplied !== true` 闸门条件<br />tools/verify-refresh-perf.mjs C 组静态断言 | node --check 过；--check-sentinels 全绿哑 0。【真机:待验证】（大表情库机型）：刷新后开屏/首屏可交互明显提前；表情包面板与朋友圈表情不丢、刷新前保存不回退 | <br /> | <br /> |
| 275 | 多机型反复（2026-09-10 OPPO Reno16/PMM110 + Chrome138 诊断 v3.26.529 报障「问题：不知」；同族已有 #280 华为畅享20Pro/Edge 的 @@m: 404 错误环、#186/#202/#262 各代修复；用户原话仍是「不要覆盖修改导致不同型号设备浏览器的bug反复出现。这个问题其他设备型号也有出现」）：诊断错误环被「资源加载失败 <img> https://…/mochi/@@m:<md5>」刷满（同批哈希 ×2~×6、一天多次启动持续失败）＝聊天里这批表情/图片永久空白气泡 | 根因（备份链路×媒体池，#142/#186/#202/#280 都没堵住的传播链）：「只备份文字」strip 导出只剥 data: 前缀载荷——消息里的 @@m: 令牌不匹配被原样保留，媒体池键值（xy-home-v2:media:<hash> 本身是 dataURL）却被逐条剥成空串。导入后：①渲染端 resolveImg 的 typeof 判定放行空串→map 永久缓存 ''+img.src=''（解析成页面 URL）＝永久坏图且 #202 占位误报「网络不通或原图已失效」；②空串条目 ≤20KB 走备份小键段，随今后每次【完整】备份继续传给对方设备＝跨设备跨机型复发；③池真缺失（历史损伤）时令牌 src 被当相对路径打网络必 404，每次渲染都把「资源加载失败」刷进错误环掩盖真错误（OPPO 诊断 20 条错误全是它） | 修复（零机型分支，五道各在一层）：①data-backup.js 文字模式 cfg.skip 加 MEDIA_POOL_KEY_RE（在读值前生效＝整键跳过，strip 绝不剥值留键）；②导出 LS 小键段循环同样认 cfg.skip（≤20KB 池条目不进 ls 段防被剥空）；③doImportGo 开头 scrubMediaPool 把空串/非 data: 脏池条目直接丢弃（键保持缺席→#186「图片丢失：媒体数据缺失」准确占位；合法池值一律不动；日后导入完整备份即自愈）；④media-pool.js resolveImg 池值体检（值非 data:image/ 开头与池缺失同路：不入 map 不改写 src，且不入负缓存＝可重试可自愈）；⑤device.js 错误记录器对未解析令牌 404 静默（用 getAttribute 原始值判令牌；#280 的 SW 快速 404 与渲染占位链路不受影响） | 哨兵 5 条（build.mjs FIX_SENTINELS #275 ×5，各在自己文件里唯一）：`MUSIC_KEY_RE.test(k) || MEDIA_POOL_KEY_RE.test(k)`／`if (cfg.skip(k)) continue;`／`function scrubMediaPool(obj) {`／`v2.indexOf('data:image/') !== 0`（js/media-pool.js）／`window.mochiMediaIsToken(imTok)`（js/device.js）。行为断言 tools/verify-media-pool.mjs 扩 T8（池条目空串→令牌 src 原样保留、不被改写成空/页面 URL）+T9（池值补回→同令牌新渲染自愈＝完整备份导入恢复路径）。src 三文件修复已随 4fcb012 收口构建进产物（五 needle 产物逐一实证在位），本批只补登记资产未重建。node --check 过；--check-sentinels 全绿哑 0。【真机:待验证】（OPPO Reno16 及任意机型）：①「只备份文字」导出→导入后原图片消息显示「图片丢失」占位、错误环不再刷 @@m: 404；②完整备份导出→他机导入，同批图片全部恢复显示；③正常表情/图片发送显示、收藏、引用、群聊令牌化全不回归 | <br /> | <br /> |
| 282 | 用户报障（荣耀90GT + Edge150，诊断 v3.26.529；多机型同报）：信息诊断错误环「[屏幕适配] 底部少填 277px｜env=0 diff=181 inner=633 phone底=356」（9/9、9/10 反复自动采集）；另 4 条「资源加载失败 <img> …/@@m:…」已由 #275/#280 令牌 404 静音批处理，非本条范围 | 根因（device.js screenDiagJudge ④/⑤b 判定缺口）：本机走 interactive-widget=resizes-visual——键盘只缩可视视口（vv 633→356）、布局视口 inner 不动，.phone 按设计停靠到键盘上沿 356＝布局正确；但 ④/⑤b 只对照 inner 期望底（633），无键盘停靠豁免。sdTick 的输入焦点守卫只挡得住打字期：Edge 收键盘后 vv 读数残留（#236 同族）扩大失焦窗口，输入框一失焦自动采集即误报「少填/悬空」入环 | 修复（判定器内，零机型分支）：④/⑤b 加深收缩豁免——vv 缩幅 ≥ inner×22%（沿用 #236 已验证键盘下限：真键盘缩幅均 >200px，几十 px 只可能是壳残留带）判「键盘停靠期」不判底、记 ✓ 条目可对号；#236 壳残留带（缩幅 <22%）仍照常上报、真残留自愈仍归 syncAndroidKb #236/#267/#209 看门狗，豁免不掩盖 | 哨兵 1 条（build.mjs FIX_SENTINELS #282，js/device.js 唯一逻辑锚）：`_kbShrink >= Math.round(inp.innerH * 0.22)`。tools/verify-viewport-form.mjs 新增 E 组 5 断言（停靠期豁免/✓记录可对号/vv 回基准真残留照报/残带照报），**86/86 通过**；node --check 过；--check-sentinels 全绿哑 0。未构建（树上有并行批次在途，见 WORKLOG）。【真机:待验证】（荣耀90GT+Edge 优先，任意安卓 resizes-visual 机型）：聊天打字→失焦回桌面页，错误环不再新增「底部少填」条目；键盘真停靠期输入栏仍正常贴键盘上沿；#236 机型残留带报障仍能对号 | <br /> | <br /> |
| 284 | 用户报障（vivo X200s + Edge150，诊断 v3.26.536，一加Ace3+Edge 同族多机型）：错误环反复「(promise) BodyStreamBuffer was aborted」成批突发（×9/×10/×11，page-music 最密）；另「@@m: 图片 404」旧版遗留已由 #275/#280 覆盖 | 根因（music-player.js 三处「收完响应头即 abort+cancel body」清理链）：#216 已知该噪音并把 abort 挪到 setTimeout(0) 异步，但 `r.body.cancel()` 返回 Promise——流已被超时 abort 打断时 cancel() 必然 reject，旧式外层 try/catch 只拦同步异常拦不住 → 变成未处理 rejection 入诊断错误环；弱网/代理慢时 8s 计时器成批 abort，一次爆十条 | 修复（拒绝安全，零行为变化）：新增模块级 `mochiSafeCancelBody(r)`——cancel() 挂空 catch 吞预期内拒绝；resolveCoverDirect / resolveNeteaseDirectUrl / 音频探测三处调用点统一替换。该断照断、清理语义不变，仅不再把预期内拒绝当错误记环 | 哨兵 1 条（build.mjs FIX_SENTINELS #284，js/music-player.js 唯一逻辑锚）：`if (p && typeof p.catch === 'function') p.catch(function () {});`。node --check 过；--check-sentinels 全绿哑 0。跨域改动（AI-A 名下 music-player.js，理由：错误环噪音根因修复，AI-B 域诊断链收口配套），已在 WORKLOG 声明。未构建（树上有并行批次在途）。【真机:待验证】（vivo X200s+Edge 优先，弱网更易复现）：音乐页播放/导歌后错误环不再新增「BodyStreamBuffer was aborted」；播放、封面、直链解析功能正常 | <br /> | <br /> |
| 283 | 用户报障（vivo S60/V2571A + Chrome149，诊断 v3.26.536 ts=1789037994889，多机型同报「经常卡，按不动」，用户明说「这个问题其他设备型号也有出现」并要求不要覆盖修改引发跨机型回归）：实测帧率≈25fps、JS 堆 307.1MB、长任务 >50ms 最近 8 条（50~405ms，集中在切后台/收键盘窗口）、发消息/收键盘/离页回前台全在卡；数据面 IDB default:chat-msgs=79.2MB（2277 条）、字卡库 cc-groups=16.38MB | 根因（chat.js 媒体池 normalize 覆盖缺口，与机型/浏览器无关）：#142 池 v1 的 `mochiMediaTokenize` 只收 `data:image/`——历史语音/语音字卡以「名称\|\|\|data:audio;base64…」整份内联在消息 text （语音内容唯一，内容寻址去重对总库无能为力，但每次 `saveMsgs` 落盘/离页 `flushSave` 都对整包做 structured clone）。本机图片早已令牌化（历史里已有 @@m: 引用），79.2MB 大头即漏收的语音：低端机（vivo/OPPO 等）每次落盘 clone 几十 MB＝100~400ms 长任务+GC 风暴＝「经常卡、按不动」；堆 307MB 常驻又放大 GC。触发面也窄：pass 只挂 mochi-restore-done（IDB 整轮挂起时永不到达）与 contact-switched——不切桌面的设备历史语音永不被收口 | 修复（media-pool.js + chat.js，**零机型分支**，语义与 #142/#186 全保留）：① 池收音频——tokenize 放行 data:audio/；音频纪律：不进 map 热缓存（迁移批量不把省下的内存吃回去）、新增 `mochiMediaExpandAsync` 按需 idbGet （校验 data:audio/ 前缀，缺失→null 由调用方占位）；② normalize pass 令牌化三种形态：「名称\|\|\|data:audio/…」→「名称\|\|\|@@m:hash」/裸 data:audio→「\|\|\|@@m:hash」/m.voice 字段，名称段原样保留；迁移期每 32 条分批 `mochiMediaFlush`（writeBuf 与单条 idbSetAll 事务封顶，已冲批次从 #186 回滚账除名防堆尖峰），写池失败回滚语义不变；③ 播放链路：fillVoiceBubble 点按遇令牌先 ExpandAsync 取回再播（_vExp 防连点双播，池缺失 toast「语音数据缺失」），聊天/收藏共用；④ 收藏语音识别加令牌形态、tokenizeFavList 同步收语音、搜索结果语音显示「[语音] 名称」、normCell 给裸 data:audio/「\|\|\|@@m:」补 type=voice；⑤ 冷启动收敛：loadMsgs 权威就绪后 12s 也调度 pass | 哨兵 6 条（build.mjs，js/media-pool.js 1 + js/chat.js 5，均唯一逻辑锚）：池收音频双 indexOf 门／语音令牌化 `m.text.indexOf('data:audio/', _bar + 3) === _bar + 3`／播放取回 `mochiMediaExpandAsync(v.src, function (data) {`／迁移分批冲池 `const _okMid = await window.mochiMediaFlush();`／冷启动触发 `scheduleMediaPass(12000)`／收藏识别 `@@m:[0-9a-f]{32}$/.test(f.text)`。行为断言 `tools/verify-voice-pool.mjs` 8 项（令牌化/池落盘/异步取回/音频不进缓存/落盘无 data:audio/裸形态/收藏/幂等），已纳入 verify:all。【无头】构建后 8/8 过＋verify-media-pool 保持全绿；【真机:待验证】（vivo S60 等）：更新后打开聊天一次（约 12s 后后台跑迁移）→ 设置里看 chat-msgs 应从 79MB 级降到 MB 级、语音气泡可正常播放、发消息/收键盘不再长卡顿 | <br /> | <br /> |
| 287 | 用户报障「群聊里我无法点击联系人的头像对联系人使用拍一拍」：单聊聊天页收到的消息点头像即弹拍一拍面板（chat.js renderMsg 的 av click → openPokeCard），群聊从未实现同款入口（git -S 全史无 openPokeCard）＝功能缺口非回归；群聊成员本来就只会「被动拍我」（memberReply 的 gc-touch-prob 分支），用户主动拍成员无路径 | 根因（group-chat.js renderMsg）：群聊消息头像 fillAv 后未绑任何点击事件；群聊也没有拍一拍选择面板（#poke-card 是聊天页 DOM，群聊页不可复用） | 修复（group-chat.js + template.html，对齐单聊语义）：①template 群聊页内加 #gc-poke-card 面板（复用聊天页 .poke-card 壳样式）；②renderMsg 成员消息（side==='in' 且有 cid）头像绑 click → gcOpenPokeCard(cid)，cursor/title 同单聊；③字卡来源同聊天页拍一拍面板：预设 6 条 + 公用拍一拍字卡（getPokeCards）+ 当前桌面「我的拍一拍」自定义分组（poke-groups-mine/存量 poke-user-mine，去重平铺）；④发送文本 gcPokeTextOf 与聊天页 sendPoke 人称映射同构（你→成员名/句中我→成员名/句首我去掉改我的昵称开头/句首我整句形态保留），落库即定稿（同成员拍我 gcPokeText 口径）；⑤拍后该成员按既有 memberReply 链反应（可能拍回/回消息，概率走群聊回复设置）；⑥开面板先收气泡菜单+输入框失焦，点外关闭 | 哨兵 1 条（build.mjs FIX_SENTINELS #287，js/group-chat.js 唯一逻辑锚）：`gcOpenPokeCard(rec.cid)`。行为等价性验证 tools/verify-poke-map.mjs（已入库，可复跑）：程序化提取双端映射函数对 16 种字卡形态（你/我/句首我/无代词/混合）逐一比对，单聊渲染结果与群聊定稿文本 16/16 全等（本会话实跑 17/17 含提取完整性断言）。node --check 过；--check-sentinels 625 条全绿哑 0。跨域改动（AI-B 名下 mobile-adapt.js 两组浮层选择器加 #gc-poke-card，理由：背景滚动锁/键盘停靠登记，同 #poke-card 家族），已在 WORKLOG 声明。端到端 tools/verify-poke-gc.mjs（已入库）：真实 UI 全链路 10/10（发消息→成员回复→点头像弹面板→选字卡→special=poke 落库+居中渲染+面板关→被拍成员反应→零未捕获错误）。已随本批构建（sw mochi-mtvstgaz）。【真机:待验证】群聊点任一成员头像弹「拍一拍·成员名」面板→选字卡→居中系统条「我的昵称 XX成员名XX」→被拍成员按概率拍回/回复；点面板外/✕ 可关闭；我的拍一拍自定义字卡出现在面板里 | <br /> | <br /> |
| 288 | 用户反馈「群聊的美化设置不完整，和聊天里的美化设置不一样（ui可以重新设计）」：实测截图对比——聊天设置→美化是整页卡片式 UI（them-tabs + gs-title 分组标题 + set-group.glass 玻璃卡片 + set-row 图标行 + 副标题说明），群聊设置→美化聊天是底部半框里的纯文字行列表（gc-set-title + beautyRow 无图标无卡片），视觉落差即「不完整」观感；选项覆盖本身两侧已齐（群聊 v3.28.x 起还多出 时间轴颜色/正在输入颜色/发送按钮组） | 根因（group-chat.js renderBeautyView + group-chat.css）：#251/#v3.28.x 对齐批只对齐了「功能键」，渲染层仍走 v3.9.x 的 beautyRow 纯文字行，未跟上聊天设置页的卡片化视觉 | 修复（group-chat.js + group-chat.css，纯渲染层重设计、零功能变化）：renderBeautyView 改产出 gs-title 分组标题 + set-group.glass 卡片 + set-row gc-set-row 图标行（.ico 线性 SVG + .txt 行名 + .val 值回显），17 个图标与聊天设置页同款线性风格；低对比警告条收进卡片内；行类保留 gc-set-row、.txt 仅放行名——verify-gc-color（.gc-set-row .txt 文本精确点行）与 verify-gc-settings（innerText 含标签）兼容零改动；beautyRow 保留给主视图（成员昵称显示行）；新增 .gc-set-body 内 gs-title/set-group/set-row 布局 CSS + 暗色 active | 哨兵 1 条（build.mjs FIX_SENTINELS #288，js/group-chat.js 唯一逻辑锚）：`'set-row gc-set-row'`（回退纯文字行即消失）。UI 预览验证 tools/_tmp-gcv-preview.mjs（临时）：产物全量 CSS + src 新渲染函数 + 桩合成真实 DOM 截图——壁纸/气泡与文字（含警告条）/发送按钮/气泡外观/字体与样式/美化方案 六组卡片全部正常渲染、图标/值回显/分组标题与聊天设置页同观感；拍一拍面板（#287）同框预览过。node --check 过；--check-sentinels 全绿哑 0。已随本批构建（sw mochi-mtvstgaz），构建后复跑 verify-gc-settings **34/34** + verify-gc-color **14/14**（R1/R2 即按新行标记点「我的气泡颜色/我的消息文字颜色」，兼容点实测有效）+ verify-poke-map 17/17 全过。【真机:待验证】群聊设置→美化聊天：六组玻璃卡片+图标行渲染正常、暗色模式正常、每行点开弹窗与重设计前一致 | <br /> | <br /> |
| 289 | 用户报障「桌面组件【摸鱼打卡】，有人手机总是刷新重进后需要重新打卡，不是每天打卡一次」：部分机型每次刷新/重进后打卡横幅按钮回到「打卡」状态，只能再点一次（数据层无害：logFish 按自然日去重，摸鱼天数不会虚增，但体验＝天天重打） | 根因（personalize.js 每日打卡初始化段）：按钮「已打卡」状态只在模块初始化时读一次 `store.get('checkin') === fishToday()`，而该读取发生在启动回填（idbRestore）完成之前——localStorage 写失败/配额满/IDB 为主的机型此刻 checkin 键尚未回填进内存缓存，按钮渲染成未打卡；之后回填/写日志合并把数据补齐了，却没有任何代码回头刷新按钮（同 AGENTS.md 已知坑「回填完成前读到的键可能为空」）。健康机型 LS 直读命中所以不复现＝「有人手机」 | 修复（personalize.js 一处，最小改动）：按钮状态同步抽成 `syncCheckinBtn()`（含未打卡反向复位，对齐 contact-switched 分支的既有双向逻辑）；初始化调一次 + 监听 `mochi-restore-done`（回填完成）与 `mochi-wrj-heal`（写日志 IDB 合并自愈）再同步按钮与 `updateFishDays()`（摸鱼天数同族时序）。切桌面（contact-switched）已有重判断逻辑不动；点击去重语义不变 | 哨兵 1 条（build.mjs FIX_SENTINELS #289，js/personalize.js 唯一逻辑锚）：`document.addEventListener('mochi-restore-done', function () { try { syncCheckinBtn(); updateFishDays(); } catch (e) {} });`。验证：node --check personalize.js/build.mjs 过；`node build.mjs --check-sentinels` 626 条全绿、哑哨兵 0。未构建（git status 有他人在途 chat.js/device.js/music-player.js 等半成品，按协议不夹带），待构建者收口。【真机:待验证】LS 写失败/回填慢机型：打卡后刷新重进，按钮应直接显示「✓ 已打卡」、摸鱼天数不回退、无需重新点击 |
| 291 | 用户报障（OPPO Reno6 5G + 雨见浏览器/Firefox152，诊断 v3.26.541）：「经期组件文字有重叠」——桌面经期卡（desk-period）内文字叠字 | 根因（home.css .desk-period 几何，与机型无关的跨浏览器非确定性布局）：160px 卡内 .dpd-inner 绝对定位 inset:0 垂直居中、无底部预留；底部周期进度条组（.dpd-bar-wrap bottom:10，含说明行 .dpd-bar-cap）是另一层绝对定位。居中列内容（标签+50px 天数+副题）的高度按浏览器默认行高计算：Chrome 恰好擦边不重叠，Gecko 系（Firefox/雨见）默认行高更高 → dpd-sub 尾边越过 dpd-bar-cap 顶边＝文字叠字 | 修复（纯 CSS，文案与功能零变化）：.dpd-inner 加 `padding-bottom:26px` 给进度条区让位（居中列上移，与 cap 几何间距 ≈12px，行高加压场景仍 ≈8px）；.dpd-label/.dpd-sub 显式 `line-height:1.25`（行高跨浏览器确定化，不再赌系统字体度量）；标签/天数/副题/进度条/阶段徽章全保留 | 哨兵 1 条（build.mjs FIX_SENTINELS #291，css/home.css 唯一逻辑锚）：`padding-bottom:26px`。行为断言 tools/verify-period-desk-card.mjs（新增，纳入 verify:all）：360×800 无头实测 dpd-sub 底边与 dpd-bar-cap 顶边间距 ≥4px（修复前同环境为负距＝叠字，脚本有牙）+ Gecko 行高加压（line-height:1.6 !important 注入）仍不叠 + 进度条隐藏态内容不溢出卡底。构建哨兵全绿。【真机:待验证】（OPPO Reno6/雨见 优先，任意机型）：桌面经期卡在 经期中/排卵期/未记录 三种状态下文字不再叠字 | <br /> | <br /> |
| 290 | #289 同批追问「摸鱼天数错了怎么自动修正」：已摸鱼天数=fish-log（全局日期列表）长度，两种可自愈错源——①虚高：重复日期/脏值混入（logFish 读-改-写竞态、旧格式残留）多算天数；②偏低：各联系人命名空间旧 fish-log 副本未合并进全局键——migrateFishLogGlobal 原来只在模块加载跑一次（早于 IDB 回填），副本只存 IDB 时合并扑空且不再重试＝永久漏算 | 修复（personalize.js 同段，随 #289 收口）：①`normalizeFishLog()` 规范化——只留 YYYY-MM-DD 合法日期 + Set 去重，**有变化才写回**（防回填前空读 blind-wipe）；logFish/updateFishDays 全部改走规范化出口；②回填完成（mochi-restore-done）/写日志自愈（mochi-wrj-heal）后再跑一次 `migrateFishLogGlobal(false)` + 规范化 + 刷显示（与 #289 按钮重同步同一时序防线）。③IDB 旧快照遮蔽不在此设防（idb.js retainValue LS 优先 + wrj 时间戳守卫既有兜底） | 哨兵 1 条（build.mjs FIX_SENTINELS #290，js/personalize.js 唯一逻辑锚）：`document.addEventListener('mochi-restore-done', fishLogHeal);`。验证：node --check 过；--check-sentinels 全绿哑 0。未构建（同 #289，待构建者收口）。【真机:待验证】多桌面且有旧副本的账号：刷新重进后已摸鱼天数只增不减、与实际使用自然日一致；正常账号天数不跳变 |
| 292 | 用户新功能需求「问问ta 新增批量导入单选题 + 设置问卷答题结束时间」：批量导入时【】里为问题、其后每行一个选项答案，用于批量问联系人问题（非 bug，登记目的＝哨兵防并行覆盖，见表内说明） | 现状（ta-ask.js 批量导入）：仅支持「一行一个问题」纯文字题，【】格式无解析＝单选题只能逐条手动添加；询问无任何截止机制 | 实现（ta-ask.js + template.html）：①批量导入改状态机解析——「【问题】」行开一道单选题、其后至下一个【】之间每行一个选项，选项≥2 落 type:'single'+options（不足按文字题导入），普通行仍一行一题完全向后兼容，toast 报「含 M 道单选题」；②设置区新增 datetime-local「问卷答题结束时间」（settings.deadline 毫秒，0=未设）+清除按钮；过点后三闸门：自动触发 return／手动提问 toast 拦截／作答统一拦在 openAskReply 入口 + chatAskReply 包装层（文字+单选两路径全覆盖，deskCk 查岗卡豁免） | 哨兵 2 条（build.mjs FIX_SENTINELS #292，js/ta-ask.js 各自唯一逻辑锚）：批量导入解析 `if (cur.opts.length >= 2) { q.type = 'single'; q.options = cur.opts.slice(); singles++; }`；作答闸门 `if (askDeadlinePassed(taAskLoad())) { toast('已过问卷答题结束时间，不能再作答'); return undefined; }`。node --check 过；--check-sentinels 630 全绿哑 0。跨域改动 build.mjs（哨兵登记）已在 WORKLOG 声明。未构建（树上多批在途，待构建者收口）。【真机:待验证】①导入「【今晚想吃什么？】/火锅/烧烤/寿司」→「单选·3选项」行 + 聊天卡片就地点选；②普通行混合导入不回归；③结束时间设为过去后手动提问与作答均被拦、清除后恢复 | <br /> | <br /> |
| 293 | 用户报障「手机后台浏览器弹了联系人来电通知，马上打开浏览器却没有通话弹窗、接不到电话」：通知正常弹（响铃挂起链路已发），回前台 3 分钟窗口内也未超时，但来电面板不出现，聊天里直接补「来电 · 未接听」 | 根因（call.js resumeHeldCall 重响条件）：原条件 `h.cid === (window.__activeCid\|\|\|'default')` 要求回到应用时恰好停在来电归属联系人的桌面——①跨桌面来电（查岗/求聊天队列 #204 路径触发）归属的往往不是当前桌面，切回应用落在别的桌面＝永不成立；②安卓后台久了页面被杀冷启动时，恢复链路在 mochi-restore-done 后 1.2s 跑（bootCallResume），此刻 `__activeCid` 可能还在等 IDB 回填校正（contacts.js correctCidFromIdb 异步），同一判式同样不成立。两条路都静默走 notifyCallEnd 补未接＝用户永远接不到 | 修复（call.js resumeHeldCall，最小改动）：cid 不匹配时不再直接判未接——归属 cid 在联系人名册内（防切到已删除桌面造成空命名空间，同 contacts.js applyCidCorrection 口径）则 `setActiveContact(h.cid)` 先切到归属桌面、切换成功立即 `incomingCall(true)` 重响；名册不含/切换失败/超时/已在通话仍走原补未接路径。顺带消解冷启动 cid 校正竞态（强制切目标桌面不依赖 __activeCid 当前值） | 哨兵 1 条（build.mjs FIX_SENTINELS #293，js/call.js 唯一逻辑锚）：`known = window.getContacts().some(c => c && c.id === h.cid);`。node --check 过；--check-sentinels 632 全绿哑 0。未构建（树上多批在途，待构建者收口）。【真机:待验证】①让联系人在后台来电（通知弹出后）点开浏览器落在别的联系人桌面→应自动切到来电联系人桌面并弹来电面板可接听；②超 3 分钟打开仍正常补「未接听」不误弹；③冷启动打开同样能重响 |
| 294 | 用户报障「后台弹窗显示联系人发朋友圈的通知，最右不显示联系人头像了，显示全黑」：系统通知右侧大图标位整块黑色方块 | 根因（bg-keep.js makeAvatarThumb 头像缩略链）：canvas 按 96px 等比缩放后直接 `toDataURL('image/jpeg')` 导出——JPEG 无透明通道，canvas 未铺任何底色，凡带透明区域的头像（PNG 上传图/默认图）透明像素在 JPEG 里落成纯黑＝缩略图整体或局部全黑；#292 顺带修零固有尺寸图（部分 SVG）缩成 1×1 的问题（零宽高直接回退原图） | 修复（bg-keep.js makeAvatarThumb 一处）：drawImage 前先 `ctx.fillStyle='#ffffff'; ctx.fillRect(0,0,w,h)` 铺白底，透明区域渲染为白色而非黑色；图像零宽高时 cb('') 回退原图（宁缺勿黑）。裁剪失败回退原图/BigIcon 兜底 mochi 图标的既有降级链全部不动 | 哨兵 1 条（build.mjs FIX_SENTINELS #294，js/bg-keep.js 唯一逻辑锚）：`ctx.fillStyle = '#ffffff';`。node --check 过；--check-sentinels 632 全绿哑 0。未构建（树上多批在途，待构建者收口）。【真机:待验证】带透明区域的头像触发后台通知（朋友圈/来电均可）→ 通知右侧头像正常显示，不再全黑；无头像时仍回退 mochi 字母图标 |
| 295 | 用户报障「【帮我决定】和【群聊决定】的自定义选项输入框里，输入法弹窗打开时，自定义选项页面无法上滑」：键盘弹出期整卡（tabs/问题框/按钮）滑不动 | 根因（chat-main.css dec-opts/gd-opts 的 overscroll-behavior:contain，同族第三次：v3.23 modal-textarea→v3.25 选项框高度上限→本次）：安卓 textarea 被 mobile-adapt 转成 contenteditable .ce-box（max-height:176px + overflow-y:auto 自成滚动容器）后，contain 会把「框内无内容可滚/已滚到边界」的滚动链也一并拦断；键盘弹出期面板被 kbDockPanels 重锚到输入栏上方、可用高度骤减最需要滑动，而手指自然落在正聚焦的选项框上——手势被 ce-box 吃掉，外层 .poke-card-scroll 收不到滚动＝整卡无法上滑 | 修复（两处一行改 overscroll-behavior:auto）：框内有溢出仍先在框内滚（v3.25 的高度上限修复不回退），无溢出/到边界时滚动链放行给外层面板正常上滑 | 哨兵 2 条（build.mjs FIX_SENTINELS #295，css/chat-main.css 各自唯一逻辑锚）：`data-for="dec-opts"] { max-height:176px; overflow-y:auto; overscroll-behavior:auto; }` / `data-for="gd-opts"] { max-height:176px; overflow-y:auto; overscroll-behavior:auto; }`（改回 contain 即失守）。node --check 不适用（CSS）；--check-sentinels 见 WORKLOG。未构建（待构建者收口）。【真机:待验证】安卓任一机型：帮我决定/群聊决定→自定义选项→点选项输入框弹出键盘→手指在输入框上上下滑动，面板整卡应能滚动查看；选项多于框高时仍先在框内滚动 |
| 296 | 用户新功能需求「回复设置里缺少 关闭联系人写信 / 关闭联系人发布朋友圈 的设置按钮」（非 bug，登记目的＝哨兵防并行覆盖） | 现状：信箱「主动写信概率」走 mailCfg 的 prob() 兜底——存 0 会被回退默认 30（防旧坏数据的自我保护反而堵死「用概率=0 关闭」的路）；朋友圈「主动发朋友圈概率」虽支持 0 但无一键总开关。实现（reply-settings.js + template.html + mail.js + feed.js）：①新增 ml-write-en / fd-post-en 总开关（默认开，按联系人桌面隔离），进 syncUI/开关监听/保存全部三处清单；②信箱面板「联系人主动写信」组首行、朋友圈面板「TA 发朋友圈设置」组首行各加开关行；③闸门在触发链首行——mail.js maybeIncomingLetterFor 读 mailCfgFor().writeEn 为 0 即 return（写信裸读不过 prob() 兜底），feed.js maybeAutoPostFor 读 feedCfgFor().postEn 为 0 即 return；回信（ml-reply-*）、每周摸鱼小结、朋友圈点赞/评论互动均不受影响 | 哨兵 2 条（build.mjs FIX_SENTINELS #296，各自文件唯一逻辑锚）：js/mail.js `if (!cfg.writeEn) return;` / js/feed.js `if (!cfg.postEn) return;`。node --check 过；--check-sentinels 636 全绿哑 0。跨域改动 build.mjs（哨兵登记）已在 WORKLOG 声明。未构建（树上多批在途，待构建者收口）。【真机:待验证】①回复设置-信箱→关「联系人主动写信」→保存→等触发周期 TA 不再来信，重新打开恢复；②回复设置-朋友圈→关「联系人主动发朋友圈」→TA 不再自动发动态；③开关默认开，旧用户升级后行为不变；④「保存全部桌面联系人设置」能把开关同步到所有桌面 |
| 306 | 用户报障（v3.26.x 产物）「连连看、消消乐、心意拍卖会 UI 都显示不正常」+ 新需求「所有小游戏都要有可以点击使用的全屏按钮」。实测三处显示异常：①连连看头部 6 个控件（难度下拉+主题+提示+洗牌+音效+关闭）把标题挤成一字一行竖排；②连连看/消消乐棋盘最右一列被截断；③拍卖会「不拍了」按钮看起来凭空消失。五子棋因头部控件少、棋盘无 gap 不受影响（用户未报，作对照组） | 根因：①.poke-card-head 是 space-between flex 且标题 span 可被压缩换行——控件总宽超出时标题 span 被压到 1 字宽，CJK 逐字换行=竖排；②linkup/match3 的 fitBoard 以 floor(容器宽/列数) 算格宽、再把 格宽×列数 写成棋盘宽，但 .lk-board/.m3-board 有 3px grid gap，实际总宽=格宽×列数+(列数-1)×3px，恒溢出右缘 16~27px 被卡体裁切（gomoku 棋盘无 gap 故幸免）；③「不拍了」复用 .pong-overlay-btn2（半透明白底+白字，为深色覆盖层设计）落在白色卡体上=白字白底对比度 0，按钮其实在（实测 72px 可点击）只是完全看不见 | 修复（最小改动）：①chat-main.css .poke-card-head 加 flex-wrap:wrap+row-gap:6px、标题 span 加 white-space:nowrap（放得下时形态零变化，放不下时控件整组换到第二行右对齐，snake-head-actions 加 margin-left:auto+flex-shrink:0 配套）；②linkup.js/match3.js fitBoard 先扣 (列数-1)×3px 再取整格宽，棋盘宽写成 格宽×列数+(列数-1)×3px；③chat-pages.css 给 .au-bids .pong-overlay-btn2 独立可见样式（浅灰底+深字，暗色反向）；④全屏：新增共享 .poke-card.game-fs（fixed 满视口，形态同 pong-fs/snake-fs，高度用 iOS 修复同款 min(var(--mochi-ios-h,100dvh),100dvh)），rps/fish/c4/ms/memory/gomoku/linkup/match3/auction 九个面板头部各加 ⛶ 按钮+各自 JS 内 toggle（open 时先退出防兄弟互斥隐藏残留，gomoku/linkup/match3/c4/ms 切换后 60ms 重跑 fitBoard 重排棋盘）；pong/snake/brick 原有专属全屏按钮不动 | 哨兵 6 条（build.mjs FIX_SENTINELS #306，各自文件内唯一逻辑锚）：js/linkup.js 的 Math.floor((w-(st.cols-1)*GAP)/st.cols)／js/match3.js 的 Math.floor((w-(N-1)*GAP)/N)／css/chat-main.css 标题 nowrap／css/chat-pages.css .au-bids .pong-overlay-btn2 可见样式／css/chat-pages.css game-fs 的 iOS 高度表达式／js/gomoku.js toggle game-fs（代表登记）。行为断言 tools/verify-new-games.mjs 扩 B 组：棋盘 scrollWidth≤stage 宽、全屏按钮点击切 .game-fs、不拍了按钮前景色非纯白。node --check 十文件过。【真机:待验证】①连连看普通/挑战两档最右列完整可见；②消消乐第 8 列完整；③拍卖会「不拍了」清晰可见可点；④九个游戏逐个点 ⛶ 进全屏（面板满屏）、⤢ 退出；⑤五子棋/连连看/消消乐全屏后棋盘按大屏重排不溢出 |
| 308 | 用户报障「游乐室右边的×点击关不掉」，并明确其他设备型号也有出现＝设备无关 | 根因（arcade.js，#301 批新建文件引入）：第 18 行 `getElementById('arc-close')` 取到 closeBtn 后**全文件从未绑 click 事件**——按钮纯摆设，点了 `closePanel()` 永远不执行；无全局 `.poke-card-close` 委托（各半框自己绑自己的 ×），`closeArcadePanel` 也无任何外部调用方，故无兜底路径。与机型/浏览器无关的漏绑，任何设备都复现（`.poke-card[hidden]{display:none}` 本身正常，红包等其余半框同款按钮都能关＝对照印证） | 修复（最小改动，chat.js rpCloseBtn 同款模式）：closePanel 定义处补 `if (closeBtn) closeBtn.addEventListener('click', (e) => { e.stopPropagation(); closePanel(); });`，stopPropagation 防冒泡触发 more 面板委托 | 哨兵 1 条（build.mjs FIX_SENTINELS #308，js/arcade.js 唯一逻辑锚：closeBtn click 绑定行，删则构建报警）；行为断言 tools/verify-arcade-close.mjs 7 断言（A1-A2 src 逻辑锚 / B1 开面板×可见 / B2 点×收起＝主断言 / B3 关后重开再关 / B4 对照：猜拳半框照常 / C1 零未捕获错误），verify-suite 自动发现。node --check 过；构建哨兵 647/647 全绿。【真机:待验证】任一机型：聊天更多功能→小游戏→游乐室，点右上 × 半框应收起，重开再关仍正常 |

| 301 | 用户报障「手机端 UI 位置偏离了，桌面底部三行导航变成竖着了，UI 完全乱了」+截图：整页被放大且左侧裁切（自绘状态栏 Mochi 只剩尾巴）、右侧露灰底+白浮条。无头 390×844 复现实锤与机型无关：.phone 落 x=-59、body scrollWidth=449、.tabbar 落 [331,449]（视口 390）被挤出屏右 | 根因：07a6cab 起的 template.html「红包」注释行结尾误写 */} 漏 -->，注释吞到下一个 -->（#297 注释尾），净吃掉「红包」标题行+<div class="set-group">开标签+整行 cs-rp-auto-prob；其后 </div> 连锁把 them-sec(function)→#page-chat-settings→.phone 手机壳逐一提前闭合，tabbar/sm-float/tc-mask/desk-msg 落成 body 直接子节点；html/body 是 flex 横排 justify-content:center——手机壳(390)与 tabbar(min 118)并排成 508px 行，居中=壳 x=-59、tabbar 出屏、右侧露灰底。div 总数恰好配平（补偿在别处），肉眼/配平检查/verify-desk-layout 全查不出，只能真解析器见；禁 JS 对照实证纯解析层、与 JS 无关 | 修复：①注释行补 -->（1 行）；②build.mjs 构建期硬校验模板 <!--/--> 配平（失衡退出 1，防同类）；③哨兵+1（红包注释闭合锚）；④新增 tools/verify-dom-shell.mjs（L1 禁 JS 纯解析：tabbar/sm-float/tc-mask/desk-msg 父节点=.phone+设置页数据分区在设置页内；L2 运行时：phone x=0、tabbar 完整在视口、body 无水平溢出），verify-suite 自动发现 | 哨兵 647 全绿哑 0；verify-dom-shell 8/8（修复前产物 5/8 红=有牙）；verify-dom-shell 已入 verify:all。【真机:待验证】手机端刷新后桌面位置回正、底部导航可见、右侧灰底消失 |
| 309 | 用户复报（#306 修复上线后）「依旧打不开正常使用，功能里ui只有一条横线啊」。线上部署页无头复现实锤：打开连连看（未开局）面板塌成小条，「开始对局」覆盖层被压成一条黑色横线（消消乐同款）——用户说的「一条横线」即此 | 根因：`.lk-stage`/`.m3-stage` 高度完全由内部棋盘撑起——未开局时棋盘为空 → stage 高 0 → 绝对定位其上的 `.pong-overlay`（标题+开始按钮）随之压成一条横线，面板只剩头部+几行字＝观感「打不开/没法用」。#306 批修复的都是开局后的形态（截图均在开局后拍），未开局态漏验——验证盲区：专项脚本都是先点「开始对局」再断言 | 修复（一行 CSS ×2）：`.lk-stage`/`.m3-stage` 加 `min-height:190px` + `align-items:center`（开局后棋盘 5~6 行 230px+ 不受它约束；gomoku 打开即 buildBoard 有高度无需处理；auction 的拍品卡/rps 按钮区天然有高度） | 哨兵 1 条（build.mjs FIX_SENTINELS #309，css/chat-pages.css 唯一逻辑锚）：`.lk-stage { position:relative; width:100%; min-height:190px;`。行为断言 tools/verify-new-games.mjs 新增 B0：未开局直接量 stage≥150px 且开始按钮≥30px。构建哨兵 647 全绿、verify-new-games 27/27、布局 10/10。【真机:待验证】打开连连看/消消乐（不点开始）→ 面板中部应有完整的「开始对局」按钮区（不是一条黑线），点开始后棋盘正常展开；另注：截图里盖住半屏的「TA的小问题」问答弹窗是查岗/互动功能本体，点选项或 × 关闭即可，非故障 |
| 310 | 用户报障「联系人使用正常的句子，也发送成断断续续的造句」——普通聊天回复被抽去词典拼字、切成 2~7 条气泡逐条连发，体感「好端端一句话被截断」。随批新增需求：①单气泡拼字形态——切出的词用空格连成一张字卡发进同一个聊天气泡，气泡下带「词典拼字」小标签（与情绪标签同款 chip）；②该形态与逐词连发共用同一「拼字概率」混合触发（命中后各 50% 掷币）；③提供可关闭「普通字卡被截断」的设置 | 根因：#298「词典拼字」的「混用自定义字卡」（qs-cc）默认开启（=1）——quoteSpellPick 把 getPool() 的普通文本字卡并进抽句池（3~26 字含 ≥2 汉字的正常回复卡全有资格），拼字概率默认 25% 每回复掷一次，命中后该条回复被替换成抽中句按词典切词连发。功能本身按设计工作，是抽句池范围默认值过宽 | 修复（三处）：①reply-settings.js DEFAULTS qs-cc 1→0（默认只用词典语录抽句）+ migrateQsCcOld 一次性迁移存量桌面已写盘的旧值 1→0（只跑一次，reply-qs-cc-migrated 标记防覆盖用户此后自改的开启）；②quote-spell.js 新增 qs-one 单气泡开关（默认开）：命中拼字后 Math.random()<0.5 返回 {segs, one:true}（旧纯数组=逐词连发形态不变，chat.js 兼容两种返回）；③chat.js replyOnce 新增 rep.spellOne 分支：addIn(rep.spell.join(' '), {tag:'词典拼字', tagNoDup:true})——空格连卡进同一气泡，tag 复用情绪 chip 链路持久化（重进聊天仍在、tagNoDup 不重复正文）；template.html 词典拼字组加「单气泡拼字」开关行+说明改写；notice.json 公告行同步 | 哨兵 3 条（build.mjs FIX_SENTINELS #310：chat.js 的 spellOne 单气泡 addIn 锚、quote-spell.js 的掷币返回锚、reply-settings.js 的迁移写入锚，各文件唯一）；行为断言 tools/verify-quote-spell.mjs 扩 H 组 3 条+D 组 3 条：qs-one=0 只返回纯数组、qs-one=1 两种形态混合出现（120 掷双形态）、单气泡 segs 可空格连卡、D2 四键默认值、D3b 迁移在位、D7 单气泡 tag 接线，45/45 过。node --check 三文件过；--check-sentinels 651 全绿哑 0。【真机:待验证】①普通聊天不再（或极少，仅词典语录按 25% 概率）出现断续造句；②设置→回复设置→词典拼字→「单气泡拼字」开关在位；③命中拼字时约半数是单气泡形态（词间空格一张卡+「词典拼字」小标签）、半数逐词连发；④关「单气泡拼字」后全部为逐词连发；⑤重进聊天后单气泡消息下「词典拼字」标签仍在 |
| 311 | 用户要求「字卡库→系统预设字卡→聊天默认字卡→词典 tab 里的【词库·自建】分组删掉」。实测根因：default-cards.js mergeDictCustom 把自建词并组时 find('词库·自建') 永远落空（内置组叫「词库」，扩展组叫「词库·双字/三字/…」），于是每次都 push 一个独立「词库·自建」组且空数组也 push＝词典 tab 常驻一个（多数时候空的）【词库·自建】分组 | 修复（最小改动，mergeDictCustom 一处）：自建词改为并入第一个组名以「词库」开头的内置组（仍是「词库*」前缀，quote-spell 切词词典消费不变）；「语录·自建」「词库·自建」兜底组仅在有内容时才创建，空组不再占位；自建徽标（__dictCustomSet）与单条删除功能不变 | 哨兵：#301 词典自建词条锚点随逻辑更新（needle 改为 `const gw = base.find(g => g[0].indexOf('词库') === 0)`，js/default-cards.js 文件内唯一），--check-sentinels 651 全绿哑 0；node --check 过。【真机:待验证】词典 tab 不再出现【词库·自建】独立分组；自建词（如有）出现在内置「词库」组内并带「自建」徽标、仍可删除 |

| 314 | 用户报障「收藏页面的批量管理无法点击 多选」，并明说「这个问题其他设备型号也有出现」「不要覆盖修改导致不同型号设备的bug反复出现」。实测（无头 Chrome 390×844 触摸仿真复现）：进批量管理正常、单条勾选正常，但点【全选】后勾选瞬间全部清零（勾选 2→0、删除键回灰、按钮文案停在「全选」）；切分类/切页签同样丢勾——多选功能形同虚设。各机型都复现＝与设备无关 | 根因（chat.js v3.31.x 批量管理段，纯逻辑与设备无关）：`getFav()` 每次 `JSON.parse(store.get('fav-msgs'))` 返回**全新对象**，而 `favBatchSel` 存的是上一次渲染那代对象的**引用**；任何 `renderFav()` 重渲染（点全选/切分类/切页签/删除后都触发）末尾的 `favBatchSel = favBatchSel.filter(s => list2.indexOf(s) >= 0)` 按引用过滤必然全部失配 → 勾选静默清零。批量删除 `favBatchArr.indexOf(s)` 同样引用失配（确认弹窗打开期间数组已被重排） | 修复（chat.js 最小改动，**零机型分支**）：勾选身份从对象引用改为 `favItemKey(f)` 内容指纹 key（=归属 by+类型 kind+时间戳 ts+q/text 截断 120 字+special/mailType+side，与 favDup 判重同一套指纹逻辑）——①renderFav 收窄勾选改 `visKeys = new Set(list2.map(favItemKey))` 按 key 过滤；②条目点击/勾圈渲染用 `favItemKey(f)` 比对；③全选存 `favBatchVis.map(favItemKey)`；④批量删除按 key 集合匹配 + 倒序 splice（防前删后索引错位）。重渲染后勾选稳定保留，切 tab/分类仍按「当前筛选可见」正确收窄 | 哨兵 1 条（build.mjs FIX_SENTINELS #314，js/chat.js 内唯一逻辑锚）：`const visKeys = new Set(list2.map(favItemKey));`（删则退回引用勾选=复发）。行为断言 tools/verify-fav-batch.mjs **11/11**（A1 进批量出操作栏/A2 条目出勾选圈/B1 单条勾选亮删除键/C1 全选 3 条/C2 按钮变「取消全选」/D1 切分类往返勾选保留/D2 切 TA 页签收窄/E1 删除弹确认/E2 确认可用/E3 收藏库未丢/F1 取消退出恢复；修复前产物 C1/C2/D1/E 组红＝有牙）。node --check 过；构建哨兵 653 全绿哑 0。【真机:待验证】（任意机型，用户反馈机型未知）：收藏页 → 顶栏勾选图标 → 勾几条/点全选 → 勾选不再自动消失、删除键亮起并显示条数 → 删除后条目消失且收藏夹其余内容完好；切「联系人的收藏」/分类再切回，我的勾选不丢 |
