### 2026-09-11 12:0x（功能大全补顶部分类切换 + 补全缺失功能——用户反馈「功能大全顶部没 tag 分类切换、里面功能不全」；**本次构建者：本会话**）
- [跨域 src/template.html（featurehub 页搜索框下加 `<div class="fhub-tabs" id="fhub-tags">` 锚点）+ src/js/feature-hub.js（HUB 补 5 条：游乐室 #more-arcade / 心情日记 #more-mood / 设备兼容诊断 #row-diagnostics / 屏幕适配诊断 #row-screen-diag / 功能诊断 #row-func-diag）]（AI-B 域 template.html 属跨域改动，理由见本条起点；**构建状态：已构建·本条目内收口**）。
- 功能：页面顶部新增分类胶囊 tag（全部/聊天传讯/字卡库/互动与心意/小游戏/手机桌面与工具/记录与统计/系统与设置），点击只显示对应分组；搜索时高亮回「全部」做全局过滤，清空搜索恢复所选分类。样式由 feature-hub.js 自注入 .fhub-tabs/.fhub-tag（不动 base.css）。
- 验证：（feature-hub.js node --check 过）→ --check-sentinels **653 全绿哑 0**（本批无哨兵改动）→ node build.mjs 哨兵 653/653、sw 15/15 → npm run verify 布局 **10/10**；产物 index.html 已含 fhub-tags 与 #more-arcade。
- 待对方处理：无。【真机:待验证】功能大全顶部出现 8 个分类胶囊可切换；游乐室/心情日报/三条诊断可点直达。

### 2026-09-11 11:3x（#313 心意集市「TA 送我礼物」总开关——用户要求「心愿集市和心意柜设置里新增禁止联系人给我送礼物，禁用这个功能」——**本次构建者：本会话，整树收口树上 #310/#311/#312 三批在途改动**）
- [AI-A 域 src/js/gift-shop.js（giftInOn 总开关：默认 0=禁止，接入 maybeAutoGift ①心愿单兑现/④随机送礼两闸口 + 设置面板首行开关 + 帮助文案 + 心愿单面板/购买弹窗提示按开关态切换）+ 跨域 build.mjs（哨兵 +1，理由：新功能防并行覆盖，跨域一词请知悉）+ WORKLOG.md + 产物 index.html/sw.js/version.json]（**构建状态：已构建·本条目内收口**）。
- 方案：设置存 GSTORE `market-wl-settings` 新键 giftInOn（默认 0＝按用户要求开箱即禁用，可随时打开恢复）；关闭时 TA 不会买礼物送你（①④ 不触发），②TA 自己买/③TA 加自己心愿单/我送礼给 TA 均不受影响。拍卖会「TA 回寄拍品」是独立玩法（进 🎒 收藏非心意柜），不在本开关范围，如需再议。
- 验证：node --check 过；--check-sentinels **653 全绿哑 0**（needle `st.wlOn && st.giftInOn && !capped` 文件内唯一）；构建哨兵全绿见构建输出。
- 待对方处理：无。树上 #310（单气泡拼字）/#311（词库·自建分组）/#312（开屏署名，template.html）三批在途，最终由 #310 会话整树收口构建（sw mochi-mtwcy3md）并提交。【真机:待验证】心意集市底部「设置」/心意柜 hero「⚙」→ 首行「TA 送我礼物」默认关闭；正常聊天不再收到 TA 礼物（心愿单兑现与随机送礼都停）；打开开关后恢复且概率行照常生效。

### 2026-09-11 11:1x（#312 开屏顶部补「设计与开发均由作者一个人独立完成」说明——跨域改动 src/template.html）
- 跨域改动 src/template.html（AI-B 域）：①splash-brandcard 署名下方加 splash-source-line「设计与开发均由作者一个人独立完成。」；②splash-notice 最顶部新增 splash-alert 置顶声明「原作者声明」（data-anti-scam="a" 非数字，避开 clock.js 的 1/2/3('s'/bulletin)——编号 3 会被 ensureBulletin 删除/覆盖故弃用）。**构建状态：未构建**，请构建者收口；产物同步需跑 node build.mjs。
- 待对方处理：构建收口（`node build.mjs` → 哨兵 + verify；sw 版本随构建改写）。

### 2026-09-11 11:0x（#311 删掉词典 tab 的【词库·自建】独立分组——用户点名——**本次构建者：本会话**）
- [AI-A 域 src/js/default-cards.js（mergeDictCustom 一处：自建词并入内置「词库」组+空组不再 push）+ 跨域 build.mjs（#301 哨兵 needle 随逻辑更新，不改数量 651）+ FIX-REGRESSION.md（+311 行）+ WORKLOG.md + 产物 index.html/sw.js/version.json]（**构建状态：已构建·本条目内收口**）。
- 用户要求「系统预设字卡→聊天默认字卡→【词库 自建】帮我删掉」。根因：mergeDictCustom 对词库侧 find('词库·自建') 永远落空（内置组名是「词库」），恒 push 独立「词库·自建」组且空数组也 push＝词典 tab 常驻（多为空的）【词库·自建】分组。
- 修复：自建词并入第一个「词库*」前缀组（quote-spell 切词消费不受影响）；语录/词库兜底组仅非空才创建；徽标与删除功能不变。验证：node --check 过；--check-sentinels 651 全绿哑 0；构建后哨兵全绿 + verify-quote-spell 复跑见下行。
- 待对方处理：无。【真机:待验证】词典 tab 无【词库·自建】独立分组；自建词（如有）落在「词库」组内带徽标可删。

### 2026-09-11 11:0x（#310 词典拼字「单气泡拼字」形态 + 普通字卡被截断修复——用户报障「正常句子被命中截断成断续造句」+ 三条随批需求（单气泡形态/同概率混合/带词典拼字小标签）——本次构建者：本会话）
- [AI-A 域 src/js/chat.js（replyOnce 拼字段重写：spellOne 单气泡分支）+ src/js/quote-spell.js（pick 返回 {segs,one}）+ src/js/reply-settings.js（DEFAULTS 四键 + migrateQsCcOld 迁移）+ src/template.html（「单气泡拼字」开关行+说明）+ src/pwa/notice.json（公告行同步）+ 跨域 build.mjs（哨兵 +3=651）+ FIX-REGRESSION.md（+310 行）+ tools/verify-quote-spell.mjs（C/H/D 组扩断言）+ WORKLOG.md + 产物 index.html/sw.js/version.json]（**构建状态：已构建·本条目内收口**）。
- 报障根因：#298「混用自定义字卡」（qs-cc）默认开——getPool() 普通文本字卡并入抽句池，25% 每回复掷一次，命中即整句被抽句替换、切词连发。功能按设计工作，默认值过宽。修复：qs-cc 默认 1→0（只用词典语录）+ migrateQsCcOld 存量旧值 1 一次性迁移为 0（reply-qs-cc-migrated 标记只跑一次，防覆盖用户此后自改）。
- 新增「单气泡拼字」（qs-one 默认开）：命中拼字后 50% 掷成单气泡形态——rep.spell.join(' ') 词间空格连成一张字卡进同一气泡 + addIn tag:'词典拼字'/tagNoDup:true（复用情绪 chip 链路持久化）；旧纯数组返回=逐词连发不变，两种形态共用同一拼字概率混合。设置页加开关行；关闭即全部逐词连发。
- 验证：verify-quote-spell **45/45**（H1 qs-one=0 纯数组/H2 120 掷双形态混合/H3 单气泡 segs 空格连卡、D2 四键/D3b 迁移/D7 tag 接线）；node --check 四文件过；--check-sentinels **651 全绿哑 0**；构建哨兵 651/651。
- 树上收口声明：src/js/dict-ext-data.js 在途改动（#301 v5 语素块禁+白名单过滤数据重生成，非本批触碰）已随本批构建入库——verify A3 38760 词/G1/G2 过、data 完整；README 来源行未动。
- 收口补充（构建后）：并行批 #311/#312/#313 已由 #313 会话先行提交（fa43802）；本会话最终复构建 sw mochi-mtwd18w2 带上其后在途的 feature-hub 分类 tag/新条目与 template 署名修订，verify 全绿（quote-spell 45/45、dom-shell 8/8、feature-hub 冒烟过——其 A1/A4 期望 79 条为 #313 会话未同步的旧数，新条目实际 84 条）；收尾提交含 dict-ext 注释/再生成脚本（tools/gen-dict-ext.mjs 可复现）与 verify-fav-batch.mjs。
- 收口补充（构建后）：树上随后落地的并行批 #311（default-cards.js 词库·自建分组取消）/ #312（template.html 开屏署名）/ #313（gift-shop.js giftInOn 礼物总开关）已随本批**最终构建**（sw mochi-mtwcy3md，哨兵 653/653 全绿哑 0）一并入库，产物级 verify 复跑：quote-spell 45/45、dom-shell 8/8、new-games 27/27、feature-hub/arcade-close 全过。git 提交按整树收口口径一次提交。
- 【真机:待验证】①普通聊天不再被抽普通字卡拼字（仅词典语录按 25%）；②回复设置「词典拼字」组四控件在位；③命中时约半数单气泡（空格连卡+「词典拼字」标签）半数逐词连发；④关「单气泡拼字」全逐词；⑤重进聊天标签仍在。
### 2026-09-11 10:2x（#309 连连看/消消乐「面板只有一条横线、打不开」根因修复——未开局空棋盘把开始覆盖层压成横线——本次构建者：本会话）
- [AI-A 域 src/css/chat-pages.css（.lk-stage/.m3-stage 各 1 行加 min-height:190px+align-items:center）+ 跨域 build.mjs（哨兵 +1=648）+ FIX-REGRESSION.md（+309 行）+ tools/verify-new-games.mjs（新增 B0 未开局断言；r0 局部变量适配并行重排）+ WORKLOG.md + 产物 index.html/sw.js/version.json]（**构建状态：已构建·本条目内收口·sw mochi-mtwbw0jq**）。
- 用户复报「依旧打不开正常使用，功能里ui只有一条横线啊」。对线上部署页无头复现实锤：未开局的连连看 stage 高 0，绝对定位其上的「开始对局」覆盖层被压成一条黑横线、面板塌成小条＝用户所见（消消乐同款）。#306 批只验了开局后形态——专项脚本都先点开始再断言，未开局态是验证盲区。另：用户截图里盖住半屏的「TA的小问题」问答弹窗是查岗/互动功能本体（点选项或 × 关闭），非故障。
- 修复（两行 CSS）：舞台最小高度 190px，开局后棋盘 230px+ 不受约束；gomoku 打开即建盘、auction/rps 内容天然有高度，均无需处理。验证：verify-new-games **27/27**（B0：未开局 stage≥150px 且开始按钮≥30px）、布局 10/10、构建哨兵 648 全绿哑 0、本地产物截图复验未开局面板完整。
- 待对方处理：无。【真机:待验证】打开连连看/消消乐不点开始 → 面板中部应有完整「开始对局」深色覆盖层而非一条黑线，点开始后棋盘正常展开。编号：#308 已被游乐室关闭批占用，本批顺延 #309（前条 #306 尾注「从 #308 顺延」作废，后续从 #310 顺延）。

### 2026-09-11 10:1x（#308 游乐室半框 × 关不掉——用户报障「游乐室右边的×点击关不掉，其他设备型号也有出现」——**本次构建者：本会话，热修收口**；顺带把树上 #301 v4 词典删专名批整树带上）
- [AI-A 域 src/js/arcade.js（1 行绑定 + 1 行标记注释）+ 跨域 build.mjs（哨兵 +1=647，理由：#308 修复防并行覆盖）+ tools/verify-arcade-close.mjs（新增 7 断言）+ FIX-REGRESSION.md（+308 行 + 设备索引「全机型通用」行）+ WORKLOG.md + 产物 index.html/sw.js/version.json]（**构建状态：已构建·本条目内热修收口·sw mochi-mtwbgxgs**）。
- 根因（arcade.js，#301 批新建文件引入）：第 18 行 `getElementById('arc-close')` 取到 closeBtn 后**全文件从未绑 click**——按钮纯摆设，`closePanel()` 永远不执行。无全局 `.poke-card-close` 委托（各半框自绑自己的 ×）、`closeArcadePanel` 无外部调用方＝无兜底路径。设备无关的漏绑，任何机型都复现，与用户「其他设备型号也有出现」吻合（红包等其余半框同款按钮都能关＝对照印证；`.poke-card[hidden]{display:none}` 本身正常）。
- 修复（最小改动，chat.js rpCloseBtn 同款模式）：`if (closeBtn) closeBtn.addEventListener('click', (e) => { e.stopPropagation(); closePanel(); });`，stopPropagation 防冒泡触发 more 面板委托。
- 验证：node --check 过；构建哨兵 **647/647 全绿哑 0**、sw 15/15；**verify-arcade-close 7/7**（A1-A2 src 逻辑锚 / B1 开面板×可见 / B2 点×收起主断言 / B3 关后重开再关 / B4 对照猜拳半框照常 / C1 零未捕获错误；首轮 B3 失败为脚本自身时序缺陷——同一 eval 里 click 后才读开态，已修脚本、产品零改动）。树上 #301 v4 词典批随本构建整树带上，verify-quote-spell 复跑 **40/40**（其台账声明完整，构建即收口）。
- 待对方处理：无。【真机:待验证】任一机型：聊天更多功能→小游戏→游乐室，点右上 × 半框应收起，重开再关仍正常；词典字卡无「中国/鄂州」类专名（#301 v4 随批请一并真机抽查）。

### 2026-09-11 09:1x（#306 小游戏 UI 收口：连连看/消消乐/拍卖会 UI 显示异常修复 + 全部小游戏通用全屏按钮——用户点名两事——**本次构建者：非本会话**（在途改动已随 f975221 被收口会话整树带上并构建，本会话只补台账））
- [AI-A 域 src/js/linkup.js、src/js/match3.js（fitBoard 扣 gap）+ src/js/gomoku.js、src/js/auction.js、src/js/connect-four.js、src/js/coop-mine.js、src/js/memory-game.js、src/js/fishing.js、src/js/chat.js（rps 段，各 +fsBtn/toggleFs 全屏接线）+ src/css/chat-main.css（头部防挤压）+ src/css/chat-pages.css（.game-fs 共享全屏类 + .au-bids btn2 可见样式 + snake-head-actions 收缩换行）+ src/template.html（9 个 ⛶ 按钮）+ 跨域 build.mjs（哨兵 +6）+ FIX-REGRESSION.md（+306 行）+ tools/verify-new-games.mjs（A9a-c/B10/B10b/B11/B12/B12b 扩断言）+ WORKLOG.md]（**构建状态：已构建·随 f975221，产物 sw mochi-mtw944sk**；改动全部已入库）。
- 用户报障「连连看 消消乐 心意拍卖会 ui都显示不正常」+「所有小游戏 都需要可以点击使用全屏的按钮」。无头 390×844 截图复现实锤三处：①连连看头部 6 控件把标题挤成一字一行竖排；②连连看/消消乐棋盘最右列被截断（fitBoard 以 floor(容器宽/列数) 定格宽、棋盘宽=格宽×列数，漏算 3px grid gap，实际总宽多 (列数-1)×3px 溢出右缘；五子棋无 gap 幸免＝用户未报的对照印证）；③拍卖会「不拍了」复用为深色覆盖层设计的 .pong-overlay-btn2（半透明白底+白字）落白卡＝白字白底隐形（实测在位可点只是看不见）。
- 修复（最小改动）：①.poke-card-head 加 flex-wrap+标题 nowrap + snake-head-actions 允许收缩且内部按钮可换行（放得下时形态零变化）；②linkup/match3 fitBoard 先扣 (列数-1)×3px 再取整、棋盘宽含 gap；③.au-bids .pong-overlay-btn2 独立可见样式（浅灰底深字，暗色反向）；④全屏：共享 .poke-card.game-fs（fixed 满视口，形态同 pong-fs，高度用 iOS 修复同款 min(var(--mochi-ios-h,100dvh),100dvh)），rps/fish/c4/ms/memory/gomoku/linkup/match3/auction 九面板头部各加 ⛶ + 各文件 toggle（open 时先退出防兄弟互斥残留，有 fitBoard 的五个切换后 60ms 重排）；pong/snake/brick 原有专属全屏不动。
- 验证：verify-new-games **26/26**（含新增：棋盘 scrollWidth≤stage、全屏 9/9 往返切换、全屏态棋盘重排不溢出、不拍了前景色非纯白；B4 原断言只扫第一个同款对＝flaky，改用 __lkDebug.allPairs 判「存在可连对」）；布局 10/10；四子棋 36/36 复跑过（首轮 35/36 为时序抖动）；node --check 十文件过。复验截图：连连看标题横排/8 列完整、消消乐全屏 46px 格满铺、拍卖会「不拍了」清晰可见。存量红项说明：verify-memory-flip B6b（TA 字卡回应）、verify-fishing-ui T7（图鉴 items 17≠14）与本批无关——本批在两文件的 hunk 仅全屏块，红项源于 #301-arcade/词典批的数据面漂移，且 ae0e949 收口时 verify:all 77 失败与基线持平。
- **编号撞号说明**：游乐室/游戏扩容批在 WORKLOG 自称「#306」，本批哨兵名与 FIX-REGRESSION 行也用了 #306（先登记先占号）——build.mjs 六条 `#306 小游戏…` 哨兵与 FIX-REGRESSION `| 306 |` 行均指**本 UI 批**；后续批次请从 #308 顺延。
- 待对方处理：无（本批改动已被收口会话随 f975221 构建入库；待 git push 部署后线上生效）。【真机:待验证】①连连看普通/挑战两档最右列完整、标题不再竖排；②消消乐第 8 列完整；③拍卖会「不拍了」可见可点；④九个游戏（猜拳/钓鱼/四子棋/扫雷/记忆翻牌/五子棋/连连看/消消乐/拍卖会）逐个点 ⛶ 进全屏、⤢ 退出，全屏中棋盘按大屏重排；⑤iOS PWA 下全屏高度不越界（game-fs 用 --mochi-ios-h 同款表达式）。

### 2026-09-11 08:5x（#301 手机端整页 UI 错乱根因修复——红包注释漏 `-->` 吞标签连锁打碎 .phone 手机壳，底部导航被 flex 居中挤出屏——本次构建者：本会话，热修收口）
- [AI-B 域 src/template.html（1 行）+ 跨域 build.mjs（注释配平守卫 + 哨兵 +1）+ tools/verify-dom-shell.mjs（新增 8 断言）+ FIX-REGRESSION.md（+301 行）+ WORKLOG.md + 产物 index.html/sw.js/version.json]（**构建状态：已构建·本条目内热修收口**）。
- 用户报障「手机端 UI 位置偏离/桌面底部导航变成竖着/UI 完全乱了」+ 截图（整页被放大左裁、右侧露灰底+白浮条）。无头复现实锤：390×844 标准视口下产物 `.phone` 落 x=-59、body scrollWidth=449、`.tabbar` 落 [331,449]（视口 390）＝截图形态，与机型/浏览器无关。
- 根因（追溯至 07a6cab 引入，4fcb012 收口构建起线上带病）：template.html「红包」注释行结尾误写 `*/}` 漏 `-->`，注释一路吞到下一个 `-->`（#297 注释尾），净吃掉「红包」标题行 + `<div class="set-group">` 开标签 + 整行 cs-rp-auto-prob；其后 `</div>` 连锁把 them-sec(function) → #page-chat-settings → **.phone 手机壳**逐一提前闭合，tabbar/sm-float/tc-mask/desk-msg 全部落成 body 直接子节点。html/body 是 `flex; justify-content:center` 横排居中：手机壳(390) 与 tabbar(min 118) 并排成 508px flex 行，居中＝壳 x=-59、tabbar 出屏右、右侧露灰底。**div 总数恰好配平**（文本计数恒定），肉眼/普通配平检查/verify-desk-layout 全查不出——只能靠真解析器；禁 JS 对照实证为纯解析层问题，与任何 JS 无关。
- 修复：①该注释行补 `-->`（1 行）；②build.mjs 构建期硬校验模板 `<!--`/`-->` 配平，失衡退出 1（防同类）；③哨兵 +1（红包注释闭合锚，删则构建拦截）；④新增 tools/verify-dom-shell.mjs：L1 禁 JS 纯解析断言（tabbar/sm-float/tc-mask/desk-msg 父节点=.phone、设置页数据分区在设置页内）+ L2 运行时断言（phone x=0、tabbar 完整在视口内、body 无水平溢出），verify-suite 自动发现。
- 验证：node build.mjs 构建过、哨兵全绿哑 0、verify-dom-shell 8/8（修复前产物 5/8 红＝脚本有牙）。
- 待对方处理：无（本批独立热修；树上另有 docs/阿里云OSS 文档与 #306 会话的 tools/verify-new-games.mjs 噪音，均不入包）。【真机:待验证】手机端刷新后：桌面整体位置回正、底部导航可见、右侧灰底消失。

### 2026-09-11（云端备份设计文档 v1.2：按真实代码库+浏览器协议三轮核查修订 v1.1-fixed，共 11 项——仅 docs 改动，零 src/零构建）
- [仅 docs/阿里云OSS云端备份-设计方案-fixed.md（未跟踪文件，本会话覆写），未碰任何 src，构建状态：不适用]（本次构建者：非本会话；#302 收口构建与本条无关）。
- 高危修正（原稿照做会 403/无法实现）：① `Date` 是浏览器禁设头（Fetch/XHR forbidden header names），签名改 `x-oss-date`——值占 StringToSign 时间戳位且同时计入 CanonicalizedOSSHeaders（对齐官方 ali-oss SDK signUtils.js，N5）；② 「301 解析 Location 重签」浏览器不可实现（自动跟随+跨域剥 Authorization+拿不到 Location）→ 降级为 List 探测预检 + 403 分类（⑤'）；③ ListObjects `?prefix=/max-keys=` 必须入 CanonicalizedResource 签名（N7）；④ 上传体钉死 `createJsonPack().finish()` Blob 单对象 `xhr.send(blob)`，Content-Type 修订为「发送头与签名串严格一致=application/json;charset=utf-8」（Blob 自动带 type，原「钉死为空」不可行）。
- 一致性/规范修正：存储键去冒号改 `cloud-oss-*` 七根键 + contacts.js `EXCLUDE` 登记为必需项（N8，同 fish-log 惯例防 migrateLegacy 误迁）；时钟偏差 RequestTimeTooSkewed 错误映射（N9）；自动备份 ≥24h 节流+当日退避 + `cloud-oss-lastfail` 状态键（N10）；§9 哨兵按 v3.27.x 铁律改逻辑锚点（N11，弃 window.cloudOss 函数名锚）；§9 手测 11 与 §11.1 N2 行同步修订版政策（清掉「静默降级」残留）；新增 §11.2 三轮核查清单（N5~N11+核对属实项，官方依据已注明）。
- 验证：docs-only 无需哨兵检查；全文 grep 复查 §C 悬空引用/旧行号 L423/L491/旧键名/`XMLHTTPRequest`/SSE-OOS 零残留（§11.2 小修行内对旧拼法的追溯性提及除外）。
- 待对方处理：无。文档状态=待审阅；实现时按 §6 清单（cloud-backup.js/data-backup.js/contacts.js 均属 AI-B 域，跨域规则照旧）。

### 2026-09-11（#302 朋友圈「贴纸回复」+「回忆闪回」 / #303 心情日记本——**本批完工，并按树上各批委托完成整树收口构建**）
- [AI-A 域 src/js/feed.js（#302）+ src/js/mood-diary.js（新建，#303）+ src/css/chat-pages.css（尾部追加两段样式）+ src/template.html（page-mood 页 + more-mood 工具按钮 + 功能介绍页 01 章三行/lg-count 22→25）+ 跨域 src/js/tabs.js（FULL_PAGES +page-mood）+ 跨域 src/js/mobile-adapt.js（FLOAT_SELECTORS +#feed-sticker-card，一词登记）+ 跨域 build.mjs（jsFiles +mood-diary.js，auction 后 sfx 前）+ src/pwa/notice.json（summary +1 条 hl）+ WORKLOG.md + 产物 index.html/sw.js/version.json]（**构建状态：已构建·sw mochi-mtw7pgd0**）。
- #302 贴纸回复：配图动态操作栏新增「贴纸」（postCardHtml/postCardHtmlAll 两模板同加），选贴纸面板复用评论条同一贴纸来源（TA 表情包+我的表情包，只收 dataURL，#feed-sticker-card 固定底部 poke-card）；贴纸 {src|emoji,x,y,ts,role,owner,authorName} 存 post.stickers（上限 5 张），contentHtmlFor 渲染 .feed-sticker 绝对定位叠图（x/y 区块百分比）；自己贴的可点撤回（openModal 确认）；贴后 TA 按 fd-comment-prob 概率、评论速度同源延迟回贴一张（70% 用 TA 表情包、否则随机可爱 emoji）并 addNotice('comment') 进朋友圈通知。
- #302 回忆闪回：render() 置顶「📅 回忆闪回 · N 年的今天」记忆卡（同月同日且年份更早的最新一条，缩略图+摘要）；点击定位原动态（超出「查看更早」窗口自动扩窗重渲染后 scrollIntoView + feed-hl 高亮 2.4s）；✕ 当天不再显示（键 feed-mem-dismiss=toDateString，activeStore）。
- #303 心情日记：mood-diary.js 自包含 IIFE（游戏批同款自绑定 more-mood，不改 chat.js），window.openMoodDiary 开 page-mood；9 款心情（😊🥰😄😌🤒😔😢😡😴，分值 5~1）+ 一句话备注，存 activeStore 'mood-diary'（{d:{YYYY-MM-DD:{m,n,ts}}}，IDB 镜像 activePrefix():mood-diary）；TA 心情=hash(桌面+日期) 伪随机（同日稳定，35% 概率跟随我当天）；月视图 SVG 双曲线（我粉/TA 蓝，缺天断线）+ 心情日历格（双表情）+ 统计（本月记录/连续天数/最常心情）；页脚返回按来源回聊天/桌面（window.__moodFrom）。
- 验证：node --check feed.js/mood-diary.js 过；构建哨兵 **638/638 全绿哑 0**、sw 15/15；产物 grep openMoodDiary/feed-sticker-card/回忆闪回 有命中；npm run verify:all **183 过 / 77 断言失败 / 1 超时**——失败 77 项与 #291 批基线完全持平＝零新增回归（超时 7→1，pong-balance 600s 为最慢项非撞车）。
- 本批同时整树带上树上全部已声明完整批：#299 应用锁提醒、#300 次数上限、#301 词典拼字（quote-spell.js/dict-ext-data.js）、#304 壁纸图库+feature-hub、#306 游戏扩容+arcade.js、#307 壁纸五项优化（该批已注明「本次构建者：#302 会话」）等；各批真机验证项见各自条目。?? diag-*.mjs 临时脚本未入库（用完即删）。
### 2026-09-11（#307 壁纸图库五项优化收口——用户点名 1-5 全做——**本次构建者：非本会话**（#302 会话），只改 src 已自检）
- [AI-A 域 src/js/chat-settings.js + AI-B 域 src/js/personalize.js + WORKLOG.md]（**构建状态：未构建**；node --check 两文件过；--check-sentinels 我触碰文件全绿，树上另有 1 条红项见「需要对方处理」）。
- 需求（承接 #304 优化清单，用户回复「12345」全做）：①图库面板内存瘦身；②壁纸/图库同步到全部联系人；③主题色从壁纸取色；④批量换图撤销+同图应用到全部图标；⑤图库删除 5 秒内可撤销。
- 方案：①新键 `cs-bg-active-id`/`phone-bg-active-id`（小键）记录当前生效壁纸条目，面板高亮/删除判断只看 id、渲染只解码缩略图；`csBgReconcileActive`/`pbgReconcileActive` 对账（稳态 1 次内存读+1 次字符串比对；升级首次/美化方案直写 cs-bg/phone-bg/预设覆盖失配时读一轮全图找回并回写 active-id）；上传/切换/迁移/撤销恢复各路径均写 active-id，`clearPhoneBg` 与聊天「移除壁纸」行同步清键。②两图库面板各加「把壁纸和图库同步到全部联系人」：`window.getContacts()` 遍历 + `xyStore('xy-home-v2:<cid>')` 逐键复制全图+缩略图+glist+active-id；桌面侧连带 pos 三键、目标端清 phone-bg-preset/phone-bg-solid；openModal pills 二次确认（明示覆盖对方）；仅一个联系人时提示跳过。③主题色弹窗加「从壁纸取色」pill：24×24 降采样+4bit/通道桶计数，按「出现次数×饱和度」挑主色桶（纯平均会得灰杂色；接近纯白/纯黑像素不参与），先取桌面壁纸、其次聊天壁纸，取完直接应用并 toast 回显色值。④openIconMenu 批量分支换图前留底 `{key, prev}`（prev=null=原默认 SVG）进 `window.__iconBatchUndo`；新批量开始清栈（只保最近一次）；row-icon-batch 检测到遗留栈先弹「新批量/撤销上次」选择；撤销=逐键还原 prev（null 则 remove）+ `restoreAppIcons()` 重绘。另：图标菜单加「同图应用到全部图标」pill（各图标自身透明度设置保留）。⑤两图库删除改单击即删+面板底部「撤销」条（留底挂面板元素 `m.__undoItem`/`m.__undoTimer`，5 秒作废；每次删除覆盖上一条只保最近一张；撤销恢复全图/缩略图/列表位次及「当时是否生效壁纸」状态）。
- 验证：node --check chat-settings.js/personalize.js 过；`node build.mjs --check-sentinels` 我触碰文件相关锚点全绿哑 0（树上 1 条红项与本案无关，见下）。真机建议：开满 12 张的图库面板看流畅度、双联系人互同步、从壁纸取色观感、批量换图→撤销上次、删除→5 秒内撤销。
- **需要对方处理（收口构建者）**：`src/js/default-cards.js` 在途重写（+111 行，非本批触碰）使 #301 哨兵 needle `['语录', pq.concat(qs)],` 在 src 中 0 命中，当前 `--check-sentinels` 报红、构建会被拦截——若「自建词条并入词典分类」逻辑还在只是形态变了，请在 build.mjs 把该 needle 更新为新逻辑锚点；若逻辑真丢了请补回。

### 2026-09-11（#306 小游戏丰富度十连：TA 邀请扩容 / TA 气泡 / 消消乐特殊棋子 / 连连看连击+主题 / 拍卖会蒙面+回寄+转赠 / 五子棋悔棋 / 游乐室 / 幸运游戏 / 跨游戏掉落 / verify——用户点名清单 1,2,4~11——**本次构建者：非本会话**，src 已改待收口；#302 批正以构建者身份收口在途批，本批不构建不提交防撞车）
- [AI-A 域 src/js/chat.js + src/js/p2-features.js + src/js/gomoku.js + src/js/linkup.js + src/js/match3.js（三消核心重写，加特殊棋子）+ src/js/auction.js + src/js/arcade.js（新建，游乐室）+ src/template.html + src/css/chat-pages.css + 跨域 src/js/mobile-adapt.js（FLOAT 两清单 +1 #chat-arcade-panel，跨域一词登记请知悉）+ 跨域 build.mjs（jsFiles +1：arcade.js 在 auction 后、mood-diary 前；未动 FIX_SENTINELS）+ tools/verify-new-games.mjs（新增 18 断言）+ WORKLOG.md]（**构建状态：未构建**；node --check 全过、--check-sentinels **638 全绿哑 0**、verify-new-games **18/18**）。
- 各项落点：①**TA 邀请扩容**——chat.js GINV_POOL 六选一（+gomoku/linkup/match3/auction 文案）、INVITE_KIND_META/openInvitePanelFor 四款直达（open 前各游戏文件自收兄弟半框）、p2-features GAME_KIND 补标签供小游戏记录识别；②**TA 气泡**——四款游戏 stage 内 .tg-bubble DOM 冒泡（1.6s 自散）：五子棋堵成五点/胜负、连连看 smart 找对/wild 点错、消消乐连锁≥3/炸弹/彩虹/手滑、拍卖会跟价/bluff 戳破/落槌；③**消消乐特殊棋子**——值域扩 10+c 炸弹（被消除炸 3×3 可连锁）、20 彩虹（换色全消/双彩虹随机两色；不参与连线、TA 枚举不用），四连生成炸弹、五连生成彩虹（仅交换首段，放交换格优先），findRuns/clearWithSpecials/simulateClear 全链适配；④**连连看**——连击（连续配对计数，点错/提示断连，最高连击折算默契上限 +10，信息栏 🔥）+ 图案主题包（水果/甜品/海洋 ×15 emoji，头部 🎨 循环，per-cid 记住）；⑤**拍卖会**——4 件 mystery 蒙面拍品（🎁+描述猜，落槌揭晓）、TA 拍走 2~4 天后回寄（prefix:au-gifts-pending，开面板+每 30s 投递，chatAddIn 到账留言）、🎒 列表内「送TA」转赠（recordGiftBox 写心意柜 out 记录 + 系统消息 + TA 感谢）、幸运日 TA 心理价位 ×0.8（手松）；⑥**五子棋悔棋**——每局 1 次（头部 ↩，撤 TA+自己各一手，自己回合可用）；⑦**游乐室 arcade.js**——战绩聚合（pong/c4/gomoku/memory/linkup/match3/auction 七键只读）+ 11 枚徽章 + 12 件限定摆件图鉴 + 幸运横幅，入口在小游戏分类首位；⑧**幸运游戏**——arcadeLuckyKey 按日期种子固定一款、arcadeMult ×2（游戏发放处接入+结算行「🍀 幸运 ×2」）、arcadeMarkLuckyPlayed 记幸运星徽章；拍卖会幸运语义=TA 手松；⑨**跨游戏掉落**——arcadeTryDrop 8% 概率掉未收藏摆件进 prefix:arcade-drops（五子棋/连连看/消消乐胜利时调用，结算行+气泡）；花园/房间无物品 API，摆件收藏即串联形态；⑩**verify+公告**——verify-new-games.mjs 18 断言全过（静态接线 8 + 无头行为 9 + 零未捕获错误 1，拍卖会用模拟钱包断言真实扣款路径）。
- 接线清单（收口者请核对）：build.mjs jsFiles +arcade.js；template.html more-arcade（game 分类首位）+ chat-arcade-panel + gk-undo/lk-theme 按钮 + 功能介绍页 05 章（14 项/13 项入口行 + 游乐室/邀请两行）；mobile-adapt FLOAT 两清单 + #chat-arcade-panel（共 5 新面板 ×2 在册）。**notice.json 不动**：曾插「🆕 小游戏上新」小节，用户拍板撤下（让玩家自己发现），本批不涉开屏公告。
- 验证：node --check 八文件 + build.mjs + verify 脚本全过；`node build.mjs --check-sentinels` **638 全绿哑 0**；`node tools/verify-new-games.mjs` **18/18**（首跑 14/18，四项失败均为脚本自身断言缺陷——A4 正则漏 g/B4 相邻恰好同色/B6 测试覆盖炸弹格/B7 误用 over 当落槌标志——修脚本后全绿，产品零改动）。
- 待对方处理：①构建者收口（与 #299~#305 一起整树带上；本批与 #302/#303 同文件 chat-pages.css/template.html/mobile-adapt.js 均为分区块追加，无覆盖冲突）；②真机验证：TA 邀请六款出现且同意直达、四款游戏对局中气泡出现、消消乐四连出💥五连出🌈且炸弹可连锁、连连看🎨换肤配对不乱、拍卖会蒙面拍品落槌揭晓/TA 回寄 2-4 天到账/🎒转赠后心意柜出现「我送TA」记录、五子棋悔棋每局 1 次、游乐室七行战绩+徽章点亮、幸运日结算行带「🍀 ×2」。
- 编号说明：原拟 #301 已被并行批（词典拼字）占用，#302~#305 亦被占，本批顺延 **#306**。

### 2026-09-11（#305 新增【功能大全】：设置内可搜索的全功能直达索引页（灵感来源：回音机，用户点名要同款）——**本次构建者：非本会话**，src 已改待收口；树上多批在途请统一收口）
- [AI-B 域 src/js/feature-hub.js（新建）+ src/template.html（设置页入口行 #row-featurehub + page-featurehub 页面壳 + 功能介绍页灵感来源行）+ src/js/tabs.js（FULL_PAGES +1：page-featurehub）+ build.mjs（jsFiles +1：feature-hub.js 在 mobile-adapt 前）+ src/pwa/notice.json（公告 +1）+ README.md（灵感来源 +1）+ tools/verify-feature-hub.mjs（新增）+ WORKLOG.md]（**构建状态：未构建**；node --check 过、--check-sentinels 638 全绿哑 0、verify-feature-hub 9/9）。
- 需求：设置里新增和回音机【功能大全】一样的功能。形态：可搜索的功能索引页，点条目直达对应功能（与 page-about 静态说明互补）；入口 = 设置 → 功能大全。
- 方案：79 条功能分 7 组（聊天传讯/字卡库/互动与心意/小游戏/手机桌面与工具/记录与统计/系统与设置）；**跳转 = 链式 .click() 既有入口元素**（桌面图标 .app[data-app=…] / 设置行 #row-* / 聊天 more 面板按钮 #more-* / 字卡库 #li-*，全部同步 handler 且首元素负责切页），不重实现任何打开逻辑，功能入口变了改 HUB 数组即可；无可靠直达的（引用回复、应用锁）点击弹位置提示 toast。搜索按名称/描述/关键词过滤。样式全复用 set-row/gs-title/card-search，零新增 CSS，深色模式自动适配。许可合规：README + 功能介绍页均已标注「灵感来自回音机」。
- 验证：跳转目标 77 个选择器逐一在 src 校验存在（tongpin/shenshou/water/eat/piggy/pomo/memo 7 个为 makeApp 运行时注入图标已单独确认绑定）；tools/verify-feature-hub.mjs **9/9**（内存按 build.mjs 同序拼装 src、不写产物：渲染 7 组 79 条、搜索「红包」=1 条、清空恢复、花园页跳转、猜拳聊天面板跳转、where 条目 toast、返回设置页）。
- 待收口者：①本批纯增量，与树上在途各批无交集；②功能介绍页 lg-count 有错标（21 群聊 标注5/实际6；01/08 本会话核对时疑为其他批编辑中未定稿态），收口时顺手核一遍；③真机验证：设置 → 功能大全 → 搜索「红包」点条目（应进聊天+红包面板）、点「花园」（应进花园页）、点「引用回复」（应弹位置提示）、返回键可逐级回退。

### 2026-09-11（#304 壁纸图库×2 + 批量上传图标 + 主题色手输兜底——用户四项需求——**本次构建者：非本会话**（#302 会话已声明构建者，src 完整可随其收口一并构建），本会话只改 src 已自检）
- [AI-A 域 src/js/chat-settings.js + AI-B 域 src/js/personalize.js + src/template.html + src/pwa/notice.json + 跨域 src/js/mobile-adapt.js（FLOAT_SELECTORS 末尾 +2：#cs-bg-panel、#phone-bg-gallery-panel，一词登记）+ WORKLOG.md]（**构建状态：未构建**；node --check 三 JS 过、notice.json JSON 解析过、--check-sentinels 638 全绿哑 0）。
- 需求：①聊天设置美化：聊天壁纸可上传多张保存、点击切换（原只能一张）；②桌面美化：手机背景同样多张保存+点击切换；③桌面美化新增批量上传图标图片；④桌面美化「自定义主题颜色」需详细说明——部分手机看不见颜色/调不了（原生取色器打不开）。
- 方案：①聊天壁纸图库——键 `cs-bg-glist`（id 数组小键）+ `cs-bg-item-<id>`（全图，>200KB 走 idb 只进 IDB 既有通道）+ `cs-bg-item-thb-<id>`（240px 缩略图，面板只解码小图防 N×4MB 同时解码卡顿）；「上传聊天壁纸」行改为开图库面板（3 列缩略图、点图切换、× 两击删除、上传可多选、清除当前），当前生效键仍是 `cs-bg`＝聊天页渲染/美化方案导出/渲染防护零改动；旧单图首次开面板自动收为第 1 张；容量 12 张。②桌面壁纸图库完全同构（`phone-bg-glist`/`phone-bg-item-<id>`/`-thb-`，面板 #phone-bg-gallery-panel），上传沿用 compressImageFit+phoneBgMaxSide；「壁纸定位与缩放」无当前壁纸时改开图库；启动恢复已存壁纸的 sanitizeBg+applyPhoneBg 两行保留未动。③批量图标：新增 #row-icon-batch 行——一次多选→逐张 256px 压缩→挂 `window.__iconBatchQ`→enterDecor 装修模式→按顺序点图标每点一个换一张（openIconMenu 顶部队列分支，透明度沿用已存值）；退出装修未点完队列作废并提示；再点该行可取消。④主题色：弹窗加 staticText 详细说明（色块一键选用最可靠/系统取色器部分手机不支持/手输 #RRGGBB）+「手动输入色值」pill 二级弹窗（openHexColorModal：容错补 #、正则校验、ctl.stay+hint 原地报错）；「边看边调」抽屉取色行加独立色块预览+当前色值文字+「手输」按钮（input[type=color] 渲染成文本框的手机也能看见并改色）；壁纸「自定义纯色」同加手输兜底；template 主题色行加 sub 说明。
- 文案：template 功能介绍页两处 lic-li（桌面美化/聊天美化各+1 行）+ 开屏公告 notice.json summary +1 条 hl。
- 验证：node --check chat-settings/personalize/mobile-adapt 全过；notice.json 可解析；`node build.mjs --check-sentinels` 638 全绿哑 0、sw 源锚点 13/13。新增键/面板 id 全库 grep 无撞名。待构建者随树收口构建+复跑 verify:all；真机建议：多张壁纸切换、批量换图、内置浏览器里主题色手输。
- 备注 Personalize 域 SCOPE/方案导出/2436 重置清单未动：重置只清当前生效键、图库图片保留（与「清除当前壁纸（图库保留）」语义一致）；图库键自动进 data-backup 全量备份（按前缀整包）。

### 2026-09-11（#302 朋友圈「贴纸回复」+「回忆闪回」 / #303 心情日记本——用户点名两个趣味功能——**本次构建者：本会话（AI-B）**，收口树上 #299/#300/#301 等在途批 + 本批）
- [AI-A 域 src/js/feed.js（#302）+ src/js/mood-diary.js（新建，#303）+ src/css/chat-pages.css + src/template.html + 跨域 src/js/tabs.js（FULL_PAGES +page-mood）+ 跨域 src/js/mobile-adapt.js（FLOAT_SELECTORS +#feed-sticker-card）+ 跨域 build.mjs（jsFiles +mood-diary.js）+ WORKLOG.md + 产物]。
- 需求：①朋友圈照片可「贴纸回复」（在配图上贴表情贴纸，TA 概率回贴）；②打开朋友圈顶部显示「那年今天」回忆卡；③心情日记：每天选心情表情+一句话，月视图两人心情曲线对照。
- 进度：src 开发中，详见本文件后续完工条目。**构建收口时请整树带上（#299 applock 提醒、#300 次数上限、#301 词典半批、auction 一行差量均在树上，各条目已声明完整）。**

### 2026-09-11（#301 词典拼字+系统预设字卡新增【词典】大分类——用户需求「联系人给我发消息，可用开关使用词典拼字：语录词库随机抽句、随机概率是否拼字发送、按词典切词，内容来源可选 自定义字卡+词典语录混用/只用词典语录」——本批大部分改动被并行收口 a10c60a 扫入历史（详下），剩余半批 src 已改待再收口）
- [AI-A 域 src/js/quote-spell.js（新建）+ src/js/chat.js + src/js/default-cards.js + src/js/default-cards-data.js + src/js/reply-settings.js + src/template.html + README.md + src/pwa/notice.json + 跨域 build.mjs（jsFiles+哨兵+2=638，理由：新文件登记+新功能防并行覆盖）+ WORKLOG.md]（**构建状态：未构建——a10c60a 已带上本批一半，收口时把当前工作区 9 文件改动整批带上即完整**）。
- **⚠️ a10c60a 扫入清单（线上惰性无害）**：chat.js 拼字钩子、quote-spell.js、default-cards.js dict 注册、dict 数据旧快照（缺切词补丁）已入库；但 build.mjs 的 jsFiles 登记+哨兵、reply-settings.js qs 三键、template.html 词典 tab/设置组、README/notice 当时未保存完→未入库 → a10c60a 产物未加载 quote-spell.js，window.quoteSpellPick 为 undefined，chat.js 钩子 `window.quoteSpellPick && …` 静默跳过＝**线上无此功能也无损伤**；下次构建若不带当前 build.mjs，#298 两条哨兵会按设计构建失败拦住。
- **功能**：①【词典】大分类进系统预设字卡页（dc-tabs 词典 tab + dc-cat-dict 分类开关 + 单卡开关，数据=DEFAULT_CARD_DATA.dict：「语录」128 句=拼字抽句库（受分类/单卡开关控制），「词库」760 词=切词词典（FMM 正向最大匹配，最长 4 字，英文/数字整段，标点吸附前段；只决定切分不作消息内容））。②回复设置→聊天 tab 新增「词典拼字」组：qs-en 总开关（默认开）、qs-prob 拼字概率（默认 25%，0=不触发）、qs-cc 混用自定义字卡（默认开=字卡池+语录合并抽句，关=只用词典语录）。③chat.js replyOnce 在 genOneReply 后调 quoteSpellPick(c)：命中则把本条回复换成抽句按词切分逐条连发（首条带引用+通知，后续条 silent，条间打字指示 0.9~1.8s，每条 sameCid 守卫，parts 附末条）；未命中/切不出 2~7 段照常单条回复，下游收藏/心情分享/情绪链/撤回/统计全链路复用＝与现有回复引擎完全兼容；群聊不涉及。
- **#301 v2 追加（用户二轮要求：词典在系统预设字卡顶部 + 里面真的是词典、新增内容自由）**：④词典 tab 移到**第一位**（BASE_KEYS ['dict',…] + 模板 sel，进页默认落词典 tab）。⑤词典 tab 内**自由新增/删除词条**：新增行（输入框+存为语录/存为词/删自建）仅词典 tab 显示；存储=全局命名空间 `xy-home-v2:dict-custom-quotes / dict-custom-words`（词典是语言资源，不随桌面隔离，随备份走）；每次从内置快照 PRESET_DICT 重建合并进「语录/词库」分组（mergeDictCustom，重复调用安全），列表自建词条标「自建」徽标（makeNode 读 window.__dictCustomSet）；自建语录进拼字抽句池、自建词进切词词典（词长上限 8 字，quote-spell dictMax 动态窗口 + quoteSpellResetDict 缓存重建）；删除走 openModal 输原文精确匹配（内置词条不可删、走单卡关闭）。哨兵 +1=**639**。
- **#301 v3 追加（用户三轮要求：要真词典，760 词太少）**：⑥新增 `src/js/dict-ext-data.js`（jsFiles 登记，在 default-cards-data.js 与 default-cards.js 之间）——**真实中文分词词典 3.8 万词**，筛选自 jieba 词库 dict.txt（MIT License，README 已标注来源）：2~8 字纯汉字、按词频降序、与基础词库去重，按字数分组「词库·双字 27881 / 词库·三字 6953 / 词库·四字 2792 / 词库·五字及以上 374」；文件内词串以空格分隔存储（301KB，载入 split 还原，低于 iOS 500KB 单块解析上限）。⑦PRESET_DICT 改为 dict+dict_ext 全量快照，自建词条并组改为「语录·自建」「词库·自建」专属分组；quote-spell 组名前缀匹配（语录* 进抽句池、词库* 进切词）。词典 tab 合计 **38,760 词条 + 128 语录**，虚拟列表渲染（#91 架构原生支撑）。#301 哨兵 needle 随 mergeDictCustom 重构更新为 `base.find(g => g[0] === '词库·自建')`（计数仍 639）。verify 扩到 **38/38**（新增：扩展词库 2 万级/A5 分组/E8 jsFiles/F3 天气/F4 北京·火锅 真词典切分断言）。生成脚本用完即删（tools/tmp-gen-dict.mjs 等 3 个已清理），词库再生成方法已写入 dict-ext-data.js 文件头注释。
- **#301 v4 追加（用户四轮要求：删掉中国/天安门/人民政府/鄂州市这类地点——情侣用的词典不该有这些）**：⑧按 jieba 词性标注重筛扩展词库——剔除 ns 地名 17397 / nt 机构 4379 / nr,nrt 人名 78313 / nz 其他专名 10372，另加显式黑名单兜底（中国/天安门/人民政府/鄂州/国务院/解放军/国家/祖国/首都等 13 词）；普通常用词按词频降序回填至 3.8 万（双字 29248/三字 5253/四字 3374/五字+ 125，297KB）。生成脚本内含黑名单自检+断言固化：verify 扩到 **40/40**（G1 十二个地名/机构词不在基础+扩展词典、G2 火锅/天气/旅行 等普通词仍在、F4 反转为「北京不再整词出现」）。tools/tmp-gen-dict2.mjs 用完即删。
- **#301 v5 追加（用户五轮要求：词典全量审计，只要日常+情侣可用词）**：⑨全量按类目审计（政治 238/军事 267/法律犯罪 36/金融 43/宗教神鬼 66/病灾死亡 54/古代帝制 64/IT 23 条命中）后升级过滤——语素块禁扩到 39 字（+兵/窃/武/贪/奸/淫/妓/嫖/毒/枪/灾/祸/骗），词级剔除补 武器/贪污/爆炸/骗子 等投机型漏网与宗教/帝制/病灾精确词，白名单保日常（仿佛/冠军/亚军/赌气/打赌/胆小鬼/死心塌地/凶巴巴/贪吃/贪玩/贪睡——弹钢琴/偷偷/商量 等靠语素不误伤自然保留）；共剔 9245+126 条，回填后仍 3.8 万词（双字 29008/三字 5386/四字 3484/五字+ 122，298KB）。生成脚本内置黑名单自检（政治/军事/犯罪/金融/宗教/帝制/病灾/IT 26 样例词必须不在）+白名单在位检查，用完即删；verify G1 扩为 33 词永久断言（防再生成回流），**40/40 全过**、哨兵 648 全绿哑 0。
- **#301 v6 追加（用户六轮点名：五字组仍有 本行政区域/自然保护区/马克思主义/万平方公里/发展中国家/民主集中制/毛主席纪念堂 + 后续继续深挖）**：⑩**「五字及以上」分组整体删除**（jieba 的 5+ 字词几乎全是政治/行政/机构/计量长词，情侣拼字用不上；长句由 2~4 字词组合切分），只收 2~4 字词。⑪语素块禁再 +14：宪/革命/阶级/剥削/立法/司法/执法/普法/国有/私有/公有/共产/司令/劳模/少先/国务/纪委/主义/纪念/人民/民主/区域/平方/主席/马克思/国际，词级补 政治委员会/人大/国务/纪委/国共/社会学术/迷信占卜/商业经济/综合治理等 ~80 词；白名单再保 恐龙/欺负/抱怨/埋怨/怨言/怨气/纪念品/纪念册（情侣日常不误伤）。终扫政治残留 0 条。⑫生成脚本转正为 **tools/gen-dict-ext.mjs（永久维护工具，词库再生成用）**——此前 tmp-* 版被并行会话按规清理过一次，转正防再丢。词典终态：**38,000 词（双字 29131/三字 5438/四字 3431）+ 128 语录，297KB**。verify 扩到 **45/45 全过**（G1 禁词 49 个：地名/机构/政治/军事/犯罪/宗教/帝制/病灾/IT/行政长词全类目）。【注意】本批改动在 src，页面要等收口构建后才能看到。
- **事故记录**：本会话期间 C 盘满（ENOSPC 0 字节），default-cards-data.js 曾被写崩成 0 字节，已 git 恢复重套；已清 Temp 释放 22.6GB（现余 ~21GB）。若其它会话 07:40~08:10 间遇写盘失败即此因。
- **编号**：本批自称 #301——「#298 词典拼字」的编号在 a10c60a commit message 里已被「通话概率说明」占用（非 WORKLOG 台账冲突，仅批号让位，与本批无关）。
- 验证：node --check ×7 过（含 dict-ext-data.js）；tools/verify-quote-spell.mjs **38/38 全过**（数据规模/无重复/全部语录 2~7 段/拼接还原/词组命中/英文整段/标点吸附/三档闸门/源码接线 + v2：词典 tab 首位默认选中/新增删除控件/mergeDictCustom/自建徽标/缓存重建/哨兵在位/5 字自建词整词切分 + v3：扩展词库 2 万级/F3「天气」/F4「北京·火锅」真词典切分）；node build.mjs --check-sentinels **639 全绿哑 0**。行为层断言纯 node 可复跑：node tools/verify-quote-spell.mjs。
- 待对方处理：构建者收口构建+提交（本批与 #299~#306 多批同文件 template.html/build.mjs/notice.json 均为分区块追加，无覆盖冲突；收口前照例核对各批完整性）；【真机:待验证】①回复设置开「词典拼字」→发消息后联系人按概率连发逐词气泡、句尾标点附末词；②概率调 0 → 永不拼字；③关「混用自定义字卡」→只发词典语录；④字卡库→系统预设字卡：**顶部第一个 tab 就是词典**（3.8 万词条+128 语录，虚拟列表应流畅）、逐张关语录后不再被抽中、关「词典」分类开关→语录不参与拼字；⑤词典 tab 新增一条语录→拼字抽句池出现它；新增一个 5~8 字的词→含该词的句子按整词切开；删自建→词条消失且不再参与；⑥「保存全部桌面联系人设置」能把 qs 三键同步到所有桌面；⑦低端机开词典 tab 滚动/搜索性能（38k 词条虚拟列表，-headless 390×844 布局 verify 可跑）。

### 2026-09-11（#291 用户报障 OPPO Reno6 5G + 雨见浏览器(Firefox152)：①「聊天界面滚动特别卡、回答不了卡片问题、退不出去要大退」②「经期组件文字有重叠」——本次构建者：本会话（AI-B），收口 #291 本批 + 树上 #289~#300 全部已声明完整在途批）
- [跨域改动（AI-A 名下 src/css/home.css，理由：经期桌面卡文字重叠为纯 CSS 几何修复，dpd-inner 底部预留+两行显式行高，period.js 无需动）+ AI-B 域 build.mjs/FIX-REGRESSION.md/WORKLOG.md]（**改动文件：src/css/home.css、build.mjs（#291 哨兵 +1、v3.32.x 老 gd-opts needle 随 #295 单行化同步校正）、FIX-REGRESSION.md（+291 行）、tools/verify-period-desk-card.mjs（新增 6 断言）、WORKLOG.md + 产物 index.html/sw.js/version.json**；构建状态：**已构建·整树收口**）。
- ①聊天滚动卡顿：诊断实证 IDB cmtqatpcu6de:chat-msgs=48.1MB/2529 条＝#283 同族根因（历史语音整份内联、每次落盘 clone 整包＝长任务+GC 风暴＝滚动卡/点不动/退不出），修复已随并行批 74e5a00 入库、本批产物带上；更新后首次打开聊天页约 12s 后台跑一次性语音令牌化迁移，chat-msgs 应降至 MB 级。另诊断错误环 `redeclaration of let JSInterface`/`JSInterface.sysonSelectTextChange is not a function` 在 src 全库 grep 零命中＝雨见浏览器自身注入脚本报的错，与本应用无关、无需处理。
- ②经期桌面卡文字重叠根因（详见 FIX-REGRESSION #291）：160px 卡内 .dpd-inner 绝对居中无底部预留，Gecko 默认行高更高，dpd-sub 尾边越过绝对定位进度条说明 dpd-bar-cap 顶边＝叠字（Chrome 擦边幸免故仅部分浏览器现形）。修复：dpd-inner `padding-bottom:26px` + dpd-label/dpd-sub `line-height:1.25`。
- 验证：构建哨兵 636/636 全绿哑 0、sw 15/15；verify-period-desk-card **6/6**（360×800 无头实测间距 11.6px/Gecko 加压 7px，修复前算术叠字 -1.4px＝脚本有牙）；verify-voice-pool 9/9、verify-desk-beauty 17/17、verify-desk-layout-guard 10/10、verify-applock 61/61；verify:all 汇总 173 过/77 断言失败/2 环境/7 超时（失败为在册存量红项口径，本批触碰区域脚本全绿）。收口备注：#295 把 gd-opts 限高行并成单行致 v3.32.x 老 needle 失配，已随代码形态同步校正（构建拦截生效一次＝防线有牙）。
- 【真机:待验证】（OPPO Reno6/雨见 优先）：①更新后打开聊天页→滚动恢复、能答字卡、能退出，诊断 chat-msgs 降到 MB 级；②桌面经期卡 经期中/排卵期/未记录 三态文字不再叠字；③打卡后刷新重进按钮直接「✓ 已打卡」（#289）。
- 编号说明：顺延 #291（#287~#290 已被并行批占用；#292~#300 各批 src 本批一并收口入库，各自真机验证项见各条目）。观察项（不在本批动）：cc-groups-public 公用字卡库已达 33.73MB，chatcard 侧已有解析缓存，若后续仍有点开卡顿再立项收口。

### 2026-09-11（#300 两项每日次数上限可设：TA 申请心意币上限 + TA 发红包上限——本次构建者：非本会话，src 已改待收口）
- [AI-A 域 src/js/chat.js + src/js/chat-settings.js + src/js/p2-features.js + WORKLOG.md]（**构建状态：未构建**；node --check 三文件过，--check-sentinels 636 全绿哑 0）。
- 需求：①联系人申请心意币新增「每日申请上限次数」可自由设置；②红包新增「联系人每日发红包上限次数」可自由设置。现状：发红包原为写死 5 次/日（chat.js trySystemAutoSend `rpDailyCount() >= 5`），且 template.html 已预置设置行 #cs-rp-daily-max 但两端均未接线；申请则计数器（ml2_ask_daily_*）已有但完全无上限。
- 方案：①红包——chat-settings.js 新增 #cs-rp-daily-max 行点击弹窗（存对话键 cs-rp-daily-max，每联系人独立，默认 5，0=不限，显示「N 次/不限」），chat.js 新增 rpDailyMax() 读同键替换写死 5；②申请——存钱罐右上角设置弹窗由三步扩为四步，第四步「申请每日上限（0-999，0=不限）」存根键 piggy-coin-ask-limit（默认 0=不限，向后兼容），chat.js trySystemAskMochi 开头读 askDailyMax() 闸门（>0 时 askDailyCount 达限即早退，不再 incr/入账/发卡）。上限计数复用既有当日计数器，跨天自动清零逻辑不变。
- 验证：node --check ×3 过；node build.mjs --check-sentinels 全绿。待构建者收口构建 + 复跑 verify:all。
- 待对方处理：template.html 里 #cs-rp-daily-max 行注释标「v3.27.x #297」与现行编号批次冲突（#297 已被小游戏四连批占用），本次功能按 **#300** 登记，template 注释未改（template.html 属 AI-B 域，请对方顺手改号或忽略）。

### 2026-09-11（#299 开屏问答暗号弹窗提示 + 问答通过后一次性提醒【应用锁】密码功能——文案两处已随 a10c60a 入库；提醒功能为 a10c60a 之后新改，**src 已改待再收口**）
- [src/js/applock.js（①qaSkipAsk / 管理验证弹窗文案两行——已随 a10c60a 构建入库；②新增 K_PIN_HINT 键 + pinHintThen()，evalLock 问答通过回调接入一次性提醒——**在途未构建**）+ WORKLOG.md]。
- 需求：①跳过开屏问答的暗号弹窗新增提示「时间就在开屏里可以找到，非常简单，解出请勿二传，这么简单真没必要二传。不输入暗号也不影响正常使用。」；②暗号文案明确为「99 + mochi 字卡生日的 4 位数字，一共 6 个数字」（跳过弹窗 + 管理验证弹窗两处同改）；③答对简答题（或输暗号）通过问答门后，若本机未设数字密码锁，提醒一次新增的【应用锁】自定义锁屏密码功能（防偷看聊天记录）。
- 方案：①②纯文案；③applock.js 新增 `K_PIN_HINT='applock-pinhint'`（根命名空间全局键，每台设备只提示一次）+ `pinHintThen(next)`——已设密码或已提示过直接放行，否则 info 弹窗（标题「小提醒：应用锁」，盾牌图标）说明功能与入口「设置 → 应用锁」，点「知道了」继续原解锁流程（密码锁或完成解锁）；evalLock 的 qaStart 回调改为经 pinHintThen 包裹。问答门已跳过（qaskip=1）的本机不进 qaStart，不会被打扰。
- 验证：node --check 过；`node build.mjs --check-sentinels` 636 全绿哑 0；verify-applock **61/61**（现 src 口径）。**待收口者**：a10c60a 产物不含 pinHintThen（grep 0 命中），请随下批重建；真机复验：答对问答→出提醒弹窗一次→刷新不再出；已设密码锁的本机不弹。

### 2026-09-11（#298 通话设置说明文案重写：用户报「总有人看不懂触发概率/为什么高概率」——本次构建者：非本会话，src 已改待收口）
- [AI-B 域 src/template.html（仅 page-call-settings 的「通话概率」提示块一行重写，未动任何结构/锚点）+ WORKLOG.md]（**构建状态：未构建**——本会话磁盘临时区满、shell 不可用，无法执行 build.mjs/--check-sentinels，请收口者照常构建；本改动纯文案不涉及哨兵 needle）。
- 需求：通话设置里用户看不懂来电概率怎么触发、为什么设得不高却来电很勤。方案：提示块重写为分块问答式——①触发机制列全 3 类「机会」（TA 每次回复掷一次 / TA 每次主动发消息掷一次 / 应用开着后台每 1~2 分钟兜底掷一次）+ 5 分钟冷却 + 设 0 永不来电；②新增「为什么设得不高却感觉来得挺勤」块：每次机会单独掷骰、非每日总概率，聊天越勤机会越多，举 15%×聊 20 句≈20 次掷骰的例子，调小 10 倍≈来电少 10 倍；③明确标注来电概率与通话中挂断概率均支持 0.01 粒度、最低可设 0.01（0.0几 都行），0.01≈每 100 次机会才响 1 次；接听/忙线/拒接/改数值两块保留原文。逻辑零改动（call.js / reply-settings.js 未动），无行为风险。

### 2026-09-11（#297 小游戏四连上新：五子棋 / 连连看 / 消消乐 / 心意币拍卖会——用户点名四款加入【聊天更多功能→小游戏】——本次构建者：非本会话，src 已改待收口）
- [AI-A 域 src/js/gomoku.js、src/js/linkup.js、src/js/match3.js、src/js/auction.js（四款全部新建）+ src/template.html + src/css/chat-pages.css + 跨域 src/js/mobile-adapt.js（FLOAT_SELECTORS / FLOAT_PANEL_SELECTORS 各 +4 个面板 id，跨域一词登记请知悉）+ 跨域 build.mjs（**仅 jsFiles 数组 +4**，未动 FIX_SENTINELS，理由：新文件必须登记才会被打包）/WORKLOG.md]（**构建状态：未构建**；发现 07:11 有并行产物 mtw55k0b 在途未台账、树上 #289~#296 多批未收口，本批按「禁止并行构建/提交」停手，收口时随树统一走）。顺带修复：chat-pages.css 文件尾一行 GBK 乱码注释按原文修复回「聊天记录待回标签」，无样式改动。
- 四款玩法（骨架全复用现有游戏：.poke-card 半框 + TA 行为状态机 + activePrefix 战绩 + chatAddSystem/getInteractPool 字卡回应 + giftWalletChange 双方同额日封顶 ¥104 + Web Audio 音效 + 各文件自绑定入口不改 chat.js）：
  - **五子棋 gomoku.js**：11×11 迷你盘 vs TA（手机半框放得下、节奏更快），三档难度同四子棋权重；TA = 候选打分（成五/活四/冲四/活三棋型分级，防守权重 0.9）+ 每回合重抽 认真/正常/放水/失误，玩家成五点连续无视超底线必堵；先手/战绩规则同四子棋，special:'gomoku'。
  - **连连看 linkup.js**：合作轮流消除（经典 ≤2 拐角连线，棋盘外算空），难度 6×5/8×6/10×6；TA 每回合抽 smart 全盘扫对 / memory 只用「记得」的牌 / wild 随缘乱点（点错抖一下再认真找）；提示×3、洗牌×2、死锁自动洗牌，默契分结算，special:'linkup'。
  - **消消乐 match3.js**：8×8 轮流交换相邻格凑三连、重力补落 + 连锁连消，共冲目标分（300/600/1000，头部下拉）；TA = 枚举全部可消步打分后按状态走 最优/前五随机/放水/手滑（无效交换先抖一下）；死锁自动洗牌，默契分结算，special:'match3'。
  - **心意币拍卖会 auction.js**：每场 3 件拍品轮番举牌（＋¥1 / ＋¥5 / ＋¥13.14 / 不拍了）；TA 每件暗抽心理价位（底价 × 状态系数：eager 志在必得 / normal 常规 / stingy 抠门 / bluff 虚张声势，bluff 价位一到 35% 概率戳破）；我赢 = **真实扣款**（giftWalletGet/Set 直改，同心意集市购买路径，不进赚钱流水）+ 收进 🎒 拍品收藏（prefix:auction-items，头部 🎒 可查）；TA 拍走 / 流拍不动账本；余额不足自动禁用加价键；special:'auction'。
- 接线清单（收口者请核对）：build.mjs jsFiles 已含四新文件（memory-game.js 之后、sfx.js 之前）；template.html 新增 more-gomoku / more-linkup / more-match3 / more-auction 四按钮（data-mcat="game"）+ chat-gomoku / chat-linkup / chat-match3 / chat-auction 四面板 + 功能介绍页 05 章（lg-count 9→13、入口行改 12 款、新增四行玩法说明、更多功能总览行同步）；mobile-adapt 两清单已登记（背景滚动锁 + 键盘停靠）；各游戏文件自带完整 hideIds + MutationObserver 兄弟互斥清单（含全部新旧半框 23 项）。
- 验证：node --check 四新文件 + mobile-adapt.js / build.mjs 全过；`node build.mjs --check-sentinels` **636 全绿哑 0**（四新文件未删任何既有锚点，与 #296 批口径一致）。
- 收口状态：本批已随 #291 批整树收口入库并提交（a10c60a，产物 sw mochi-mtw6pg23，构建/提交由 #291 构建者完成，commit 已点名 #297）；**收口后又追加一行 src 修订**——auction.js 切联系人清空进行中场次（🎒 按联系人桌面隔离，防跨桌续拍把拍品收进别桌收藏），该行在 a10c60a 之后的树上，node --check 过、--check-sentinels 复跑 636 全绿哑 0，**待下次构建带上（一行差量）**。
- 待对方处理：①下次收口顺带带上上述 auction.js 一行差量；②notice.json 公告未补（同 #292/#296 口径，收口可顺带）；③真机验证：四游戏出现在 小游戏 分类、与既有半框互斥无叠开；五子棋 TA 会堵玩家的成五点；连连看无可连对时自动洗牌；消消乐无效交换回弹 + 连锁计分；拍卖会 余额不足禁用加价、落槌后心意柜余额同步减少、TA 拍走不扣款；四款结束均写聊天系统消息 + TA 回应。
- 编号说明：原拟 #291~#294 已被并行批占用（#291 经期卡 / #292 问问ta / #293+#294 来电与通知头像 / #295 决定卡滚动 / #296 回复设置开关），本批改用 **#297** 一号覆盖四款（mobile-adapt.js / chat-pages.css 注释已同步改号）。

### 2026-09-11（#296 回复设置补「联系人主动写信/联系人主动发朋友圈」两个总开关——用户需求「【回复设置】里缺少关闭联系人写信/关闭联系人发布朋友圈的设置按钮」——本次构建者：非本会话，src 已改待收口）
- [AI-A 域 src/js/reply-settings.js + src/js/mail.js + src/js/feed.js + src/template.html + 跨域 build.mjs（哨兵+2=636，仅 FIX_SENTINELS 数组尾部追加，理由：新功能防并行覆盖登记）/FIX-REGRESSION.md（+296 一行）/WORKLOG.md]（**构建状态：未构建**；树上 #289/#290/#292/#293/#294/#295 等多批在途，收口前请先核对 git status）。
- 现状/根因：写信概率 ml-write-prob 走 mailCfg 的 prob() 兜底——存 0 会被回退默认 30（防旧坏数据的保护反而堵死「概率=0 关闭」的路）；朋友圈概率虽支持 0 但无一键总开关。
- 方案：①新增 `ml-write-en` / `fd-post-en` 总开关（默认开，per-cid 桌面隔离，DEFAULTS 登记）；②设置页信箱面板「联系人主动写信」组首行、朋友圈面板「TA 发朋友圈设置」组首行各加开关行（复用既有 .toggle 壳）；③两键进 syncUI/开关监听/saveCurrentReplyPage 三处清单（「保存全部桌面联系人设置」自动带上）；④闸门在触发链首行——mail.js maybeIncomingLetterFor `if (!cfg.writeEn) return;`（writeEn 走 mailCfg 裸读不过 prob() 兜底，mailCfgFor per-cid 覆盖表补该对），feed.js maybeAutoPostFor `if (!cfg.postEn) return;`（postEn 走 feedCfgFor 键表+num 出口）。回信（ml-reply-*）、每周摸鱼小结、朋友圈点赞/评论互动均不受影响。
- 验证：node --check 四文件过；`node build.mjs --check-sentinels` 636 全绿哑 0（#296 两锚各自文件唯一逻辑锚）。
- 待对方处理：构建者收口构建+提交；notice.json 功能公告未补（收口时可顺带或下一批补，不阻塞）。【真机:待验证】①回复设置-信箱关「联系人主动写信」保存→TA 不再主动来信，重新打开恢复；②回复设置-朋友圈关「联系人主动发朋友圈」→TA 不再自动发动态；③开关默认开，存量用户升级后行为不变；④「保存全部桌面联系人设置」能把两开关同步到所有桌面。

### 2026-09-11（#295 帮我决定/群聊决定「自定义选项」键盘弹出期整卡无法上滑——用户报障「输入法弹窗打开时自定义选项页面无法上滑」——本次构建者：非本会话，src 已改待收口）
- [AI-A 域 src/css/chat-main.css + 跨域 build.mjs（哨兵+2=634，理由：#295 防覆盖登记，仅数组尾部追加）/FIX-REGRESSION.md（+295）/WORKLOG.md]（**改动文件：如列；构建状态：未构建**；git status 另有 #289~#294 等多批在途未收口，收口前请先核对）。
- 根因（同族第三次：v3.23 modal-textarea → v3.25 选项框高度上限 → 本次）：安卓 textarea 被 mobile-adapt 转成 contenteditable .ce-box（dec-opts/gd-opts 带 max-height:176px + overflow-y:auto 自成滚动容器）后，`overscroll-behavior:contain` 把「框内无内容可滚/已到边界」的滚动链也一并拦断；键盘弹出期 kbDockPanels 把面板重锚到输入栏上方、可用高度骤减最需要滑动，而手指自然落在正聚焦的选项框上——手势被 ce-box 吃掉，外层 .poke-card-scroll 收不到滚动＝整卡无法上滑。
- 修复（两处一行改动）：`overscroll-behavior:contain → auto`——框内有溢出仍先在框内滚（v3.25 高度上限修复不回退），无溢出/到边界时滚动链放行给面板。.modal-textarea.ce-box 的 contain 不动（其拦的是穿透到背景，语义不同）。
- 验证：`node build.mjs --check-sentinels` 634 条全绿哑 0（含 #295 两条新锚点，css/chat-main.css 内各自唯一）。三犯家族（modal-textarea/dec-opts/键盘期面板），如真机复发按熔断补 tools/verify-*.mjs。
- 待对方处理：请构建者收口构建+提交；【真机:待验证】安卓任一机型：帮我决定/群聊决定→自定义选项→点选项输入框弹键盘→手指在输入框上滑动，整卡应能滚动；选项多于框高时仍先框内滚。

### 2026-09-11（#293 后台来电点开通知不弹通话面板 + #294 后台通知右侧头像全黑——用户连报「马上打开浏览器也没有电话弹窗，接不到」「朋友圈通知最右头像显示全黑」——本次构建者：非本会话，src 已改待收口）
- [AI-B 域 src/js/call.js + src/js/bg-keep.js + build.mjs（哨兵+2=632）/FIX-REGRESSION.md（+293/+294）/WORKLOG.md]（**改动文件：如列；构建状态：未构建**；只追加未触碰他批块，git status 另有 personalize.js/#289/#290、ta-ask.js/#292、home.css/#291 等在途批次未查明收口状态，收口前请先核对）。
- **#293 不弹通话面板**：根因＝resumeHeldCall 重响条件 `h.cid === __activeCid` 要求回到应用时恰好停在来电归属桌面——跨桌面来电（#204 查岗队列路径）落别的桌面永不成立；冷启动时 __activeCid 还在等 IDB 回填校正（bootCallResume 在 restore-done 后 1.2s，correctCidFromIdb 异步可能更晚）同样不成立→两条路都静默补「未接听」。修复＝cid 不匹配时若归属 cid 在名册内（同 applyCidCorrection 口径防切空命名空间）先 `setActiveContact(h.cid)` 切到归属桌面再 `incomingCall(true)` 重响；超时/已在通话/名册不含仍走原补未接。
- **#294 头像全黑**：根因＝makeAvatarThumb canvas 缩放后直接 `toDataURL('image/jpeg')`——JPEG 无透明通道且 canvas 未铺底色，带透明区域的头像透明像素落成纯黑＝通知右侧大图标全黑方块；顺带零固有尺寸图回退原图防 1×1。修复＝drawImage 前 `fillRect` 铺白底（`ctx.fillStyle='#ffffff'`），降级链全不动。
- 验证：node --check call.js/bg-keep.js 过；`node build.mjs --check-sentinels` 632 条全绿哑 0（含 #293/#294 新锚点，各自文件唯一）。verify 脚本未新增（两条均首犯，无复发史；如复发再按熔断补 tools/verify-*.mjs）。
- 待对方处理：请构建者收口构建+提交；【真机:待验证】①后台来电通知弹出后点开浏览器（无论停在哪个桌面/冷启动）→ 应自动切到来电联系人桌面并弹来电面板可接听，超 3 分钟仍正常补未接不误弹；②带透明区域的头像触发后台通知 → 右侧头像正常不再全黑。

### 2026-09-11（#292 问问ta新增「批量导入单选题」+「问卷答题结束时间」——用户需求：批量导入时【】里为问题、其后每行一个选项，批量问联系人问题，并可设置问卷答题截止时间——本次构建者：非本会话，src 已改待收口）
- [AI-A 域 ta-ask.js + template.html + 跨域 build.mjs]（**改动文件：src/js/ta-ask.js（批量导入解析 + deadline 设置/闸门）、src/template.html（设置行 + 批量导入占位文案）、build.mjs（跨域：哨兵+2=630，理由：新功能防并行覆盖登记，仅数组尾部追加）、WORKLOG.md；构建状态：未构建**；本次构建者：非本会话，树上 #289/#290（personalize.js）等在途批未收口，收口前请先核对 git status）。
- **批量导入单选题**：批量导入解析改为状态机——`【问题】`开头行开一道单选题，其后到下一个【】之间的每行为选项；普通行仍按「一行一个问题」导入（完全向后兼容）。选项 ≥2 才落 type:'single'+options（不足 2 个按文字题导入，只取问题行），空【】跳过。导入 toast 改「已导入 N 个问题（含 M 道单选题）到…」。单选题落库后走既有 pushAsk 单选链路（进聊天卡片就地点选，无弹窗），无需改 chat.js。
- **问卷答题结束时间**：设置区新增 `datetime-local` 行（#ta-ask-deadline + 清除按钮 #ta-ask-deadline-clear），存 `settings.deadline`（毫秒时间戳，0=未设置）。过点后三处闸门：①maybeTriggerTAAsk 自动触发直接 return；②triggerTaAskNow 手动提问 toast 拦截；③作答统一拦截——openAskReply（文字题弹窗）入口 + chatAskReply 包装层（单选点选/聊天内联两条路径都经此）toast「已过问卷答题结束时间，不能再作答」。deskCk 查岗卡豁免（其本就不属于问卷）。渲染回显走 renderAskSettings（fmtDeadlineLocal 本地格式）。
- 跨域改动 build.mjs：FIX_SENTINELS 尾部追加 #292 两条（解析锚点 + 作答闸门锚点，均在自己 file 内唯一，--check-sentinels 哑哨兵 0）。
- **追加（同批，用户补需求「单选题输入栏最右没有和帮我决定一样的 ❌ 快捷清除」）**：①单题添加表单（askAddFormHtml）的问题输入框与选项 textarea、②批量导入 textarea（template.html）三处输入栏包 `.dec-inp-wrap` + `.dec-inp-clear` ✕（复用帮我决定全局 CSS/暗色款）；chat-pages.css 补 3 行包装层 flex 接管（.ta-inp-flex/.ta-opts-flex + 框内 padding-right:38px）；清空逻辑同 decision.js（含安卓 contenteditable 幽灵框 textContent 清空 + toast「已清空」）；ta-type 切换显隐的 ce-box 兜底从 nextElementSibling 改按父容器扫（textarea 旁边现在是清空按钮不是 ce-box）。改动文件补：src/css/chat-pages.css（AI-A 域）。
- 验证：node --check ta-ask.js/build.mjs 过；`node build.mjs --check-sentinels` 630 全绿哑 0。【真机:待验证】①批量导入「【今晚想吃什么？】/火锅/烧烤/寿司」→ 我的添加出现「单选·3选项」行，聊天发出后卡片可就地点选；②混合普通行仍按一行一题导入；③设置结束时间为过去时间后「让TA现在问一次」被拦、点旧询问卡不能再答；④清除后恢复。
- 待对方处理：构建者收口构建+提交；notice.json 功能公告未补（收口时可顺带或在下一批补，不阻塞）。
- 编号说明：#291 已被并行批（经期卡防重叠）占用，本批顺延 **#292**。

### 2026-09-11（#289 摸鱼打卡刷新后要求重打 + #290 摸鱼天数自动修正——用户报障「桌面组件【摸鱼打卡】有人手机总是刷新重进后需要重新打卡」并追问「天数错了怎么自动修正」——本次构建者：非本会话，src 已改待收口）
- [AI-B 域 personalize.js + build.mjs/FIX-REGRESSION.md]（**改动文件：src/js/personalize.js（每日打卡初始化段 + 摸鱼天数段）、build.mjs（哨兵+2=628）、FIX-REGRESSION.md（+289/+290 两行）、WORKLOG.md；构建状态：未构建**；本次构建者：非本会话，git status 另有 chat.js/device.js/music-player.js/sw.js 等在途改动未查明归属，收口前请先核对）。
- 根因：打卡按钮「已打卡」状态只在 personalize.js 模块初始化读一次 `store.get('checkin')`，早于启动回填（idbRestore）完成——LS 写失败/配额满/IDB 为主机型此刻键未进内存缓存，按钮渲染成未打卡，之后数据补齐也没人回头刷新（AGENTS.md 已知坑「回填完成前读到的键可能为空」）。健康机型 LS 直读命中所以只有「有人手机」复现。数据层无害：logFish 按自然日去重，天数不虚增。
- 方案：按钮同步抽成 `syncCheckinBtn()`（双向：已打卡置 done/跨天复位），初始化一次 + `mochi-restore-done` / `mochi-wrj-heal` 各再同步按钮与 `updateFishDays()`（摸鱼天数同族时序）。contact-switched 既有重判断与点击去重语义不动。
- **#290 天数自动修正**（同段实现）：①`normalizeFishLog()`——fish-log 只留合法 YYYY-MM-DD + Set 去重，有变化才写回（防回填前空读 blind-wipe）；logFish/updateFishDays 走规范化出口；②restore-done/wrj-heal 后再跑 `migrateFishLogGlobal(false)`+规范化+刷显示（治「各桌面旧副本只存 IDB、模块加载时合并扑空＝永久漏算」）。IDB 旧快照遮蔽不重复设防（retainValue/wrj 既有兜底）。
- 验证：node --check personalize.js/build.mjs 过；`node build.mjs --check-sentinels` 全绿哑 0（627 条，含 #289/#290 新锚点）。
- 待对方处理：请构建者收口构建+提交；真机验证＝打卡后刷新重进按钮应直接「✓ 已打卡」、摸鱼天数不回退。


### 2026-09-11（#287 群聊点头像拍一拍 + #288 群聊美化视图卡片化重设计——用户连报「群聊里我无法点击联系人的头像对联系人使用拍一拍」「群聊的美化设置不完整，和聊天里的美化设置不一样（ui可以重新设计）」——本次构建者：本会话（收口），已构建 sw mochi-mtvstgaz）
- [AI-A 域 group-chat.js/group-chat.css/template.html + 跨域 mobile-adapt.js + build.mjs/FIX-REGRESSION.md/WORKLOG.md]（**改动文件：src/js/group-chat.js、src/css/group-chat.css、src/template.html、src/js/mobile-adapt.js（跨域）、build.mjs（哨兵+2=627）、FIX-REGRESSION.md（+287/+288 两行）、tools/verify-poke-map.mjs（新增 17 断言）、tools/verify-poke-gc.mjs（新增 10 断言）、WORKLOG.md + 产物 index.html/sw.js/version.json**；构建状态：**已构建·sw mochi-mtvstgaz**。⚠️ 编号交接备注：本批 build.mjs 哨兵两行先于并行批 #283 收口（74e5a00）落在树上，被该批选择性提交随 build.mjs 一并带入历史（当时产物未含本批 src，未误发）；本批收口构建补齐产物与本台账，哨兵/台账/src/产物就此对齐）。
- **#287 拍一拍**：需求/反馈＝单聊点头像即拍（chat.js av click→openPokeCard），群聊从未实现（git -S 全史无）＝功能缺口。根因＝group-chat.js renderMsg 头像 fillAv 后无点击绑定、群聊页无拍一拍面板（#poke-card 属聊天页）。方案＝①template 群聊页内加 #gc-poke-card（复用 .poke-card 壳样式）；②成员消息（side in 且有 cid）头像 click→gcOpenPokeCard(cid)，cursor/title 同单聊；③字卡＝预设6 + getPokeCards 公用 + 当前桌面「我的拍一拍」（poke-groups-mine/存量 poke-user-mine，activeStore 读，去重平铺）；④发送文本 gcPokeTextOf 与 sendPoke 人称映射同构（含「句首我+句中有你」整句保留分支），落库定稿（同 gcPokeText 口径）；⑤拍后 memberReply 反应链（拍回/回复走群聊回复设置概率）；⑥开面板先收气泡菜单+input.blur，点外/✕ 关闭。验证＝行为等价性 tools/verify-poke-map.mjs：16 种字卡形态 单聊渲染 vs 群聊定稿全等（17/17）；端到端 tools/verify-poke-gc.mjs：真实 UI 全链路 **10/10**（发消息→成员回复→点头像弹「拍一拍·成员名」→选字卡→special=poke 落库+居中渲染+面板关→被拍成员反应→零未捕获错误）。跨域改动 mobile-adapt.js：FLOAT_SELECTORS + FLOAT_PANEL_SELECTORS 各加 '#gc-poke-card'（背景滚动锁/键盘停靠登记，同 #poke-card 家族，跨域一词登记请知悉）。【真机:待验证】群聊点成员头像→弹面板→选卡→居中「我的昵称 XX成员」→被拍成员按概率拍回/回复；「我的拍一拍」自定义字卡出现在面板。
- **#288 美化视图重设计**：需求＝群聊美化 UI 与聊天设置页落差（实测截图：聊天侧整页卡片+图标，群聊侧半框纯文字行）＝纯渲染层落后，选项覆盖两侧本已齐（群聊 v3.28.x 起还多 时间轴颜色/正在输入颜色/发送按钮组）。方案＝renderBeautyView 重写为 gs-title 分组 + set-group.glass 卡片 + set-row gc-set-row 图标行（17 个线性 SVG 图标同聊天设置风格，.val 值回显），低对比警告条收进卡片；**兼容点全保留**：行类含 gc-set-row、.txt 仅行名（verify-gc-color 按文本精确点行）、全部行标签/弹窗调用不变、beautyRow 保留给主视图成员昵称行；group-chat.css 增 .gc-set-body 内布局 CSS + 暗色 active（.glass 走变量暗色自动适配）。验证＝截图预览（临时脚本，已删）六组卡片/图标/值回显/警告条与聊天设置页同观感；构建后复跑 verify-gc-settings **34/34** + verify-gc-color **14/14**（R1/R2 即按新行标记点行，兼容实测有效）。【真机:待验证】群聊设置→美化聊天 六组卡片渲染+暗色+每行弹窗与重设计前一致。
- 编号说明：#280~#286 已被并行批占用（cf605fe #280/#281、#282 viewport、#283 语音池、#284 音乐 abort、#285 开关体检、#286 核实），本批顺延 **#287/#288**。
- 验证汇总：node --check 过；构建哨兵 **625/625 全绿哑 0**、sw 15/15；verify-gc-settings **34/34**、verify-gc-color **14/14**、verify-poke-map **17/17**、verify-poke-gc **10/10**。
- 全量套件基线（本批收口后首跑，供后续批对照）：verify:all @ 3e82516＝**185 通过 / 72 断言失败 / 0 环境 / 0 撞车 / 1 超时（258 项）**。本批改动半径定向复跑（gc/poke/group/beauty 过滤 18 项）红项 4 个经对照全为存量：gc-more(3)/gc-pool-scope(1)/gc-send(1)＝#130 在册口径；verify-poke-emoji-tabs 6/15 在基线 74e5a00 worktree 红绿对照**逐字同红**＝存量，本批零新增回归。72 项全量红为长期在库积压（含 pong-balance 694s/avatar-ta-change 超时等长跑型），按套件指引逐批对照台账清理，不属本批。
- 待对方处理：无。 【真机:待验证】见上两条。

### 2026-09-11（#286 用户报障 iPhone16P/Safari「歌曲复制出来听不了、歌单复制只复制出一首」+「字卡批量导入页面固定最顶无法滑动」——本会话只核实零改动：报障全部出自 9/7 旧版，修复均已上线）
- [AI-A 域核实任务，未碰任何 src]（**改动文件：仅 WORKLOG.md；构建状态：未构建，无 src 改动**；本次构建者：非本会话）。
- 核实链：两份诊断均 v3.26.520 构建 ts=1788740934736＝**2026-09-07 08:28 旧版**，早于其后四批音乐/弹窗修复。①诊断自带错误环 `floorPick` ReferenceError（page-room）→ #255（a736c98 9/7 23:44，room.js floorPick 补齐）已修；②「歌单导入不全/只有 10 首」→ #263（499a1c5 9/10 19:07，fetchNeteasePlaylist 多源并发取最长，实测 62 首歌单全量）已修，已验证 origin/main 产物含该源（grep api.qijieya.cn playlist =1）；③「链接导入的 VIP/付费歌无法播放」是产品既定语义（UI 已注明），非 bug；④「字卡批量导入弹窗固定最顶」同批 #255 弹窗放大/顶对齐相关改动在用户版本里根本不存在，无从对号。最新版识别实测（node 直跑最新正则）：music.163.com 各形态歌单/单曲链接、纯数字 ID、分享混排文本全部正确识别。
- 结论与处理：与 #260 vivo 停旧版同族——先让手机更新到最新（顶部更新条「刷新使用新版」或关全部标签页重开；#273/#279 冷加载自动升级会自动跟版）再复测。遗留观察项（新版复测仍现才立项）：iOS 网易云 App 分享的 `163cn.tv` 短链无 CORS 头（curl 实测 302 无 ACAO），浏览器端无法跟随解析出歌曲/歌单 ID，属平台限制；用户应改用含完整 music.163.com 链接的分享文本。
- 待对方处理：无。树上 #282~#285 在途批次不受影响。
- 编号说明：顺延 #286（#285 本会话开工时刚被 AI-B 占用）。

### 2026-09-11（#285 用户报障「小米17Pro+Edge152 诊断信息有错误」（明说多机型同现）：「开关持久化体检」读取列恒缺失＝#234 首修 SP 拼法仍是双冒号——本次构建者：非本会话（树上 #282/#283/#284 在途，待收口构建顺带））
- [AI-B 域 src/js/device.js + 登记资产 build.mjs（#234 哨兵名更新，needle 不变）FIX-REGRESSION.md（#234 行补复发修正）tools/verify-exclude-feed-schemes.mjs（V6 断言反向修正）WORKLOG.md]（构建状态：**未构建，需构建者收口**；只追加未触碰他批块）。
- 需求/反馈：小米17Pro（25098PN5AC）+Edge152 诊断 v3.26.539 输出「cs-voice-send：LS=\ 读取=缺失」；用户强调其他设备型号也有、勿覆盖式修改。
- 根因（装置性、与机型无关）：#234 首修把 xyStore 前缀写成 SP = G + ':' + cid，但本文件 G='xy-home-v2:' 已带尾冒号，拼出 xy-home-v2::<cid>:: 双冒号键，「读取（内存优先）」列恒缺失——首修只是把双冒号从尾部挪到中间，且 V6 静态断言钉死的正是笔误形态（验证断言写成了病句同款）。
- 方案（最小改动，零机型分支）：① device.js SP = G + cid（等价 contacts.js 家族 GNS+':'+cid 正确形态）；② V6 断言反向修正：断 SP=G+cid 在位、断 G+':'+cid 与 xyStore(P) 两款病句清零；③ #234 哨兵名更新（needle=window.xyStore(SP).get(short) 不变、仍唯一）。
- 验证：node --check 过；tools/verify-exclude-feed-schemes.mjs 34/34（含新增反向断言）；node build.mjs --check-sentinels 623 全绿哑 0。修复后该列将如实反映 xyStore 内存优先读值（cs-voice-send 应显示读取=\）。【真机:待验证】（任意机型）：诊断「开关持久化体检」读取列不再恒缺失、与 LS 列一致。
- 编号说明：顺延 #285（276~284 已占）。

### 2026-09-11（#283 用户报障「vivo S60+Chrome 经常卡、按不动，其他机型也有」：聊天语音令牌化——79.2MB chat-msgs 每次落盘 clone 整包是长任务/卡顿元凶——本次构建者：本会话（顺收口在途 #282/#284））
- [跨域改动（AI-A 名下 chat.js，理由：语音令牌化主链在消息 normalize/播放，媒体池既有架构延伸；media-pool.js 无归属默认共享）+ AI-B 域 build.mjs/FIX-REGRESSION.md/WORKLOG.md]（**改动文件：src/js/chat.js、src/js/media-pool.js（已随并行批入库）、build.mjs、FIX-REGRESSION.md、tools/verify-voice-pool.mjs（新增）、WORKLOG.md**；构建状态：**本条目后立即构建收口**）。
- 需求/反馈：vivo S60/V2571A + Chrome149 诊断 v3.26.536（帧率≈25fps、堆 307.1MB、长任务 50~405ms 集中在切后台/收键盘；IDB chat-msgs=79.2MB/2277 条）；用户明说「其他设备型号也有出现」，要求不要覆盖修改引发跨机型回归。
- 根因（与机型/浏览器无关）：#142 池 v1 `mochiMediaTokenize` 只收 `data:image/`——历史语音/语音字卡以「名称|||data:audio;base64…」整份内联在消息 text。图片早已令牌化（本机历史里已有 @@m: 引用），79.2MB 大头即漏收的语音：每次 saveMsgs 空闲落盘/离页 flushSave 都对整包 structured clone＝低端机 100~400ms 长任务+GC 风暴＝「经常卡、按不动」。触发面也窄：pass 只挂 mochi-restore-done（IDB 挂起时永不到达）与 contact-switched，不切桌面的设备永不收敛。
- 方案（零机型分支，#142/#186/#202 语义全保留）：① 池收音频：tokenize 放行 data:audio/；音频不进 map 热缓存（迁移不把省下的内存吃回去）；新增 `mochiMediaExpandAsync` 按需 idbGet（校验 data:audio/ 前缀，缺失→null 占位）。② normalize pass 令牌化三种形态：「名称|||data:audio/…」→「名称|||@@m:hash」/裸 data:audio→「|||@@m:hash」/m.voice 字段；迁移期每 32 条分批 mochiMediaFlush（writeBuf/单事务封顶+#186 回滚账除名防堆尖峰），写池失败回滚不变。③ fillVoiceBubble 点按遇令牌先异步取回再播（_vExp 防连点；池缺失 toast「语音数据缺失」），聊天/收藏共用。④ 收藏语音识别加令牌形态、tokenizeFavList 收语音、搜索结果「[语音] 名称」、normCell 给裸形态补 type=voice。⑤ 冷启动收敛：loadMsgs 权威就绪后 12s 调度 pass。语音内容唯一=去重不缩总库，但落盘 clone 从 79MB→44 字符引用，长任务根因消除。
- 验证：node --check 过；`node build.mjs --check-sentinels` 621 全绿哑 0（新增 #283 哨兵 ×6：池收音频/语音令牌化/播放取回/分批冲池/冷启动触发/收藏识别）；`tools/verify-voice-pool.mjs` 8 项断言（构建后跑）；verify-media-pool 复跑防图片路径回归。【真机:待验证】（vivo S60 优先）：更新后开一次聊天页（≈12s 后台迁移）→ 诊断里 chat-msgs 应从 79MB 级降至 MB 级、语音气泡可播、发消息/收键盘不再长卡顿。
- 编号说明：原拟 #279 与并行「自动升级防打断」批撞号，本批改号 #283（build.mjs/注释/FIX-REGRESSION 均已 #283）。
- 待对方处理：无（本构建顺收口树上已声明完整的 #282 device.js 键盘停靠豁免、#284 music-player 安全 cancel——各自 owner 真机验证项照台账执行）。

### 2026-09-11（#284 用户报障「信息诊断显示有错误」（vivo X200s+Edge150）：错误环「BodyStreamBuffer was aborted」成批刷＝cancel() 拒绝裸奔——本次构建者：非本会话（src 已落，待收口构建））
- [跨域改动（AI-A 名下 music-player.js，理由：错误环噪音根因修复，与 AI-B 域诊断错误环收口配套，改动最小 3 处调用点+1 helper）；改动文件：src/js/music-player.js、build.mjs、FIX-REGRESSION.md、WORKLOG.md]（构建状态：**未构建，需构建者收口**；树上多批在途，编号顺延至 #284）。
- 需求/反馈：vivo X200s+Edge 错误环「(promise) BodyStreamBuffer was aborted」×9~×11 成批；@@m: 图片 404 旧版遗留已由 #275/#280 覆盖未重复处理。
- 根因：music-player 三处「收完响应头即 abort+cancel body」里，r.body.cancel() 返回 Promise，流已被超时 abort 打断时 cancel() 必 reject，外层 try/catch 拦不住 → 未处理 rejection 入环；弱网成批 abort 一次爆十条。
- 方案：模块级 mochiSafeCancelBody(r)（cancel() 挂空 catch），三处调用点统一替换；清理语义不变。
- 验证：node --check 过；--check-sentinels 全绿哑 0（#284 锚在位）。【真机:待验证】（vivo X200s+Edge 优先，弱网复现）：播放/导歌/封面正常，错误环不再新增该条目。
- 待对方处理：构建者收口 node build.mjs；AI-A owner 知悉本次跨域改动（仅 cancel 清理行，未触播放逻辑）。

### 2026-09-11 00:2x（#280/#281 用户报障「华为畅享20 Pro + Edge 刷新黑屏卡顿几分钟、十多次，其他机型也有出现」：@@m: 令牌裸路径 SW 快速 404 + my-emoji-groups 启动取回延迟——本次构建者：本会话（AI-B 域；chat.js 跨域改动已声明））
- [AI-B 域 sw.js（#280 fetch 头部对 '@@m:' 裸令牌路径本地快速 404，禁发真实网络请求）+ 跨域 chat.js（#281 启动取回延迟 restore-done+4s、myEmojiSave 防盲写闸门扩口径 __myeIdbApplied；跨域理由：用户点名修复的启动卡顿同源于聊天大键启动链）+ build.mjs（#281 FIX_SENTINELS ×2 + #280 swNeedlesSrc/swNeedles 各 ×1）+ FIX-REGRESSION.md（+280/+281 两行）+ tools/verify-refresh-perf.mjs（新增 9 断言）]（改动文件：src/pwa/sw.js、src/js/chat.js、build.mjs、FIX-REGRESSION.md、tools/verify-refresh-perf.mjs、WORKLOG.md；构建状态：已构建·sw mochi-mtvqctt9——src 三处已被并行 4fcb012 先行收编，本提交补台账/verify 脚本/产物并随收 media-pool.js #279 池收音频（接线未提交前纯休眠、已验证自洽））。
- 需求/反馈：畅享20Pro+EdgA149（诊断 v3.26.536，堆 117MB/32fps/IDB 60.9MB）「刷新出现黑屏卡顿，几分钟后恢复。目前出现十多次了」＋明说其他机型同现＋要求不覆盖式修复（同 #265/#278 口径）。
- 根因（两处，均机型无关→零机型分支）：① 诊断实证「资源加载失败 <img> …/mochi/@@m:<md5>」×3——池令牌解析失败/未完成（安卓 Edge IDB 挂起族高发）时裸令牌留在 img.src 被当相对 URL 发真实同源请求；SW 3.5s 网络优先超时后 catch 链还有一次【无超时】fetch，GitHub Pages 国内弱网每个挂 30s~几分钟，失败图多时挤占同源连接+拖累 version/notice+占位分钟级不上屏＝黑屏卡顿主成分。② chat.js 脚本求值即 idbGet(17.26MB my-emoji-groups)+主线程 JSON.parse 整包，压在开屏/首屏关键窗口（#250 治过 contact-switched、启动路径漏了）。
- 方案：① sw.js fetch 头部 pathname 含 '@@m:' → 本地 404（该路径永远非真实资源，真数据在 IDB media:<hash>；池命中场景观察器照常改写 src，被取消的 404 无副作用）；② 启动取回延迟到 mochi-restore-done 后 4s（表情面板/朋友圈插入面板打开本就 reloadMyEmojiFromIdb 现读权威＝#172 防丢主链不变），保存闸门扩为「未应用过 IDB 权威值 或 仍在挂起名单」堵延迟窗口盲写覆盖 IDB 全量的新窗口。
- 验证：node --check 过；--check-sentinels 全绿哑 0；tools/verify-refresh-perf.mjs 9/9（B 组真实 SW 行为：SW 接管后注入 '@@m:' img → error 2ms、本地服务器零命中）；verify-media-pool 10/10、verify-chat-pgjump 6/6、verify-viewport-form 86/86。【真机:待验证】（畅享20Pro/Edge 弱网优先）：刷新不再长时间黑屏卡顿；池缺失图即时显示「图片丢失」占位；大表情库机首屏可交互明显提前、表情包不丢不回退。
- 待对方处理：① 树上 #279 语音令牌化批（media-pool.js 已随本提交、chat.js/device.js/build.mjs 在途）完成后需再构建收口（本批产物只含当时快照）；② tools/diag-cc-list.mjs、tools/diag-page.mjs、docs/阿里云OSS 文档为前序会话遗留未跟踪文件，请 owner 处置；③ 保活（音频+WebRTC+screen wakeLock）在刷新销毁期可能拖慢旧渲染进程回收（Edge 安卓），本次未动（保护功能红线），若真机复现建议后续批次评估 pagehide(persisted=false) 时静默释放。

### 2026-09-11（#275 用户报 OPPO Reno16「问题：不知」：错误环刷满 @@m: 令牌 404＝「只备份文字」备份把媒体池剥成空串的跨设备腐蚀链——本会话修复主体；src 已随并行 #279 会话 4fcb012 收口进产物，本批补登记资产）
- [跨域改动（AI-B 域 src/js/data-backup.js、src/js/device.js，理由：腐蚀根因在备份导出/导入链路与错误记录器；media-pool.js 无归属默认共享；均为最小追加、未动既有逻辑块）+ 登记资产 build.mjs（#275 哨兵 ×5）FIX-REGRESSION.md（+275 行）tools/verify-media-pool.mjs（+T8/T9）WORKLOG.md]（**改动文件：src/js/data-backup.js、src/js/media-pool.js、src/js/device.js（三 src 已随 4fcb012 入库进产物）、build.mjs、FIX-REGRESSION.md、tools/verify-media-pool.mjs、WORKLOG.md**；构建状态：**src+产物已随 4fcb012 上线（sw mochi-mtvq8r8x），本批只补登记、未重建**）。
- 需求/反馈：OPPO Reno16/PMM110 + Chrome138 诊断（v3.26.529）错误环 20 条里 19 条为「资源加载失败 <img> https://…/@@m:<md5>」（同批哈希 ×2~×6、当天多次启动持续失败）＝聊天里这批表情/图片永久空白气泡；用户「问题：不知」+明说其他设备型号也有、要求勿覆盖式修改。
- 根因（#142/#186/#202/#280 家族都没堵住的传播链）：「只备份文字」strip 导出只剥 data: 前缀载荷——消息里的 @@m: 令牌不匹配被原样保留、媒体池键值（本身就是 dataURL）被逐条剥成空串。导入后：①渲染端 resolveImg 的 typeof 判定放行空串→map 永久缓存 ''+img.src=''（解析成页面 URL）＝永久坏图+#202 占位误报「网络不通」；②空串池条目 ≤20KB 走备份小键段，随今后每次【完整】备份继续传给对方设备＝跨设备跨机型复发；③池真缺失（历史损伤）时令牌 src 被当相对路径打网络必 404，每次渲染刷「资源加载失败」错误环掩盖真错误（#280 已在 SW 层快速 404，本批修数据根因+清日志噪音）。
- 方案（零机型分支，五道各守一层）：①data-backup.js 文字模式 cfg.skip 加 MEDIA_POOL_KEY_RE（读值前生效＝整键跳过，strip 绝不剥值留键）；②导出 LS 小键段循环同认 cfg.skip（≤20KB 池条目不进 ls 段防被剥空）；③doImportGo 开头 scrubMediaPool 把空串/非 data: 脏池条目直接丢弃（键保持缺席→#186「图片丢失：媒体数据缺失」准确占位；合法池值一律不动；日后导入完整备份即自愈）；④media-pool.js resolveImg 池值体检（值非 data:image/ 开头与池缺失同路：不入 map、不改写 src、不入负缓存＝可重试可自愈）；⑤device.js 错误记录器对未解析令牌 404 静默（getAttribute 原始值判令牌，#280 SW 快速 404 与渲染占位链路不受影响）。
- 验证：node --check 三 src 过；`node build.mjs --check-sentinels` **615 条全绿哑 0**；`node tools/verify-media-pool.mjs` **10/10**（新增 T8 池条目空串→令牌 src 原样保留不被改写成空/页面 URL、T9 池值补回→同令牌新渲染自愈＝完整备份导入恢复路径）；产物（HEAD 4fcb012）五 needle 逐一 grep 实证在位。【真机:待验证】（OPPO Reno16 及任意机型）：①「只备份文字」导出→导入后原图片消息显示「图片丢失：媒体数据缺失」占位、错误环不再新增 @@m: 404；②完整备份导出→他机导入，同批图片全部恢复显示；③表情/图片发送、显示、收藏、引用、群聊令牌化全不回归。
- 编号说明：#275 沿用 #277 批 WORKLOG 让位预留（「原拟 #275 让位给媒体池批并留编号注」）；#282 批留言所述「@@m: 404 由 #275 device.js 静音覆盖」属实。待对方处理：无（#279 语音入池批在途的 media-pool.js 未动，构建者下次收口自然带上）。
- 提交说明：本批提交仅含登记资产（build.mjs/FIX-REGRESSION/WORKLOG/verify-media-pool）；同文件里 #282 批已保存完整的登记块随本次一并入库（其 src 仍在途，由构建者下次收口构建），未夹带任何 src。

### 2026-09-11（#282 用户报障「信息诊断显示有错误」（荣耀90GT+Edge150）：错误环「[屏幕适配] 底部少填 277px」＝键盘停靠期判定无豁免——本次构建者：非本会话（src 已落，待收口构建））
- [AI-B 域 device.js（screenDiagJudge ④/⑤b 加 vv 深收缩键盘停靠豁免）+ tools/verify-viewport-form.mjs（E 组 5 断言）+ build.mjs（#282 哨兵 ×1）+ FIX-REGRESSION.md（+282 行）+ WORKLOG.md]（**改动文件：src/js/device.js、tools/verify-viewport-form.mjs、build.mjs、FIX-REGRESSION.md、WORKLOG.md**；构建状态：**未构建，需构建者收口**；⚠️ 树上多批在途（#275/#277/#278/#279/#281 等），本次只追加未触碰他批块；编号 276~281 均已被占，本批顺延 #282）。
- 需求/反馈：荣耀90GT+Edge 错误环反复「[屏幕适配] 底部少填 277px｜env=0 diff=181 inner=633 phone底=356」；用户强调多机型出现、勿覆盖式修改。另 4 条「@@m: 图片 404」错误已由在途 #275（device.js 静音）/#280（media-pool 快速 404）覆盖，本批未重复处理。
- 根因：resizes-visual 下键盘只缩 vv（633→356）、inner 不动，.phone 停靠 356＝布局正确；诊断 ④/⑤b 只对照 inner 期望底、无键盘停靠豁免，Edge 收键盘 vv 读数残留扩大失焦窗口 → 自动采集误报入环。
- 方案（零机型分支）：vv 缩幅 ≥ inner×22%（#236 已验证键盘下限）判键盘停靠期，④/⑤b 不判底、记 ✓ 可对号；#236 残留带（<22%）照常上报，真残留自愈仍归看门狗。
- 验证：node --check 过；verify-viewport-form.mjs **86/86**（E 组含「vv 回基准真残留照报」「残带照报」反向断言，豁免不扩权）；--check-sentinels 全绿哑 0。
- 待对方处理：构建者收口 node build.mjs（树上多批在途一并构入）+ 复跑 verify-viewport-form / verify-suite。【真机:待验证】（荣耀90GT+Edge 优先）：打字失焦后错误环不再新增「底部少填」；键盘停靠期输入栏仍正常贴键盘上沿。

### 2026-09-11（#277 用户报障 iPhone 17/Safari「未知 bug」：iOS standalone 切后台回来底部白带+导航栏悬空＝env 探针缓存中毒永不自愈——本会话修复主体；上线随并行 #279 会话 4fcb012 收口）
- [AI-B 域 mobile-adapt.js（syncVvFit env 缓存矛盾自愈：standalone 且 screen−inner≥20 而缓存=0 或与缺口差>8 → 节流 5s 重探，`_envTopCacheAt` 节流基准）+ build.mjs（#277 哨兵 ×1，矛盾判定表达式锚）+ FIX-REGRESSION.md（+277 行；原拟 #275 让位给媒体池批并留编号注）+ tools/verify-env-reprobe.mjs（新增，S/R/P 三组）]（**改动文件：src/js/mobile-adapt.js、build.mjs、FIX-REGRESSION.md、tools/verify-env-reprobe.mjs、WORKLOG.md**；构建状态：**已上线·随 4fcb012 收口构入·HEAD sw mochi-mtvqctt9**）。
- 根因（证据链三支柱）：①诊断 env 探针独立缓存实测 env=62＝真实覆盖形态；②应用侧 1s 自愈循环坏态期间每秒都跑却没修正＝自愈入参与诊断实测不同源；③--mochi-ios-h 三个写点全经共享判定器，写 894 只能来自 envTop=0 入参 → env 缓存只在旋转失效、standalone 冷启动早帧探 0 被永久缓存（稳定后实为覆盖形态 env=62）；expBase 少算 env 段（min(956,0+894)=894），切后台 inner 短报 894 时 .phone 底部 62px 白带/tabbar 悬空，自愈按同值反复「确认」永不自愈（错误环 9/8~9/10 反复采集「底部少填」同一签名；WebKit26 standalone 同族机型通用）。
- 验证：node --check 过；verify-env-reprobe.mjs **13/13**（红对照：HEAD 旧源 **6/11**，红项恰为 S1~S5 五条修复点＝脚本有牙；R 组确认 #148 旋转失效/#189 迟滞写未被顺手破坏；P 组产物接入）；收口构建哨兵全绿。【真机:待验证】（iPhone 17 及任意 iOS standalone 机型）：切后台/锁屏再回前台，屏幕底部不再白带、桌面 tabbar 贴底不悬空；屏幕适配诊断全 ✓；反复切前后台不再新增「[屏幕适配] 底部少填」错误环条目。
- 回应 #279 批留言：①「env 批 FIX-REGRESSION 行号 275 与哨兵 #277 不一致」已在贵批提交快照前对齐（HEAD 265 行=#277+编号注，build.mjs=#277）；②群聊批 #276 行未丢，FIX-REGRESSION 266 行在位（曾与我行粘接，已拆为独立行）。

### 2026-09-11 00:13（#279 用户报 iQOO12 Chrome「不清楚问题的bug」（附诊断）：#273 冷加载自动升级重载砸进会话中途＝「页面自己重开、问答门重答」——本次构建者：本会话，收口构建 sw mochi-mtvq8r8x）
- [AI-B 域 pwa.js（用户活动感知捕获监听 + refreshNow(auto,autoTs)/tryAutoUpgrade(autoTs) 防打断守卫 + __pwaAutoUpgradeTest 只读探针）+ build.mjs（#279 哨兵 ×1）+ FIX-REGRESSION.md（+279 行）+ tools/verify-auto-upgrade-guard.mjs（新增）+ WORKLOG.md]（**改动文件：src/js/pwa.js、build.mjs、FIX-REGRESSION.md、tools/verify-auto-upgrade-guard.mjs、WORKLOG.md**；构建状态：**已构建·sw mochi-mtvq8r8x**；本批收口构建同时把树上在途各批次一并构入：#276 群聊撤回图、#277 env 探针、#278 device.js 误判环、红包概率、#272 暗号提示等）。
- 需求/反馈：iQOO12 Chrome「不清楚问题的bug」，用户明说其他设备型号也有、要求不要机型化修补（防覆盖式回归）。诊断解读（device.js 记录器口径：交互=click 捕获 / 触摸=touchstart / 输入轨迹 n=文本长度、st/sh/ch=scrollTop/scrollHeight/clientHeight）：**19:39:48 用户还在应用内点 tab 切页（当时无锁屏遮挡），19:39:56 却点了开屏「进入」＝两步之间页面被重载过一次**；随后 19:39:57~19:40:00 问答门两题作答全程正常（ce-box→applock-txt 同步成对出现、每次点击有响应、无死点击、卡死逃生记录空）＝输入/点击链路无恙，异常就是「会话中途自动重载」。已排除：#134 截断自愈（仅文档截断且限 1 次/会话）、SW updatefound 通道（v3.5.114 起只弹条）、personalize/data-backup 的 reload（均用户主动触发）。
- 根因（pwa.js）：#273 冷加载自动升级 tryAutoUpgrade → refreshNow() 的 reload 在 PRECACHE_NOW 预取完成那一刻无条件落地——弱网（GitHub Pages 国内 3.6MB+ 产物）预取可达几十秒，正好砸进用户已开始的会话，被迫回开屏+问答门重答。与 v3.5.114 撤销 SW 通道自动刷新（「刚进入桌面就被打断回到开屏」）同族；#273 设计说明自己写着「避免打断会话中途操作」，但只在轮询/SW 通道做了，自动升级通道漏了。
- 方案（通用根因、零机型分支）：①捕获级记录本页面首次交互（touchstart/pointerdown/mousedown/keydown，只记时刻不记内容）；②refreshNow(auto, autoTs) 的 doReload 落地前最后一刻复核 autoReloadAllowed()——页面 hidden 放行（回前台即新版、重载无感）、零交互放行（保留 #273「冷加载一跳进新版」快路径）；用户已操作且前台→放弃自动重载、退回 showVerBar(autoTs) 常驻更新条；③tryAutoUpgrade(autoTs) 入口先查活动、不满足 return false 让调用方弹条；手动点「刷新使用新版」不受限；session 守卫/预取失败退回更新条逻辑不变。
- 验证：node --check 过；构建哨兵 **610/610 全绿哑 0**、sw 哨兵 15/15；`tools/verify-auto-upgrade-guard.mjs` **6/6**（P1 零交互 allowed=true / P2 mousedown 后 false / P3 重载+touchstart 仍拦截 / P4 产物接线静态锚）；复跑 verify-applock **61/61**、verify-env-reprobe **13/13** 无连带回归。【真机:待验证】（iQOO12 Chrome 及任意机型/浏览器）：①部署本版后打开旧版放着不动→仍自动进新版；②正在操作（答问答门/打字/切页）时部署新版→页面不再自己重开、顶部出「刷新使用新版」条；③后台挂起时部署新版→回前台已是新版。
- 编号说明：本批原拟 #276，与群聊批撞号（276 先被群聊批占用、277 被 env 批 build.mjs 哨兵占用、278 被 device.js 误判环批占用）→ 定 **#279**。⚠️ 需要对方处理：①群聊批 WORKLOG 声称「FIX-REGRESSION.md（+276 行）」但该行不在当前文件中（疑被并发覆写丢失）——请群聊会话自行补写 276 行；②env 批 FIX-REGRESSION 行号 275 与 build.mjs 哨兵名 #277 不一致，请该会话对齐；③树上 `?? tools/diag-cc-list.mjs`/`diag-page.mjs` 为误名的字卡库脚本副本（非本批产物），请 owner 按临时脚本规则处理。

### 2026-09-11（#278 用户报障「信息诊断显示有错误」：多安卓机型错误环「底部超出/底部导航栏被裁 diff=-535」＝坏 screen.height 误报——本次构建者：非本会话（src 已落，待收口构建））
- [AI-B 域 device.js（mochiViewportForm 期望底边 min 钳制加 screenH≥innerH 坏值门）+ build.mjs（#278 哨兵 ×1；#210 force 分支 needle 随代码演进同步）+ tools/verify-viewport-form.mjs（#278 台账 fixture）+ FIX-REGRESSION.md（+278 行）+ WORKLOG.md]（**改动文件：src/js/device.js、build.mjs、tools/verify-viewport-form.mjs、FIX-REGRESSION.md、WORKLOG.md**；构建状态：**未构建，需构建者收口**；⚠️ device.js/mobile-adapt.js/build.mjs 树上有并行会话 #275/#277 在途改动，本次只**追加**未触碰其块）。
- 需求/反馈：华为畅享70Pro+Chrome 诊断错误环反复「[屏幕适配] 底部超出／底部导航栏被裁｜diff=-535 inner=1331 phone底=1332」；用户强调多机型出现、勿覆盖式修改。
- 根因：该批安卓机 Chrome 的 screen.height 报数坏值（screen=796 < 实际 inner=1331，物理不可能）；判定器 min(screenH, envTop+innerH) 取坏值 796 当期望屏底，.phone 贴 inner 正常铺满（底=vv=1332、空隙 0）被误判「底部超出 535px」刷错误环。**布局本身无故障，纯诊断误报**。
- 方案（零机型分支）：screenH<innerH 视为坏值弃用、回退 envTop+innerH；forceCover 分支同款门。执行器与诊断共用判定器两处同源，正常机型（screen≥inner）min 语义零变化。
- 验证：node --check 过；verify-viewport-form.mjs **80/80**（新增 #278 fixture）；--check-sentinels **608 条全绿哑 0**。
- 待对方处理：构建者收口 node build.mjs（连同树上 #273/#274/#275/#276/#277 在途一并构入）+ 复跑 verify-viewport-form / verify-suite。【真机:待验证】（报障机型优先）：屏幕适配诊断全 ✓，错误环不再新增「底部超出 diff=-535」。

### 2026-09-10 深夜（vivo X100s Chrome 149「后台保活失效/锁屏不提示」报障核实：#260 修复已上线，用户手机停在 9/7 旧版——本会话只核实零改动）
- [AI-B 域核实任务]（**改动文件：仅 WORKLOG.md；构建状态：未构建，无 src 改动**）。
- 核实链：src/js/bg-keep.js 含 #260 全部逻辑锚（amp `0.002 : 0.02`／WebRTC 回环 `mochi-ka`／`__ka-hb` 心跳／`__kaProbe`）；本地产物与 `git show HEAD:index.html` 三锚全在；HEAD==origin，线上 version.json ts=1789044254502（9/10 20:44）＝已部署产物含修复。防线无缺口：FIX-REGRESSION #260 台账行（L247）、build.mjs 5 条哨兵、tools/verify-keep-audio.mjs 均在位。
- 结论：用户诊断 build ts=1788796421477＝**2026-09-07 23:53 旧版**，早于 #260 构建两天半；Chrome 升 149 后旧版单音频锚 0.006 电平丢冻结豁免＝#260 同根因复发。处理＝手机更新一次（顶部更新条「刷新使用新版」或关全部标签页重开；新构建含 #273 冷加载自动升级，此后刷新自动跟版，不会再停旧版）；更新后仍失败则诊断信息【保活现场】行（通知权限/心跳断流秒数）直出证据再按数据修。
- 待对方处理：无（本会话未碰 src，在途并行批次不受影响）。

### 2026-09-10（#276 群聊撤回图片可看缩略图——未构建，待收口）
- [AI-A 域 group-chat.js + 跨域 build.mjs/FIX-REGRESSION.md]（**改动文件：src/js/group-chat.js（新增 gcRetractMediaHtml：image/sticker/parts 含图撤回记录从 rec.text/rec.parts 即时重拼 img，data:/@@m: 令牌均可显；renderMsg 撤回分支改为「媒体即时视图 > 存量快照 rec.orig > gcRetractFallbackHtml 占位」取值）、build.mjs（哨兵 +1=607，#276 逻辑锚）、FIX-REGRESSION.md（+276 行）；构建状态：**未构建**，本次构建者默认 AI-B，请收口时一并构入）。
- 需求/反馈：用户「群聊里联系人撤回的消息的图片显示为【图片】的文字，无法查看撤回的图片的缩略图」。根因：#245/#247 防臃肿批规定媒体类撤回不存 DOM 快照，点击查看只给占位文字；但图片 src 本就持久化在 rec.text/rec.parts（含 @@m: 令牌），渲染时即时重拼即可看图，防臃肿语义不变（快照仍不存 base64）。
- 验证：node --check 过；`node build.mjs --check-sentinels` 本条锚点在位（另见 device.js #210 红=你口在途未提交改动所致，非本批引入，收口时以你口定稿为准）。git status 树上你口另有 applock.js/data-backup.js/device.js/media-pool.js 在途 M，本口未触碰。
- 【真机:待验证】群聊联系人发图→撤回→点提示应显示缩略图；表情包/图片+文字组合同理；语音撤回仍[语音]占位属正确。
### 2026-09-10（聊天消息操作菜单加【复制】按钮——未构建，待收口）
- [AI-A 域 chat.js + template.html]（**改动文件：src/js/chat.js、src/template.html**；构建状态：**未构建**，本次构建者默认 AI-B，请收口时一并构入）。
- 需求/反馈：用户「聊天消息出现的引用窗口（气泡操作菜单），没有【复制】复制文字的功能」。
- 方案：#msg-actions 在「引用」与「收藏」之间新增 `data-act="copy"` 按钮（复制图标）；chat.js msgActions 点击处理加 `copy` 分支——文字取 `quoteTextOf(rec).text`（语音取名称、图片/表情只回文字），先 `mochiMediaExpand` 展开媒体池令牌再判空/判 dataURL（纯图片/语音提示「该消息没有可复制的文字」）；复制走 textarea+execCommand('copy') 优先、clipboard API 兜底、`mochiKillCopySelection` 收选区（device.js #261 同款防安卓全选条卡屏），toast 反馈。
- 验证：node --check chat.js 过；`node build.mjs --check-sentinels` 我的改动无锚点缺失（现存 1 条 device.js #210 报警为 AI-B 在途改动，非本次引入，未触碰）。
- 待对方处理：构建者收口 `node build.mjs`；群聊（gc-msg-actions）如需同款复制可后续照搬。

### 2026-09-10（#274 用户反馈「刷新/重新打开网站应用锁被关了」：自愈误关加固——本次构建者：本会话（AI-B 域）收口）
- [AI-B 域 applock.js（evalLock 同步自愈 → selfHealChecked 先查 IDB，防「一时未回填/ LS 快照缺 pin」误关锁）+ build.mjs（#274 哨兵 ×1 `gSet(K_PIN, v); evalLock();`）+ FIX-REGRESSION.md（+274 行）+ tools/verify-applock.mjs（新增 R 组 2 断言 + H7 静态锚）+ WORKLOG.md]（**改动文件：src/js/applock.js、build.mjs、FIX-REGRESSION.md、tools/verify-applock.mjs、WORKLOG.md**；构建状态：**待收口构建**）。
- 需求/反馈：用户「设置了应用锁，但刷新重新打开网站，应用锁被关了」（澄清：安卓同一标签页刷新，既不弹锁屏、设置页开关也显示关闭）。
- 根因（applock.js）：`evalLock` 里 `if (enabled() && !pinHash()) setEn(false)` 同步自愈——只要本次同步读到「en=1 但读不到密码」就当异常态把锁置 0。安卓「数据主要在 IDB、LS 仅快照」下刷新首帧 pin 常未回填 / LS 配额静默写失败只剩 en → 首帧自愈误关锁＝门户大开（用户「开关也变关」正是指向 setEn(false) 只有自愈/手动两条路）。
- 方案（最小改动、零机型分支）：新增 `selfHealChecked()`——读到 en=1 但同步读不到 pin 时，不立即关锁，改先异步 `window.idbGet('xy-home-v2:applock-pin')`：IDB 有密码就 `gSet(K_PIN, v)` 回填并重进 evalLock 正常锁屏；仅双端（LS+IDB）都确认无密码才 `setEn(false)`（防「忘密码锁死」初衷保留）。正常开启/双写一致零行为变化。
- 验证：node --check applock.js 过；verify-applock.mjs 新增 R1/R2（IDB 有 pin、LS 删 pin → 刷新仍锁屏且开关不被误关、密码可解锁）+ H7（产物含 `gSet(K_PIN, v); evalLock();`）；既有 A（冷启动锁屏）/F（真异常自愈置 0）不变。【真机:待验证】（安卓）：设好密码锁后普通刷新仍要求输密码、设置页开关保持开启。
- 待对方处理：构建者收口 `node build.mjs`（本批含 #273 未构建在途 pwa.js，一并构入）+ 复跑 `node tools/verify-applock.mjs`。

### 2026-09-10（#273 用户反馈「手机刷新总是进不去网页最新版」：冷加载自愈进新版——本次构建者：待定（收口请构建））
- [AI-B 域 pwa.js（新增 tryAutoUpgrade + pageshow 冷加载自动升级，会话守卫防死循环）+ build.mjs（#273 哨兵 ×1，needle 用字符串键锚防压缩改名）+ FIX-REGRESSION.md（+273 行）]（**改动文件：src/js/pwa.js、build.mjs、FIX-REGRESSION.md、WORKLOG.md**；构建状态：**未构建，需构建者收口**；src 已 `node --check` 过、`--check-sentinels` 哑 0）。
- 需求/反馈：用户「手机刷新总是进不去网页最新版」。
- 方案（零机型分支）：新增 `tryAutoUpgrade()`——冷加载（普通刷新/重进/standalone 启动）`pageshow(persisted=false)` 时对比 `./version.json`，云端 ts>页面 `data-build-ts` 且本会话未尝试过（sessionStorage `xy-home-v2:auto-upgrade-session` 守卫）则直接走 `refreshNow()`（PRECACHE_NOW 预取最新 index 落盘再 reload）一跳进新版；命中则刷新后不再重复（守卫置位）；预取异常刷新后仍回旧版时守卫已置位→退回收 `showVerBar()` 常驻更新条，绝不无限循环。周期轮询（15s/5s）与 SW `updatefound` 通道不动、只弹条不自动刷新，避免打断会话中途操作。
- 验证：pwa.js `node --check` 过；`node build.mjs --check-sentinels` 哑 0（#273 needle `'xy-home-v2:auto-upgrade-session'` 在 pwa.js 唯一）。未构建未跑产物侧哨兵（等构建者收口）。【真机:待验证】（任意机型）：①云端部署新版后手机普通刷新一次即自动进新版；②弱网预取失败极端场景应出现常驻更新条、点「刷新使用新版」仍可到达最新；③同版本不会反复自动刷新。
- 待对方处理：构建者收口 `node build.mjs`（构入 #273）+ `node build.mjs --check-sentinels` 复验。

### 2026-09-10（#273b 用户反馈「网页顶部刷新按钮经常不出现」→ 弱网兜底——本次构建者：待定（并入 #273 收口））
- [AI-B 域 pwa.js（checkVersion 的 catch 分支加 `maybeNetHint()` 弱网兜底）+ build.mjs（+1 哨兵，needle `'网络异常，未能确认最新版本'`）+ FIX-REGRESSION.md（+273b 行）]（**改动文件：src/js/pwa.js、build.mjs、FIX-REGRESSION.md、WORKLOG.md**；构建状态：**未构建，需构建者收口**；src 已 `node --check` 过、`--check-sentinels` 哑 0）。
- 需求/反馈：用户「网页顶部没有出现刷新的按钮」「之前不是有刷新的设置吗，最近消失了」——弹条只在「线上 ts>页面 data-build-ts」才出，页面常驻最新版时正常无提示（非故障）；真正让「刷新入口消失」的是弱网拉 version.json 失败时 catch 静默跳过。
- 方案（零机型分支，并入 #273）：`maybeNetHint()`——连续失败 ≥2 次（弱网持续、非单次抖动）复用更新条显示「网络异常，未能确认最新版本」+「重试刷新」，点击复用 `refreshNow()`（PRECACHE_NOW 预取最新 index 落盘 + reload）；内存守卫每页面加载只提示一次，不随 5s 轮询反复闪；网络恢复且真有新版时正常 then 分支 showVerBar 覆盖本文案。
- 验证：pwa.js `node --check` 过；`node build.mjs --check-sentinels` 哑 0。【真机:待验证】弱网下顶部出现网络异常提示+重试刷新；每页只提示一次；联网正常且已最新时顶部无提示（正确）；联网且有新版时仍出「刷新使用新版」。
- 待对方处理：构建者收口时连同 #273 一起 `node build.mjs` 收。

### 2026-09-10 19:2x（#272 用户需求：开屏问答门①第 2 题加「答案为【一个字】」提示②开屏跳过暗号更新并加提示；顺带收口 #271 + 修 #271 哑哨兵——本次构建者：本会话）
- [AI-B 域 applock.js（DEFAULT_QA 第 2 题文案、QA_SKIP_CODE 更新、qaSkipAsk 暗号屏提示文案）+ contacts.js（注释同步）+ build.mjs（#271 哨兵 needle 从注释改为代码锚点、注释同步）+ tools/verify-applock.mjs（暗号断言/用例同步新值）]（**改动文件：src/js/applock.js、src/js/contacts.js、build.mjs、tools/verify-applock.mjs、WORKLOG.md**；构建状态：**已构建·sw mochi-mtvg4tlo**）。
- 需求/反馈：用户①「开屏的第二个问题，增加提示 答案为【一个字】」；②「开屏的按钮密码修改成新值」并加暗号提示，最终提示文案「暗号是 99 + mochi 字卡出生日期，一共 6 个数字」。
- 方案：① 只改 DEFAULT_QA 第 2 题 `q` 文案（答案仍『是』）；② QA_SKIP_CODE 更新（含 comments 与 contacts.js 注释同步），qaSkipAsk 暗号输入屏 sub 追加提示；verify-applock.mjs 暗号输入用例与 K3 静态断言同步新值。
- 顺带：#271 哨兵 needle 原为整行注释（minifyJs 丢整行 `//`）→ 产物永不命中、构建恒退出码 1；按 build.mjs 提示「换成同行代码特征」改为 applock.js 内唯一代码锚 `if (done) done(q, a);`（已 grep 确认文件内仅 1 处），本次构建一并收口 #271 修复进产物。
- 验证：node --check 过；构建哨兵 **601/601 全绿哑 0**；`tools/verify-applock.mjs` **58/58**；产物已实证 `QA_SKIP_CODE` 已更新、含「答案为【一个字】」与暗号屏提示文案，全产物无残留旧暗号。
- 待对方处理：无。产物未提交，等用户指示提交。
### 2026-09-10（#271 修复：应用锁「设置安全问题」点【完成】无反应＝完成后关闭面板——本次构建者：非本会话（请收口构建者构入 #271））
- [AI-B 域 applock.js（askQaSetup 答案屏 onSubmit 补「置空+隐藏遮罩」）+ 登记 build.mjs（FIX_SENTINELS #271 ×1）FIX-REGRESSION.md（+#271 行）tools/verify-applock.mjs（新增 P 组 6 断言）]（**改动文件：src/js/applock.js、build.mjs、FIX-REGRESSION.md、tools/verify-applock.mjs、WORKLOG.md**；构建状态：**未构建，需构建者收口**；src 已 `node --check` 过）。
- 需求/反馈：【应用锁】→设置安全问题，设置完问题点【完成】无反应。根因（无头复现实证）：askQaSetup 答案屏 onSubmit 只调 done(q,a)（内部 save+toast+syncUi）、**不清遮罩**→面板一直滞留此屏、用户看不到已保存（数据其实已落库 applock-qa）＝「点完成无反应」。修复（最小改动、零机型分支）：done 后补 `cur=null; buf=''; maskEl().hidden=true;` 即关闭面板（两个调用方 flowQaEdit / 开启锁时引导设问答共用此链）。
- 验证：node --check applock.js 过；哨兵 `#271` 锚 src 在位（产物未重建，待构建者收口后跑 `node build.mjs --check-sentinels` + `node tools/verify-applock.mjs` 复验 P5「完成后遮罩关闭」、P6「已保存」）。【真机:待验证】（任意机型）：设置→应用锁→修改安全问题→输问题→下一步→输答案→点「完成」→对话框立即关闭、重新进入能看到新问题；忘密码答对即重设。
- 待对方处理：⚠️ applock.js 正被并行会话并发编辑（#270 移除「编辑问答题」），本 #271 已并入当前工作区；若该会话旧缓冲回写覆盖掉 #271 的「完成即关闭」块，哨兵 #271 会红，请构建者收口前 `node build.mjs --check-sentinels` 确认。

### 2026-09-10（新增：每个联系人可分别设置「TA 自动发红包概率」——本次构建者：未构建，需构建者收口）
- [跨域改动（AI-A 名下文件，理由：用户需求）；改动文件：src/template.html、src/js/chat-settings.js、src/js/chat.js；构建状态：**未构建**]。
- 需求/反馈：新增联系人也想能单独设置 TA 主动发红包给我的概率（原本 trySystemAutoSend 硬编码普通 4% / 七夕 8%，不可调、也不区分联系人）。
- 方案（最小改动）：① template.html 聊天设置·功能标签新增「红包」分组，行 `cs-rp-auto-prob`，显示 val `cs-rp-auto-prob-val`；② chat-settings.js 绑定该行：点按 openModal 输入 0-100%，存 per-cid 键 `cs-rp-auto-prob`（默认 4%），切联系人重同步；③ chat.js trySystemAutoSend 改读 `activeStore().get('cs-rp-auto-prob')` 作为 baseRate（0-100 钳制 /100），七夕仍 ×2。默认值参数、行为与原 4%/8% 完全一致，仅多出可调入口。
- 验证：node --check chat-settings.js / chat.js 均过。未构建、未跑哨兵（非构建者）。【真机:待验证】（任意机型）：聊天设置→功能→红包→设不同概率，观测 TA 主动发红包频率随联系人各自变化。
- 需要对方处理：下次构建收口（含本批一起 build 进产物），并给 build.mjs 哨兵/FIX-REGRESSION 判断是否登记。

### 2026-09-10 深夜（#270 用户纠正定案：开屏问答门「无法被别人编辑，是固定的 2 个问题」——移除全部编辑机制——本次构建者：本会话（AI-B 域））
- [AI-B 域 applock.js + applock.css + 登记资产 build.mjs/FIX-REGRESSION.md/tools/verify-applock.mjs]（**改动文件：src/js/applock.js、src/css/applock.css、build.mjs、FIX-REGRESSION.md、tools/verify-applock.mjs、WORKLOG.md**；构建状态：**已构建·sw mochi-mtvfqbib**）。
- 需求/反馈：用户纠正「开屏问答门无法被别人编辑，是固定的 2 个问题」——原 v3.31.x 实现把题目做成可增删改（设置页「编辑问答题」入口 + 管理面板，经 qaGuard 验证即可改题），违背「固定 2 个问题」定案。
- 方案（零机型分支）：① 删除全部编辑机制——kind:'qalist' 面板渲染、onQal/qaEditItem/qaRerender/qaOpenEdit/qaPanel 整套逻辑、data-qal 按钮及点击分发、K_QA_LIST(applock-qalist) 常量与读写、设置页「编辑问答题」入口、.al-qa-* 样式；② qaList() 不再读任何已存储的自定义列表，恒返回 DEFAULT_QA 固定 2 题（mj→梦角 / 知晓→是），答案仍只存 cyrb53 摘要；旧版本遗留 applock-qalist 数据不迁移不删除、留在原键位对行为零影响（contacts EXCLUDE 保留防迁移）；③ 设置页文案改「2 道固定问答题」；④ __applockQaTest 只留 getList()。
- 验证：node --check applock.js 过；--check-sentinels **600 全绿哑 0**；verify-applock.mjs 删 L 组（题目管理 12 断言）、K5/K6 反转为「产物不含 data-qal/.al-qa-row」、新增 K7「产物含固定 2 题梦角」；构建产物哨兵 #270 两条（固定列表逻辑锚 + data-qal absent）全绿。
- 需要对方处理：无。本批构建顺带收编树上在途的 #269（chat-main.css gd-opts）、default-cards.js、template.html 等改动（已在产物中），各 owner 自行确认。
- 【真机:待验证】（任意机型）：设置→应用锁→开屏问答门 无「编辑问答题」入口；每次打开仍固定先答 mj/知晓 两道题；曾编辑过题目的设备刷新后仍回到固定 2 题。

### 2026-09-10（#269 修复：手机端【帮我决定】/【多人决定】「自定义选项」选项输入框一直跳且拉不上去——本次构建者：本会话（改动域：AI-A 的 css/chat-main.css，bug 修复）
- [AI-A 域 chat-main.css 补 gd-opts 限高]（**改动文件：src/css/chat-main.css、build.mjs、FIX-REGRESSION.md、WORKLOG.md**；构建状态：**未构建，需构建者收口**）。
- 需求/反馈：手机端【帮我决定】【多人决定】的「自定义选项」窗口一直跳且拉不上去。根因：#chat-decision-body 的 dec-opts 早已限高框内滚动，但 #chat-gdecision-body 的 gd-opts 漏了同款——安卓被转成 contenteditable .ce-box 后随输入行数无限增高，把下方控件顶出屏、整列无法上划（与 dec-opts 修法同）。
- 方案：给 #chat-gdecision-body .dec-inp-wrap .ce-box[data-for="gd-opts"] 补 max-height:176px + overflow-y:auto + overscroll-behavior:contain，与 dec-opts 完全对齐。已登记 build.mjs 哨兵（598 条全绿哑 0）。
- 验证：node build.mjs --check-sentinels 598 全绿哑 0；build.mjs/FIX-REGRESSION.md/WORKLOG.md 均已按回归防线登记。【真机:待验证】（任意安卓）：多人决定→自定义选项→选项框输多行→框内滚动查看、控件不顶出屏、可正常上划。
- 待对方处理：下次构建收口（含本批改动一起 build 进产物）。
### 2026-09-10 18:5x（#265 收口：构建者补完——把已落 src 的桌面图标顺序修复打进产物并全量复核；本次构建者：本会话）
- [AI-B 域补完登记]（改动：无新增 src——#265 修复已在 src/js/personalize.js 由原会话落好，本会话只执行 **node build.mjs 收口构建**；产物已含 5 条 #265 needle、含 6 批在途 #260~#267 改动，sw mochi-mtveqate）
- 前置校验：node --check 九处在途改动 JS 全过；`node build.mjs --check-sentinels` 595 全绿哑 0（src 锚点全在位）后才构建。
- 验证：`node tools/verify-desk-icon-order.mjs`（读仓库根产物）**15/15 全绿**（用户屏上症状链 T1-1/T1-4「退出浏览器还原初始」、切桌面顺序跟随 T3 全通过；T4 大图键 IDB 补读不误伤）；`npm run verify:all` 后台跑，桌面族 verify-desk-layout-guard 10/10、verify-desk-incoming 39/39、verify-desk-icon-order 15/15 全绿，现存红项均为各批既有的台账在册/口径过期项（desk-persist 拖拽、longpress 导出 UI 等，归属其 owner），无本批新引回归。
- 【用户侧输入】修复后真相：之前一直「还原」是因为修复只改在 src/ 没进产物，用户测试的 index.html 是旧版；本次收口后产物才真正带上修复。真机待验证同原 #265 条目。
### 2026-09-10 深夜（#266 补完登记：字卡库列表页兜底取回 IIFE「漏调用括号」死代码＝「导入字卡过一段时间就没了，刷新就消失」——本次构建者：非本会话（#267 会话已把修复随构建带上线），本会话只补登记）
- [AI-A 域 chatcard.js 修复主体（并行会话已落 src，`})()` 改 `})();`）＋本会话补登记 build.mjs（FIX_SENTINELS 尾部 #266 ×1，597）、FIX-REGRESSION.md（+#266 行，插在 265/267 之间、行尾保持 CRLF）]、tools/verify-cc-lib-hydrate.mjs（src 静态检查，已由对方会话新增）]（**改动文件：build.mjs、FIX-REGRESSION.md、WORKLOG.md**；构建状态：**已构建·产物已含本修复——index.html:39346 的 `})();` 实证，无需再构建**）。
- 需求/反馈：iPhone iOS13 基础+Chrome/Edge「导入了好多次字卡都是过一段时间就没了，刷新就消失字卡」。根因（chatcard.js，f143621 引入）：列表页兜底取回 IIFE 把结尾 `})();` 误改 `});`——语法合法、node --check 过、旧哨兵查「文本锚点在」也过，但整段变死代码；iOS 回填被打断/大库挂起后内存与 LS 两路读空、只剩 IDB 权威，唯一会在查看时点把库拉回来的 hidden 观察者就此断掉＝刷新字卡消失。同 #262/#265 族「名字在、逻辑没了」。
- 验证：`node --check` 过；`node tools/verify-cc-lib-hydrate.mjs` **5/5**（A3 用 matchBrace 结构断言结尾紧跟 `)(`，防注释假绿）；`node build.mjs --check-sentinels` **597 全绿哑 0**；产物 `})();` 已实证在。
- 需要对方处理：无（#267 会话构建已带上本修，下次其重建自然复验）。【真机:待验证】（该 iPhone iOS13+Chrome/Edge 及任意机型）：导入/编辑字卡后过段时间或刷新仍在列表，不再消失。

### 2026-09-10 19:0x（#268 修复：聊天「分页上划看不到以前记录 / 搜索·引用提交后不跳转不高亮」——本次构建者：本会话（AI-B 域））
- [AI-B 域 src/js/chat.js（jumpToMsg 补下界外扩窗）+ 登记 build.mjs（FIX_SENTINELS +1 #268 逻辑锚）FIX-REGRESSION.md（+#268 行）tools/verify-chat-pgjump.mjs（新增，841 条真实复现，verify-suite 自动发现）]（**改动文件：src/js/chat.js、build.mjs、FIX-REGRESSION.md、tools/verify-chat-pgjump.mjs、WORKLOG.md**；构建状态：**已构建·sw mochi-mtvf13m1**。
- 需求/反馈：聊天会话用户报障「①分页加载，上划无法查看以前的聊天记录 ②搜索提交了也无法跳转到位置」。
- 根因：聊天长历史渲染窗口 renderStart..renderEnd（RENDER_MAX=200/WINDOW_MAX=400），上翻 `loadOlderIncremental` 会触发 `pruneWindowBottom` 从下界裁剪，使 `renderEnd < msgs.length`；而 `jumpToMsg`（搜索跳转与点击引用块跳原文共用）只处理 `idx < renderStart`（上界外扩窗），对落在裁剪下界之外的 `idx >= renderEnd` 完全不处理 → querySelector 查不到 target、函数 return false＝搜索/引用点了没反应。分页本身是好的（增量上翻能一路加载回最老消息，diag 实测 firstIdx≈0）。
- 方案（最小改动）：jumpToMsg 在 target 不在 DOM 时补 `idx >= renderEnd` 分支，`loadNewerIncremental(Math.min(idx+JUMP_VIEW+1, len))` 循环向下展开直到 renderEnd>idx，再重新 querySelector→scrollIntoView 居中+highlight。增量展开触发 pruneWindowTop 顶上裁剪无副作用；原有上界外扩/居中/高亮/引用 resolveQuoteTarget 零改动。
- 验证：`node --check` chat.js 过；**tools/verify-chat-pgjump.mjs 6/6**——种 841 条真实复现：①首屏只渲染最近 ~200 条（分页生效）②持续上滑（拖到最顶）firstIdx 一路压回 0＝分页能加载回最老消息 ③搜索点最老区 idx=40→`hasHl:true/visible:true` ④搜索点近最新区 idx=820（下界裁剪区）→`hasHl:true/visible:true`＝下界外扩生效。分页复核通过。
- ⚠️ 本次构建输出仍报 `#266 字卡库兜底取回 IIFE` 锚在产物缺失比 src 少——chatcard.js（AI-A 域）的在途修复未接入，属并行待收口，请 owner 复核（AGENTS.md：遇对方文件 bug 不去改，留言待处理）。
- 【真机:待验证】（任意机型）：历史翻到很靠前后搜一条老消息/点引用块 → 视口居中定位并闪烁高亮，之后可继续上下翻。

### 2026-09-10 晚（#267 收口构建复验：荣耀 X50 自带浏览器「点开键盘无输入框 / 输入框出现空白 / 输入法完全遮挡输入栏打不出字」——本次构建者：本会话（AI-B 域））
- [AI-B 域 mobile-adapt.js + device.js（修复主体为并行会话已落 src）+ 收口登记 build.mjs（FIX_SENTINELS 尾部 #267 ×8，595）FIX-REGRESSION.md（+#267 行 + 设备索引「荣耀 X50（HonorBrowser）」）tools/verify-kb-pan-dock.mjs（已由对方会话新增，本批纳入）]（**改动文件：src/js/mobile-adapt.js、src/js/device.js、build.mjs、FIX-REGRESSION.md、WORKLOG.md**；构建状态：**已构建·sw mochi-mtvepm84**。
- 需求/反馈：荣耀 X50（ALI-AN00/Android15/HonorBrowser-Chrome116）「点开键盘没有输入框，或者点开输入框会出现一部分空白；输入法弹窗完全遮挡输入栏这一行，无法正常打字发送」。并行会话已把修复写进 src（#267：平移型键盘内核按实测平移停靠 _aPanSeen + 主链路接管清推顶防 kb/prov 并存 + 看门狗两条卡死自愈 + 诊断埋点），但未构建，用户线上一测仍是旧版老大难＝「修了一半」。本会话负责收口：重构 → 全绿 → 登记台账。
- 根因（详见 build.mjs 注释 / FIX-REGRESSION #267）：此类内核键盘弹出不缩 visualViewport.height（664 与 254 同会话交替），靠平移露焦点；旧逻辑只 58% 盲猜停靠＝IME 占 62% 时输入栏整行在键盘下，且 _aKb/_aProv 并存堵死四条复原路＋收键盘不再派 resize＝卡死空白。
- 验证：`node build.mjs` 哨兵 **595/595 哑 0**；`tools/verify-kb-pan-dock.mjs` **42/42**；键盘家族回归 kb-overlay-kernel 10/10、ios-pwa-kbd 18/18、kb-residue-heal 10/10、ios-typing-flash 9/9、stuck-escape 28/28、copy-selection 23/23；`npm run verify` 布局 10/10；node --check 过。
- 需要对方处理：本批构建顺带收编了树上在途的 #260~#266 各批 src（bg-keep/device/contacts/chat/chatcard/incoming-requests/personalize/music-player 等），未逐一复核其内容，请各 owner 在各自【真机:待验证】通过后自行确认产物是否已含本批；若还有未收口的 src 改动，请另行构建。
- 【真机:待验证】（荣耀 X50/HonorBrowser 及任意安卓内核）：点输入栏弹键盘 → 输入栏整行浮到键盘上沿之上、可打字发送、下方无空白；收键盘后 `.phone` 贴回视口底；再弹键盘仍正常（不被自愈闩锁死下一次会话）。

### 2026-09-10 17:3x（#265 修根因：桌面图标顺序「手动改完布局、退出浏览器就还原初始布局」——小米13+Edge 报障、明说其他机型同现；本次构建者：非本会话（树上 #260~#264 在途且已由对方会话重建产物），本批未构建·请收口时构建后复验）
- [AI-B 域 personalize.js + 跨域登记资产 build.mjs/FIX-REGRESSION/tools]（**改动文件：src/js/personalize.js（三处根因，见下；`restoreAppIconOrder` 自身实现零改动）；build.mjs（FIX_SENTINELS 尾部追加 5 条 #265 逻辑锚，583→588）；FIX-REGRESSION.md（+#265 行、设备索引「多机型 LS 回滚家族」行补 265，全文件行尾保持 CRLF）；tools/verify-desk-icon-order.mjs（新增，15 条行为断言，verify-suite 自动纳管）**；构建状态：**未构建**——本会话只跑 `--check-sentinels`（只读、不写产物），588 条 src 锚全绿哑 0；树上产物是别的会话 17:0x 那几次构建的，**不含本批 #265 修复**，所以产物侧哨兵会报我这 5 条缺失属预期，收口必须先 `node build.mjs` 再提交。）
- 需求/反馈：用户「小米13+Edge+手动修改桌面布局退出浏览器后，会还原初始布局」＋「这个问题其他设备型号也有出现」＋「帮我修复，并且不要覆盖修改导致不同型号设备浏览器的 bug 反复出现」⇒ 修根因、零机型/UA 分支、不回退任何既有设备防线。
- 根因（无头 CDP 多实例复现 + TEMP 隔离副本红绿对照实证，与机型/内核无关）：**R1** 启动期「自定义图标大键 IDB 补读块」对本命名空间下所有 `app-icon-*` 键无条件 `store.set` 回写，把项目自家「LS 比 IDB 新鲜」优先级（`idb.js` retainValue / #40 / #226 / #229 判据）反向覆盖了——`idbSet` 是异步 fire-and-forget，Edge/真我/荣耀/小米 杀进程或事务挂起时丢掉这一笔写，IDB 落后于 LS，于是启动补读把陈旧 IDB 值写进内存缓存+LS、盖掉用户刚存的新顺序；本次会话 DOM 已按新值排好（当场看不出），下次启动读到的就是被盖掉的旧值＝「退出浏览器后还原」。次生两处同族：**R2** 图标顺序只在脚本加载期应用一次，`mochi-restore-done` 之后不重放（LS 副本缺失/配额满的设备整会话停在默认排布）；**R3** `contact-switched` 重应用清单有 `restoreAppIcons`（图片）没有 `restoreAppIconOrder`（顺序）→ 切桌面顺序互相串。
- 方案（最小改动）：① 补读只填「现在读不到」的键（`if (store.get(rel) !== null) return;`），大图键 >200KB 不进 LS 的正常补读场景原样保留（T4 反向对照专防此处被过度收窄）；② 补读前缀只取一次 `activePrefix()`（filter 与 slice 共用 `iconPfx`）——原两次调用之间 #88/#90 异步 `correctCidFromIdb` 可能改 cid，前缀与截取长度错配会跨桌面搬键；③ `mochi-restore-done` 重绘清单补 `restoreAppIconOrder`（重排幂等，节点移动不重建）；④ `contact-switched` 补 `restoreAppIconOrder`，与 `applyHiddenIcons` 成对。**未动**：#140/#151 小组件卡片 `desk-layout` 重放链、#40/#226/#229 写自愈（`__wr-journal`／`__wr-j:<key>`／`mochi-wrj-heal`）、`idb.js` 存储层、备份导入链路。曾评估给 `buildDeskPages`「布局收缩」分支加持久化守卫，实测该分支启动期不可达，按最小改动原则不加。
- 验证：`node --check` 过；`tools/verify-desk-icon-order.mjs`（独立 Chrome 实例×N、独立 profile、多次 `Page.navigate` 模拟「杀进程重开」）**15/15**；**红绿三向**＝①`SERVE_DIR=<HEAD 源构建的隔离副本>` 同脚本 **9 通过 6 失败**，红项恰为 T1-2/T1-3/T1-4（陈旧 IDB 回写 LS、内存缓存被污染、第三次启动还原初始布局＝用户屏上症状）、T2-3（LS 失效设备未按 IDB 权威值重排）、T3-1/T3-2（切桌面顺序不跟随）；②**定点摘锚**＝在同一份已构建好的修复产物上把 R1/R2/R3 三处锚逐条删掉再复跑，仍 **9 通过 6 失败**（红项 T1-1/T1-2/T1-3/T1-4 + T3-1/T3-2）——单锚失效当场变红，不是靠整批回退才报红；③摘锚副本 T4 三条全绿＝反向对照没把「大图键仍只存 IDB 也能补回」这条原意图一起弄坏。`node build.mjs --check-sentinels` **588 全绿哑 0**（登记本批 5 条后即时数；收口前复跑已 595＝并行会话又追加了自己的锚，哑仍 0）。相邻回归（desk-persist / desk-incoming / 图标隐藏与美化同族脚本）绿；两条既有红项（desk-persist 的拖拽模拟、longpress 方案导出 UI）经 HEAD 与仓库产物复跑**字节一致**，归属其 owner，非本批引入。
- 脚本踩坑留档（后来人写 CDP 类 verify 直接抄）：① 随机调试端口会连上本机常驻 Chrome（曾撞 9990/9991）→ 用 `--remote-debugging-port=0` + 读 profile 里 `DevToolsActivePort`；② CDP `Runtime.evaluate` 要**自己包并调用** IIFE，且 `send()` 已解包过一层 `m.result`，取值是 `.result?.value`（多套一层＝所有探针静默返回 undefined＝假失败/假绿的复合来源）；③ `boot()` 不能只看 `readyState==='complete'`（about:blank 本来就是 complete）；④ **反向对照必须双向**：页内 stub 不跨导航存活，T1「Edge 丢 IDB 写」首版因此被写自愈机制治好＝假绿，改成 LS 布防 + `addScriptToEvaluateOnNewDocument` 持久注入；T2 同理必须连 `__wr-journal` 的 LS 镜像键一起遮（该键带 `xy-home-v2:` 前缀，精确 `=== 0` 匹配会漏）并加 DOMContentLoaded 现场快照当前提。
- 需要对方处理（构建者）：① 收口构建后跑 `node tools/verify-desk-icon-order.mjs`（默认读仓库根产物），应 15/15 且 5 条 #265 needle 产物全绿；② **编号**：本批占 **#265**（开工时 `grep -c '#265'` 在 build.mjs/FIX-REGRESSION 均为 0 才用；#264 让位给 incoming-requests 会话、#266 已被 pwa.js 会话使用），请后续按台账最大号 +1 取号；③ 本会话没碰 device.js（#260/#261/#264 三批并存）与 `src/pwa/`，无跨文件冲突；README/开屏公告未动（纯失效修复、非新功能）。
- 【真机:待验证】（小米13/Edge 及其他安卓内核）：装修模式拖动图标改顺序→彻底杀掉浏览器→重开，顺序保持；备份导入后当场重排不必等重启；切换联系人时各桌面顺序各自跟随。若仍复现，请带诊断信息（LS/IDB 键值与 `__wr-journal` 状态）回来，脚本可按同一注入手法复现。

### 2026-09-10 18:0x（#263 修【音乐】网易云歌单导入只进 10 首 + 移动端歌单链接被当单曲——iPhone 14 Pro/Safari 反馈、多机型同族；未构建·本会话非构建者（树上 #260/#261/#262 在途，且 #260 会话已重建产物并含本批改动），请构建者收口时一并核验）
- [AI-A 域业务修复 + 跨域 build.mjs/FIX-REGRESSION/tools（哨兵尾部追加 6 条、新增 #263 台账行、verify-music-playlist.mjs 新脚本、verify-music-vip-filter.mjs 桩放宽）]（**改动文件：src/js/music-player.js（extractPlaylistId 正则分隔符类补 `# ! & \s`；新增 canonicalMetingPicUrl/parseMetingPlaylist/parseOfficialPlaylist/NETEASE_TRACKS_TRUNC；fetchNeteasePlaylist 整体重写为「多源并发 + 取最长」；importNeteasePlaylist/importPlaylistIds 与两处导入面板 toast 补 miss 缺口记账）；build.mjs（FIX_SENTINELS +6）；FIX-REGRESSION.md（+#263 行）；tools/verify-music-playlist.mjs（新增 23 断言）；tools/verify-music-vip-filter.mjs（stub 改 host 无关：本批改多源后若仍精确匹配 injahow，该测试会打真网络破坏纯度；断言零改动）**；构建状态：**未构建**——开工时树上另有 #260（bg-keep/device/contacts）/#261（device/mobile-adapt）/#262（chat）三批未提交在途，且对方在本会话改完 music-player.js 之后已重建过产物（实测 sw mochi-mtvavof9（对方又重建过一次）的 index.html 内 6/6 条 #263 needle 全在＝本批修复已在产物中，重建只是复现同一结果），按 AGENTS.md 不代构建防夹带半成品）。
- 需求/反馈：用户 iPhone 14 Pro + Safari（PWA standalone，诊断 v3.26.520 ts=1788740934736，歌单 18366934337 共 62 首）「链接导入网易云歌单 无法导入完整歌单 只能导入 10 首」，明说其他机型也有，并要求「不要覆盖修改导致不同型号设备浏览器的 bug 反复出现」。诊断附带 playlist/detail 代理网络失败 ×N 与一条 `music.163.com/song?id=<歌单ID>`。
- 根因（两条，均与机型/内核/网络无关，所以不需要任何机型分支来修）：① 网易 detail 的 `tracks` 对**用户自建歌单只给首屏 10 首**（全量在 trackIds/trackCount，官方榜单才全给），而旧 fetchNeteasePlaylist 串行问源、「第一个非空源即收口」→ 首位 injahow 永远以 10 首抢收，后面的全量源根本没被问到。实测同一张歌单 qijieya 62 / injahow 10（真网络 + 无头桩双向证实，非推测）。② extractPlaylistId 分隔符类不含 `#`/`!` → 移动端「复制链接」`m/playlist#!?id=xxx` 漏判，整张歌单落到单曲路径（诊断里 `song?id=<歌单ID>` 即实证）。
- 方案：多源并发取最长（同数取靠前者）＋「曲目数恰为 10＝截断签名」不可信收口（1.5s 宽限 / 8s 硬兜底 / 每路 7s abort，死代理不再拖长导入）＋ trackCount 差值如实提示「数据源受限，另有 N 首未取到，稍后重导同一链接可补齐」（去重只跳已有 ⇒ 重导真能补齐）＋ 各实例封面 URL 归一 injahow 接回 #216 迁移链。官方 v6 代理源排 meting 之后＝不抢既有链路、只在给出更长列表时胜出（顺带自动吃到 fee 前置过滤与 dt 时长）。既有资产（#254 可播放性判据、#216 封面迁移、#157 窗口化时长探测、meting 播放链、fee 语义）零改动。
- 验证：node --check 过；`node build.mjs --check-sentinels` **573 全绿哑 0**；tools/verify-music-playlist.mjs **23/23**（S1-S9 静态接线，含 S8「修复函数体内零 UA/机型判定」反向断言；P1-P14 走真实「链接添加」面板端到端：62 首全量、全量源死亡仍 best-effort 10 首、两源各 10 不误伤、官方榜 200 首快速收口、直链/封面归一、移动端分享链、官方兜底 fee 过滤+时长、缺口如实提示、重导补齐 52 零重复、单曲/mp3/纯数字反向不误判、全源失败无未捕获异常、混合批 63）；**SRCDIR=HEAD src 快照红绿对照 9/23**——红项恰为 P1「导入 10 首」（用户屏上原文复现）/P7「链接音乐已添加」1 首/P5·P6 单实例一挂整单失败/P9·P10·P14 缺口路径；家族回归单跑 vip-filter 6/6、vip-clean 8/8、dur-cover 9/9、cover-direct 17/17、history-split 14/14、filter 15/15。
- 需要对方处理：① 下一次 `node build.mjs` 时确认 6 条 #263 needle 产物全绿（当前产物已含，正常重建不变）；② 若后续再加歌单数据源，verify-music-vip-filter 的桩已 host 无关、无需再改；③ README/开屏公告未动（纯失效修复、非新功能，文案「链接导入网易云歌单」仍准确，如需播报由构建者决定）。
- 【真机:待验证】（iPhone 14 Pro/Safari 及任意机型）：贴 62 首歌单链接 → toast「已导入 1 个歌单 / 62 首」；手机端「复制链接」的 `m/playlist#!?id=` 链接整单导入不再只进 1 首；只拿到首屏时如实提示「另有 N 首未取到」且稍后重导同一链接能补齐不重复；官方榜单/含 VIP 的歌单行为与之前一致。

### 2026-09-10 17:3x（#264 修根因：跨桌面查岗/来电「开了一星期一次都没触发」＝孤儿 pending 永久挡死 hasPending——本次构建者：非本会话（#260 会话 17:03 已构建），本批未构建·请收口时先重新构建再提交）
- [跨域改动登记（incoming-requests.js 属 AI-A 域、device.js 属 AI-B 域，两处均为本批主体）]（**改动文件：src/js/incoming-requests.js（调度层修复主体）、src/js/device.js（诊断新增「跨桌面来消息体检」行，与 #260「保活现场」/ #261 复制塌选区同文件不同区块）、build.mjs（FIX_SENTINELS 尾部追加 10 条 #264，总数 573→583）、FIX-REGRESSION.md（+#264 行；设备索引 +iPhone 12 Pro）、tools/verify-desk-incoming.mjs（新增，39 条行为断言）**；构建状态：**未构建**——17:03 那次构建已收编本批前 9 条锚，但缺 17:04 才补的「逃逸额度一次性消费」（实测 `escape = false` src=4 / 产物=1），产物在这一条行为上落后 src；树上另有 #260/#261/#262/#263 在途未提交，按 AGENTS.md 不代构建防夹带。）
- 需求/反馈：用户 iPhone 12 Pro + 自带 Safari（诊断 v3.26.529 ts=1788796421477）「多角色，一直开启跨桌面查岗和跨桌面打电话，频率选的是标准，但最近一星期多了一次都没有过」＋「帮我修复，并且不要覆盖修改导致不同型号设备浏览器的 bug 反复出现。这个问题其他设备型号也有出现」⇒ 修根因、零机型分支、不回退任何设备/内核历史防线。
- 根因（无头复现 + HEAD 红绿对照实证，与机型/内核无关，所以不需要任何机型分支来修）：跨桌面请求队列存 localStorage（跨刷新存活），而 `openModal` 弹窗只活在投出它的那个页面会话里——刷新 / iOS 内存回收重载 / 返回键（tabs.js popstate）/ 被别的 openModal 顶掉 / #257 逃生门复位，任何一条不走弹窗自身回调的路径都会留下永不应答的 `status:'pending'`，`hasPending(cid)` 从此挡死该联系人的一切跨桌面触发（查岗＋来电＋求聊天全灭）；一星期下来每个非激活桌面各留一个孤儿＝用户视角「一次都没有」。放大器：只在页面活着且前台时掷骰（iOS 后台冻结定时器）、首查延迟 30~90s 让「开一下看一眼就走」的短会话一次都掷不到、多联系人同轮投进全站唯一的 `#modal-mask` 互相顶掉（自造孤儿）。
- 方案：① 投递写 `sid` 会话归属 + `PENDING_TTL_MS`=10min 跨会话孤儿自愈（超龄转 seen 并留释放日志）；② `liveModals` + `reconcileLiveModals()` 每轮对账「遮罩在且标题仍是当初投出的那个」，被顶掉/关闭当场释放、不干等 TTL；③ 三态闸门 `hardLocked`（#applock-mask，#259 冷启动默认开）/`typingBusy`（抢焦点吞 IME 组合字）/`layerBusy`（modal/tc/qa/call 四类），拒绝即不入队、不写冷却，下一轮照样有机会；④ 让路上限 `BUSY_ESCAPE`=3 轮后动用一次性逃逸额度照投、投成功即收回（防软互斥变成新的永不触发，也防逃逸轮同轮投 2 个弹窗再造孤儿）；⑤ 首查 30~90s→12s ＋ 回前台 3s 补掷（iOS 冻结后切回来不白等一整分钟）；⑥ 每轮至多一个弹窗（`deliver` 返回布尔）；⑦ 诊断出口一行直出现场。**未动**：概率/冷却口径、#159·#204 后台与系统通知路径、两段式「确认」弹窗、`avFixed` 头像权威、#255/#257 键盘与逃生门逻辑。
- 验证：node --check 三文件过；`node tools/verify-desk-incoming.mjs` **39/39**；**红绿双向**＝`SRCDIR=<HEAD src 快照>` 同脚本 **11/39**（红项恰为本批逐条行为：孤儿不自愈、被顶掉不释放、锁屏/打字照投、浮层顶掉用户弹窗、首查 30~90s）；`node build.mjs --check-sentinels` **583 全绿哑 0**。
- 需要对方处理（构建者）：① **收口时先 `node build.mjs` 再提交**——当前产物缺「逃逸额度一次性消费」，只提交产物＝逃逸轮同轮开 2 个弹窗的老行为上线；构建后 `PRODUCT=1 node tools/verify-desk-incoming.mjs` 应仍 39/39，10 条 #264 needle 产物全绿。② device.js 现三批并存（#260 保活现场、#261 mochiKillCopySelection、#264 跨桌面来消息体检），本批只动自己新增的区块，请 diff 自查未互相吞行。③ **编号让位**：本批开工时占的 #260 已被 bg-keep 保活修复使用，源码注释与台账已统一改记 **#264**，请 #260 会话不必再回头动我这些行。④ README/开屏公告未动（纯失效修复、非新功能）。
- 【真机:待验证】（iPhone 12 Pro/Safari 及任意机型）：更新构建后 1~2 天内应遇到跨桌面查岗/来电；刷新或切走再回来后仍正常触发（不再被旧 pending 永久卡死）；若仍为零，设置→诊断信息「跨桌面来消息体检」行直出轮询次数/闸门/下次可掷/近期释放，下一步按现场取证而非再猜。

### 2026-09-10 17:1x（#260 修后台保活失效：vivo X200s Edge 152「浏览器后台依然保活失败，弹窗收不到消息，以前可以」——本次构建者：本会话（AI-B 域））
- 需求：用户「帮我修复，并且不要覆盖修改导致不同型号设备浏览器的bug反复出现。这个问题其他设备型号也有出现」⇒ 零机型分支，#88/#150/#153/#190/#207 历史防线全保留。
- 根因（bg-keep.js 通用根因）：保活只押「正在播放音频」一个冻结豁免信号；#190 后数字电平 0.006×0.05=0.0003 距 Chromium audible 判定线仅 20% 余量，Edge 151→152 收紧判定即丢豁免 → Chromium 139+ 的 1 分钟后台冻结直接生效（#153 的隐藏期补播钳制只能保证「音频被认定在播时」静默窗口短，豁免本身丢了它救不回）。「以前可以」=0.02×0.05=0.001 电平时代；底噪根因在 220Hz 频率（#207 已换 18kHz）不在幅度。
- 改动文件：**src/js/bg-keep.js**（①amp 0.006→0.02 恢复 4 倍电平余量，18kHz 频率与 iOS 220Hz@0.002 bit 级不动；②WebRTC 页内回环双 RTCPeerConnection + 'mochi-ka' 数据通道=第二冻结豁免锚，host candidate 本机回环零外发流量/无音频焦点/无声，断线 30s 静默重建，start/stop/heal 三处接线；③后台心跳：隐藏期每 30s 写 IDB xy-home-v2:__ka-hb（计数+最近 8 拍轨迹）回前台补记 resumed，独立 visibilitychange 监听不与 #153 共用；④window.__kaProbe 诊断出口）；**src/js/device.js**（诊断新增【保活现场】行：保活/通知/音频/媒体条/WebRTC/心跳，相邻拍>90s 直出「冻结证据：心跳断流Xs」）；**src/js/contacts.js**（EXCLUDE + '__ka-hb'）；**build.mjs**（#190 needle 0.006→0.02 同步 + 尾部 4 条 #260 哨兵，总 567）；**tools/verify-keep-audio.mjs**（幅度断言同步 0.02 + 新增 7 条双锚/心跳/探针断言）；FIX-REGRESSION.md（#260 行）；WORKLOG.md。
- 协作回执：#261 会话在 WORKLOG 请求的「#190 哨兵 needle 更新为新幅度锚」已随本批完成（567 条全绿哑 0，pre-commit 不再被该锚卡住）。
- 验证：node --check 五文件过；--check-sentinels 567 全绿哑 0；已构建 sw mochi-mtvavof9（version.json ts=1789031023461）产物哨兵 567/567；verify-keep-audio **25/25**；verify-copy-selection 23/23（device.js 同文件改动未破坏 #261 断言）；npm run verify 10/10。
- 注意：17:03 构建按惯例收编了当时在途的并行改动（chat.js #262 已完稿自验 24/24、music-player.js、incoming-requests.js 等）——本会话未逐一复核对方内容。未提交、未推送。
- 【真机:待验证】（vivo X200s Edge 152 及任意机型）：更新构建后挂后台 10 分钟以上 → 通知栏照常弹 TA 消息；若仍失败，诊断里新增的【保活现场】行直接出「心跳断流Xs」冻结实锤，下一步修复有数据可依、不再盲改。

### 2026-09-10 17:0x（#262 修复：表情包「内容为空」误报——canvas 画不出动画图第二帧，正常动图被判坏图（iPhone 17 Safari 报、机型无关）；本次构建者：本会话（AI-B 域））
- 需求（用户两轮）：①iPhone 17 + 默认 Safari 诊断报「消息出现"表情包为空"提示，但字卡库中无可清理表情包」；②「帮我修复，并且不要覆盖修改导致不同型号设备浏览器的bug反复出现。这个问题其他设备型号也有出现」。
- 根因（实锤非猜测，双引擎复现）：屏上原文＝#205 的「表情内容为空…到字卡库清理该分组」。#205 用 `canvas.drawImage` + 24×24 alpha 全 0 判空，而 **canvas 画动画图只画得出第一帧**；表情包 GIF 首帧经常是整幅全透明清屏帧（disposal=2），且字卡库 GIF **直存原图保留动画**（chatcard.js v3.7.x）→ 正常播放的动图被整块换成占位文案，图本身健康所以字卡库当然「无可清理」。无头 Chromium 与 WebKit 跑同一张首帧透明的 GIF/APNG **同果＝与机型/内核无关**，任何共用该字卡库的设备都中招。红方向实测：回退 chat.js 到 HEAD 重新构建，verify 脚本 A3/A4 直接复现用户屏上原文（10/24）。
- 改动文件：**src/js/chat.js**（`bindMediaFailPlaceholder` 内 #205 load 监听三道防线——①`alphaCheckable(src)` 静态图门禁：仅 data:image/{png,gif} base64、≤96KB、GIF 数 `21 F9 04` ≥2 即多帧不判、PNG 块表见 `acTL` 不判、远程图/`@@m:` 令牌/webp·svg/结构异常一律不判；②首采全 0 后 350ms 独立复采仍全 0 才下结论；③占位整行可点「点此恢复显示」复位 `<img>` 并置 alphaChecked 不复判，文案不再单方面指向字卡库。口径同 #186/#247「宁可不报绝不误报」；#205 真阳性与 #202/#186 占位全保留）；**build.mjs**（+5 条 #262 逻辑锚哨兵）；**tools/verify-media-empty.mjs**（新增：S 组从 src 抽 `alphaCheckable` 直接跑纯逻辑 13 条 + A/B/C 双引擎端到端 11 条）；**FIX-REGRESSION.md**（+#262 行）；WORKLOG.md。
- 验证：node --check 过；**verify-media-empty chromium 24/24 + webkit 24/24**（A3/A4 本 bug 核心、A1/A2 #205 不失守、B1/B2 #202·#186 不回归、C1/C2 恢复出口）；红绿双向＝HEAD 源 10/24 且 5 条哨兵报缺；家族回归 verify-sticker-retract 12/12、verify-media-pool 8/8、verify-sticker-dup 32/32、布局 verify 10/10。sw **mochi-mtvan370**／version.json ts=1789030622700＝树上最新产物（恢复 chat.js 后重建那一次；#261 会话已在其条目里明确把构建者身份让给本会话、不再重复构建）。【真机:待验证】iPhone 17/Safari 及任意机型：动图不再报空；真空白图仍提示且点提示能恢复。
- ⚠️【给 #261/#260 并行会话】①**build.mjs #190 哨兵**：你们已自行把 needle 同步到 bg-keep.js 的 #260 新幅度（复查 `--check-sentinels` 现 **567 条全在位、哑 0**），本条不再挂账——只提醒 **needle 改动务必与 bg-keep.js 同批入库**，否则下次构建又会红。②**我误伤了你的 FIX-REGRESSION #261 行**：我登记 #262 时 Edit 用错 old_string 把 `| 261 | 用户报障 荣耀畅玩40 Plus + 夸克` 行首吃掉，随后 `git checkout HEAD -- FIX-REGRESSION.md` 回退（当时 git status 未列该文件＝你的 #261 行是**未提交在途改动**、HEAD 只到 #259）。已按我读取到的原文逐字重建该行（现台账 249 行，顺序 259→260→261→262 完整，#260 行是你们自己补的），**请 #261 会话 diff 自查措辞是否与你的原稿一致**，若你编辑器里还有原始缓冲请覆盖回写。
- 【产物纯度·待用户裁决】本会话构建的 index.html/sw.js/version.json 含对方在途未提交 src：device.js/incoming-requests.js/mobile-adapt.js/music-player.js/bg-keep.js。**未提交未推送**。另注：本产物构建时点早于 #260 会话随后的 bg-keep.js/contacts.js/verify-keep-audio.mjs 编辑（其 #190 哨兵已自行同步、现全绿），**提交前需重新构建一次**让产物与 src 对齐。提交路径二选一：a) 等两批在途 src 各自收口后一并构建提交（线上一次带走三件事）；b) 按 WORKLOG 先例「备份→回退 HEAD→构建→还原」做纯净构建，产物＝已收口 src + 本批 chat.js/build.mjs。请用户确认走哪条再提交。

### 2026-09-10 17:0x（#261 修根因：安卓桌面卡着消不掉的黑色【全选】＝复制残留选区喂大的原生选择工具条——本次构建者：本会话（AI-B 域），但已被并行会话 16:53 的构建收编，见「交付」）
- 需求（用户两轮）：①报障 荣耀畅玩40 Plus + 夸克「桌面的今日情话右边出现了【全选】的黑色按钮，退出重进后还是一样没有消失」（附完整诊断：viewport 423×853、ce-box 转换生效、无相关 JS 报错）；②「帮我修复，并且不要覆盖修改导致不同型号设备浏览器的bug反复出现。这个问题其他设备型号也有出现」⇒ 要求修根因、跨机型安全、且不得回退历次已修的设备/内核行为。
- 根因（排除法 + 现场 DOM 探针）：黑色【全选】不是本项目 DOM（全站只有各批量管理条的 .fb-btn/.ti-batch-btn，都不在桌面、都不随页面 hidden 渲染），它是**浏览器原生文字选择工具条**（ActionMode 浮层，归浏览器所有 → 切后台/重进都不销毁），宿主＝文档/文本控件选区。选区来源两处：①device.js 三处复制兜底（copyText/sdCopy/fCopy）隐藏 textarea `select()` → `execCommand('copy')` 后不塌选区、800ms 后连节点 removeChild；②桌面 home.css `user-select:none` 被部分内核无视自造选区。**实测新发现（写进哨兵与脚本，防后人再猜错）**：Blink 移除选区宿主时把选区**重挂到 document.body**（仍活着）而非留孤儿 ⇒ 回收器按设计看不见它，**复制站点当场塌选区才是主防线**，回收器只是桌面内自造选区的兜底。
- 改动文件（全部 AI-B 域 + 跨域登记资产）：**src/js/device.js**（新增 `window.mochiKillCopySelection(ta)`＝setSelectionRange(0,0)＋仅它是 activeElement 时 blur＋removeAllRanges；三处复制站点在 execCommand('copy') 之后、延迟 removeChild 之前调用，调用点带 `&&` 守卫防 helper 所在 IIFE 早退把已成功的复制抛成「复制失败」＋重复走 clipboard API）；**src/js/mobile-adapt.js**（新增 `reapDeadSelection()` 回收器：编辑区/桌面外一律不动、只在「非编辑区 且 在 #page-phone 内」才收；`window.__mochiReapSelection` 只读探针；接线 pointerdown/touchstart 捕获＋selectionchange＋visibilitychange＝「重进没消掉」路径当场补收）；**build.mjs**（FIX_SENTINELS 尾部追加 3 条逻辑锚，总数 563）；**tools/verify-copy-selection.mjs**（新增行为断言脚本，verify-suite 自动纳管）；FIX-REGRESSION.md（新增 #261）、WORKLOG.md。
- 未动的设备防线（用户红线）：execCommand 优先于 navigator.clipboard 的顺序不变（v3.26.x 防 WebView 权限弹窗/整页重载）；不重新引入 `ta.focus()`（v3.27.x 防弹键盘）；不扩全站 user-select；零机型/零 UA 分支。
- 验证：node --check 三文件过；**tools/verify-copy-selection.mjs 23/23**（S1-S8 静态＋产物接入；A1-A4 复制点塌选区，每条先立「选区确已建立」前置＝双向咬合；B1-B6 回收器含 B3a 旧路径对照组与三条「不误清」反向断言；C1 桌面禁选 CSS 未被顶掉；C2 全程零未捕获 JS 错误）；`--check-sentinels` 563 条锚点全在位。反向对照：`git show HEAD` 的 device.js/mobile-adapt.js 里 mochiKillCopySelection／reapDeadSelection 命中 0 ⇒ 无修复时 S1/S3/S4 必红。脚本调试中修正的 5 处均为**断言侧**问题（注释里的 execCommand 误算站点／跨域 divination 站点未列允许清单／无头 blur() 不改 activeElement／Blink 重挂 body 导致 B3 前置假设错／ceConvert 把非 readonly textarea 的值搬走），产品代码未因断言而放宽。
- 交付：本会话曾构建一次（sw mochi-mtva4on0）；**16:53 并行会话又跑了一次 build（现 sw mochi-mtvai5ny），其产物已含本修复**（verify S2 covered=3、S7/S8 探针均为 function 实证），故本会话不再重复构建、构建者身份让给它，以免再夹带对方在途的 chat/bg-keep/music-player/incoming-requests 半成品。**未提交、未推送。** 该次构建之后本批只改过 device.js/mobile-adapt.js 的 #261 **注释措辞**（把「孤儿选区」改成实测的「重挂 body」，逻辑逐字未动，改后 verify 仍 23/23）——产物里的注释暂落后于 src，下一次构建自然收编，不必为此单独 build。
- 需要对方处理：①**AI-A**：`divination.js` copyFallback（585 行）是同族残留——`ta.select()` → execCommand → 立即 removeChild 且无 readonly（还会被 ceConvert 转换），本次跨域未改，已在 verify S2 登记为「已知跨域站点」（对方修好后自动转全绿，新增别的漏站仍会红）。②**#260 会话（bg-keep.js）**：`#190 保活音频幅度` 哨兵 needle `kaIsIOS() ? 0.002 : 0.006` 已被该会话有意改成 `0.02`（回调根因写在注释里）， needle 未同步 → `--check-sentinels` 现报该条 src 锚点缺失，**pre-commit 钩子会因此拒绝任何含 src/ 改动的提交**（包括我这一批），请该会话把 #190 哨兵 needle 更新为新幅度锚。③**verify-diag-report.mjs 1 项红＝过期期望、非回归**：终态断言「终态有复制结果反馈（hint 文案变化）」要求诊断弹窗打开即自动复制并改 hint，而 v3.26.x 已刻意取消自动复制（device.js 1830-1835 注释；该断言 TIP 里的「请点下方【复制】」字符串在 device.js 已不存在），终态 hint 停在 TIP_WAIT 是设计行为；脚本自 0b4ad34 未更新，且相关行不在本批 diff 内（与 #261 无关）。请脚本 Owner 把期望改为「打开不自动写剪贴板（hint 保持读取中）」＋「点【复制】后 hint 才变」。④**verify-scroll-lock-ghost.mjs 在 suite 并发 3 下 T4/T5 红、单跑 9/9 绿**＝250ms 看门狗被并发无头 Chrome 拖慢的 flake，不是回归（本批同跑 verify.mjs 10/10、verify-stuck-escape 28/28 均绿）。

### 2026-09-10 12:2x（用户反馈收口：问答门暗号 990915 撤下屏 + 问答门改为「未输暗号时每次页面加载必问」——本次构建者：本会话（AI-B 域））
- 需求（用户两轮）：①「为什么【输暗号 990915，本机永久跳过问答】在页面直接显示出来了，没有隐藏」；②「我是要在没有输入暗号时，2 个问答题是每次打开都出现，不回答无法关闭」（经 AskUserQuestion 确认「含同标签刷新」）。排查结论：不是渲染 bug——三处均为硬编码文案无隐藏逻辑；问答门又共用密码锁的会话豁免（evalLock `if (sessOk()) return`），同标签刷新会免问。
- 改动文件：**src/js/applock.js**（①syncQaUi 开/关两态文案去 990915；②qaAsk 底部入口 label 改「输暗号跳过问答（本机永久）」；③evalLock 拆分会话豁免——needQa 分支前置不读 sessOk（每次加载必重答），密码锁保留 sessOk 短路；问答答对后 `needPin && !sessOk()` 才进密码屏；④头部「解锁态」注释同步）、**src/template.html**（#applock-qa-sub 静态文案去 990915）、**tools/verify-applock.mjs**（I5 断言反转=I5「同标签刷新仍问答 1/2」+ 新增 I5b「sess=1 仍问」，头部覆盖说明同步）、**FIX-REGRESSION.md**（新增 #259）、WORKLOG.md。build.mjs 无改动。
- 验证：node --check 两文件过；**构建哨兵 555/555 全绿哑 0**；**tools/verify-applock.mjs 64/64**（I5/I5b 新断言通过；D1「密码锁同标签刷新不重锁」保持绿=未越界改密码锁；N 组默认开启、J 组双重验证、L 组题目管理全绿）；产物 grep「990915」仅剩常量 1 处（实证不再上屏）。
- 交付：sw mochi-mtv0sacf；version.json ts=1789014069087。开工前 git status 仅 docs/ 有未提交改动、src 干净无并行在途；提交用显式文件清单（不含 docs/）。【真机:待验证】设置页/问答屏不再出现暗号数字；未输暗号时刷新仍要重答两道题且无法关闭；输对暗号后本机永久不问；数字密码锁同标签刷新仍不重锁。

### 2026-09-09 14:0x（开屏问答门改为「默认开启」——用户反馈换浏览器/新设备首启没有问答（此前=设置开关默认关+每浏览器独立存储）；按 AskUserQuestion 确认改为默认开启：键 applock-qa-en 未显式设置即视为开启，存 '0' 才显式关。实现：qaEnabled() 键值 '1'/'0' 优先、null→真机默认开；自动化豁免 = 无头 UA(Headless) 或 navigator.webdriver（实测 headless=new 下 webdriver=false 不可靠，UA 判定才稳）→ 无头空库回归脚本不被问答层挡崩；evalLock 启动时序改「真机等开屏消失后再弹问答」（符合点我已阅读并知晓进入后才问答的需求，开屏仍在则 500ms 轮询最多 30s 兜底），自动化立即评估保测试兼容；设置卡片文案标注「默认开启·关掉即本机不再询问」。改动：applock.js / template.html（#applock-qa-sub 文案）；verify-applock.mjs 新增 N 组（注入非无头 UA+webdriver=false+摘除开屏模拟真机 → 键未设仍出问答 N1、答对放行 N2）。构建哨兵 555/555 哑 0；verify 63/63（原 61 + N1/N2）。注意：gc-send 脚本 4/5 系既有失败（旧产物同样失败，与本次无关）。已构建·已验证·本次构建者：本会话）

### 2026-09-09 13:5x（修复：应用锁/开屏问答门遮罩「看不见」——maskEl 只设 id 未加 class，CSS 全用 .applock-mask 类选择器 → 遮罩无 fixed/层级/遮底样式，视觉不出现（DOM 在、hidden=false，无头验证只查 DOM 抓不到）；真机反馈「点我已阅读并知晓后没有问答也没有锁屏」。修复：applock.js maskEl 补 MASK.className='applock-mask'（保留 id 供 mobile-adapt FLOAT_SELECTORS/脚本定位）；verify-applock.mjs 新增 A3 视觉断言（class+position:fixed+z-index:999999+遮底背景），防此类回归。构建哨兵 555/555 哑 0；verify 61/61（原 60 + A3）。暗号 990915 不受影响。已构建·已验证·本次构建者：本会话）

### 2026-09-09 13:4x（开屏问答门（防偷看升级）——设置页新增「开屏问答门」独立开关：开启后冷启动先答问答题（默认两道，可增删改），可不设数字密码单独用；与应用锁同开＝先问答再密码双重验证；锁屏可输暗号 990915 本机永久跳过问答层；已构建·已验证 60/60·本次构建者：本会话）
- [AI-B 域功能增强（applock.js/css/template.html/contacts.js/build.mjs/verify 均 AI-B；开工 git status 仅 docs/ 未提交，src 干净，无并行在途）]（**改动文件：src/js/applock.js（问答门存储层[答案摘要不落明文/qaRaw/qaList/qaSave/qaSkip 键]；evalLock 改双门禁[问答门→数字锁，任一可独立]；textAsk 支持 links/预填 value；qalist 题目管理面板[增删改，答案留空=保留]；qaGuard 前置验证[有密码输密码/无密码输暗号]；设置页 #applock-qa-en/#applock-qa-sub 同步与绑定；本会话免问走 applock-qaskip）；src/css/applock.css（.al-qa-list/.al-qa-row 题目管理列表样式）；src/template.html（「开屏问答门」设置卡片 + 开关行）；src/js/contacts.js（applock-qa-en/qalist/qaskip 进 EXCLUDE）；build.mjs（contacts 哨兵 needle 更新 + 问答门本体/设置行哨兵×2）；tools/verify-applock.mjs（seed 清键扩展；I 组问答门 12 断言/J 组双重验证 4 断言/K 组静态防线 6 断言）**；构建状态：**进行中·本会话执行**）。
- 需求：用户在应用锁基础上追加——开屏进入前再问两道题（mj 是什么意思→梦角；是否已知晓全站为随机代码运行不含任何 AI 不要添加负面字卡吓唬自己→是），「应用锁」可不设；新增暗号可跳过两问答题每次不用回答。AskUserQuestion 确认：搭配＝问答门+密码双重验证（先答对两题再输密码）；暗号效果＝本机永久免问（localStorage qaskip，不随会话）；题目可修改（qalist 管理）。**暗号最终定为 990915**（用户嫌 0929 太简单改 990929，后又定为 990915；实现按 990915，所有文案/验证同步）。
- 安全边界同步：问答答案 cyrb53 摘要不落明文；纯前端防日常偷看，懂技术者可读本机存储——设置页文案保留提醒。
- 验证：node --check 全过；构建哨兵 **555/555 全绿哑 0**（contacts needle 更新后无哑哨兵命中）；tools/verify-applock.mjs **60/60**（A–H 原 24 项无回归；I 问答门 12 项[问答屏/答错/答对/同标签会话/暗号 990915 永久跳过/恢复]/J 双重验证 4 项[先问答后密码]/K 静态 6 项/L 题目管理 13 项[增删改、答案留空保留、删到 1 禁止]/M 钩子 1 项）。发现并修复：qaEditItem 新增/修改提交后误调 qaRerender（需 qalist 屏）→ 改 qaPanel() 重开面板。【真机:待验证】设置→开屏问答门→开启→关浏览器重开答两题；点「编辑问答题」改题；点「输暗号 990915」后重启不再问。

### 2026-09-09 12:4x（应用锁（防偷看）功能落地——设置页新增「应用锁」：数字密码+安全问答重置；已构建·sw mochi-mttlonw1·本次构建者：本会话）
- [AI-B 域新功能（无并行在途：开工 git status 仅 docs/阿里云OSS 文档未提交，src 干净）]（**改动文件：src/js/applock.js（新增，隐私锁全逻辑：冷启动/新开标签锁屏、cyrb53 摘要存 PIN/答案、安全问答重置、设置页开关与子按钮、全交互自绘[数字键盘/文本输入/说明屏]不依赖 openModal——其取消无回调会卡死且层级低）；src/css/applock.css（新增，遮罩/键盘/输入屏样式，全变量深浅双主题）；src/template.html（设置页加「应用锁」开关组 #applock-en/#applock-sub）；src/js/contacts.js（applock-en/pin/qa 进 migrateLegacy EXCLUDE）；src/js/mobile-adapt.js（FLOAT_SELECTORS +#applock-mask 背景滚动锁）；build.mjs（jsFiles/cssFiles 注册+哨兵+3=553：applock 本体/EXCLUDE 锚/template 入口；#121 call-active 哨兵 needle 同步防失配）；tools/verify-applock.mjs（新增无头 24 断言）**；构建状态：**已构建·sw mochi-mttlonw1·本会话执行**）。
- 需求：用户要「防别人偷手机打开这个网站偷看聊天记录」。形态经用户确认：数字密码锁（4-6 位）；仅打开时锁（冷启动/新开标签要密码，同标签刷新不重锁，切后台不锁）；忘记密码走安全问题问答重设。安全边界已如实告知：纯前端本地锁挡日常偷看，懂技术者可直读本机存储。
- 设计要点：解锁态存 sessionStorage（非数据层）；en=1 无密码异常态自愈关闭防把用户锁死；锁屏/问答/重设全在 #applock-mask 单遮罩内流转；README 未动（用户可感知新功能待用户决定是否加开屏介绍）。
- 验证：node --check 全过；构建哨兵 **553/553 全绿哑 0**；tools/verify-applock.mjs **24/24**（A 冷启动锁/B 错密/C 解锁+session 标记/D 同标签刷新不重锁/E 会话清后重锁/F 异常自愈置 0/G 忘密码问答全流程新旧密替换/H 静态防线）。【真机:待验证】设置→应用锁→开启设 4-6 位密码（建议顺手设安全问答）→ 关闭浏览器重开 → 输密码进入；关开关/改密码需先验证当前密码。


- [AI-B 域+跨域 build.mjs/FIX-REGRESSION（开工时树上另有 #251 群聊设置批/#257 死点击批在途，其 build.mjs 哨兵与 src 改动已随本批产物一并构建，均已 WORKLOG 声明；本会话未触碰其文件内容）]（**改动文件：src/css/dark.css（749 条嵌套规则机械展平为带 [data-theme="dark"] 完整前缀的扁平选择器——语义等价：特异性/级联顺序/garden·memo 镜像链 +1 特异性全保持；文件头加「禁止 CSS 原生嵌套」铁律）；src/css/chat-main.css（删拍一拍 tab 段一个不配对多余 `}`）；build.mjs（哨兵+1=550）；tools/verify-dark-mode.mjs（扩 E1-E3 静态断言：产物含展平拍一拍规则/产物零嵌套深色块/src dark.css 平衡零嵌套）；FIX-REGRESSION.md（+#258 行）**；构建状态：**已构建·sw mochi-mtreyay3·本会话执行**）。
- 需求/根因：用户报「深色模式聊天打开拍一拍功能的页面，拍一拍字卡全是白的看不见文字」。无头 Chrome（新内核）复测一切正常=老内核专属病：dark.css 深色组件覆盖全用 CSS 原生嵌套（需 Chromium 112+/iOS 16.5+），夸克/华为/UC/旧 Edge/iOS≤16.4 等把嵌套规则整段丢弃——浅色白底扁平规则（#poke-list .cc-item）独存+文字 var(--ink) 被扁平变量翻白=白卡白字；桌面 tabbar 白条/各页组件回白同源。排查期间顺带证伪：单聊拍一拍面板三 tab 在新内核深色渲染本就正常（临时探针已删）。
- 验证：node --check 过；构建哨兵 **550/550 哑 0**；verify-dark-mode **32/32**（D 段三档真实路径+新 E 段静态防线）；布局 verify **10/10**。【真机:待验证】（老内核机型——夸克/华为/UC/旧 Edge/iOS≤16.4 优先）：深色下拍一拍字卡深底亮字；桌面/聊天/设置各组件深色不再回白；新内核机型零变化。

### 2026-09-08 00:2x（#257 整页「点不动」死点击逃生门+诊断现场补齐——小米MIX4/Edge「点进聊天页什么按钮都点不了退不出」多机型同族；未构建·本会话非构建者，树上 #255/#256 两批在途未收口，请构建者收口时一并重建并复跑 verify-stuck-escape）
- [AI-B 域+跨域 build.mjs/FIX-REGRESSION（哨兵尾部追加 2 条；开工时树上另有 #255 弹窗键盘顶对齐批[mobile-adapt/chat/room/base.css]与 #256 表情包重复批[chat.js]在途，其 mobile-adapt.js 曾 stash 循环一次卷掉本批首版块、已重落）]（**改动文件：src/js/mobile-adapt.js（scroll-lock IIFE 尾新增 #257 死点击逃生门：同点 3 快击[1.6s 窗/相邻<650ms/单击<350ms/位移<12px]零 click 跟随=死点击→证据逐层复位[残留 scroll-lock/.phone 键盘期内联 height/alignSelf/top 仅安卓/iOS 由 healViewport 自治/失焦]+#cc-toast 播报+LS 落现场 xy-home-v2:__diag-stuck 环形 3 条+window.__mochiStuckProbe 只读探针；浮层打开/键盘会话[__mochiAndroidKb/__mochiIosKb]/长按/滚动四豁免，能点动的设备条件永不齐备=零行为变化）；src/js/device.js（①触摸轨迹采集 __diag-touch 环形 6 条[只记元素标识+所在页 chat/gc，与交互轨迹并排读=触摸有 click 无即死点击实锤]②「键盘/锁残留」行补打印浮层开清单[vg.lock.open 此前收集从未输出]+逃生探针③诊断文本新增「触摸轨迹」「卡死逃生记录」两段跨重启可读）；build.mjs（哨兵+2=545：mobile-adapt 判定锚 `_escLastClickAt >= tapEndAt`+device 采集锚 `'xy-home-v2:__diag-touch'`）；FIX-REGRESSION.md（+257 行）；tools/verify-stuck-escape.mjs（新增 28 断言）**；构建状态：**未构建（树上在途批未收口，不代构建防夹带）**）。
- 需求/定性：用户（小米 MIX 4+Edge 151，明说其他机型也有）报「点进聊天页面什么按钮都点不了、退也退不出来只能杀进程」。诊断实锤签名=死点击：卡死时段（长任务 385/133/139/160ms+前后台切换窗）交互轨迹[click]整段空白、前后时段正常——触摸活着、click 死。成因族（残留锁/键盘内联残留/焦点劫持/合成层输入管线）重启后取诊断现场已洗掉、无头空库复现不出（#148 同族），单根因不可实锤→按「证据门控不猜机型」落逃生门+现场采集双保险，下次报障诊断自带根因证据。
- 验证：node --check 两文件过；tools/verify-stuck-escape.mjs **28/28**（S1-S10 静态接线；B1 健康点击不触发/B2-B2c 死点击 3 连触发 toast+落盘 n=3+无残留标注/B3-B3d 残留锁+.phone 内联+失焦复位且明细入账/B4 长按/B5 滑动/B6 浮层打开/B7 键盘会话四豁免不触发/B8 第 3 击 click 迟到=误报退出/B9 间隔>650ms 不触发/B10 探针字段/B11-B11b iOS 不越界清 .phone 但仍清锁；R-HEAD 旧源死点击零响应=判定力证明）；--check-sentinels **545 全绿哑 0**。
- 【真机:待验证】（小米 MIX 4/Edge 及任意机型，随下次构建上线后）：复现点不动时同点快速轻点 3 次→toast「已尝试恢复（明细）」且页面恢复可点；诊断报告「卡死逃生记录/触摸轨迹/浮层开」三处可读；正常使用（弹层内/打字中/长按菜单/滚动）永不弹恢复提示。若逃生门落盘 tag=「无残留可清」则根因指向合成层/输入管线，诊断三件套可直接定案下钻。

### 2026-09-07 23:5x（#254 音乐「去除VIP歌曲/清理会员歌曲」全机型失效收口——代理死链换 meting 播放同源探测；已构建·sw mochi-mtre9k4g·本次构建者：本会话）
- [AI-A 域+跨域 build.mjs/FIX-REGRESSION（开工时树上 #252/#251 已由各自会话收口提交[562293c]、#253 字卡批亦已收口[60b8d73，其对我 #254 在途做了 stash 隔离]；树上另有 #256 表情包重复/chat.js+#255 弹窗键盘顶对齐/mobile-adapt.js+room.js+base.css 两批在途未收口——本批产物全程「备份→回退 HEAD→构建→还原」隔离，产物=已收口 src+本批，不夹带在途；docs/阿里云OSS*.md 未声明的表格格式化改动留在工作区未提交）]（**改动文件：src/js/music-player.js（fetchNeteaseFees 整体重写：原 proxy.cors.sh/allorigins CORS 代理查官方 fee→域名 DNS 已注销 ENOTFOUND/allorigins 522=华为 nova8 Pro+Edge、红米 K70+Chrome 等所有机型点击必「检测失败」；改 meting ?type=url 播放同源逐首探测可播放性（与播放/时长探测同依赖面无独立死点，与 resolveNeteaseDirectUrl 同判据：免费歌 302→CDN 或 audio/*、VIP/失效歌 200+空正文无跳转），判不可播记 fee=1 沿用过滤语义，探测失败不计账绝不误删；并发 8 worker pool+30s 兜底收口；响应头同步记账+异步 abort/cancel 防诊断噪音（记账不进定时器：无头隐藏页 Intensive Wake Up Throttling 会把延迟回调拖到分钟级）；openVipClean 加 vipCleanBusy 防重入+面板文案如实化「经播放探测不可用（会员/付费或链接已失效）」）；build.mjs（哨兵 +1=543：记账判据锚 `playable ? 0 : 1`；#108 旧哨兵 needle 同步——其锚的代理 5xx 重试链路已随死代理整体移除，台账注明由 #254 继承）；tools/verify-music-vip-clean.mjs（T4/T5 桩改造为新 meting 通道[Response 桩：1=text/html 空正文/0=audio/*]；新增 T7 探测全失败不弹面板不误删、T8 部分探不到只移除判死歌曲=防误删回归防线）**；构建状态：**已构建·本会话执行**）。
- 需求/根因：用户华为 nova8 Pro+Edge 报「去除VIP歌曲失效」并明说其他机型也有，红米 K70+Chrome 诊断同症状同根因（点 music-vip-clean 后 proxy.cors.sh 网络失败三连、BodyStreamBuffer×15 刷错误环）——非机型 bug，是 v3.14.x 起依赖的第三方公共 CORS 代理持续死亡（corsproxy.io 401→codetabs 超时→proxy.cors.sh 注销+allorigins 522），修代理=按住葫芦浮起瓢，故换与播放同依赖面的通道，无任何机型分支。
- 验证：node --check 过；tools/verify-music-vip-clean.mjs **8/8**（红绿对照 HEAD **6/8**，T4/T8 红恰为修复点）；相邻回归 verify-music-vip-filter 6/6、verify-music-dur-cover 9/9；--check-sentinels **543 全绿哑 0**；布局 verify **10/10**。
- 【纯度声明】本批 commit 仅含：music-player.js+build.mjs+verify-music-vip-clean.mjs+FIX-REGRESSION.md+WORKLOG.md+三产物；#256/#255 在途 src（chat/mobile-adapt/room/base.css）未进本批产物，备份在 /tmp/inflight2556 提交后原样还原，请两会话继续各自收口。
- 【真机:待验证】（华为 nova8 Pro/红米 K70 及任意机型）：音乐页→设置内【清理会员歌曲】→toast「正在探测 N 首歌曲的播放可用性…」→数秒内弹清理面板或「未发现会员/付费歌曲」，不再报「检测失败」；断网/服务挂时如实报失败且绝不误删；VIP 歌确认移除后列表消失。

### 2026-09-07 23:4x（#256 联系人表情包消息「重复一条、刷新又消失」——相邻重复判定三处口径收口；未构建·待构建者收口）
- [AI-A 域]（**改动文件：src/js/chat.js（①normCollapseRange/collapseRapidDups/addRec 三处相邻重复判定收口到唯一窗口源 dupGapMs（文本 2500ms 不变；img/voice/special 字段型沿用 60000ms；text 即媒体的 sticker/image/voice 型仅收件侧扩到 60000ms——发件侧 60s 内重发同图是人为行为不吞）；②新增 mediaTxtEq 跨形式比对：@@m:令牌经 mochiMediaExpand 展开为原数据再比，addRec 实时去重改走它；③dupSig 的 x 字段令牌展开归一（池未热载 expand null 回退原文不引入误判））；tools/verify-sticker-dup.mjs（新增 32 断言：dupGapMs 七窗口口径/mediaTxtEq 跨形式五断言/dupSig 展开/归一化行为九场景/addRec 静态锚七条）**；构建状态：**未构建·树上在途**）。
- 需求：小米15Pro+Chrome 用户报「联系人发表情包，聊天里表情包消息会重复一条，其中一个刷新几下会消失变回正常一条」，明说其他设备型号也有（逻辑无设备分支）。附带诊断里的 `AbortError: BodyStreamBuffer was aborted` 为音乐直链解析 fetch abort 的已知噪音（music-player #216 已做异步 abort 兜底，与表情包无关），未处理。
- 根因：①addRec 实时去重窗 1200ms，多字卡回复条间隔 randInt(1200,2800)ms 整体落在窗外——表情包池小/表情包概率拉满时一批两回复抽中同款表情包＝屏上两张；②刷新归一化只删 ≤2500ms 且文本形式一致的对，#142 令牌化竞态下同一条内容一处 @@m:令牌一处 data:base64，直比不等＝跨形式永久漏判，要等令牌化收敛后再一轮归一化才消（体感「刷好几下才好」）。修复后实时去重与刷新归一化同口径＝屏上所见即刷新后所见，不再翻饼。
- 验证：node --check 过；node tools/verify-sticker-dup.mjs **32/32**；--check-sentinels 543 全绿哑 0（只读未构建）。
- 【撞号更正】本批原编号 #255，与并行会话同日登记的 iPhone 16 Pro 批撞号，已全部改为 **#256**（代码注释/verify 脚本/FIX-REGRESSION 行均同步）；FIX-REGRESSION #256 行已登记。
- **【需要构建者处理】** build.mjs 本批未动（并行会话在途占用）：收口时请在 FIX_SENTINELS 补登 3 条（chat.js 域防回归锚，逻辑锚点、文件内唯一）：`{ name:'chat-dup-addrec-gap', file:'chat.js', needle:'dts <= dupGapMs(rec)) { saveMsgs(); return null; }' }`、`{ name:'chat-dup-mediatxteq', file:'chat.js', needle:'function mediaTxtEq(a, b) {' }`、`{ name:'chat-dup-dupsig-expand', file:'chat.js', needle:'const ex = window.mochiMediaExpand(x); if (ex) x = ex;' }`，并跑 `node tools/verify-sticker-dup.mjs`。
- 【并行协调】本会话编辑 chat.js 期间恰逢并行收口的 stash 隔离往返，chat.js 一度被还原后随 stash pop 完整还原（git diff 已核对四段修复全在）；并行会话收口时若再 stash 本批在途 chat.js，请随批保留。
- 【真机：待验证】（小米15Pro 及任意机型）：①联系人连续回复表情包不再出现同款两条；②刷新后条数与刷新前一致、不再「弹没」一条；③自己 60s 内重发同款表情包仍正常显示（发件侧不吞）；④普通文字/图片/语音消息收发行为无变化。

### 2026-09-07 24:0x（#255 弹窗键盘顶对齐+批量导入弹窗放大+room.js floorPick 补齐——iPhone16P/iOS26 Safari 报障三件；src 在途未构建·本次构建者：本会话但本批【未构建】）
- [AI-A 域+跨域 mobile-adapt.js/base.css（AI-B 域文件，跨域声明：问题本身是 iOS 键盘×弹窗交互，base.css 的 .modal 与 mobile-adapt 的键盘会话是唯一根因位；开工时树上无该四文件在途认领）]（**改动文件：src/js/mobile-adapt.js（iOS 分支新增 syncModalKbDock：键盘会话 _kbActive/_iProv 给 #modal-mask 挂 modal-kb-dock 顶对齐，开启/restoreKb/推定停靠/推定清除/250ms 轮询兜底五处接线；安卓/桌面不进 isIOS 分支零影响）；src/css/base.css（.modal-mask.modal-kb-dock 顶对齐规则+安全区上边距；.modal--big 330→420px/94vw）；src/js/chatcard.js（批量导入弹窗 opts.big+textareaRows:8——注意：该两行因并行 #253 提交时显式 add chatcard.js 随 b761bb9 入库，其产物未含，待下次构建接入）；src/js/room.js（floorPick 整个函数缺失=装扮选墙纸确定必抛 ReferenceError[用户诊断 ×6 实证]，按墙纸同款补齐地板弹窗）；build.mjs（哨兵+4=543）；FIX-REGRESSION.md（+#255 行）；tools/verify-modal-kb-dock.mjs（新增 13 断言）**；构建状态：**已构建·sw mochi-mtresyym·本会话执行（#254 收口后树净窗口，备份-回退-还原隔离 #256/#257 在途 chat.js/device.js/build.mjs 编辑后纯净构建：产物=已收口 src+本批，547/547 哨兵全绿）——本口提交 src 三文件+build.mjs 哨兵+产物+verify 脚本；WORKLOG/FIX-REGRESSION 行与 chatcard 放大两行此前已分别随 9667215/b761bb9 入库**）。
- 需求/根因/方案详见 FIX-REGRESSION #255：①键盘期居中弹窗随 .phone 高度变化反复取中=输入框上滑→键盘会话顶对齐锚定；②弹窗 272px 太小→宽版+rows=8；③floorPick 缺失→补齐。
- 验证：node --check 四文件过；--check-sentinels **543 全绿哑 0**（src 只查模式）；tools/verify-modal-kb-dock.mjs **13/13**（SRCDIR 红绿对照 HEAD 4/13，9 红恰为修复点）。
- 【真机:待验证】（iPhone16P Safari 及任意机型，需更新到含本批的构建后）：①字卡批量导入弹窗打字时输入框位置稳定不再上滑；②弹窗明显变大（宽版+8 行框）；③小屋→装扮→选墙纸确定后正常弹地板选择不再报错；④其他弹窗（改昵称等）在 iOS 键盘期变为顶部对齐属预期行为，收键盘回居中。
- 【给 #254 音乐批会话】其 fetchNeteaseFees meting 探测与木口曾计划的单曲导入探测同源同判据（redirected||audio CT）——若其收口后「上传网易云歌曲播不了」仍有反馈缺口，可考虑导入时即探测+可播性如实提示，本口不再动 music-player.js。

### 2026-09-07 23:5x（#253 字卡导入全局崩溃修复：所有格式导入成功解析后必崩 ReferenceError「fromPubFallback is not defined」——华为Pro70+Edge 报障实证、机型无关；已构建·sw mochi-mtrdrlst·本次构建者：本会话）
- [AI-A 域]（**改动文件：src/js/chatcard.js（applyImportData 两行声明提升：bag/fromPubFallback 从「全量备份提取」分支块内提升到函数作用域——函数尾部 #139 防复制守卫读它们块级不可见，任何格式（milk/星言/本应用导出/全量备份）成功解析出字卡走到守卫必抛 ReferenceError，saveGroups 永不执行；崩溃在写入前=库不受损。零设备分支零语义变化）；build.mjs（哨兵+2=542）；FIX-REGRESSION.md（+#258 行）；tools/verify-cc-import-apply.mjs（新增 21 断言端到端行为验证）**；构建状态：**已构建·本会话执行**）。
- 需求：华为Pro70（ADY-AL00）+Edge 151 用户报「公用字卡和专享字卡无法导入 milk 网站的字卡库 json，其他设备型号也有」——诊断「启动文件异常」三条 `applyImportData 异常 fromPubFallback is not defined`。根因是 #139 防复制守卫的作用域缺陷（声明在备份提取分支块内、守卫在函数尾部读取），#171 修好的解析分流本身无恙、milk 只是受害者之一——所有导入自 #139 起全被拦死。按用户要求「不要覆盖修改导致不同机型 bug 反复出现」走最小根因修复：只提升两行声明，#171/#182 解析自救链与全部设备兼容逻辑未触碰，无任何机型分支。
- 验证：node --check 过；tools/verify-cc-import-apply.mjs **21/21**（抽真实 applyImportData+writeImport 源码桩环境端到端：milk 真实导出结构 merge 7 张落位/专属页导入/#139 守卫「合并=公用库」跳过写入保持/备份提取+标签/媒体白名单/replace 全键归位；**HEAD 红绿对照全红 ReferenceError 恰为修复点**）；--check-sentinels **542/542 哑 0**；cc 导入家族复跑：verify-cc-import-parse 23/23、verify-cc-import-guard 19/19、布局 verify 10/10；verify-cc-batch-import 12/13（用例1 FAIL 经 stash 红绿对照 HEAD 产物同红=存量，与本批无关，挂账待查：批量粘贴导入「字面
 文本」用例过期口径）。
- 【并行声明】#251/#252 两批已由各自会话收口推送。本批收口期间并行 #254 音乐批（music-player.js 改造+#108→#254 哨兵改写+verify-music-vip-clean）在途落盘：其 src 曾被本会话首次构建卷入产物且致 #108 锚红——已按惯例 **stash 隔离其在途文件重建产物三次**，最终产物=已收口 src（不含 #254 内容），其 build.mjs 在途编辑以磁盘快照保留、本批提交版 build.mjs 仅含 #253 增量。**需要对方处理**：①其收口时自行重建产物并让 #108→#254 needle 落定；②本批提交窗内其 music-player.js 短暂 stash/pop，内容未动。
- 【真机:待验证】（华为Pro70+Edge 及任意机型）：milk 导出 json 在 公用字卡/专享字卡 两入口按 追加（自动去重）/导入到当前分类/替换 三模式导入均成功 toast，字卡落位对应分类；全量备份 json 经字卡入口导入仍走「（全量备份提取）」、防复制守卫不回归。

### 2026-09-07 23:0x（#252 深色模式选「浅色」颜色依旧不对——#107 复发收口；已构建·sw mochi-mtrcqne0·本次构建者：本会话）
- [AI-B 域+跨域 build.mjs/FIX-REGRESSION（树上有 #251 并行批在途 group-chat/template 尾部/verify-gc-settings，其 WORKLOG 11:0x 已声明；本批构建产物同时包含其已声明完整批次的 src 改动——构建前树上无半成品标记，视同其 11:0x 条「本次构建者：本会话」随库收口，请其知悉并复跑 verify-gc-settings）]（**改动文件：src/css/base.css（:root color-scheme:light→only light，#107 修复升级：裸 light 是偏好声明不是 Chrome Auto Dark 退出开关，系统深色下部分安卓 Chromium/WebView 仍压黑=「选浅色颜色依旧不对」）；src/template.html（头部启动脚本三档语义：dark 或「非 light 且系统 prefers-dark」→挂属性，否则 removeAttribute——补 auto 档首帧落位修白闪 FOUC+浅色档强制清残留 data-theme）；src/js/personalize.js（applyThemeMode 浅色档显式 removeAttribute 双保险，注释固化语义）；build.mjs（#107 needle 同步 only light + 哨兵+2=540）；tools/verify-dark-mode.mjs（D 段适配 v3.27.x 三档弹窗真实用户路径：行点击→点「浅色」胶囊，旧脚本直接点行从未提交=此前 D 段 5 项恒假失败的口径修复）；FIX-REGRESSION.md（+#252 行）**；构建状态：**已构建·sw mochi-mtrcqne0·本会话执行**）。
- 根因/方案/验证详见 FIX-REGRESSION #252。验证：--check-sentinels 与构建哨兵 **540/540 全绿哑 0**；verify-dark-mode **29/29**（新增 D0 断言，D5 实测 `light only`）；布局 verify **10/10**；verify-device-env **27/27**、verify-contact-switch-perf **9/9** 复测无回归。
- 【真机:待验证】（报障机型优先）：系统深色+app 选「浅色」→界面真回浅色不再发黑；「跟随系统」开屏不白闪；深色档与 #107 清单（iQOO Neo10 群聊换色）复测正常。

### 2026-09-07 23:0x（OPPO Reno6 5G/雨见「其他功能字卡页下面无法滑动显示字卡」——定性=v3.26.513 旧版残留（#239 修复上线于 516），非回归；verify-fun-cards-layout 加固 360×658 断言；src 无产品改动·本会话未构建·本次构建者：无——本批纯工具+台账）
- [AI-A 域 tools/ + 共享 FIX-REGRESSION.md/WORKLOG.md]（**改动文件：tools/verify-fun-cards-layout.mjs（SERVE_DIR 红绿对照支持+S 段 OPPO 同款 360×658 三断言）、FIX-REGRESSION.md（#239 行复发定性+设备索引 OPPO Reno6 行+239）**；构建状态：**src 无改动无需构建**——本批=诊断定性+防复发加固，产物与远端一致）。
- 需求：OPPO Reno6 5G/雨见报「【系统预设字卡】的【其他功能字卡】页面下面无法滑动显示字卡…其他设备型号也有」，附 v3.26.513 诊断。
- 定性（时间线+红绿对照实证，非新 bug）：513 构建于 04:19（9fe669d），同页同症状 #239 修复（概率框插头部把 #fc-list 压成 6px 首屏在视口外）上线于 05:11 的 516（11abd7d），远端 08:28 已部署新 ts——该机停留旧版。git worktree 检出 513 产物 SERVE_DIR 对照 4/10：S1 首屏 inVp:0、列表 rectH=6、S2 CDP 触摸滑动 scrollTop 推进 335px 后可见字卡 visN:0=报障原文精确复现；HEAD 产物 10/10 全绿。设备索引已有 Reno6（169/200/228 语音家族），本次+239 滚动域。
- 防复发加固（用户点名「不要覆盖修改导致不同型号反复出现」）：verify-fun-cards-layout ①SERVE_DIR=目录 可对任意历史产物对照；②S 段 360×658（OPPO 同款视口）：S1 首屏有字卡+页面可滚、S2 CDP dispatchTouchEvent 真实手势下滑 scrollTop 推进+滚出屏外新字卡、S3 概率框可滚入视口——「下面滑不出内容」类跨机型复发未来直接红绿判定，不再依赖机型猜测。
- 验证：node --check 过；--check-sentinels 540 全绿哑 0（src 未动）；HEAD 10/10、513 基线 4/10（红项=1/2/3/7+新增 S1/S2，恰为修复点与新增断言）；S2 用 CDP touchStart/Move/End（mouse 拖拽不走触摸平移路径，首版断言误红已自纠）。
- 【真机:待验证】（OPPO Reno6 5G/雨见）：更新到 516+（顶部更新条刷新或关全部标签页重开）后——字卡库→系统预设字卡→其他互动功能字卡：打开即见摸鱼字卡列表可点，往下滑能滚出更多字卡，页面底部是「使用概率」13 行；若更新后仍异常再按新版本号报障。
- 顺带核对：诊断里 SyntaxError: redeclaration of let JSInterface / TypeError: JSInterface.sysonSelectTextChange×2 全部出自雨见壳自身注入脚本（全仓 grep 无 JSInterface，@anonymous:1:1 入栈），与本页滚动无关，无需处理。

### 2026-09-07 11:0x（#251 群聊设置对齐聊天设置·输入/数据/删除消息——已构建·本会话末次重建 sw mochi-mtrcyncj·本次构建者：本会话；#252 会话已先行随库收口过本批）
- [AI-A 域]（**改动文件：src/js/group-chat.js、src/css/group-chat.css、src/template.html（仅群聊气泡菜单段）、build.mjs（哨兵+2=540）、tools/verify-gc-settings.mjs（扩到 34 断言）、tools/verify-gc-color.mjs（美化入口定位改按文本，#251 后首个 .gc-set-link 不再是美化入口）**；构建状态：**已构建·sw mochi-mtrcyncj·本会话执行**——#252 会话 23:0x 构建时已把本批 src 随库打入（其 WORKLOG 已声明），本会话在其上复跑全部验证并末次重建刷新版本号，产物=当前树 src）。
- 需求：用户反馈「群聊右上角没有聊天设置里的全部功能」。盘点结论：美化类已全有（壁纸/气泡/字体/方案），真缺口=回车发送开关、批量发送/语音/隐藏表情包/允许删除四个开关、导出/导入/清空记录、删除消息。
- 方案：群聊设置主视图新增「输入与消息」（5 开关，读写与聊天设置同一批键 cs-enter-send[on/off]/cs-batch-send/cs-voice-send/cs-del-ta-msg/hide-ta-sticker）+「数据」组（导出=流式 Blob JSON、导入=兼容单聊/裸数组/整份备份、清空=二次确认，均只作用当前群）；气泡操作菜单加「删除」按钮（ma-del-gc，按 cs-del-ta-msg 显隐，仅成员消息）；keydown 回车读 cs-enter-send==='off' 放行换行（与 chat.js 8751 同语义）。
- 验证：node --check 过；verify-gc-settings **34/34**（新增 S9-S12 静态锚+R17-R20 运行时：5 开关渲染/写键、hide-ta-sticker 走根命名空间、数据三入口渲染；红绿对照修正 R7 美化入口定位）；构建哨兵 **540/540 哑 0**；布局 verify **10/10**；group-chat-fixes **19/19**、gc-continue 1/1、cc-group-off 12/12、group-decision 13/13、group-desk-icon 6/6；群聊家族存量红 stash 对照逐条一致=非本批（gc-more 3 项=more-cats 同源口径过期、gc-pool-scope T3=#157 语义改版、gc-send 1 项=批量断言过期，均已在 #130 挂账）；verify-dark-mode 29/29（#252 同树复测）。
- 【真机:待验证】群聊设置新两组操作正常、导出文件可再导入、删除消息刷新后不复活。

### 2026-09-07 09:0x（设备判定收口第二批：mochiDevice 新增 env 能力层，清掉 v3.16.x 漏网的 4 处 UA/平台特判复刻——已构建·sw mochi-mtr7zghg·本次构建者：本会话）
- [AI-B 域+跨域 chat.js/music-player.js/data-backup.js（开工前 WORKLOG 已声明占用，树上无在途认领）]（**改动文件：src/js/device.js（mochiDevice 新增 env 能力层：isAndroidWebView[空 UA 保守 true 原语义]/brokenFileShare/apiBlockedHint/notifyQuirk 四布尔标记，UA 嗅探唯一处）；src/js/chat.js（isAndroidWebView 改读 env，18 壳大正则删除）；src/js/data-backup.js（brokenFileShare 改读 env，/huaweibrowser|quark/ 删除）；src/js/music-player.js（两处 QQ|Quark 拦截提示改读 env.apiBlockedHint）；src/js/bg-keep.js（kaIsIOS 改薄壳读 mochiDevice.isIOS[函数名保留=#207 哨兵与 verify-keep-audio 抽取契约]；小米通知提示改读 env.notifyQuirk）；src/js/idb.js（armFgIdbReset gate 改读 mochiDevice.isIOS，touchMac 复刻实现删除——device.js isIOS 本身含 #144 Macintosh 伪装分支，行为语义等价）；build.mjs（哨兵+4=533[env 挂载锚/chat 消费锚/data-backup 消费锚/bg-keep 薄壳锚]+#144 idb needle 同步 1 条）；FIX-REGRESSION.md（#144 行更新+新增 #249 收口批条目）；tools/verify-keep-audio.mjs（抽取壳加 window.mochiDevice 桩适配薄壳化 kaIsIOS）；tools/verify-device-env.mjs（新增 27 断言）**；构建状态：**已构建·sw mochi-mtr7zghg·本会话执行**）。
- 需求：用户问「手机端 bug 多且修复被覆盖怎么优化」→ 扫描确认 mochiDevice 自 v3.16.x 已是唯一判定源但能力型判定无处安放，4 文件各拼一套 UA 正则+2 文件复刻 iOS 判定（同批浏览器名单 4 处、iPad 伪装检测 2 处，改漏一处=修一半）。本批收口后逻辑零变化、纯判定源唯一化。
- 验证：node --check 六文件过；--check-sentinels **533 全绿哑 0**；**verify-device-env 27/27**（A 段 device.js 真实 env 表达式 13 场景求值：微信/三星→WebView=true、标准 Chrome→false、华为/夸克→brokenFileShare、QQ→apiBlockedHint 且 brokenFileShare 不误扩、iOS→notifyQuirk=false、空 UA 保守 true、mochiDevice.env 接线；B 段消费端接线+旧正则全仓清零 9 条；C 段 kaIsIOS 薄壳行为 4 条）；verify-keep-audio **18/18**（iOS 220Hz/0.002 与安卓 18kHz/0.006 行为断言零回归）；verify-fullscreen-ipad **25/25**（#144 isIOS 五场景行为零变化）；布局 verify **10/10**；verify-device-regression **37/37**、verify-voice-send **24/24**。红项均为存量口径非本批（stash 红绿对照逐条一致）：verify-ios-pwa-kbd A5（断言停在 #190 前 0.02 幅度）、verify-voice-record A14（断言停在 #228 前 mp4 优先级硬编码）——两脚本断言已过期，后继修 B 可顺手改期望。
- 【跨域声明】chat.js 仅动 isAndroidWebView 函数体（#241 修复点/inplacePatch 等未触碰，#228 语音哨兵 4 条照常在位）；music-player/data-backup 各 1-2 行消费替换；无他人文件卷入，git add 显式清单。
- 【真机:待验证】（等价性重构，四条既有链路复测即可）：①标准安卓 Chrome 语音录制走 webm/opus 无爆音、任意 WebView 内录制有声可播；②华为/夸克导出备份仍直接「确定后下载」不卡分享面板；③小米/红米后台通知测试提示文案仍在；④iPad 伪装 UA 场景 #144 原清单复测（全屏开关有反应/ios-pwa-standalone 类在/杀后台重开数据正常）。

### 2026-09-07 08:3x（#247/#248 群聊媒体令牌化+历史分页——用户确认的结构性缺口批（存储膨胀/历史只进不出）；已构建·本次构建者：AI-A 本会话）
- [AI-A 域+跨域 build.mjs/template.html]（**改动文件：src/js/group-chat.js（①#247 令牌化：新增 gcNormalizeMedia 落盘前把 msgs 全部 data:image≥1KB 载荷批量 tokenize 换 @@m:hash（sticker/image 的 text、parts 图、quote 图），gcWriteMsgs 改 normalize→mochiMediaFlush→落盘（#142 同顺序）；调度器 Promise.resolve 兼容异步 writer，saveNow 走 quick=true 离页快写防 unload 丢写；gcPlayVoice 加 mochiMediaExpand 兜底；②撤回快照防臃肿：mediaish 守卫（媒体类/超 20KB 快照走 gcRetractFallbackHtml 占位，**#244 哨兵 needle 已同步更新为带长度守卫版**，回应并行会话 08:0x 条点名）；③#248 历史分页：gcRenderStart 渲染窗口起点+列表顶部「查看更早的消息（还有 N 条）」入口按 150 条块向顶部补渲，renderMsg/gcPlaceMsg 支持 beforeEl 锚点前插（拍一拍/系统消息同支持），scrollHeight 差值回补 scrollTop 视口不跳，DOM 裁剪跳过分页按钮）；src/css/group-chat.css（.gc-earlier 分页入口样式）；src/template.html（功能介绍·群聊行补多群聊分组/查看更早/媒体池一句）；build.mjs（哨兵+4=529，另 #244 needle 同步 1 条）；FIX-REGRESSION.md（+#247/#248 两行，#246 留空防撞号）**；构建状态：**已构建·sw 视 version.json·本会话（AI-A）执行**）。
- 需求：用户问「群聊模式有什么缺陷」→ #242-244 修复后用户确认继续开结构性缺口批（存储膨胀+历史只进不出）。#247 复用 media-pool 全套设施（文档级观察器自动解图=群聊零渲染改动；池 GC 引用扫描已含群聊键 #186；池 tokenize 现仅收 data:image，语音暂保持内联为日后预留）。
- 【并行批次收口×2 记录】本口开工后并行 AI-A 会话的 #245 聊天快照批两轮在树上完整声明（其 WORKLOG 07:2x/08:0x 条），其会话未自行提交——本口按惯例以 stash 隔离本口在途 src 后重建产物代收口：**32f7f67（第一轮①④段）+ e51274d（第二轮②③段+TASKS #133）**，产物=src 均保证。其「请收口时同步 #244 needle」已办（本批 build.mjs 更新）。
- 验证：node --check 过；--check-sentinels 529 全绿哑 0；群聊家族脚本+verify-chat-lite-upgrade+布局 verify 结果（构建后补）。
- 【跨域声明】build.mjs 哨兵追加+needle 同步（group-chat 域配套）；template.html 仅群聊功能介绍 1 行（AI-A 功能域文案）；两次代收口提交均显式文件清单、内容为对方已声明批次原样。
- 【真机:待验证】（任意机型）：①群聊发几张表情/图片→group-chat-msgs 键体积不随图片数线性膨胀、重进后表情图片正常显示；②老群聊点顶部「查看更早的消息」历史按块补出、视口不跳、翻到顶按钮消失；③底部收发贴底跟随不受影响。

### 2026-09-07 08:0x（#245 打开聊天「闪屏+弹一下」第二轮收口：lite 残留原位升级+预权威 LS 快照合并+账本矛盾守卫——用户报障 #241 后真机依旧；本次构建者：AI-A 本会话）
- [AI-A 域+跨域 build.mjs]（**改动文件：src/js/chat.js（①inplacePatchIfSameWindow/renderWindow：渲染时刻登记 windowRenderedLite，权威收尾对精简残留下标原位 replaceChild 换节点[batchRendering 抑制入场动画/滚动副作用、草稿 collect/restore、贴底保持/高度差补偿、节点缺失仍整窗兜底]，不再见残留即整窗重渲；②新增 mergeLsSnapshotWith——saveMsgs/saveMsgsNow 预权威分支的 LS 快照保存改「与既有快照按 ts|side|text 去重合并」，不再整包覆盖[旧覆盖=启动期签到/TA 消息保存顶掉 LS 历史=首渲缺历史+权威合并重排后前缀凭据失配=同一消息屏上两份]；③loadMsgs 权威未就绪即解析 LS 并与内存新增去重合并[旧门 !persistTimer&&!msgs.length 被启动期新增双双跳过]；④#90「确认空库」分支加账本矛盾守卫：账本 n>0 与 isMiss 矛盾=探测说谎，按读取失败重试，绝不拿 LS 会话快照回写 IDB）；build.mjs（哨兵+1=525）；FIX-REGRESSION.md（#245 行两轮更新）；TASKS.md（+#133：idb.js 冷启动早期 idbGet/HasKey 对已存在大键误报「无」，数据层口核查）；tools/verify-chat-lite-upgrade.mjs（新增 9 断言）**；构建状态：**已构建·sw 视 version.json·本会话（AI-A）执行·本批 src 部分 32f7f67 已由并行会话代收口（仅含①④段），②③段与台账更新在树上待下次收口**）。
- 需求/根因（两轮排查）：用户（小米15Pro 同机）报「打开聊天页依旧闪屏+弹一下后恢复正常」。第一轮定性①：大历史 LS 快照必被 liteSnapArray 剥负载，权威回读后旧逻辑见残留即整窗重画。第二轮深挖实锤②③：预权威保存整包覆盖 LS 快照+权威合并重排后补丁前缀凭据失配=同一消息屏上两份+权威收尾缺历史整段补入（「恢复正常」全靠相邻重复归一化兜底）——HEAD 红绿对照复现（HEAD 5/9：种子消息×2、图片未补真身）。③副产品登记 TASKS #133（idb.js 探测层说谎，AI-B 域，未动）。
- 验证：node --check 过；tools/verify-chat-lite-upgrade.mjs **9/9**（首渲后零 rm≥5 整窗批/原位升级恰 rm1+add1/图片补真身 dataURL/种子消息零重复/贴底不弹/重开零重建/四处静态锚），红绿对照 HEAD 旧产物 **5/9**（红项=J2/J3/J3b/J6 恰为修复点）；布局 verify 10/10。注意：最后一次构建的产物包含并行会话 group-chat.js/template.html/group-chat.css 在途改动（其 #244 needle 暂失配报❌属其在途态），请其收口时重建产物并同步 needle。
- 【真机:待验证】（小米15Pro 及任意机型，更新到最新 sw 后）：打开有图片/语音历史的聊天页不再整屏闪+弹；消息不重复；历史不再先缺后补；图片气泡从占位平滑补齐为真图、滚动位置不跳。
- 【并行声明】32f7f67 系并行会话对本口 08:0x 声明批次的代收口（stash 隔离其 src 后重建），本口后续增量（②③段+台账）未入库，挂在树上待收口；git add 显式列文件。

### 2026-09-07 07:2x（#242/#243/#244 群聊串群三连收口+撤回查看安全化——用户问「群聊模式有什么缺陷」后代码核查自查出、经用户确认修复；已构建·本次构建者：AI-A 本会话）
- [AI-A 域+跨域 build.mjs]（**改动文件：src/js/group-chat.js（三处修复：①#242 串群——scheduleReply/gcContinueSay 捕获来源群 gid 穿透 memberReply，新增 gcDeliverReply/gcReadGroupKey/gcWriteGroupKey 统一投递（同群原路径，跨群读改写来源群存储键不碰当前 msgs/DOM，群已删丢弃），retractGcMsg(idx,gid) 跨群落来源群存储，打字指示 show/hide 加 gid===curGid 守卫+进群/切群 hideTyping 清共享指示器残留；②#243——loadMsgs 的 idbGet 回调首行加 key!==groupMsgKey(curGid) 整包丢弃守卫；③#244——retractGcMsg 撤回前存渲染快照 rec.orig（无 DOM 走 gcRetractFallbackHtml 安全回退：媒体占位/文本 escTxtBr），渲染分支 dataset.orig 不再兜底直出 rec.text）；build.mjs（哨兵+4=524）；FIX-REGRESSION.md（+#242/#243/#244 三行）**；构建状态：**已构建·sw 视 version.json·本会话（AI-A）执行**）。
- 根因回顾：①#242=v3.26.x 多群聊分组引入可达缺陷——回复/撤回 setTimeout 链在执行时刻读模块级 curGid，发消息后切群：回复写进新群+原群丢失、撤回 myIdx 撤错消息；②#243=loadMsgs 的 IDB 异步回填只比长度不校验 key，旧群回调切群后 resolve 整包覆盖 msgs 并被下次保存回写污染新群键；③#244=群聊撤回「点击查看」v3.9.x 上线时未对齐单聊 chat.js retractMsg 的渲染快照方案，innerHTML 直出原始 rec.text（多行丢换行/媒体点开整屏 base64/字卡含 HTML 被当标签执行）。
- 验证：node --check 过；--check-sentinels 524 全绿哑 0（构建后同）；群聊家族脚本：verify-group-chat-fixes 19/19、gc-settings 26/26、gc-color 14/14、gc-continue 1/1、voice-quote-gc 7/7、group-decision 13/13、gc-input 12/12；gc-send 4/5、gc-more 12/15、gc-pool-scope 9/10 三脚本失败项经 stash 红绿对照与 HEAD 逐条一致=TASKS #130 在册口径过期存量，本批零新增回归；布局 verify 10/10。
- 【跨域声明】build.mjs 仅 FIX_SENTINELS 数组尾部追加 4 条（group-chat.js 域配套防回归锚，同 #237/#238 惯例）；未触碰他人文件，本口开工时树净（69d6992 之后）。
- 【真机:待验证】（任意机型）：①A 群发消息立刻切 B 群→回复只进 A 群不串 B；②两群快速连切 5~10 次不串不胀；③撤回概率拉满→点「撤回了一条消息」文本带换行还原、表情/图片不铺 base64。

### 2026-09-07 05:1x（#238 第 2 轮：备忘提醒从每日一次放宽为至少隔 2 天——用户反馈「不用提醒太频繁」）
- [AI-A 域]（**改动文件：src/js/memo-app.js（闸门 done 日标记→last 时间戳，命中后至少隔 2 天再提醒；v1 done='YYYY-MM-DD' 自动迁移为该日 23:59:59 时间戳不丢起点；概率 toast/注释同步）；build.mjs（#238 间隔闸哨兵 needle 更新）；src/template.html（介绍行改「命中后至少隔 2 天一次，不频扰」）；FIX-REGRESSION.md（#238 行补 2 轮记录）；**构建状态：已构建·sw 视 version.json·本会话（AI-A）执行**）。
- 频率现状：默认概率 2%/4 分钟一掷+2 天间隔闸+23-6 静默 ≈ 两三天最多催一次；概率调 100% 也被间隔闸限住。
- 验证：node --check 过；--check-sentinels 520 全绿哑 0；playwright：3 天前 last+prob100→触发催办进聊天，紧接二发被闸。【真机:待验证】催一次后 2 天内不再出现第二条。
- 【提交纪律修正】本次起 git add 改显式文件清单（d50bc5a 卷入并行会话文件的教训）。
### 2026-09-07 05:2x（#239/#240/#241 小米15Pro Chrome 四问题报障收口——①互动功能字卡页被概率框挤没 ②背景模糊载体 backdrop→壁纸自滤 ③聊天快照缺尾部整窗重画→尾部增量 ④启动闪定性非应用内 bug；已构建·sw mochi-mtqapsy8·本次构建者：AI-B 本会话）
- [AI-B 本会话+跨域 chat.js/chat-pages.css/home.css/template.html（cross-domain 声明：四问题均为 AI-A 域功能文件 bug，树上当时无该四文件在途认领；其中 4 文件主体修复已被 d50bc5a 卷入随 mtq9p7iu 上线、本批为其收口=编号改 #239/#240/#241+#241 自纠两处）]（**改动文件：src/js/chat.js（#241 自纠：inplacePatchIfSameWindow 加 windowRenderedN===0 门[无屏上凭据的首渲走原整窗]+grown>0 增量后同步 windowRenderedN=len[loadNewerIncremental 只更 renderEnd 不更凭据，不补则下次收尾 grown 错位仍整窗]）；src/css/chat-pages.css、src/css/home.css、src/js/personalize.js、src/template.html（注释编号 #237/#238→#239/#240/#241，逻辑零改动）；build.mjs（哨兵+3：fc-list 整页滚动锚/.desk-blur-on 载体锚/增量追加循环锚）；FIX-REGRESSION.md（+#239/#240/#241 三行）；tools/verify-fun-cards-layout.mjs（新增 7 断言）；tools/verify-desk-blur-layer.mjs（新增 8 断言）；tools/verify-chat-jump-incremental.mjs（新增 6 断言）；tools/verify-desk-beauty.mjs（M2 随 #240 载体更新：blur-on+backdrop→desk-blur-on+壁纸层 filter）**；构建状态：**已构建·sw mochi-mtqapsy8·本口执行·哨兵 520/520 哑 0·sw 14/14**）。
- 需求（小米15Pro+Chrome 151，v3.26.491=7744d09 诊断报障四条）：①手机美化背景模糊/遮罩调整无感；②点开聊天消息有时先跳动一下才正常；③桌面快捷方式打开先闪一下才进界面；④其他互动功能字卡看不到、点击没有内容。
- 定性与修复：**①=#239**（实锤复现：#132 的 dcf-prob-box 13 行 stepper 实测 794px 插 fc 页头部，.card-list{flex:1;overflow-y:auto} 因 overflow 非 visible flex 最小尺寸归 0→列表压成 6px、首条字卡 top=1271/视口 808 全在屏外=「看不到了点击没内容」；修复=概率框整块移到 #fc-list 之后[template.html]+fc/dk 页照搬 dc 页整页滚动规则[chat-pages.css]）。**②=#240**（无头全绿复现不了[样式 z2/blur/白 50% 全对+像素 diff 31 显著]，定性真机 Chrome 151 backdrop-filter 对 z1 常驻图层采样不生效；修复=模糊载体改壁纸层自滤 filter[.desk-blur-on+#phone-bg-layer filter:blur+四边外扩 24px 防边缘发虚]，白遮罩层与 #219 z2 结构不动[哨兵锚未破]，blur=0 不挂类=卡顿红线语义不变；自滤不依赖 backdrop 采样路径，任何内核行为一致）。**③=#241**（#220 只覆盖「重开同窗同貌」，快照与权威条数不同[大历史 LS 快照缺尾/新消息只在 IDB]时整窗清空重画；无头实录 rm+全量 add 复现；修复=放宽前缀判定+loadNewerIncremental(len) 尾部增量追加，首渲后零 rm；自纠两处见上）。**④=非应用内 bug**（启动时序探针逐 100ms 采样 3.5s：splash/备份条/壁纸层/可见页时间线干净无抖动、无二次导航；pwa.js 无自动 reload[仅用户点「刷新使用新版」才刷]；结合本站一天十几次部署+#225v2 一版一弹更新条，定性=更新条滑入观感+Chrome standalone 系统启动画面，应用内无异常，不乱修）。
- 验证：node --check 过；verify-fun-cards-layout **7/7**（384x808 真实产物：fc 首屏字卡可见[修复前 0]/概率框在列表下方/列表不再 6px 塌缩[1004px]/点 chip 内容变化[16→10]/切 tab 正常/dc 页不回归/静态锚）；verify-desk-blur-layer **8/8**（desk-blur-on 挂上+壁纸层 computed filter=blur(12px)+四边-24px+白遮罩 z2 50% 保留+模糊开关像素 diff 69.7[高频棋盘格壁纸；首版用平滑渐变测出 1.7 假阴性，渐变模糊前后本就近似]+blur=0 类摘除 filter 归 none）；verify-chat-jump-incremental **6/6**（种消息→chat-meta b 吹到 9MB 造大历史懒读形态[小历史会被 chatPrefetchIfLight 预读合并测不到该路径，首版脚本即踩此坑]→LS 回滚 1 条+权威 IDB 4 条→打开聊天：首渲清静态占位后**权威收尾纯增量追加零 rm**、气泡补齐 4、重开零重建[#220 不回归]、静态锚）；家族回归 verify-chat-rebuild **16/16**、verify-rp-claim **11/11**、verify-desk-beauty **17/17**（M2 已随 #240 载体更新）、布局 verify **10/10**；--check-sentinels 520 全绿哑 0。
- 【响应 22c4ac9 通报】①d50bc5a 卷入的 4 文件当时确已保存完整（本批即其收口，#241 两处自纠为本批新增、随 mtqapsy8 首次上线——**线上 mtq9p7iu 的 #241 是不完整版**[缺凭据同步与 N=0 门，真实大历史形态下仍会整窗一次]，本批修正）；②编号已按让渡改为 #239/#240/#241（代码注释+台账+哨兵 name 同步）；③本口 git add 全程显式列文件、未用 -A。
- 【真机:待验证】（小米15Pro/Chrome 151 及任意机型，先更新到 mtqapsy8）：①字卡库→其他互动功能字卡：打开即见摸鱼字卡列表、切 tab/分组/搜索即时出内容，概率设置在列表底部；②设置→手机美化→背景模糊 12px+遮罩 50%：回桌面壁纸变糊变淡、调整即时可见；③收新消息后退出重进聊天：消息区不再整屏跳动、新消息平滑追加；④若背景模糊真机仍无感，请整段复制诊断反馈（该形态将指向更底层的合成器问题，需另路）。
- 【AI-A 备忘录会话请查收】chat.js 本批自纠只动 inplacePatchIfSameWindow 尾部与 loadNewerIncremental 签名（#241 注释标注），你的 memo-app-remind 链路（chatAddIn/memoRemindTick）未触碰；你的 #238 哨兵 2 条照常在位（520 含你的）。

### 2026-09-07 04:45（⚠️ 事故通报+编号让渡：d50bc5a 卷入并行会话进行中改动；#237/#238 编号已被备忘录修复占用）
- [AI-A 本会话=备忘录域]：#237(添加备忘触发聊天提问,9fe669d)/#238(概率催办提醒,d50bc5a) 已提交推送。**d50bc5a 的 `git add -A` 把并行会话在我两提交之间写入工作区的 4 个文件一并卷入并推送**：src/js/chat.js(inplacePatch 前缀放宽)/src/js/personalize.js(desk-blur-on 接线)/src/css/home.css(模糊改壁纸层自滤)/src/css/chat-pages.css(fc/dk 整页滚动)——内容带完整 FIX 注释、node --check 过、desk-blur-on/--desk-bg-blur/loadNewerIncremental 接线核对在位、哨兵 517/517、布局 10/10，已随 sw mochi-mtq9p7iu 上线。**需要对方处理**：①确认这 4 处当时确已保存完整、后续收口按原计划继续（产物已含它们）；②你的 FIX-REGRESSION/注释里的小米15Pro 两单与我的备忘录 #237/#238 撞号——**台账登记时请改用 #239(字卡页滚动)/#240(壁纸模糊载体)+聊天窗口补丁 #241**，代码注释里的编号也请同步改，防台账一号多修；③我看到你仍在途的部分未动、不会代收后续。
### 2026-09-07 05:0x（#238 备忘提醒：概率触发 TA 在聊天里催办备忘——复刻吃饭提醒模式）
- [AI-A 域+跨域 contacts.js 一行]（**改动文件：src/js/memo-app.js（DEF_MEMO_REMIND 三组话术+memo-app-remind 单键配置[en 默认开/prob 默认 2/done 日标记]+memoRemindTick 每 4 分钟一掷·23-6 静默·每天最多 1 条+memoRemindFire 按紧急度挑事项[过期>今日到期>积压>普通]经 chatAddIn 发{tag:'备忘提醒'} chip+manage 行新增「备忘提醒」「提醒概率」两钮+暴露 window.memoRemindTickNow）；src/css/memo.css（.memo-manage 改 flex 换行容三钮）；src/js/contacts.js（EXCLUDE+memo-app-remind 一行，跨域声明：防 migrateLegacy 误迁，同 v3.26.x 三键先例）；build.mjs（哨兵+2=517：发送锚+每日一次闸）；src/template.html（功能介绍行补概率催办描述）；FIX-REGRESSION.md（+#238 行，顺带补 #237 行缺失的两个尾列）；**构建状态：已构建·sw 视 version.json·本会话（AI-A）执行**）。
- 需求：用户「备忘我需要概率触发联系人在聊天里发出来提醒我」——#237 只覆盖添加当下回应，待办挂着时聊天侧零提醒。
- 验证：node --check 过；--check-sentinels 517 全绿哑 0；playwright 冒烟（prob=100→tickNow→聊天出现带「备忘提醒」chip 催办+done 标记落位+同日二次不重发）；布局 verify。【真机:待验证】调概率→等命中→聊天收催办，同日仅 1 条。
- 【跨域声明】contacts.js 仅 EXCLUDE 追加 1 键名（memo-app 域配套，理由见上）。
### 2026-09-07 04:18（#237 备忘录添加备忘触发聊天提问——链路缺失补齐）
- [AI-A 域+跨域 build.mjs/template.html]（**改动文件：src/js/memo-app.js（新增 DEF_MEMO_ASK 语料 4 条+memoAddFromInput 保存后经 window.chatAddIn 发「回应+追问」带 { tag:'备忘' } chip——此前新增零聊天联动、仅完成/分享两通道）；build.mjs（哨兵+1=515：chatAddIn(memoPick(DEF_MEMO_ASK) 逻辑锚）；src/template.html（生活工具·备忘录功能介绍行补「添加备忘 TA 会在聊天里回应追问」，顺带删已下线的「桌面横幅实时显示待办数」过期描述）；FIX-REGRESSION.md（+237 行）；**构建状态：已构建·sw 视 version.json·本会话（AI-A）执行**）。
- 需求：用户报「备忘录添加的备忘无法触发聊天里联系人提问」。排查定性：非回归，该链路从未存在（聊天侧仅勾选完成[开关默认关]/手动分享；BUGS/台账/git 历史均无记录）→ 按现有「系统消息进聊天」模式（喝水/吃饭提醒同款 tag chip 通道）补齐。
- 验证：node --check 过；--check-sentinels 515 全绿哑 0；构建后哨兵 515/515。【真机:待验证】备忘录加一条→聊天 TA 立即有带「备忘」chip 的追问、可回复续聊。
- 【跨域声明】build.mjs 仅追加 1 条哨兵行；template.html 仅改 1 行功能介绍文案（理由如上，AI-A 功能域文案）。
### 2026-09-07 05:0x（自定义字卡全量导入导出：字卡库列表页新增两入口，一份 json 覆盖 公用/专属聊天字卡+功能卡+寻踪+情话+TA 六类题库的我的添加；已构建·本次构建者：AI-A 本会话）
- [AI-A 域+跨域 template.html/notice.json]（**改动文件：src/js/chatcard.js（新增「自定义字卡全量导入导出」段：导出=hydrateLibScopes 权威取回后收集 cc-groups-public/cc-groups（含分组停用开关）+checkin-cards-*+quote-cards*+ta-ask/ta-choose/ta-curious/ta-roast/ta-checkin/ta-invite 的 questions+groups（不含 settings/问答历史）下载 mochi自定义字卡全量.json；导入=追加合并（cc 同名分组按内容去重/条目按文本归一去重/分组定义按 id+名去重/TA 题库按文本+ID 去重/停用开关并集）或整包替换（文件包含的库按文件写入），读写前均走 hydrateLibScopes 权威取回防空快照覆盖（#193 同防线），文件读取带 BOM/UTF-16/裁剪自救）、src/template.html（cc-sect-custom 尾部两入口行 li-cc-full-export/li-cc-full-import，已随并行 #236 5c2262d 入库、产物当时未含，本口构建接入）、src/js/quote-cards.js+p2-features.js（各 1 行暴露 window.quoteCardsRefreshCounts/ckCardsRefreshCounts 供导入后刷列表页角标）、src/pwa/notice.json（新增【九、自定义字卡全量导入导出】章节）、build.mjs（哨兵 +2：ccFullApply 逻辑锚+template 入口锚）**）；构建状态：**已构建·sw 见 version.json·本口执行**。
- 需求：用户「现在自定义字卡，只有【公用字卡】和【专属字卡】可以导入导出，里面其他的缺少导入导出。缺少【自定义字卡】全量导入导出」。根因：功能卡本就存 cc-groups 双作用域随旧出入口走，但 寻踪日常/今日情话/TA 六类的「我的添加」与自定义分组共 10+ 个键散落各模块、全无出入口，换机只能整包备份恢复。
- 验证：node --check 四文件过；--check-sentinels 512 全绿哑 0（构建前）；tools/verify-cc-full-transfer.mjs 新增 **27/27**（vm 切片跑真实合并函数：cc 合并/条目归一/分组定义/TA 题库/停用开关/ccFullRd 容错+静态接线；**红绿对照：HEAD 旧源切片红退出码 1 → 修复后 27/27**）；构建后哨兵 514/514。
- 【真机:待验证】（任意机型）：①字卡库→可自定义字卡 底部「自定义字卡·全量导出」→下载 json 含各库计数提示；②「全量导入」选该文件→追加合并→各库「我的添加」角标增加且重复内容不翻倍；③换联系人桌面导入专属部分落在对应桌面；④整包替换后各库=文件内容。
- 【并行 #236 会话声明】树上你口无在途改动，本口开工树净（git status 仅本口 3 src + 1 新 verify）；你在 5c2262d 随库带走的 template.html 两锚点即本口功能，本口构建已把产物接入（index.html grep li-cc-full-export>0）。

### 2026-09-07 04:0x（#236 OPPO K13 Turbo Pro+HeyTapBrowser「屏幕下方大片空白」双洞收口：①安卓浏览器覆盖形态执行器缺失（covered 执行侧全在 isIOS 分支，安卓永无 mochi-cover-top=顶部重叠 #114 形态安卓版）②收键盘后 vv 恒卡 inner−底栏高 → _aKb 卡真 .phone 锁死 652=底部空白+tabbar 悬空；判定器 coverBrowser 扩 sig.andr+诊断③有效顶位；已构建·sw mochi-mtq84lcq·本次构建者：AI-B 本会话）
- [AI-B 域]（**改动文件：src/js/mobile-adapt.js（①!isIOS 块尾新增 _aSyncCoverTop 安卓覆盖形态执行器：env 探针按横竖屏缓存→共享判定器→safeTop>0 写 --mochi-safe-top+挂 mochi-cover-top/否则摘除，resize/orientationchange 接线；②1s 看门狗新增键盘会话卡死自愈（真键盘证据=vv 缩幅≥min(_aIH,_aH)×22% 或 inner 同缩，都不成立而缩幅落 13~22% 残留带+会话超 1.5s+vv 稳 1.2s → 清 _aKb+置 _aVvStale 闩）+open 判定加残留闩门+_aBump/focusin 解闩+_aKbAt/_aVvChgAt 计时+__mochiAndroidKb 探针补 staleVv）、src/js/device.js（判定器 coverBrowser 扩 (diff≤2||!!sig.andr)——安卓壳带底栏 diff>2 也入浏览器覆盖形态；screenDiagJudge/sdHistCompare 两处 sig 补传 andr；诊断③对 coverBrowser 改有效顶位=元素顶+实测 padding；形态/底部文案同步）、build.mjs（#199 判定器锚 needle 同步+#236 新哨兵 4 条）、tools/verify-viewport-form.mjs（+HeyTap 台账/iOS 零回归闸/env0 不误伤三用例+[D] screenDiagJudge 端到端 6 断言，共 76）、FIX-REGRESSION.md（#236 行+设备索引 OPPO K13 Turbo Pro 行）**；构建状态：**已构建·sw mochi-mtq84lcq·本口执行·哨兵 512/512 哑 0·sw 14/14**）。
- 需求/根因：用户诊断 SIG「form=covered/safeTop=0/期望底=760/.phone=652/kbActive=true/sb=0/tab=634」三 ✗（顶部重叠+底部少填 108+导航栏悬空 126），明说其他机型也有。①covered 形态执行侧（--mochi-safe-top/mochi-cover-top）全在 isIOS 分支，HeyTap 壳 viewport-fit=cover 生效页面画进系统状态栏下方（env=40），安卓无执行器落地+base.css 后加载 .statusbar{padding:4px} 压死 env 避让（#114 同根因安卓版）；②该壳收键盘后 vv.height 恒停 652=inner−底栏不回基准 → open 恒真 _aKb 卡真（含无聚焦纯 vv 置位会话），.phone 内联高锁死 652；#209 清扫/_aProv/focusout 400ms/250ms 轮询四条复原路全被堵死（后三条都等 vv 回基准，等不到）。
- 修复原则（回应「不要覆盖修改导致不同机型 bug 反复」）：iOS 分支（syncVvFit/键盘链）一行未动；判定器扩展用 sig.andr 显式门控（iOS 不传=原判式逐字节不变）；执行器只做「safeTop>0 写变量+挂类」，其余消费方 fallback 本就是 env() 写入同值零视觉变化，env=0 常规安卓浏览器摘除属性与旧版一致；键盘自愈判据全用视口证据（缩幅 22% 键盘下限物理区分真键盘与残留读数，真键盘缩幅>200px 永不误清；vv 稳 1.2s 避开收起动画每帧变化）。
- 验证：node --check 两文件过；tools/verify-viewport-form.mjs **76/76**（红绿对照：git stash 修复前源 72/76——HeyTap 台账 form/safeTop/expBase 3 红+诊断端到端「修复后稳态全绿」1 红=报障原文复现，修复后 76/76；「报障现场三 ✗」断言新旧源同过=钉住诊断检出力）；verify-kb-residue-heal 10/10（#209 看门狗不回归）、verify-fullscreen-ipad 25/25、布局 verify 10/10；verify-ios-kb-stuck 3 红/verify-ios-reserved-standalone 7 红/verify-screen-diag-opt 4 红经 stash 对照 HEAD 基线红项集合逐字节一致=存量（#235 批 Node 环境 fixture 未传 safMajor 口径，归属其会话），本批零新增回归。
- 【随库带上并行会话在途文档批】TASKS.md（#131 核查定性）与 WORKLOG #131 条（另一会话只读核查补证，声明不涉及 src/产物）随本口构建/提交一并入库，本口未触碰其内容。
- 【真机:待验证】（OPPO K13 Turbo Pro+HeyTapBrowser 及任意安卓壳）：①更新后桌面状态栏自动落到系统状态栏下方不再重叠；②聊一次天（键盘弹出再收）底部不再残留大片空白、tabbar 贴底；③屏幕适配诊断全 ✓，形态行显示「浏览器覆盖壳（#199/#236）」；④若仍有异常请整段复制诊断反馈（SIG 里 staleVv 字段可证残留闩状态）。

